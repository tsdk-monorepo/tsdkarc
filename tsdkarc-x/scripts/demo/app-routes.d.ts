// Auto-generated by extractAppRoutesTypes from `tsdkarc-x/extract`. DO NOT EDIT.
// Re-generate by running your build script after server boot.

import type { RequestOptions } from "./client";

export interface AppRoutes {
  /** @deprecated don't use this, it's only for types inferring */
  zResolved__: true; // 🚀 MAGIC BULLET: Tells the client to skip deep mapping
  v1: {
    users: {
      /**
       * hello health api
       */
      health: {
        /**
         * hello health api
         */
        query(input?: null | undefined, opts?: RequestOptions): Promise<string>;
      };
      ping: {
        query(input: any, opts?: RequestOptions): Promise<{ message: string; trace: string; user: string; }>;
      };
      testSubscribe2: {
        mutate(input: WebPushSubscription, opts?: RequestOptions): Promise<WebPushSubscription2>;
      };
      testSubscribe3: {
        mutate(input: Request, opts?: RequestOptions): Promise<Response>;
      };
      testSubscribe: {
        query(input: WebPushSubscription, opts?: RequestOptions): Promise<WebPushSubscription2>;
      };
      getProfile: {
        query(input: { includeHistory?: boolean | undefined; }, opts?: RequestOptions): Promise<{ history: { a: { b: number; }; }[] | null; id: string; name: string; role: string; }>;
      };
      getProfile2: {
        query(input: { includeHistory: number[]; } | undefined, opts?: RequestOptions): Promise<{ history: never[] | null; id: string; name: string; role: string; }>;
      };
      updatePassword: {
        mutate(input: { newPwd: string }, opts?: RequestOptions): Promise<string>;
      };
      updatePassword2: {
        mutate(input: { newPwd: string }, opts?: RequestOptions): Promise<string>;
      };
      registerDevice: {
        mutate(input: { deviceId: string }, opts?: RequestOptions): Promise<{ success: boolean; }>;
      };
      downloadLogs: {
        stream(input: { lines: number }, opts?: RequestOptions): Promise<AsyncGenerator<{ index: number; text: string; }, void, unknown>>;
      };
      uploadAvatar: {
        upload(input: { file: File; cropSize: number } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileName: string; size: number; croppedTo: number; }>;
      };
      triggerError: {
        query(input?: null | undefined, opts?: RequestOptions): Promise<never>;
      };
      settings: {
        getTheme: {
          query(input?: null | undefined, opts?: RequestOptions): Promise<string>;
        };
        updateTheme: {
          mutate(input: { theme: "dark_mode" | "light_mode" }, opts?: RequestOptions): Promise<string>;
        };
      };
    };
  };
  a: {
    a: {
      query(input: any, opts?: RequestOptions): Promise<{ a: number; }>;
    };
    health: {
      query(input?: null | undefined, opts?: RequestOptions): Promise<string>;
    };
    ping: {
      query(input: any, opts?: RequestOptions): Promise<{ message: string; trace: string; user: string; }>;
    };
  };
  mock: {
    authRoutes0: {
      /**
       * Multi-method login — password, OAuth, SAML, magic link, passkey.
       */
      login: {
        /**
         * Multi-method login — password, OAuth, SAML, magic link, passkey.
         */
        mutate(input: { method: "password"; email: string; password: string; totpCode?: string } | { method: "oauth"; provider: "google" | "github" | "microsoft" | "okta"; code: string; redirectUri: string; state: string } | { method: "saml"; orgSlug: string; samlResponse: string; relayState?: string } | { method: "magic_link"; token: string } | { method: "passkey"; credentialId: string; clientDataJSON: string; authenticatorData: string; signature: string } & { device?: { userAgent: string; ip: string; fingerprint?: string; platform?: "web" | "ios" | "android" | "desktop" } }, opts?: RequestOptions): Promise<{ tokens: { accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }; user: { id: UserId; role: "admin"; }; }>;
      };
      /**
       * Rotate refresh token.
       */
      refresh: {
        /**
         * Rotate refresh token.
         */
        mutate(input: { refreshToken: string; sessionId: string }, opts?: RequestOptions): Promise<{ accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }>;
      };
      /**
       * Invalidate session.
       */
      logout: {
        /**
         * Invalidate session.
         */
        mutate(input: { sessionId: string }, opts?: RequestOptions): Promise<{ revoked: boolean; sessionId: SessionId; }>;
      };
      /**
       * Validate MFA code after credential check.
       */
      mfaChallenge: {
        /**
         * Validate MFA code after credential check.
         */
        mutate(input: { challengeToken: string; code: string; method: "totp" | "sms" | "email" | "webauthn" }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Send password reset email.
       */
      forgotPassword: {
        /**
         * Send password reset email.
         */
        mutate(input: { email: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
      /**
       * Apply new password via reset token.
       */
      resetPassword: {
        /**
         * Apply new password via reset token.
         */
        mutate(input: { token: string; newPassword: string }, opts?: RequestOptions): Promise<{ success: boolean; }>;
      };
      /**
       * List active sessions with device info.
       */
      sessions: {
        /**
         * List active sessions with device info.
         */
        query(input: { cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ sessions: { id: SessionId; device: { userAgent: string; ip: string; fingerprint?: string | undefined; platform?: "web" | "ios" | "android" | "desktop" | undefined; }; createdAt: string; current: boolean; }[]; nextCursor: string | null; }>;
      };
      /**
       * Current authenticated user.
       */
      me: {
        /**
         * Current authenticated user.
         */
        query(input: any, opts?: RequestOptions): Promise<{ id: UserId; role: string; }>;
      };
      /**
       * Confirm email address via verification code.
       */
      verifyEmail: {
        /**
         * Confirm email address via verification code.
         */
        mutate(input: { code: string }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Stream login activity events for the current user.
       */
      deviceStream: {
        /**
         * Stream login activity events for the current user.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "session_created"; sessionId: SessionId; ts: string; }, void, unknown>>;
      };
    };
    usersRoutes0: {
      /**
       * Paginated user list with rich filters.
       */
      list: {
        /**
         * Paginated user list with rich filters.
         */
        query(input: { page?: number; limit?: number; roles?: Array<"owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support">; search?: string; sortBy?: "name" | "email" | "createdAt"; sortDir?: "asc" | "desc" }, opts?: RequestOptions): Promise<{ users: { id: string; email: string; emailVerified: boolean; name: string; avatarUrl: string | null; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: string[]; metadata: Record<string, string | number | boolean>; createdAt: string; updatedAt: string; displayName?: string | undefined; bio?: string | undefined; timezone?: string | undefined; locale?: string | undefined; address?: { line1: string; city: string; postalCode: string; country: string; line2?: string | undefined; state?: string | undefined; } | undefined; }[]; total: number; page: number; }>;
      };
      /**
       * Fetch single user with full profile.
       */
      get: {
        /**
         * Fetch single user with full profile.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; email: string; emailVerified: true; name: string; displayName: undefined; avatarUrl: null; bio: undefined; timezone: string; locale: string; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: never[]; metadata: {}; address: undefined; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Partial profile update with nested address.
       */
      update: {
        /**
         * Partial profile update with nested address.
         */
        mutate(input: { name?: string; displayName?: string; bio?: string; timezone?: string; locale?: string; avatarUrl?: string | null; address?: { line1: string; line2?: string; city: string; state?: string; postalCode: string; country: string }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Soft-delete user.
       */
      deactivate: {
        /**
         * Soft-delete user.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ deactivated: boolean; }>;
      };
      /**
       * Change role for many users atomically.
       */
      bulkRoleChange: {
        /**
         * Change role for many users atomically.
         */
        mutate(input: { userIds: Array<string>; role: "owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support"; reason?: string }, opts?: RequestOptions): Promise<{ updated: number; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; }>;
      };
      /**
       * All permissions grouped by resource for a user.
       */
      permissionMatrix: {
        /**
         * All permissions grouped by resource for a user.
         */
        query(input: { userId: string }, opts?: RequestOptions): Promise<{ userId: string; matrix: Record<string, Record<string, boolean>>; }>;
      };
      /**
       * Upload and crop user avatar.
       */
      uploadAvatar: {
        /**
         * Upload and crop user avatar.
         */
        upload(input: { file: File; cropSize?: number } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileName: string; size: number; croppedTo: number; }>;
      };
      /**
       * Change own password — requires MFA.
       */
      changePassword: {
        /**
         * Change own password — requires MFA.
         */
        mutate(input: { currentPassword: string; newPassword: string }, opts?: RequestOptions): Promise<{ changed: boolean; }>;
      };
      /**
       * GDPR data export streamed by resource type.
       */
      exportData: {
        /**
         * GDPR data export streamed by resource type.
         */
        stream(input: { userId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "chunk"; resource: "profile" | "tasks" | "comments" | "files"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    organizationsRoutes0: {
      /**
       * Orgs accessible to caller.
       */
      list: {
        /**
         * Orgs accessible to caller.
         */
        query(input: { page?: number; limit?: number }, opts?: RequestOptions): Promise<{ orgs: { id: string; name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; memberCount: number; logoUrl: string | null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }[]; total: number; }>;
      };
      /**
       * Fetch org by ID.
       */
      get: {
        /**
         * Fetch org by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; slug: string; plan: "enterprise"; memberCount: number; logoUrl: null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }>;
      };
      /**
       * Create new organization.
       */
      create: {
        /**
         * Create new organization.
         */
        mutate(input: { name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; logoUrl: string | null; settings: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> }; ownerId: string }, opts?: RequestOptions): Promise<{ id: string; slug: string; }>;
      };
      /**
       * Update org metadata and settings.
       */
      update: {
        /**
         * Update org metadata and settings.
         */
        mutate(input: { id: string; name?: string; logoUrl?: string | null; settings?: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> } }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Permanently delete org.
       */
      delete: {
        /**
         * Permanently delete org.
         */
        mutate(input: { id: string; confirmSlug: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated org member list.
       */
      members: {
        /**
         * Paginated org member list.
         */
        query(input: { orgId: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: string; joinedAt: string; }[]; total: number; }>;
      };
      /**
       * Add user to org with role.
       */
      addMember: {
        /**
         * Add user to org with role.
         */
        mutate(input: { orgId: string; userId: string; role: "owner" | "admin" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from org.
       */
      removeMember: {
        /**
         * Remove user from org.
         */
        mutate(input: { orgId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
      /**
       * Transfer org ownership — requires MFA.
       */
      transferOwnership: {
        /**
         * Transfer org ownership — requires MFA.
         */
        mutate(input: { orgId: string; newOwnerId: string; reason?: string }, opts?: RequestOptions): Promise<{ transferred: boolean; }>;
      };
      /**
       * Stream org audit log entries live.
       */
      auditStream: {
        /**
         * Stream org audit log entries live.
         */
        stream(input: { orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; action: string; actorId: string; ts: string; }, void, unknown>>;
      };
    };
    billingRoutes0: {
      /**
       * Full subscription with pricing model.
       */
      getSubscription: {
        /**
         * Full subscription with pricing model.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; tier: "pro"; pricing: { model: "per_seat"; pricePerSeatMonth: number; minSeats: number; maxSeats: null; }; status: "active"; seats: number; currentPeriodStart: string; currentPeriodEnd: string; cancelAtPeriodEnd: false; metadata: {}; addons: never[]; }>;
      };
      /**
       * Calculate proration before confirming.
       */
      previewUpgrade: {
        /**
         * Calculate proration before confirming.
         */
        query(input: { orgId: string; targetTier: "starter" | "pro" | "business" | "enterprise"; targetSeats: number; billingInterval: "monthly" | "annual" }, opts?: RequestOptions): Promise<{ currentAmount: number; newAmount: number; prorationAmount: number; effectiveDate: string; lineItems: { description: string; amount: number; currency: string; }[]; }>;
      };
      /**
       * Create Stripe checkout session.
       */
      createCheckout: {
        /**
         * Create Stripe checkout session.
         */
        mutate(input: { orgId: string; tier: "starter" | "pro" | "business" | "enterprise"; seats: number; billingInterval: "monthly" | "annual"; addons?: Array<{ id: string; quantity: number }>; successUrl: string; cancelUrl: string; couponCode?: string }, opts?: RequestOptions): Promise<{ checkoutUrl: string; sessionId: string; }>;
      };
      /**
       * Schedule cancellation at period end.
       */
      cancelSubscription: {
        /**
         * Schedule cancellation at period end.
         */
        mutate(input: { orgId: string; reason?: string }, opts?: RequestOptions): Promise<{ cancelAtPeriodEnd: boolean; }>;
      };
      /**
       * Paginated invoice list.
       */
      invoices: {
        /**
         * Paginated invoice list.
         */
        query(input: { orgId: string; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ invoices: { id: string; amount: number; currency: string; status: string; date: string; }[]; nextCursor: string | null; }>;
      };
      /**
       * Metered usage breakdown.
       */
      usageReport: {
        /**
         * Metered usage breakdown.
         */
        query(input: { orgId: string; breakdown?: "daily" | "weekly" | "monthly" }, opts?: RequestOptions): Promise<{ period: { start: string; end: string; }; included: number; consumed: number; overage: number; overageCost: number; series: { date: string; units: number; cost: number; }[]; }>;
      };
      /**
       * Apply promotional discount code.
       */
      applyPromoCode: {
        /**
         * Apply promotional discount code.
         */
        mutate(input: { orgId: string; code: string }, opts?: RequestOptions): Promise<{ applied: boolean; discountPercent: number; }>;
      };
      /**
       * Stream invoice line items.
       */
      invoiceStream: {
        /**
         * Stream invoice line items.
         */
        stream(input: { invoiceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "line_item"; description: string; amount: number; currency: string; }, void, unknown>>;
      };
    };
    projectsRoutes0: {
      /**
       * Paginated project list with status filters.
       */
      list: {
        /**
         * Paginated project list with status filters.
         */
        query(input: { orgId: string; status?: Array<"planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled">; search?: string; page?: number }, opts?: RequestOptions): Promise<{ projects: { id: string; orgId: string; name: string; status: "active" | "cancelled" | "planning" | "on_hold" | "completed" | "archived"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: string[]; customFields: Record<string, string | number | boolean | null>; createdAt: string; updatedAt: string; description?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Project by ID.
       */
      get: {
        /**
         * Project by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; status: "active"; visibility: "private"; startDate: null; endDate: null; ownerId: string; tags: never[]; customFields: {}; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create project.
       */
      create: {
        /**
         * Create project.
         */
        mutate(input: { orgId: string; name: string; description?: string; status: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: Array<string>; customFields: Record<string, string | number | boolean | null> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update project fields.
       */
      update: {
        /**
         * Update project fields.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; status?: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility?: "private" | "internal" | "public"; startDate?: string | null; endDate?: string | null; ownerId?: string; tags?: Array<string>; customFields?: Record<string, string | number | boolean | null>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Archive project.
       */
      archive: {
        /**
         * Archive project.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ archived: boolean; }>;
      };
      /**
       * Project member list.
       */
      members: {
        /**
         * Project member list.
         */
        query(input: { projectId: string }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "viewer" | "lead" | "contributor"; joinedAt: string; }[]; }>;
      };
      /**
       * Add project member.
       */
      addMember: {
        /**
         * Add project member.
         */
        mutate(input: { projectId: string; userId: string; role: "lead" | "contributor" | "viewer" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Aggregate project stats.
       */
      stats: {
        /**
         * Aggregate project stats.
         */
        query(input: { projectId: string; period?: "7d" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ tasks: number; completed: number; openIssues: number; velocity: number; burndownPoints: { date: string; remaining: number; }[]; }>;
      };
      /**
       * Clone project structure.
       */
      duplicate: {
        /**
         * Clone project structure.
         */
        mutate(input: { id: string; newName: string; includeTasks?: boolean; includeMembers?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream full project export.
       */
      exportStream: {
        /**
         * Stream full project export.
         */
        stream(input: { id: string; format: "json" | "csv" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; resource: "task"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    tasksRoutes0: {
      /**
       * Create task with full field set.
       */
      create: {
        /**
         * Create task with full field set.
         */
        mutate(input: { projectId: string; title: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }> }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; }>;
      };
      /**
       * Task tree with recursive subtasks.
       */
      get: {
        /**
         * Task tree with recursive subtasks.
         */
        query(input: { id: string; depth?: number }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; subtasks: any[]; customFields: Record<string, { type: "text"; value: string; } | { type: "number"; value: number; } | { type: "date"; value: string; } | { type: "boolean"; value: boolean; } | { type: "select"; value: string; optionId: string; } | { type: "multi"; value: string[]; optionIds: string[]; } | { type: "user"; value: string; } | { type: "url"; value: string; }>; }>;
      };
      /**
       * Patch any task fields.
       */
      update: {
        /**
         * Patch any task fields.
         */
        mutate(input: { projectId?: string; title?: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete task permanently.
       */
      delete: {
        /**
         * Delete task permanently.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Batch status/priority update.
       */
      bulkUpdate: {
        /**
         * Batch status/priority update.
         */
        mutate(input: { ids: Array<string>; patch: { status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; dueDate?: string | null } }, opts?: RequestOptions): Promise<{ updated: number; }>;
      };
      /**
       * Comment thread for a task.
       */
      comments: {
        /**
         * Comment thread for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; body: string; author: string; createdAt: string; reactions: Record<string, string[]>; }[]; }>;
      };
      /**
       * Post comment on task.
       */
      addComment: {
        /**
         * Post comment on task.
         */
        mutate(input: { taskId: string; body: string; mentions?: Array<string> }, opts?: RequestOptions): Promise<{ id: string; body: string; }>;
      };
      /**
       * Typed audit trail for a task.
       */
      timelineEvents: {
        /**
         * Typed audit trail for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ events: ({ type: "created"; actor: string; ts: string; } | { type: "status_change"; actor: string; ts: string; from: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; to: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; } | { type: "assigned"; actor: string; ts: string; userId: string; })[]; }>;
      };
      /**
       * Tasks assigned to current user.
       */
      myTasks: {
        /**
         * Tasks assigned to current user.
         */
        query(input: { status?: Array<"backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix">; projectId?: string; limit?: number }, opts?: RequestOptions): Promise<{ tasks: any[]; total: number; }>;
      };
      /**
       * Live task update stream.
       */
      activityStream: {
        /**
         * Live task update stream.
         */
        stream(input: { projectId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "task_updated"; taskId: string; field: string; value: string; }, void, unknown>>;
      };
    };
    filesRoutes0: {
      /**
       * Files for a resource.
       */
      list: {
        /**
         * Files for a resource.
         */
        query(input: { resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; mimeTypes?: Array<string>; page?: number }, opts?: RequestOptions): Promise<{ files: { id: string; name: string; mimeType: string; sizeBytes: number; status: "deleted" | "pending" | "processing" | "ready" | "failed"; url: string | null; thumbnailUrl: string | null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: Record<string, string | number>; createdAt: string; }[]; total: number; }>;
      };
      /**
       * File metadata by ID.
       */
      get: {
        /**
         * File metadata by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; mimeType: string; sizeBytes: number; status: "ready"; url: string; thumbnailUrl: null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: {}; createdAt: string; }>;
      };
      /**
       * Get presigned S3 upload URL.
       */
      requestUpload: {
        /**
         * Get presigned S3 upload URL.
         */
        mutate(input: { fileName: string; mimeType: string; sizeBytes: number; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string }, opts?: RequestOptions): Promise<{ uploadUrl: string; fileId: string; expiresIn: number; }>;
      };
      /**
       * Mark pending upload complete.
       */
      confirmUpload: {
        /**
         * Mark pending upload complete.
         */
        mutate(input: { fileId: string; etag: string }, opts?: RequestOptions): Promise<{ id: string; confirmed: boolean; url: string; }>;
      };
      /**
       * Delete file and storage object.
       */
      delete: {
        /**
         * Delete file and storage object.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Delete multiple files.
       */
      bulkDelete: {
        /**
         * Delete multiple files.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ deleted: number; }>;
      };
      /**
       * Multipart direct file upload.
       */
      directUpload: {
        /**
         * Multipart direct file upload.
         */
        upload(input: { file: File; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; description?: string } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileId: string; name: string; size: number; }>;
      };
      /**
       * Stream file processing events.
       */
      processStream: {
        /**
         * Stream file processing events.
         */
        stream(input: { fileId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; fileId: string; stage: "ready" | "queued" | "virus_scan" | "thumbnail"; percent: number; }, void, unknown>>;
      };
    };
    notificationsRoutes0: {
      /**
       * Paginated notification list.
       */
      list: {
        /**
         * Paginated notification list.
         */
        query(input: { unreadOnly?: boolean; types?: Array<"mention" | "task_assigned" | "task_completed" | "comment" | "billing" | "security" | "system" | "workflow_run" | "integration_sync">; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ notifications: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }[]; unreadCount: number; nextCursor: string | null; }>;
      };
      /**
       * Mark notifications as read.
       */
      markRead: {
        /**
         * Mark notifications as read.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ marked: number; }>;
      };
      /**
       * Mark all notifications read.
       */
      markAllRead: {
        /**
         * Mark all notifications read.
         */
        mutate(input: any, opts?: RequestOptions): Promise<{ marked: boolean; }>;
      };
      /**
       * Get notification preferences.
       */
      preferences: {
        /**
         * Get notification preferences.
         */
        query(input: any, opts?: RequestOptions): Promise<{ channels: Record<string, boolean>; types: Partial<Record<"billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync", boolean>>; }>;
      };
      /**
       * Update preferences.
       */
      updatePreferences: {
        /**
         * Update preferences.
         */
        mutate(input: { channels?: Record<string, boolean>; types?: Record<string, boolean> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Register Web Push subscription.
       */
      pushSubscribe: {
        /**
         * Register Web Push subscription.
         */
        mutate(input: { endpoint: string; keys: { p256dh: string; auth: string } }, opts?: RequestOptions): Promise<{ subscribed: boolean; }>;
      };
      /**
       * Real-time notification SSE.
       */
      liveStream: {
        /**
         * Real-time notification SSE.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "notification"; notification: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }; }, void, unknown>>;
      };
    };
    searchRoutes0: {
      /**
       * Full-text search across resource types.
       */
      global: {
        /**
         * Full-text search across resource types.
         */
        query(input: { q: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document">; orgId: string; limit?: number; highlight?: boolean }, opts?: RequestOptions): Promise<{ results: ({ type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; })[]; total: number; took: number; }>;
      };
      /**
       * Autocomplete suggestions.
       */
      suggestions: {
        /**
         * Autocomplete suggestions.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ suggestions: { label: string; type: "user" | "file" | "task" | "project" | "comment" | "workflow" | "document"; id: string; }[]; }>;
      };
      /**
       * Faceted filter counts.
       */
      filters: {
        /**
         * Faceted filter counts.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ facets: Record<"user" | "file" | "task" | "project" | "comment" | "workflow" | "document", number>; }>;
      };
      /**
       * Trigger manual reindex for org.
       */
      reindex: {
        /**
         * Trigger manual reindex for org.
         */
        mutate(input: { orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream results progressively.
       */
      stream: {
        /**
         * Stream results progressively.
         */
        stream(input: { q: string; orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "result"; result: { type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; }; }, void, unknown>>;
      };
    };
    analyticsRoutes0: {
      /**
       * Arbitrary analytics query.
       */
      query: {
        /**
         * Arbitrary analytics query.
         */
        mutate(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }, opts?: RequestOptions): Promise<{ rows: never[]; totals: {}; comparison: undefined; meta: { executionMs: number; rowsScanned: number; cached: false; granularity: "minute" | "hour" | "day" | "week" | "month"; }; }>;
      };
      /**
       * Multi-step funnel conversion.
       */
      funnel: {
        /**
         * Multi-step funnel conversion.
         */
        mutate(input: { orgId: string; steps: Array<{ name: string; event: string; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }> }>; dateRange: { from: string; to: string } }, opts?: RequestOptions): Promise<{ steps: { name: string; count: number; conversionRate: number; avgTimeToNextStep: number | null; }[]; overallConversion: number; }>;
      };
      /**
       * Cohort retention analysis.
       */
      retention: {
        /**
         * Cohort retention analysis.
         */
        mutate(input: { orgId: string; cohortEvent: string; returnEvent: string; dateRange: { from: string; to: string }; granularity?: "minute" | "hour" | "day" | "week" | "month" }, opts?: RequestOptions): Promise<{ cohorts: { date: string; size: number; retention: number[]; }[]; }>;
      };
      /**
       * Saved report definitions.
       */
      savedReports: {
        /**
         * Saved report definitions.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; name: string; query: { orgId: string; metrics: string[]; dimensions: string[]; filters: { field: string; operator: { op: "eq"; value: string | number | boolean; } | { op: "neq"; value: string | number | boolean; } | { op: "gt"; value: number; } | { op: "gte"; value: number; } | { op: "lt"; value: number; } | { op: "lte"; value: number; } | { op: "in"; values: (string | number)[]; } | { op: "between"; min: number; max: number; }; }[]; dateRange: { from: string; to: string; granularity: "minute" | "hour" | "day" | "week" | "month"; timezone: string; }; orderBy: { field: string; direction: "asc" | "desc"; }[]; limit: number; compareWith?: { from: string; to: string; } | undefined; }; schedule: { cron: string; recipients: string[]; } | null; lastRunAt: string | null; }[]; }>;
      };
      /**
       * Persist a report definition.
       */
      saveReport: {
        /**
         * Persist a report definition.
         */
        mutate(input: { orgId: string; name: string; query: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }; schedule?: { cron: string; recipients: Array<string> } }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream query results row by row.
       */
      exportStream: {
        /**
         * Stream query results row by row.
         */
        stream(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string }; format?: "json" | "csv" | "parquet" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; index: number; data: Record<string, string | number | null>; }, void, unknown>>;
      };
    };
    webhooksRoutes0: {
      /**
       * Webhooks for an org.
       */
      list: {
        /**
         * Webhooks for an org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ webhooks: { id: string; orgId: string; url: string; events: ("task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed")[]; enabled: boolean; secret: string; headers: Record<string, string>; retryPolicy: { maxAttempts: number; backoffMs: number; }; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Register webhook endpoint.
       */
      create: {
        /**
         * Register webhook endpoint.
         */
        mutate(input: { orgId: string; url: string; description?: string; events: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled: boolean; secret: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number } }, opts?: RequestOptions): Promise<{ id: string; url: string; }>;
      };
      /**
       * Update webhook.
       */
      update: {
        /**
         * Update webhook.
         */
        mutate(input: { orgId?: string; url?: string; description?: string; events?: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled?: boolean; secret?: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Remove webhook.
       */
      delete: {
        /**
         * Remove webhook.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Recent delivery attempts.
       */
      deliveries: {
        /**
         * Recent delivery attempts.
         */
        query(input: { webhookId: string; status?: "success" | "failed" | "pending"; limit?: number }, opts?: RequestOptions): Promise<{ deliveries: { id: string; status: number; attempt: number; ts: string; error: string | null; }[]; }>;
      };
      /**
       * Retry failed delivery.
       */
      redeliver: {
        /**
         * Retry failed delivery.
         */
        mutate(input: { deliveryId: string }, opts?: RequestOptions): Promise<{ queued: boolean; }>;
      };
      /**
       * Send test event to webhook.
       */
      testPing: {
        /**
         * Send test event to webhook.
         */
        mutate(input: { webhookId: string; eventType: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed" }, opts?: RequestOptions): Promise<{ sent: boolean; deliveryId: string; }>;
      };
      /**
       * Stream webhook delivery events.
       */
      logStream: {
        /**
         * Stream webhook delivery events.
         */
        stream(input: { webhookId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "delivery"; status: number; ts: string; }, void, unknown>>;
      };
    };
    aiRoutes0: {
      /**
       * Multi-model streaming chat with tool-call support.
       */
      chat: {
        /**
         * Multi-model streaming chat with tool-call support.
         */
        stream(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "content_delta"; text: string; finishReason?: undefined; usage?: undefined; } | { type: "finish"; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; text?: undefined; }, void, unknown>>;
      };
      /**
       * Non-streaming completion.
       */
      complete: {
        /**
         * Non-streaming completion.
         */
        mutate(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string>; stream?: false }, opts?: RequestOptions): Promise<{ id: string; role: "assistant"; content: string | ({ type: "text"; text: string; } | { type: "image_url"; url: string; detail: "high" | "low" | "auto"; } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown>; } | { type: "tool_result"; toolCallId: string; content: string; isError: boolean; })[]; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; }>;
      };
      /**
       * Batch embedding generation.
       */
      embed: {
        /**
         * Batch embedding generation.
         */
        mutate(input: { inputs: Array<string | { text: string; id: string }>; model: "text-embedding-3-small" | "text-embedding-3-large"; dimensions?: number; outputFormat?: "float" | "base64" | "int8" }, opts?: RequestOptions): Promise<{ embeddings: { index: number; embedding: number[]; inputTokens: number; }[]; model: "text-embedding-3-small" | "text-embedding-3-large"; totalTokens: number; }>;
      };
      /**
       * Run content moderation checks.
       */
      moderateContent: {
        /**
         * Run content moderation checks.
         */
        mutate(input: { inputs: Array<{ id: string; text: string }>; categories?: Array<"hate" | "harassment" | "self_harm" | "sexual" | "violence" | "dangerous"> }, opts?: RequestOptions): Promise<{ results: { id: string; flagged: boolean; categories: Partial<Record<string, boolean>>; scores: Partial<Record<string, number>>; }[]; }>;
      };
      /**
       * Structured text summarization.
       */
      summarize: {
        /**
         * Structured text summarization.
         */
        mutate(input: { text: string; format?: "bullets" | "paragraph" | "tldr"; maxLength?: number }, opts?: RequestOptions): Promise<{ summary: string; wordCount: number; }>;
      };
      /**
       * AI token usage for billing period.
       */
      usageStats: {
        /**
         * AI token usage for billing period.
         */
        query(input: { orgId: string; period?: "current" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ promptTokens: number; completionTokens: number; totalCost: number; byModel: Record<string, { tokens: number; cost: number; }>; }>;
      };
    };
    auditRoutes0: {
      /**
       * Paginated audit log with filters.
       */
      log: {
        /**
         * Paginated audit log with filters.
         */
        query(input: { orgId: string; actorId?: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled">; resourceType?: string; from?: string; to?: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ entries: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }[]; total: number; }>;
      };
      /**
       * Single audit entry by ID.
       */
      getEntry: {
        /**
         * Single audit entry by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; actorId: string; actorEmail: string; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: null; resourceId: null; metadata: {}; ip: string; userAgent: string; ts: string; }>;
      };
      /**
       * Update log retention policy.
       */
      retention: {
        /**
         * Update log retention policy.
         */
        mutate(input: { orgId: string; retentionDays: number }, opts?: RequestOptions): Promise<{ updated: boolean; retentionDays: number; }>;
      };
      /**
       * Stream audit log as NDJSON.
       */
      export: {
        /**
         * Stream audit log as NDJSON.
         */
        stream(input: { orgId: string; from: string; to: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; data: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }; }, void, unknown>>;
      };
    };
    apiKeysRoutes0: {
      /**
       * List API keys for org.
       */
      list: {
        /**
         * List API keys for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ keys: { id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: string | null; lastUsedAt: string | null; createdBy: string; createdAt: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number; } | null; }[]; }>;
      };
      /**
       * Create API key.
       */
      create: {
        /**
         * Create API key.
         */
        mutate(input: { orgId: string; name: string; scopes: Array<"read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write">; expiresAt: string | null; createdBy: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number } | null }, opts?: RequestOptions): Promise<{ key: string; id: string; prefix: string; }>;
      };
      /**
       * Revoke API key.
       */
      revoke: {
        /**
         * Revoke API key.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Rotate key secret.
       */
      rotate: {
        /**
         * Rotate key secret.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ newKey: string; id: string; }>;
      };
      /**
       * API key metadata.
       */
      get: {
        /**
         * API key metadata.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: null; lastUsedAt: null; createdBy: string; createdAt: string; rateLimit: null; }>;
      };
      /**
       * Key request usage stats.
       */
      usage: {
        /**
         * Key request usage stats.
         */
        query(input: { id: string; period?: "1h" | "24h" | "7d" | "30d" }, opts?: RequestOptions): Promise<{ requests: number; errors: number; p50Ms: number; p99Ms: number; series: { ts: string; count: number; }[]; }>;
      };
    };
    integrationsRoutes0: {
      /**
       * All integrations with status.
       */
      list: {
        /**
         * All integrations with status.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ integrations: { id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: boolean; enabled: boolean; status: "error" | "active" | "disconnected" | "syncing"; lastSyncAt: string | null; errorMessage: string | null; config: Record<string, unknown>; }[]; }>;
      };
      /**
       * Single integration status.
       */
      get: {
        /**
         * Single integration status.
         */
        query(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: true; enabled: true; status: "active"; lastSyncAt: string; errorMessage: null; config: {}; }>;
      };
      /**
       * Start OAuth install flow.
       */
      install: {
        /**
         * Start OAuth install flow.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; redirectUri: string; scopes?: Array<string> }, opts?: RequestOptions): Promise<{ authUrl: string; }>;
      };
      /**
       * Exchange OAuth code for tokens.
       */
      oauthCallback: {
        /**
         * Exchange OAuth code for tokens.
         */
        mutate(input: { code: string; state: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ installed: boolean; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; }>;
      };
      /**
       * Remove integration.
       */
      uninstall: {
        /**
         * Remove integration.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ uninstalled: boolean; }>;
      };
      /**
       * Update integration config.
       */
      updateConfig: {
        /**
         * Update integration config.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; config: Record<string, unknown> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Manual sync trigger.
       */
      triggerSync: {
        /**
         * Manual sync trigger.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream sync progress events.
       */
      syncStream: {
        /**
         * Stream sync progress events.
         */
        stream(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; itemsSynced: number; total: number; }, void, unknown>>;
      };
    };
    workflowsRoutes0: {
      /**
       * Create workflow with recursive condition tree.
       */
      create: {
        /**
         * Create workflow with recursive condition tree.
         */
        mutate(input: { name: string; description?: string; trigger: { event: string; filters?: unknown; debounceMs?: number }; steps: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; orgId: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * List workflows with run stats.
       */
      list: {
        /**
         * List workflows with run stats.
         */
        query(input: { orgId: string; enabled?: boolean }, opts?: RequestOptions): Promise<{ workflows: { id: string; name: string; enabled: boolean; lastRunAt: string | null; runCount: number; failureRate: number; }[]; }>;
      };
      /**
       * Workflow detail.
       */
      get: {
        /**
         * Workflow detail.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * Update workflow definition.
       */
      update: {
        /**
         * Update workflow definition.
         */
        mutate(input: { name?: string; description?: string; trigger?: { event: string; filters?: unknown; debounceMs?: number }; steps?: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete workflow.
       */
      delete: {
        /**
         * Delete workflow.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated run history.
       */
      runHistory: {
        /**
         * Paginated run history.
         */
        query(input: { workflowId: string; status?: "running" | "success" | "failed" | "cancelled"; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ runs: { id: string; status: string; startedAt: string; finishedAt: string | null; steps: { stepId: string; status: string; durationMs: number; error?: string | undefined; }[]; }[]; nextCursor: string | null; }>;
      };
      /**
       * Dry-run with streamed step outcomes.
       */
      testRun: {
        /**
         * Dry-run with streamed step outcomes.
         */
        stream(input: { workflowId: string; samplePayload: Record<string, unknown> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "step_result"; stepId: string; status: "success"; durationMs: number; output: Record<string, unknown>; }, void, unknown>>;
      };
    };
    documentsRoutes0: {
      /**
       * Documents in a project.
       */
      list: {
        /**
         * Documents in a project.
         */
        query(input: { projectId: string; page?: number; limit?: number; search?: string }, opts?: RequestOptions): Promise<{ documents: Pick<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "private" | "public" | "project"; authorId: string; collaborators: string[]; version: number; publishedAt: string | null; createdAt: string; updatedAt: string; }, "createdAt" | "id" | "updatedAt" | "title" | "authorId">[]; total: number; }>;
      };
      /**
       * Full document with blocks.
       */
      get: {
        /**
         * Full document with blocks.
         */
        query(input: { id: string; version?: number }, opts?: RequestOptions): Promise<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "project"; authorId: string; collaborators: never[]; version: number; publishedAt: null; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create document.
       */
      create: {
        /**
         * Create document.
         */
        mutate(input: { projectId: string; title: string; blocks: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility: "private" | "project" | "public"; authorId: string; collaborators: Array<string> }, opts?: RequestOptions): Promise<{ id: string; title: string; }>;
      };
      /**
       * Update blocks and metadata.
       */
      update: {
        /**
         * Update blocks and metadata.
         */
        mutate(input: { id: string; title?: string; blocks?: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility?: "private" | "project" | "public" }, opts?: RequestOptions): Promise<{ id: string; version: number; }>;
      };
      /**
       * Delete document.
       */
      delete: {
        /**
         * Delete document.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Publish document publicly.
       */
      publish: {
        /**
         * Publish document publicly.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ publishedAt: string; url: string; }>;
      };
      /**
       * Document version history.
       */
      history: {
        /**
         * Document version history.
         */
        query(input: { id: string; limit?: number }, opts?: RequestOptions): Promise<{ versions: { version: number; editor: string; changedAt: string; summary: string; }[]; }>;
      };
      /**
       * Collaborative edit event stream.
       */
      editStream: {
        /**
         * Collaborative edit event stream.
         */
        stream(input: { docId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "block_updated"; blockId: string; patch: Record<string, unknown>; editorId: string; }, void, unknown>>;
      };
    };
    permissionsRoutes0: {
      /**
       * All ABAC policies for org.
       */
      listPolicies: {
        /**
         * All ABAC policies for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ policies: { id: string; orgId: string; name: string; effect: "allow" | "deny"; resources: ("file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics")[]; actions: ("read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve")[]; conditions: { field: string; operator: "in" | "eq" | "neq" | "owned_by"; value: string | string[]; }[]; subjects: { type: "user" | "role" | "team"; id: string; }[]; priority: number; createdAt: string; }[]; }>;
      };
      /**
       * Create ABAC policy.
       */
      createPolicy: {
        /**
         * Create ABAC policy.
         */
        mutate(input: { orgId: string; name: string; effect: "allow" | "deny"; resources: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update policy definition.
       */
      updatePolicy: {
        /**
         * Update policy definition.
         */
        mutate(input: { orgId?: string; name?: string; effect?: "allow" | "deny"; resources?: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions?: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects?: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete policy.
       */
      deletePolicy: {
        /**
         * Delete policy.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Evaluate access for a subject.
       */
      checkAccess: {
        /**
         * Evaluate access for a subject.
         */
        query(input: { orgId: string; subjectId: string; resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }, opts?: RequestOptions): Promise<{ allowed: boolean; matchedPolicies: string[]; }>;
      };
      /**
       * Batch permission check.
       */
      bulkCheck: {
        /**
         * Batch permission check.
         */
        mutate(input: { orgId: string; subjectId: string; checks: Array<{ resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }> }, opts?: RequestOptions): Promise<{ results: { resource: "file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics"; action: "read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve"; allowed: boolean; }[]; }>;
      };
    };
    teamsRoutes0: {
      /**
       * Teams in an org.
       */
      list: {
        /**
         * Teams in an org.
         */
        query(input: { orgId: string; visibility?: "public" | "private" | "secret" }, opts?: RequestOptions): Promise<{ teams: { id: string; orgId: string; name: string; visibility: "private" | "public" | "secret"; memberCount: number; projectIds: string[]; createdAt: string; description?: string | undefined; color?: string | undefined; icon?: string | undefined; }[]; }>;
      };
      /**
       * Team by ID.
       */
      get: {
        /**
         * Team by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; description: undefined; color: string; icon: string; visibility: "public"; memberCount: number; projectIds: never[]; createdAt: string; }>;
      };
      /**
       * Create team.
       */
      create: {
        /**
         * Create team.
         */
        mutate(input: { orgId: string; name: string; description?: string; color?: string; icon?: string; visibility: "public" | "private" | "secret"; projectIds: Array<string> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update team metadata.
       */
      update: {
        /**
         * Update team metadata.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; color?: string; icon?: string; visibility?: "public" | "private" | "secret"; projectIds?: Array<string>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete team.
       */
      delete: {
        /**
         * Delete team.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Team member list.
       */
      members: {
        /**
         * Team member list.
         */
        query(input: { teamId: string; page?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "member" | "lead"; joinedAt: string; }[]; }>;
      };
      /**
       * Add user to team.
       */
      addMember: {
        /**
         * Add user to team.
         */
        mutate(input: { teamId: string; userId: string; role: "lead" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from team.
       */
      removeMember: {
        /**
         * Remove user from team.
         */
        mutate(input: { teamId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
    };
    invitesRoutes0: {
      /**
       * Pending invites for org.
       */
      list: {
        /**
         * Pending invites for org.
         */
        query(input: { orgId: string; status?: "pending" | "accepted" | "declined" | "expired" | "revoked" }, opts?: RequestOptions): Promise<{ invites: { id: string; orgId: string; email: string; role: "admin" | "owner" | "member" | "viewer"; status: "revoked" | "pending" | "accepted" | "declined" | "expired"; invitedBy: string; expiresAt: string; acceptedAt: string | null; teams: string[]; createdAt: string; personalMessage?: string | undefined; }[]; }>;
      };
      /**
       * Send invite.
       */
      create: {
        /**
         * Send invite.
         */
        mutate(input: { orgId: string; email: string; role: "owner" | "admin" | "member" | "viewer"; teams?: Array<string>; personalMessage?: string }, opts?: RequestOptions): Promise<{ id: string; email: string; }>;
      };
      /**
       * Bulk invite by email list.
       */
      bulkCreate: {
        /**
         * Bulk invite by email list.
         */
        mutate(input: { orgId: string; emails: Array<string>; role?: "admin" | "member" | "viewer"; teams?: Array<string> }, opts?: RequestOptions): Promise<{ invited: number; failed: string[]; }>;
      };
      /**
       * Accept invite via token.
       */
      accept: {
        /**
         * Accept invite via token.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ accepted: boolean; orgId: string; }>;
      };
      /**
       * Decline invite.
       */
      decline: {
        /**
         * Decline invite.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ declined: boolean; }>;
      };
      /**
       * Revoke pending invite.
       */
      revoke: {
        /**
         * Revoke pending invite.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Resend invite email.
       */
      resend: {
        /**
         * Resend invite email.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
    };
    commentsRoutes0: {
      /**
       * Comments for a resource.
       */
      list: {
        /**
         * Comments for a resource.
         */
        query(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; nextCursor: string | null; }>;
      };
      /**
       * Post a comment.
       */
      create: {
        /**
         * Post a comment.
         */
        mutate(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; authorId: string; body: string; mentions: Array<string>; attachments: Array<{ id: string; name: string; url: string }>; parentId: string | null }, opts?: RequestOptions): Promise<{ id: string; }>;
      };
      /**
       * Edit comment body.
       */
      update: {
        /**
         * Edit comment body.
         */
        mutate(input: { id: string; body: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete comment.
       */
      delete: {
        /**
         * Delete comment.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Toggle emoji reaction.
       */
      react: {
        /**
         * Toggle emoji reaction.
         */
        mutate(input: { commentId: string; emoji: string }, opts?: RequestOptions): Promise<{ added: boolean; emoji: string; }>;
      };
      /**
       * Threaded replies.
       */
      thread: {
        /**
         * Threaded replies.
         */
        query(input: { parentId: string; limit?: number }, opts?: RequestOptions): Promise<{ replies: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Stream new comments live.
       */
      liveStream: {
        /**
         * Stream new comments live.
         */
        stream(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "new_comment"; comment: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }; }, void, unknown>>;
      };
    };
    tagsRoutes0: {
      /**
       * All tags for org.
       */
      list: {
        /**
         * All tags for org.
         */
        query(input: { orgId: string; search?: string; sortBy?: "name" | "usageCount" }, opts?: RequestOptions): Promise<{ tags: { id: string; orgId: string; name: string; color: string; usageCount: number; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Create tag.
       */
      create: {
        /**
         * Create tag.
         */
        mutate(input: { orgId: string; name: string; color: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update tag name/color.
       */
      update: {
        /**
         * Update tag name/color.
         */
        mutate(input: { id: string; name?: string; color?: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete tag.
       */
      delete: {
        /**
         * Delete tag.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Merge multiple tags into one.
       */
      merge: {
        /**
         * Merge multiple tags into one.
         */
        mutate(input: { sourceIds: Array<string>; targetId: string }, opts?: RequestOptions): Promise<{ merged: number; targetId: string; }>;
      };
      /**
       * Attach tags to a resource.
       */
      attach: {
        /**
         * Attach tags to a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ attached: number; }>;
      };
      /**
       * Detach tags from a resource.
       */
      detach: {
        /**
         * Detach tags from a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ detached: number; }>;
      };
    };
    reportsRoutes0: {
      /**
       * Saved reports for org.
       */
      list: {
        /**
         * Saved reports for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; orgId: string; name: string; type: "custom" | "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest"; config: Record<string, unknown>; schedule: { cron: string; timezone: string; recipients: string[]; format: "csv" | "pdf" | "html"; } | null; lastRunAt: string | null; lastRunStatus: "success" | "failed" | "running" | null; createdBy: string; createdAt: string; }[]; }>;
      };
      /**
       * Create report definition.
       */
      create: {
        /**
         * Create report definition.
         */
        mutate(input: { orgId: string; name: string; type: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config: Record<string, unknown>; schedule: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update report.
       */
      update: {
        /**
         * Update report.
         */
        mutate(input: { orgId?: string; name?: string; type?: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config?: Record<string, unknown>; schedule?: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy?: string; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete report.
       */
      delete: {
        /**
         * Delete report.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Trigger report run.
       */
      run: {
        /**
         * Trigger report run.
         */
        mutate(input: { id: string; format?: "pdf" | "csv" | "html" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Get report download URL.
       */
      download: {
        /**
         * Get report download URL.
         */
        query(input: { id: string; runId: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream report generation.
       */
      stream: {
        /**
         * Stream report generation.
         */
        stream(input: { id: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; percent: number; stage: "collecting_data"; }, void, unknown>>;
      };
    };
    rateLimitsRoutes0: {
      /**
       * Rate limit rules for org.
       */
      listRules: {
        /**
         * Rate limit rules for org.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ rules: { id: string; orgId: string; name: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; targetId: string | null; endpoint: string | null; limits: { window: "minute" | "hour" | "day" | "second"; max: number; burstAllowance: number; }[]; action: "log" | "throttle" | "block"; enabled: boolean; createdAt: string; }[]; }>;
      };
      /**
       * Create rate limit rule.
       */
      createRule: {
        /**
         * Create rate limit rule.
         */
        mutate(input: { orgId: string; name: string; target: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId: string | null; endpoint: string | null; limits: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update rule.
       */
      updateRule: {
        /**
         * Update rule.
         */
        mutate(input: { orgId?: string; name?: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId?: string | null; endpoint?: string | null; limits?: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete rule.
       */
      deleteRule: {
        /**
         * Delete rule.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Current usage vs. limits.
       */
      currentStatus: {
        /**
         * Current usage vs. limits.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ statuses: { ruleId: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; window: string; current: number; max: number; resetAt: string; }[]; }>;
      };
      /**
       * Manually reset rate counter.
       */
      resetCounter: {
        /**
         * Manually reset rate counter.
         */
        mutate(input: { ruleId: string; targetId: string }, opts?: RequestOptions): Promise<{ reset: boolean; }>;
      };
    };
    exportsRoutes0: {
      /**
       * Queue an export job.
       */
      create: {
        /**
         * Queue an export job.
         */
        mutate(input: { orgId: string; resource: { resource: "tasks"; projectId?: string; filters?: Record<string, unknown> } | { resource: "users"; orgId: string } | { resource: "analytics"; orgId: string; metrics: Array<string>; dateRange: { from: string; to: string } } | { resource: "audit_log"; orgId: string; from: string; to: string } | { resource: "billing"; orgId: string; year: number } | { resource: "files"; resourceType: string; resourceId: string }; format: "json" | "csv" | "xlsx" | "pdf" | "ndjson" | "parquet"; notifyEmail?: string }, opts?: RequestOptions): Promise<{ id: string; status: "queued"; }>;
      };
      /**
       * Export job history.
       */
      list: {
        /**
         * Export job history.
         */
        query(input: { orgId: string; limit?: number }, opts?: RequestOptions): Promise<{ jobs: { id: string; orgId: string; resource: { resource: "tasks"; projectId?: string | undefined; filters?: Record<string, unknown> | undefined; } | { resource: "users"; orgId: string; } | { resource: "analytics"; orgId: string; metrics: string[]; dateRange: { from: string; to: string; }; } | { resource: "audit_log"; orgId: string; from: string; to: string; } | { resource: "billing"; orgId: string; year: number; } | { resource: "files"; resourceType: string; resourceId: string; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done" | "failed" | "queued" | "running"; progress: number; rowCount: number | null; downloadUrl: string | null; expiresAt: string | null; error: string | null; createdAt: string; }[]; }>;
      };
      /**
       * Export job status.
       */
      get: {
        /**
         * Export job status.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; resource: { resource: "tasks"; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done"; progress: number; rowCount: number; downloadUrl: string; expiresAt: string; error: null; createdAt: string; }>;
      };
      /**
       * Cancel queued export.
       */
      cancel: {
        /**
         * Cancel queued export.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ cancelled: boolean; }>;
      };
      /**
       * Fresh signed download URL.
       */
      download: {
        /**
         * Fresh signed download URL.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream export progress.
       */
      progressStream: {
        /**
         * Stream export progress.
         */
        stream(input: { jobId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; jobId: string; percent: number; rowsProcessed: number; }, void, unknown>>;
      };
    };
    authRoutes1: {
      /**
       * Multi-method login — password, OAuth, SAML, magic link, passkey.
       */
      login: {
        /**
         * Multi-method login — password, OAuth, SAML, magic link, passkey.
         */
        mutate(input: { method: "password"; email: string; password: string; totpCode?: string } | { method: "oauth"; provider: "google" | "github" | "microsoft" | "okta"; code: string; redirectUri: string; state: string } | { method: "saml"; orgSlug: string; samlResponse: string; relayState?: string } | { method: "magic_link"; token: string } | { method: "passkey"; credentialId: string; clientDataJSON: string; authenticatorData: string; signature: string } & { device?: { userAgent: string; ip: string; fingerprint?: string; platform?: "web" | "ios" | "android" | "desktop" } }, opts?: RequestOptions): Promise<{ tokens: { accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }; user: { id: UserId; role: "admin"; }; }>;
      };
      /**
       * Rotate refresh token.
       */
      refresh: {
        /**
         * Rotate refresh token.
         */
        mutate(input: { refreshToken: string; sessionId: string }, opts?: RequestOptions): Promise<{ accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }>;
      };
      /**
       * Invalidate session.
       */
      logout: {
        /**
         * Invalidate session.
         */
        mutate(input: { sessionId: string }, opts?: RequestOptions): Promise<{ revoked: boolean; sessionId: SessionId; }>;
      };
      /**
       * Validate MFA code after credential check.
       */
      mfaChallenge: {
        /**
         * Validate MFA code after credential check.
         */
        mutate(input: { challengeToken: string; code: string; method: "totp" | "sms" | "email" | "webauthn" }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Send password reset email.
       */
      forgotPassword: {
        /**
         * Send password reset email.
         */
        mutate(input: { email: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
      /**
       * Apply new password via reset token.
       */
      resetPassword: {
        /**
         * Apply new password via reset token.
         */
        mutate(input: { token: string; newPassword: string }, opts?: RequestOptions): Promise<{ success: boolean; }>;
      };
      /**
       * List active sessions with device info.
       */
      sessions: {
        /**
         * List active sessions with device info.
         */
        query(input: { cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ sessions: { id: SessionId; device: { userAgent: string; ip: string; fingerprint?: string | undefined; platform?: "web" | "ios" | "android" | "desktop" | undefined; }; createdAt: string; current: boolean; }[]; nextCursor: string | null; }>;
      };
      /**
       * Current authenticated user.
       */
      me: {
        /**
         * Current authenticated user.
         */
        query(input: any, opts?: RequestOptions): Promise<{ id: UserId; role: string; }>;
      };
      /**
       * Confirm email address via verification code.
       */
      verifyEmail: {
        /**
         * Confirm email address via verification code.
         */
        mutate(input: { code: string }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Stream login activity events for the current user.
       */
      deviceStream: {
        /**
         * Stream login activity events for the current user.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "session_created"; sessionId: SessionId; ts: string; }, void, unknown>>;
      };
    };
    usersRoutes1: {
      /**
       * Paginated user list with rich filters.
       */
      list: {
        /**
         * Paginated user list with rich filters.
         */
        query(input: { page?: number; limit?: number; roles?: Array<"owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support">; search?: string; sortBy?: "name" | "email" | "createdAt"; sortDir?: "asc" | "desc" }, opts?: RequestOptions): Promise<{ users: { id: string; email: string; emailVerified: boolean; name: string; avatarUrl: string | null; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: string[]; metadata: Record<string, string | number | boolean>; createdAt: string; updatedAt: string; displayName?: string | undefined; bio?: string | undefined; timezone?: string | undefined; locale?: string | undefined; address?: { line1: string; city: string; postalCode: string; country: string; line2?: string | undefined; state?: string | undefined; } | undefined; }[]; total: number; page: number; }>;
      };
      /**
       * Fetch single user with full profile.
       */
      get: {
        /**
         * Fetch single user with full profile.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; email: string; emailVerified: true; name: string; displayName: undefined; avatarUrl: null; bio: undefined; timezone: string; locale: string; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: never[]; metadata: {}; address: undefined; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Partial profile update with nested address.
       */
      update: {
        /**
         * Partial profile update with nested address.
         */
        mutate(input: { name?: string; displayName?: string; bio?: string; timezone?: string; locale?: string; avatarUrl?: string | null; address?: { line1: string; line2?: string; city: string; state?: string; postalCode: string; country: string }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Soft-delete user.
       */
      deactivate: {
        /**
         * Soft-delete user.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ deactivated: boolean; }>;
      };
      /**
       * Change role for many users atomically.
       */
      bulkRoleChange: {
        /**
         * Change role for many users atomically.
         */
        mutate(input: { userIds: Array<string>; role: "owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support"; reason?: string }, opts?: RequestOptions): Promise<{ updated: number; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; }>;
      };
      /**
       * All permissions grouped by resource for a user.
       */
      permissionMatrix: {
        /**
         * All permissions grouped by resource for a user.
         */
        query(input: { userId: string }, opts?: RequestOptions): Promise<{ userId: string; matrix: Record<string, Record<string, boolean>>; }>;
      };
      /**
       * Upload and crop user avatar.
       */
      uploadAvatar: {
        /**
         * Upload and crop user avatar.
         */
        upload(input: { file: File; cropSize?: number } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileName: string; size: number; croppedTo: number; }>;
      };
      /**
       * Change own password — requires MFA.
       */
      changePassword: {
        /**
         * Change own password — requires MFA.
         */
        mutate(input: { currentPassword: string; newPassword: string }, opts?: RequestOptions): Promise<{ changed: boolean; }>;
      };
      /**
       * GDPR data export streamed by resource type.
       */
      exportData: {
        /**
         * GDPR data export streamed by resource type.
         */
        stream(input: { userId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "chunk"; resource: "profile" | "tasks" | "comments" | "files"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    organizationsRoutes1: {
      /**
       * Orgs accessible to caller.
       */
      list: {
        /**
         * Orgs accessible to caller.
         */
        query(input: { page?: number; limit?: number }, opts?: RequestOptions): Promise<{ orgs: { id: string; name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; memberCount: number; logoUrl: string | null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }[]; total: number; }>;
      };
      /**
       * Fetch org by ID.
       */
      get: {
        /**
         * Fetch org by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; slug: string; plan: "enterprise"; memberCount: number; logoUrl: null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }>;
      };
      /**
       * Create new organization.
       */
      create: {
        /**
         * Create new organization.
         */
        mutate(input: { name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; logoUrl: string | null; settings: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> }; ownerId: string }, opts?: RequestOptions): Promise<{ id: string; slug: string; }>;
      };
      /**
       * Update org metadata and settings.
       */
      update: {
        /**
         * Update org metadata and settings.
         */
        mutate(input: { id: string; name?: string; logoUrl?: string | null; settings?: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> } }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Permanently delete org.
       */
      delete: {
        /**
         * Permanently delete org.
         */
        mutate(input: { id: string; confirmSlug: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated org member list.
       */
      members: {
        /**
         * Paginated org member list.
         */
        query(input: { orgId: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: string; joinedAt: string; }[]; total: number; }>;
      };
      /**
       * Add user to org with role.
       */
      addMember: {
        /**
         * Add user to org with role.
         */
        mutate(input: { orgId: string; userId: string; role: "owner" | "admin" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from org.
       */
      removeMember: {
        /**
         * Remove user from org.
         */
        mutate(input: { orgId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
      /**
       * Transfer org ownership — requires MFA.
       */
      transferOwnership: {
        /**
         * Transfer org ownership — requires MFA.
         */
        mutate(input: { orgId: string; newOwnerId: string; reason?: string }, opts?: RequestOptions): Promise<{ transferred: boolean; }>;
      };
      /**
       * Stream org audit log entries live.
       */
      auditStream: {
        /**
         * Stream org audit log entries live.
         */
        stream(input: { orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; action: string; actorId: string; ts: string; }, void, unknown>>;
      };
    };
    billingRoutes1: {
      /**
       * Full subscription with pricing model.
       */
      getSubscription: {
        /**
         * Full subscription with pricing model.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; tier: "pro"; pricing: { model: "per_seat"; pricePerSeatMonth: number; minSeats: number; maxSeats: null; }; status: "active"; seats: number; currentPeriodStart: string; currentPeriodEnd: string; cancelAtPeriodEnd: false; metadata: {}; addons: never[]; }>;
      };
      /**
       * Calculate proration before confirming.
       */
      previewUpgrade: {
        /**
         * Calculate proration before confirming.
         */
        query(input: { orgId: string; targetTier: "starter" | "pro" | "business" | "enterprise"; targetSeats: number; billingInterval: "monthly" | "annual" }, opts?: RequestOptions): Promise<{ currentAmount: number; newAmount: number; prorationAmount: number; effectiveDate: string; lineItems: { description: string; amount: number; currency: string; }[]; }>;
      };
      /**
       * Create Stripe checkout session.
       */
      createCheckout: {
        /**
         * Create Stripe checkout session.
         */
        mutate(input: { orgId: string; tier: "starter" | "pro" | "business" | "enterprise"; seats: number; billingInterval: "monthly" | "annual"; addons?: Array<{ id: string; quantity: number }>; successUrl: string; cancelUrl: string; couponCode?: string }, opts?: RequestOptions): Promise<{ checkoutUrl: string; sessionId: string; }>;
      };
      /**
       * Schedule cancellation at period end.
       */
      cancelSubscription: {
        /**
         * Schedule cancellation at period end.
         */
        mutate(input: { orgId: string; reason?: string }, opts?: RequestOptions): Promise<{ cancelAtPeriodEnd: boolean; }>;
      };
      /**
       * Paginated invoice list.
       */
      invoices: {
        /**
         * Paginated invoice list.
         */
        query(input: { orgId: string; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ invoices: { id: string; amount: number; currency: string; status: string; date: string; }[]; nextCursor: string | null; }>;
      };
      /**
       * Metered usage breakdown.
       */
      usageReport: {
        /**
         * Metered usage breakdown.
         */
        query(input: { orgId: string; breakdown?: "daily" | "weekly" | "monthly" }, opts?: RequestOptions): Promise<{ period: { start: string; end: string; }; included: number; consumed: number; overage: number; overageCost: number; series: { date: string; units: number; cost: number; }[]; }>;
      };
      /**
       * Apply promotional discount code.
       */
      applyPromoCode: {
        /**
         * Apply promotional discount code.
         */
        mutate(input: { orgId: string; code: string }, opts?: RequestOptions): Promise<{ applied: boolean; discountPercent: number; }>;
      };
      /**
       * Stream invoice line items.
       */
      invoiceStream: {
        /**
         * Stream invoice line items.
         */
        stream(input: { invoiceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "line_item"; description: string; amount: number; currency: string; }, void, unknown>>;
      };
    };
    projectsRoutes1: {
      /**
       * Paginated project list with status filters.
       */
      list: {
        /**
         * Paginated project list with status filters.
         */
        query(input: { orgId: string; status?: Array<"planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled">; search?: string; page?: number }, opts?: RequestOptions): Promise<{ projects: { id: string; orgId: string; name: string; status: "active" | "cancelled" | "planning" | "on_hold" | "completed" | "archived"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: string[]; customFields: Record<string, string | number | boolean | null>; createdAt: string; updatedAt: string; description?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Project by ID.
       */
      get: {
        /**
         * Project by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; status: "active"; visibility: "private"; startDate: null; endDate: null; ownerId: string; tags: never[]; customFields: {}; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create project.
       */
      create: {
        /**
         * Create project.
         */
        mutate(input: { orgId: string; name: string; description?: string; status: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: Array<string>; customFields: Record<string, string | number | boolean | null> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update project fields.
       */
      update: {
        /**
         * Update project fields.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; status?: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility?: "private" | "internal" | "public"; startDate?: string | null; endDate?: string | null; ownerId?: string; tags?: Array<string>; customFields?: Record<string, string | number | boolean | null>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Archive project.
       */
      archive: {
        /**
         * Archive project.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ archived: boolean; }>;
      };
      /**
       * Project member list.
       */
      members: {
        /**
         * Project member list.
         */
        query(input: { projectId: string }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "viewer" | "lead" | "contributor"; joinedAt: string; }[]; }>;
      };
      /**
       * Add project member.
       */
      addMember: {
        /**
         * Add project member.
         */
        mutate(input: { projectId: string; userId: string; role: "lead" | "contributor" | "viewer" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Aggregate project stats.
       */
      stats: {
        /**
         * Aggregate project stats.
         */
        query(input: { projectId: string; period?: "7d" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ tasks: number; completed: number; openIssues: number; velocity: number; burndownPoints: { date: string; remaining: number; }[]; }>;
      };
      /**
       * Clone project structure.
       */
      duplicate: {
        /**
         * Clone project structure.
         */
        mutate(input: { id: string; newName: string; includeTasks?: boolean; includeMembers?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream full project export.
       */
      exportStream: {
        /**
         * Stream full project export.
         */
        stream(input: { id: string; format: "json" | "csv" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; resource: "task"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    tasksRoutes1: {
      /**
       * Create task with full field set.
       */
      create: {
        /**
         * Create task with full field set.
         */
        mutate(input: { projectId: string; title: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }> }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; }>;
      };
      /**
       * Task tree with recursive subtasks.
       */
      get: {
        /**
         * Task tree with recursive subtasks.
         */
        query(input: { id: string; depth?: number }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; subtasks: any[]; customFields: Record<string, { type: "text"; value: string; } | { type: "number"; value: number; } | { type: "date"; value: string; } | { type: "boolean"; value: boolean; } | { type: "select"; value: string; optionId: string; } | { type: "multi"; value: string[]; optionIds: string[]; } | { type: "user"; value: string; } | { type: "url"; value: string; }>; }>;
      };
      /**
       * Patch any task fields.
       */
      update: {
        /**
         * Patch any task fields.
         */
        mutate(input: { projectId?: string; title?: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete task permanently.
       */
      delete: {
        /**
         * Delete task permanently.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Batch status/priority update.
       */
      bulkUpdate: {
        /**
         * Batch status/priority update.
         */
        mutate(input: { ids: Array<string>; patch: { status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; dueDate?: string | null } }, opts?: RequestOptions): Promise<{ updated: number; }>;
      };
      /**
       * Comment thread for a task.
       */
      comments: {
        /**
         * Comment thread for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; body: string; author: string; createdAt: string; reactions: Record<string, string[]>; }[]; }>;
      };
      /**
       * Post comment on task.
       */
      addComment: {
        /**
         * Post comment on task.
         */
        mutate(input: { taskId: string; body: string; mentions?: Array<string> }, opts?: RequestOptions): Promise<{ id: string; body: string; }>;
      };
      /**
       * Typed audit trail for a task.
       */
      timelineEvents: {
        /**
         * Typed audit trail for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ events: ({ type: "created"; actor: string; ts: string; } | { type: "status_change"; actor: string; ts: string; from: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; to: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; } | { type: "assigned"; actor: string; ts: string; userId: string; })[]; }>;
      };
      /**
       * Tasks assigned to current user.
       */
      myTasks: {
        /**
         * Tasks assigned to current user.
         */
        query(input: { status?: Array<"backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix">; projectId?: string; limit?: number }, opts?: RequestOptions): Promise<{ tasks: any[]; total: number; }>;
      };
      /**
       * Live task update stream.
       */
      activityStream: {
        /**
         * Live task update stream.
         */
        stream(input: { projectId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "task_updated"; taskId: string; field: string; value: string; }, void, unknown>>;
      };
    };
    filesRoutes1: {
      /**
       * Files for a resource.
       */
      list: {
        /**
         * Files for a resource.
         */
        query(input: { resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; mimeTypes?: Array<string>; page?: number }, opts?: RequestOptions): Promise<{ files: { id: string; name: string; mimeType: string; sizeBytes: number; status: "deleted" | "pending" | "processing" | "ready" | "failed"; url: string | null; thumbnailUrl: string | null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: Record<string, string | number>; createdAt: string; }[]; total: number; }>;
      };
      /**
       * File metadata by ID.
       */
      get: {
        /**
         * File metadata by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; mimeType: string; sizeBytes: number; status: "ready"; url: string; thumbnailUrl: null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: {}; createdAt: string; }>;
      };
      /**
       * Get presigned S3 upload URL.
       */
      requestUpload: {
        /**
         * Get presigned S3 upload URL.
         */
        mutate(input: { fileName: string; mimeType: string; sizeBytes: number; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string }, opts?: RequestOptions): Promise<{ uploadUrl: string; fileId: string; expiresIn: number; }>;
      };
      /**
       * Mark pending upload complete.
       */
      confirmUpload: {
        /**
         * Mark pending upload complete.
         */
        mutate(input: { fileId: string; etag: string }, opts?: RequestOptions): Promise<{ id: string; confirmed: boolean; url: string; }>;
      };
      /**
       * Delete file and storage object.
       */
      delete: {
        /**
         * Delete file and storage object.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Delete multiple files.
       */
      bulkDelete: {
        /**
         * Delete multiple files.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ deleted: number; }>;
      };
      /**
       * Multipart direct file upload.
       */
      directUpload: {
        /**
         * Multipart direct file upload.
         */
        upload(input: { file: File; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; description?: string } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileId: string; name: string; size: number; }>;
      };
      /**
       * Stream file processing events.
       */
      processStream: {
        /**
         * Stream file processing events.
         */
        stream(input: { fileId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; fileId: string; stage: "ready" | "queued" | "virus_scan" | "thumbnail"; percent: number; }, void, unknown>>;
      };
    };
    notificationsRoutes1: {
      /**
       * Paginated notification list.
       */
      list: {
        /**
         * Paginated notification list.
         */
        query(input: { unreadOnly?: boolean; types?: Array<"mention" | "task_assigned" | "task_completed" | "comment" | "billing" | "security" | "system" | "workflow_run" | "integration_sync">; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ notifications: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }[]; unreadCount: number; nextCursor: string | null; }>;
      };
      /**
       * Mark notifications as read.
       */
      markRead: {
        /**
         * Mark notifications as read.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ marked: number; }>;
      };
      /**
       * Mark all notifications read.
       */
      markAllRead: {
        /**
         * Mark all notifications read.
         */
        mutate(input: any, opts?: RequestOptions): Promise<{ marked: boolean; }>;
      };
      /**
       * Get notification preferences.
       */
      preferences: {
        /**
         * Get notification preferences.
         */
        query(input: any, opts?: RequestOptions): Promise<{ channels: Record<string, boolean>; types: Partial<Record<"billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync", boolean>>; }>;
      };
      /**
       * Update preferences.
       */
      updatePreferences: {
        /**
         * Update preferences.
         */
        mutate(input: { channels?: Record<string, boolean>; types?: Record<string, boolean> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Register Web Push subscription.
       */
      pushSubscribe: {
        /**
         * Register Web Push subscription.
         */
        mutate(input: { endpoint: string; keys: { p256dh: string; auth: string } }, opts?: RequestOptions): Promise<{ subscribed: boolean; }>;
      };
      /**
       * Real-time notification SSE.
       */
      liveStream: {
        /**
         * Real-time notification SSE.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "notification"; notification: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }; }, void, unknown>>;
      };
    };
    searchRoutes1: {
      /**
       * Full-text search across resource types.
       */
      global: {
        /**
         * Full-text search across resource types.
         */
        query(input: { q: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document">; orgId: string; limit?: number; highlight?: boolean }, opts?: RequestOptions): Promise<{ results: ({ type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; })[]; total: number; took: number; }>;
      };
      /**
       * Autocomplete suggestions.
       */
      suggestions: {
        /**
         * Autocomplete suggestions.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ suggestions: { label: string; type: "user" | "file" | "task" | "project" | "comment" | "workflow" | "document"; id: string; }[]; }>;
      };
      /**
       * Faceted filter counts.
       */
      filters: {
        /**
         * Faceted filter counts.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ facets: Record<"user" | "file" | "task" | "project" | "comment" | "workflow" | "document", number>; }>;
      };
      /**
       * Trigger manual reindex for org.
       */
      reindex: {
        /**
         * Trigger manual reindex for org.
         */
        mutate(input: { orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream results progressively.
       */
      stream: {
        /**
         * Stream results progressively.
         */
        stream(input: { q: string; orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "result"; result: { type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; }; }, void, unknown>>;
      };
    };
    analyticsRoutes1: {
      /**
       * Arbitrary analytics query.
       */
      query: {
        /**
         * Arbitrary analytics query.
         */
        mutate(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }, opts?: RequestOptions): Promise<{ rows: never[]; totals: {}; comparison: undefined; meta: { executionMs: number; rowsScanned: number; cached: false; granularity: "minute" | "hour" | "day" | "week" | "month"; }; }>;
      };
      /**
       * Multi-step funnel conversion.
       */
      funnel: {
        /**
         * Multi-step funnel conversion.
         */
        mutate(input: { orgId: string; steps: Array<{ name: string; event: string; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }> }>; dateRange: { from: string; to: string } }, opts?: RequestOptions): Promise<{ steps: { name: string; count: number; conversionRate: number; avgTimeToNextStep: number | null; }[]; overallConversion: number; }>;
      };
      /**
       * Cohort retention analysis.
       */
      retention: {
        /**
         * Cohort retention analysis.
         */
        mutate(input: { orgId: string; cohortEvent: string; returnEvent: string; dateRange: { from: string; to: string }; granularity?: "minute" | "hour" | "day" | "week" | "month" }, opts?: RequestOptions): Promise<{ cohorts: { date: string; size: number; retention: number[]; }[]; }>;
      };
      /**
       * Saved report definitions.
       */
      savedReports: {
        /**
         * Saved report definitions.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; name: string; query: { orgId: string; metrics: string[]; dimensions: string[]; filters: { field: string; operator: { op: "eq"; value: string | number | boolean; } | { op: "neq"; value: string | number | boolean; } | { op: "gt"; value: number; } | { op: "gte"; value: number; } | { op: "lt"; value: number; } | { op: "lte"; value: number; } | { op: "in"; values: (string | number)[]; } | { op: "between"; min: number; max: number; }; }[]; dateRange: { from: string; to: string; granularity: "minute" | "hour" | "day" | "week" | "month"; timezone: string; }; orderBy: { field: string; direction: "asc" | "desc"; }[]; limit: number; compareWith?: { from: string; to: string; } | undefined; }; schedule: { cron: string; recipients: string[]; } | null; lastRunAt: string | null; }[]; }>;
      };
      /**
       * Persist a report definition.
       */
      saveReport: {
        /**
         * Persist a report definition.
         */
        mutate(input: { orgId: string; name: string; query: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }; schedule?: { cron: string; recipients: Array<string> } }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream query results row by row.
       */
      exportStream: {
        /**
         * Stream query results row by row.
         */
        stream(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string }; format?: "json" | "csv" | "parquet" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; index: number; data: Record<string, string | number | null>; }, void, unknown>>;
      };
    };
    webhooksRoutes1: {
      /**
       * Webhooks for an org.
       */
      list: {
        /**
         * Webhooks for an org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ webhooks: { id: string; orgId: string; url: string; events: ("task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed")[]; enabled: boolean; secret: string; headers: Record<string, string>; retryPolicy: { maxAttempts: number; backoffMs: number; }; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Register webhook endpoint.
       */
      create: {
        /**
         * Register webhook endpoint.
         */
        mutate(input: { orgId: string; url: string; description?: string; events: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled: boolean; secret: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number } }, opts?: RequestOptions): Promise<{ id: string; url: string; }>;
      };
      /**
       * Update webhook.
       */
      update: {
        /**
         * Update webhook.
         */
        mutate(input: { orgId?: string; url?: string; description?: string; events?: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled?: boolean; secret?: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Remove webhook.
       */
      delete: {
        /**
         * Remove webhook.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Recent delivery attempts.
       */
      deliveries: {
        /**
         * Recent delivery attempts.
         */
        query(input: { webhookId: string; status?: "success" | "failed" | "pending"; limit?: number }, opts?: RequestOptions): Promise<{ deliveries: { id: string; status: number; attempt: number; ts: string; error: string | null; }[]; }>;
      };
      /**
       * Retry failed delivery.
       */
      redeliver: {
        /**
         * Retry failed delivery.
         */
        mutate(input: { deliveryId: string }, opts?: RequestOptions): Promise<{ queued: boolean; }>;
      };
      /**
       * Send test event to webhook.
       */
      testPing: {
        /**
         * Send test event to webhook.
         */
        mutate(input: { webhookId: string; eventType: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed" }, opts?: RequestOptions): Promise<{ sent: boolean; deliveryId: string; }>;
      };
      /**
       * Stream webhook delivery events.
       */
      logStream: {
        /**
         * Stream webhook delivery events.
         */
        stream(input: { webhookId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "delivery"; status: number; ts: string; }, void, unknown>>;
      };
    };
    aiRoutes1: {
      /**
       * Multi-model streaming chat with tool-call support.
       */
      chat: {
        /**
         * Multi-model streaming chat with tool-call support.
         */
        stream(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "content_delta"; text: string; finishReason?: undefined; usage?: undefined; } | { type: "finish"; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; text?: undefined; }, void, unknown>>;
      };
      /**
       * Non-streaming completion.
       */
      complete: {
        /**
         * Non-streaming completion.
         */
        mutate(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string>; stream?: false }, opts?: RequestOptions): Promise<{ id: string; role: "assistant"; content: string | ({ type: "text"; text: string; } | { type: "image_url"; url: string; detail: "high" | "low" | "auto"; } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown>; } | { type: "tool_result"; toolCallId: string; content: string; isError: boolean; })[]; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; }>;
      };
      /**
       * Batch embedding generation.
       */
      embed: {
        /**
         * Batch embedding generation.
         */
        mutate(input: { inputs: Array<string | { text: string; id: string }>; model: "text-embedding-3-small" | "text-embedding-3-large"; dimensions?: number; outputFormat?: "float" | "base64" | "int8" }, opts?: RequestOptions): Promise<{ embeddings: { index: number; embedding: number[]; inputTokens: number; }[]; model: "text-embedding-3-small" | "text-embedding-3-large"; totalTokens: number; }>;
      };
      /**
       * Run content moderation checks.
       */
      moderateContent: {
        /**
         * Run content moderation checks.
         */
        mutate(input: { inputs: Array<{ id: string; text: string }>; categories?: Array<"hate" | "harassment" | "self_harm" | "sexual" | "violence" | "dangerous"> }, opts?: RequestOptions): Promise<{ results: { id: string; flagged: boolean; categories: Partial<Record<string, boolean>>; scores: Partial<Record<string, number>>; }[]; }>;
      };
      /**
       * Structured text summarization.
       */
      summarize: {
        /**
         * Structured text summarization.
         */
        mutate(input: { text: string; format?: "bullets" | "paragraph" | "tldr"; maxLength?: number }, opts?: RequestOptions): Promise<{ summary: string; wordCount: number; }>;
      };
      /**
       * AI token usage for billing period.
       */
      usageStats: {
        /**
         * AI token usage for billing period.
         */
        query(input: { orgId: string; period?: "current" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ promptTokens: number; completionTokens: number; totalCost: number; byModel: Record<string, { tokens: number; cost: number; }>; }>;
      };
    };
    auditRoutes1: {
      /**
       * Paginated audit log with filters.
       */
      log: {
        /**
         * Paginated audit log with filters.
         */
        query(input: { orgId: string; actorId?: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled">; resourceType?: string; from?: string; to?: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ entries: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }[]; total: number; }>;
      };
      /**
       * Single audit entry by ID.
       */
      getEntry: {
        /**
         * Single audit entry by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; actorId: string; actorEmail: string; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: null; resourceId: null; metadata: {}; ip: string; userAgent: string; ts: string; }>;
      };
      /**
       * Update log retention policy.
       */
      retention: {
        /**
         * Update log retention policy.
         */
        mutate(input: { orgId: string; retentionDays: number }, opts?: RequestOptions): Promise<{ updated: boolean; retentionDays: number; }>;
      };
      /**
       * Stream audit log as NDJSON.
       */
      export: {
        /**
         * Stream audit log as NDJSON.
         */
        stream(input: { orgId: string; from: string; to: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; data: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }; }, void, unknown>>;
      };
    };
    apiKeysRoutes1: {
      /**
       * List API keys for org.
       */
      list: {
        /**
         * List API keys for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ keys: { id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: string | null; lastUsedAt: string | null; createdBy: string; createdAt: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number; } | null; }[]; }>;
      };
      /**
       * Create API key.
       */
      create: {
        /**
         * Create API key.
         */
        mutate(input: { orgId: string; name: string; scopes: Array<"read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write">; expiresAt: string | null; createdBy: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number } | null }, opts?: RequestOptions): Promise<{ key: string; id: string; prefix: string; }>;
      };
      /**
       * Revoke API key.
       */
      revoke: {
        /**
         * Revoke API key.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Rotate key secret.
       */
      rotate: {
        /**
         * Rotate key secret.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ newKey: string; id: string; }>;
      };
      /**
       * API key metadata.
       */
      get: {
        /**
         * API key metadata.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: null; lastUsedAt: null; createdBy: string; createdAt: string; rateLimit: null; }>;
      };
      /**
       * Key request usage stats.
       */
      usage: {
        /**
         * Key request usage stats.
         */
        query(input: { id: string; period?: "1h" | "24h" | "7d" | "30d" }, opts?: RequestOptions): Promise<{ requests: number; errors: number; p50Ms: number; p99Ms: number; series: { ts: string; count: number; }[]; }>;
      };
    };
    integrationsRoutes1: {
      /**
       * All integrations with status.
       */
      list: {
        /**
         * All integrations with status.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ integrations: { id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: boolean; enabled: boolean; status: "error" | "active" | "disconnected" | "syncing"; lastSyncAt: string | null; errorMessage: string | null; config: Record<string, unknown>; }[]; }>;
      };
      /**
       * Single integration status.
       */
      get: {
        /**
         * Single integration status.
         */
        query(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: true; enabled: true; status: "active"; lastSyncAt: string; errorMessage: null; config: {}; }>;
      };
      /**
       * Start OAuth install flow.
       */
      install: {
        /**
         * Start OAuth install flow.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; redirectUri: string; scopes?: Array<string> }, opts?: RequestOptions): Promise<{ authUrl: string; }>;
      };
      /**
       * Exchange OAuth code for tokens.
       */
      oauthCallback: {
        /**
         * Exchange OAuth code for tokens.
         */
        mutate(input: { code: string; state: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ installed: boolean; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; }>;
      };
      /**
       * Remove integration.
       */
      uninstall: {
        /**
         * Remove integration.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ uninstalled: boolean; }>;
      };
      /**
       * Update integration config.
       */
      updateConfig: {
        /**
         * Update integration config.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; config: Record<string, unknown> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Manual sync trigger.
       */
      triggerSync: {
        /**
         * Manual sync trigger.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream sync progress events.
       */
      syncStream: {
        /**
         * Stream sync progress events.
         */
        stream(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; itemsSynced: number; total: number; }, void, unknown>>;
      };
    };
    workflowsRoutes1: {
      /**
       * Create workflow with recursive condition tree.
       */
      create: {
        /**
         * Create workflow with recursive condition tree.
         */
        mutate(input: { name: string; description?: string; trigger: { event: string; filters?: unknown; debounceMs?: number }; steps: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; orgId: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * List workflows with run stats.
       */
      list: {
        /**
         * List workflows with run stats.
         */
        query(input: { orgId: string; enabled?: boolean }, opts?: RequestOptions): Promise<{ workflows: { id: string; name: string; enabled: boolean; lastRunAt: string | null; runCount: number; failureRate: number; }[]; }>;
      };
      /**
       * Workflow detail.
       */
      get: {
        /**
         * Workflow detail.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * Update workflow definition.
       */
      update: {
        /**
         * Update workflow definition.
         */
        mutate(input: { name?: string; description?: string; trigger?: { event: string; filters?: unknown; debounceMs?: number }; steps?: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete workflow.
       */
      delete: {
        /**
         * Delete workflow.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated run history.
       */
      runHistory: {
        /**
         * Paginated run history.
         */
        query(input: { workflowId: string; status?: "running" | "success" | "failed" | "cancelled"; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ runs: { id: string; status: string; startedAt: string; finishedAt: string | null; steps: { stepId: string; status: string; durationMs: number; error?: string | undefined; }[]; }[]; nextCursor: string | null; }>;
      };
      /**
       * Dry-run with streamed step outcomes.
       */
      testRun: {
        /**
         * Dry-run with streamed step outcomes.
         */
        stream(input: { workflowId: string; samplePayload: Record<string, unknown> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "step_result"; stepId: string; status: "success"; durationMs: number; output: Record<string, unknown>; }, void, unknown>>;
      };
    };
    documentsRoutes1: {
      /**
       * Documents in a project.
       */
      list: {
        /**
         * Documents in a project.
         */
        query(input: { projectId: string; page?: number; limit?: number; search?: string }, opts?: RequestOptions): Promise<{ documents: Pick<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "private" | "public" | "project"; authorId: string; collaborators: string[]; version: number; publishedAt: string | null; createdAt: string; updatedAt: string; }, "createdAt" | "id" | "updatedAt" | "title" | "authorId">[]; total: number; }>;
      };
      /**
       * Full document with blocks.
       */
      get: {
        /**
         * Full document with blocks.
         */
        query(input: { id: string; version?: number }, opts?: RequestOptions): Promise<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "project"; authorId: string; collaborators: never[]; version: number; publishedAt: null; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create document.
       */
      create: {
        /**
         * Create document.
         */
        mutate(input: { projectId: string; title: string; blocks: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility: "private" | "project" | "public"; authorId: string; collaborators: Array<string> }, opts?: RequestOptions): Promise<{ id: string; title: string; }>;
      };
      /**
       * Update blocks and metadata.
       */
      update: {
        /**
         * Update blocks and metadata.
         */
        mutate(input: { id: string; title?: string; blocks?: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility?: "private" | "project" | "public" }, opts?: RequestOptions): Promise<{ id: string; version: number; }>;
      };
      /**
       * Delete document.
       */
      delete: {
        /**
         * Delete document.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Publish document publicly.
       */
      publish: {
        /**
         * Publish document publicly.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ publishedAt: string; url: string; }>;
      };
      /**
       * Document version history.
       */
      history: {
        /**
         * Document version history.
         */
        query(input: { id: string; limit?: number }, opts?: RequestOptions): Promise<{ versions: { version: number; editor: string; changedAt: string; summary: string; }[]; }>;
      };
      /**
       * Collaborative edit event stream.
       */
      editStream: {
        /**
         * Collaborative edit event stream.
         */
        stream(input: { docId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "block_updated"; blockId: string; patch: Record<string, unknown>; editorId: string; }, void, unknown>>;
      };
    };
    permissionsRoutes1: {
      /**
       * All ABAC policies for org.
       */
      listPolicies: {
        /**
         * All ABAC policies for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ policies: { id: string; orgId: string; name: string; effect: "allow" | "deny"; resources: ("file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics")[]; actions: ("read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve")[]; conditions: { field: string; operator: "in" | "eq" | "neq" | "owned_by"; value: string | string[]; }[]; subjects: { type: "user" | "role" | "team"; id: string; }[]; priority: number; createdAt: string; }[]; }>;
      };
      /**
       * Create ABAC policy.
       */
      createPolicy: {
        /**
         * Create ABAC policy.
         */
        mutate(input: { orgId: string; name: string; effect: "allow" | "deny"; resources: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update policy definition.
       */
      updatePolicy: {
        /**
         * Update policy definition.
         */
        mutate(input: { orgId?: string; name?: string; effect?: "allow" | "deny"; resources?: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions?: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects?: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete policy.
       */
      deletePolicy: {
        /**
         * Delete policy.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Evaluate access for a subject.
       */
      checkAccess: {
        /**
         * Evaluate access for a subject.
         */
        query(input: { orgId: string; subjectId: string; resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }, opts?: RequestOptions): Promise<{ allowed: boolean; matchedPolicies: string[]; }>;
      };
      /**
       * Batch permission check.
       */
      bulkCheck: {
        /**
         * Batch permission check.
         */
        mutate(input: { orgId: string; subjectId: string; checks: Array<{ resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }> }, opts?: RequestOptions): Promise<{ results: { resource: "file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics"; action: "read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve"; allowed: boolean; }[]; }>;
      };
    };
    teamsRoutes1: {
      /**
       * Teams in an org.
       */
      list: {
        /**
         * Teams in an org.
         */
        query(input: { orgId: string; visibility?: "public" | "private" | "secret" }, opts?: RequestOptions): Promise<{ teams: { id: string; orgId: string; name: string; visibility: "private" | "public" | "secret"; memberCount: number; projectIds: string[]; createdAt: string; description?: string | undefined; color?: string | undefined; icon?: string | undefined; }[]; }>;
      };
      /**
       * Team by ID.
       */
      get: {
        /**
         * Team by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; description: undefined; color: string; icon: string; visibility: "public"; memberCount: number; projectIds: never[]; createdAt: string; }>;
      };
      /**
       * Create team.
       */
      create: {
        /**
         * Create team.
         */
        mutate(input: { orgId: string; name: string; description?: string; color?: string; icon?: string; visibility: "public" | "private" | "secret"; projectIds: Array<string> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update team metadata.
       */
      update: {
        /**
         * Update team metadata.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; color?: string; icon?: string; visibility?: "public" | "private" | "secret"; projectIds?: Array<string>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete team.
       */
      delete: {
        /**
         * Delete team.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Team member list.
       */
      members: {
        /**
         * Team member list.
         */
        query(input: { teamId: string; page?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "member" | "lead"; joinedAt: string; }[]; }>;
      };
      /**
       * Add user to team.
       */
      addMember: {
        /**
         * Add user to team.
         */
        mutate(input: { teamId: string; userId: string; role: "lead" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from team.
       */
      removeMember: {
        /**
         * Remove user from team.
         */
        mutate(input: { teamId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
    };
    invitesRoutes1: {
      /**
       * Pending invites for org.
       */
      list: {
        /**
         * Pending invites for org.
         */
        query(input: { orgId: string; status?: "pending" | "accepted" | "declined" | "expired" | "revoked" }, opts?: RequestOptions): Promise<{ invites: { id: string; orgId: string; email: string; role: "admin" | "owner" | "member" | "viewer"; status: "revoked" | "pending" | "accepted" | "declined" | "expired"; invitedBy: string; expiresAt: string; acceptedAt: string | null; teams: string[]; createdAt: string; personalMessage?: string | undefined; }[]; }>;
      };
      /**
       * Send invite.
       */
      create: {
        /**
         * Send invite.
         */
        mutate(input: { orgId: string; email: string; role: "owner" | "admin" | "member" | "viewer"; teams?: Array<string>; personalMessage?: string }, opts?: RequestOptions): Promise<{ id: string; email: string; }>;
      };
      /**
       * Bulk invite by email list.
       */
      bulkCreate: {
        /**
         * Bulk invite by email list.
         */
        mutate(input: { orgId: string; emails: Array<string>; role?: "admin" | "member" | "viewer"; teams?: Array<string> }, opts?: RequestOptions): Promise<{ invited: number; failed: string[]; }>;
      };
      /**
       * Accept invite via token.
       */
      accept: {
        /**
         * Accept invite via token.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ accepted: boolean; orgId: string; }>;
      };
      /**
       * Decline invite.
       */
      decline: {
        /**
         * Decline invite.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ declined: boolean; }>;
      };
      /**
       * Revoke pending invite.
       */
      revoke: {
        /**
         * Revoke pending invite.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Resend invite email.
       */
      resend: {
        /**
         * Resend invite email.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
    };
    commentsRoutes1: {
      /**
       * Comments for a resource.
       */
      list: {
        /**
         * Comments for a resource.
         */
        query(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; nextCursor: string | null; }>;
      };
      /**
       * Post a comment.
       */
      create: {
        /**
         * Post a comment.
         */
        mutate(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; authorId: string; body: string; mentions: Array<string>; attachments: Array<{ id: string; name: string; url: string }>; parentId: string | null }, opts?: RequestOptions): Promise<{ id: string; }>;
      };
      /**
       * Edit comment body.
       */
      update: {
        /**
         * Edit comment body.
         */
        mutate(input: { id: string; body: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete comment.
       */
      delete: {
        /**
         * Delete comment.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Toggle emoji reaction.
       */
      react: {
        /**
         * Toggle emoji reaction.
         */
        mutate(input: { commentId: string; emoji: string }, opts?: RequestOptions): Promise<{ added: boolean; emoji: string; }>;
      };
      /**
       * Threaded replies.
       */
      thread: {
        /**
         * Threaded replies.
         */
        query(input: { parentId: string; limit?: number }, opts?: RequestOptions): Promise<{ replies: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Stream new comments live.
       */
      liveStream: {
        /**
         * Stream new comments live.
         */
        stream(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "new_comment"; comment: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }; }, void, unknown>>;
      };
    };
    tagsRoutes1: {
      /**
       * All tags for org.
       */
      list: {
        /**
         * All tags for org.
         */
        query(input: { orgId: string; search?: string; sortBy?: "name" | "usageCount" }, opts?: RequestOptions): Promise<{ tags: { id: string; orgId: string; name: string; color: string; usageCount: number; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Create tag.
       */
      create: {
        /**
         * Create tag.
         */
        mutate(input: { orgId: string; name: string; color: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update tag name/color.
       */
      update: {
        /**
         * Update tag name/color.
         */
        mutate(input: { id: string; name?: string; color?: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete tag.
       */
      delete: {
        /**
         * Delete tag.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Merge multiple tags into one.
       */
      merge: {
        /**
         * Merge multiple tags into one.
         */
        mutate(input: { sourceIds: Array<string>; targetId: string }, opts?: RequestOptions): Promise<{ merged: number; targetId: string; }>;
      };
      /**
       * Attach tags to a resource.
       */
      attach: {
        /**
         * Attach tags to a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ attached: number; }>;
      };
      /**
       * Detach tags from a resource.
       */
      detach: {
        /**
         * Detach tags from a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ detached: number; }>;
      };
    };
    reportsRoutes1: {
      /**
       * Saved reports for org.
       */
      list: {
        /**
         * Saved reports for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; orgId: string; name: string; type: "custom" | "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest"; config: Record<string, unknown>; schedule: { cron: string; timezone: string; recipients: string[]; format: "csv" | "pdf" | "html"; } | null; lastRunAt: string | null; lastRunStatus: "success" | "failed" | "running" | null; createdBy: string; createdAt: string; }[]; }>;
      };
      /**
       * Create report definition.
       */
      create: {
        /**
         * Create report definition.
         */
        mutate(input: { orgId: string; name: string; type: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config: Record<string, unknown>; schedule: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update report.
       */
      update: {
        /**
         * Update report.
         */
        mutate(input: { orgId?: string; name?: string; type?: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config?: Record<string, unknown>; schedule?: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy?: string; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete report.
       */
      delete: {
        /**
         * Delete report.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Trigger report run.
       */
      run: {
        /**
         * Trigger report run.
         */
        mutate(input: { id: string; format?: "pdf" | "csv" | "html" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Get report download URL.
       */
      download: {
        /**
         * Get report download URL.
         */
        query(input: { id: string; runId: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream report generation.
       */
      stream: {
        /**
         * Stream report generation.
         */
        stream(input: { id: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; percent: number; stage: "collecting_data"; }, void, unknown>>;
      };
    };
    rateLimitsRoutes1: {
      /**
       * Rate limit rules for org.
       */
      listRules: {
        /**
         * Rate limit rules for org.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ rules: { id: string; orgId: string; name: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; targetId: string | null; endpoint: string | null; limits: { window: "minute" | "hour" | "day" | "second"; max: number; burstAllowance: number; }[]; action: "log" | "throttle" | "block"; enabled: boolean; createdAt: string; }[]; }>;
      };
      /**
       * Create rate limit rule.
       */
      createRule: {
        /**
         * Create rate limit rule.
         */
        mutate(input: { orgId: string; name: string; target: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId: string | null; endpoint: string | null; limits: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update rule.
       */
      updateRule: {
        /**
         * Update rule.
         */
        mutate(input: { orgId?: string; name?: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId?: string | null; endpoint?: string | null; limits?: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete rule.
       */
      deleteRule: {
        /**
         * Delete rule.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Current usage vs. limits.
       */
      currentStatus: {
        /**
         * Current usage vs. limits.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ statuses: { ruleId: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; window: string; current: number; max: number; resetAt: string; }[]; }>;
      };
      /**
       * Manually reset rate counter.
       */
      resetCounter: {
        /**
         * Manually reset rate counter.
         */
        mutate(input: { ruleId: string; targetId: string }, opts?: RequestOptions): Promise<{ reset: boolean; }>;
      };
    };
    exportsRoutes1: {
      /**
       * Queue an export job.
       */
      create: {
        /**
         * Queue an export job.
         */
        mutate(input: { orgId: string; resource: { resource: "tasks"; projectId?: string; filters?: Record<string, unknown> } | { resource: "users"; orgId: string } | { resource: "analytics"; orgId: string; metrics: Array<string>; dateRange: { from: string; to: string } } | { resource: "audit_log"; orgId: string; from: string; to: string } | { resource: "billing"; orgId: string; year: number } | { resource: "files"; resourceType: string; resourceId: string }; format: "json" | "csv" | "xlsx" | "pdf" | "ndjson" | "parquet"; notifyEmail?: string }, opts?: RequestOptions): Promise<{ id: string; status: "queued"; }>;
      };
      /**
       * Export job history.
       */
      list: {
        /**
         * Export job history.
         */
        query(input: { orgId: string; limit?: number }, opts?: RequestOptions): Promise<{ jobs: { id: string; orgId: string; resource: { resource: "tasks"; projectId?: string | undefined; filters?: Record<string, unknown> | undefined; } | { resource: "users"; orgId: string; } | { resource: "analytics"; orgId: string; metrics: string[]; dateRange: { from: string; to: string; }; } | { resource: "audit_log"; orgId: string; from: string; to: string; } | { resource: "billing"; orgId: string; year: number; } | { resource: "files"; resourceType: string; resourceId: string; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done" | "failed" | "queued" | "running"; progress: number; rowCount: number | null; downloadUrl: string | null; expiresAt: string | null; error: string | null; createdAt: string; }[]; }>;
      };
      /**
       * Export job status.
       */
      get: {
        /**
         * Export job status.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; resource: { resource: "tasks"; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done"; progress: number; rowCount: number; downloadUrl: string; expiresAt: string; error: null; createdAt: string; }>;
      };
      /**
       * Cancel queued export.
       */
      cancel: {
        /**
         * Cancel queued export.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ cancelled: boolean; }>;
      };
      /**
       * Fresh signed download URL.
       */
      download: {
        /**
         * Fresh signed download URL.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream export progress.
       */
      progressStream: {
        /**
         * Stream export progress.
         */
        stream(input: { jobId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; jobId: string; percent: number; rowsProcessed: number; }, void, unknown>>;
      };
    };
    authRoutes2: {
      /**
       * Multi-method login — password, OAuth, SAML, magic link, passkey.
       */
      login: {
        /**
         * Multi-method login — password, OAuth, SAML, magic link, passkey.
         */
        mutate(input: { method: "password"; email: string; password: string; totpCode?: string } | { method: "oauth"; provider: "google" | "github" | "microsoft" | "okta"; code: string; redirectUri: string; state: string } | { method: "saml"; orgSlug: string; samlResponse: string; relayState?: string } | { method: "magic_link"; token: string } | { method: "passkey"; credentialId: string; clientDataJSON: string; authenticatorData: string; signature: string } & { device?: { userAgent: string; ip: string; fingerprint?: string; platform?: "web" | "ios" | "android" | "desktop" } }, opts?: RequestOptions): Promise<{ tokens: { accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }; user: { id: UserId; role: "admin"; }; }>;
      };
      /**
       * Rotate refresh token.
       */
      refresh: {
        /**
         * Rotate refresh token.
         */
        mutate(input: { refreshToken: string; sessionId: string }, opts?: RequestOptions): Promise<{ accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }>;
      };
      /**
       * Invalidate session.
       */
      logout: {
        /**
         * Invalidate session.
         */
        mutate(input: { sessionId: string }, opts?: RequestOptions): Promise<{ revoked: boolean; sessionId: SessionId; }>;
      };
      /**
       * Validate MFA code after credential check.
       */
      mfaChallenge: {
        /**
         * Validate MFA code after credential check.
         */
        mutate(input: { challengeToken: string; code: string; method: "totp" | "sms" | "email" | "webauthn" }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Send password reset email.
       */
      forgotPassword: {
        /**
         * Send password reset email.
         */
        mutate(input: { email: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
      /**
       * Apply new password via reset token.
       */
      resetPassword: {
        /**
         * Apply new password via reset token.
         */
        mutate(input: { token: string; newPassword: string }, opts?: RequestOptions): Promise<{ success: boolean; }>;
      };
      /**
       * List active sessions with device info.
       */
      sessions: {
        /**
         * List active sessions with device info.
         */
        query(input: { cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ sessions: { id: SessionId; device: { userAgent: string; ip: string; fingerprint?: string | undefined; platform?: "web" | "ios" | "android" | "desktop" | undefined; }; createdAt: string; current: boolean; }[]; nextCursor: string | null; }>;
      };
      /**
       * Current authenticated user.
       */
      me: {
        /**
         * Current authenticated user.
         */
        query(input: any, opts?: RequestOptions): Promise<{ id: UserId; role: string; }>;
      };
      /**
       * Confirm email address via verification code.
       */
      verifyEmail: {
        /**
         * Confirm email address via verification code.
         */
        mutate(input: { code: string }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Stream login activity events for the current user.
       */
      deviceStream: {
        /**
         * Stream login activity events for the current user.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "session_created"; sessionId: SessionId; ts: string; }, void, unknown>>;
      };
    };
    usersRoutes2: {
      /**
       * Paginated user list with rich filters.
       */
      list: {
        /**
         * Paginated user list with rich filters.
         */
        query(input: { page?: number; limit?: number; roles?: Array<"owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support">; search?: string; sortBy?: "name" | "email" | "createdAt"; sortDir?: "asc" | "desc" }, opts?: RequestOptions): Promise<{ users: { id: string; email: string; emailVerified: boolean; name: string; avatarUrl: string | null; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: string[]; metadata: Record<string, string | number | boolean>; createdAt: string; updatedAt: string; displayName?: string | undefined; bio?: string | undefined; timezone?: string | undefined; locale?: string | undefined; address?: { line1: string; city: string; postalCode: string; country: string; line2?: string | undefined; state?: string | undefined; } | undefined; }[]; total: number; page: number; }>;
      };
      /**
       * Fetch single user with full profile.
       */
      get: {
        /**
         * Fetch single user with full profile.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; email: string; emailVerified: true; name: string; displayName: undefined; avatarUrl: null; bio: undefined; timezone: string; locale: string; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: never[]; metadata: {}; address: undefined; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Partial profile update with nested address.
       */
      update: {
        /**
         * Partial profile update with nested address.
         */
        mutate(input: { name?: string; displayName?: string; bio?: string; timezone?: string; locale?: string; avatarUrl?: string | null; address?: { line1: string; line2?: string; city: string; state?: string; postalCode: string; country: string }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Soft-delete user.
       */
      deactivate: {
        /**
         * Soft-delete user.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ deactivated: boolean; }>;
      };
      /**
       * Change role for many users atomically.
       */
      bulkRoleChange: {
        /**
         * Change role for many users atomically.
         */
        mutate(input: { userIds: Array<string>; role: "owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support"; reason?: string }, opts?: RequestOptions): Promise<{ updated: number; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; }>;
      };
      /**
       * All permissions grouped by resource for a user.
       */
      permissionMatrix: {
        /**
         * All permissions grouped by resource for a user.
         */
        query(input: { userId: string }, opts?: RequestOptions): Promise<{ userId: string; matrix: Record<string, Record<string, boolean>>; }>;
      };
      /**
       * Upload and crop user avatar.
       */
      uploadAvatar: {
        /**
         * Upload and crop user avatar.
         */
        upload(input: { file: File; cropSize?: number } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileName: string; size: number; croppedTo: number; }>;
      };
      /**
       * Change own password — requires MFA.
       */
      changePassword: {
        /**
         * Change own password — requires MFA.
         */
        mutate(input: { currentPassword: string; newPassword: string }, opts?: RequestOptions): Promise<{ changed: boolean; }>;
      };
      /**
       * GDPR data export streamed by resource type.
       */
      exportData: {
        /**
         * GDPR data export streamed by resource type.
         */
        stream(input: { userId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "chunk"; resource: "profile" | "tasks" | "comments" | "files"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    organizationsRoutes2: {
      /**
       * Orgs accessible to caller.
       */
      list: {
        /**
         * Orgs accessible to caller.
         */
        query(input: { page?: number; limit?: number }, opts?: RequestOptions): Promise<{ orgs: { id: string; name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; memberCount: number; logoUrl: string | null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }[]; total: number; }>;
      };
      /**
       * Fetch org by ID.
       */
      get: {
        /**
         * Fetch org by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; slug: string; plan: "enterprise"; memberCount: number; logoUrl: null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }>;
      };
      /**
       * Create new organization.
       */
      create: {
        /**
         * Create new organization.
         */
        mutate(input: { name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; logoUrl: string | null; settings: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> }; ownerId: string }, opts?: RequestOptions): Promise<{ id: string; slug: string; }>;
      };
      /**
       * Update org metadata and settings.
       */
      update: {
        /**
         * Update org metadata and settings.
         */
        mutate(input: { id: string; name?: string; logoUrl?: string | null; settings?: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> } }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Permanently delete org.
       */
      delete: {
        /**
         * Permanently delete org.
         */
        mutate(input: { id: string; confirmSlug: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated org member list.
       */
      members: {
        /**
         * Paginated org member list.
         */
        query(input: { orgId: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: string; joinedAt: string; }[]; total: number; }>;
      };
      /**
       * Add user to org with role.
       */
      addMember: {
        /**
         * Add user to org with role.
         */
        mutate(input: { orgId: string; userId: string; role: "owner" | "admin" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from org.
       */
      removeMember: {
        /**
         * Remove user from org.
         */
        mutate(input: { orgId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
      /**
       * Transfer org ownership — requires MFA.
       */
      transferOwnership: {
        /**
         * Transfer org ownership — requires MFA.
         */
        mutate(input: { orgId: string; newOwnerId: string; reason?: string }, opts?: RequestOptions): Promise<{ transferred: boolean; }>;
      };
      /**
       * Stream org audit log entries live.
       */
      auditStream: {
        /**
         * Stream org audit log entries live.
         */
        stream(input: { orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; action: string; actorId: string; ts: string; }, void, unknown>>;
      };
    };
    billingRoutes2: {
      /**
       * Full subscription with pricing model.
       */
      getSubscription: {
        /**
         * Full subscription with pricing model.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; tier: "pro"; pricing: { model: "per_seat"; pricePerSeatMonth: number; minSeats: number; maxSeats: null; }; status: "active"; seats: number; currentPeriodStart: string; currentPeriodEnd: string; cancelAtPeriodEnd: false; metadata: {}; addons: never[]; }>;
      };
      /**
       * Calculate proration before confirming.
       */
      previewUpgrade: {
        /**
         * Calculate proration before confirming.
         */
        query(input: { orgId: string; targetTier: "starter" | "pro" | "business" | "enterprise"; targetSeats: number; billingInterval: "monthly" | "annual" }, opts?: RequestOptions): Promise<{ currentAmount: number; newAmount: number; prorationAmount: number; effectiveDate: string; lineItems: { description: string; amount: number; currency: string; }[]; }>;
      };
      /**
       * Create Stripe checkout session.
       */
      createCheckout: {
        /**
         * Create Stripe checkout session.
         */
        mutate(input: { orgId: string; tier: "starter" | "pro" | "business" | "enterprise"; seats: number; billingInterval: "monthly" | "annual"; addons?: Array<{ id: string; quantity: number }>; successUrl: string; cancelUrl: string; couponCode?: string }, opts?: RequestOptions): Promise<{ checkoutUrl: string; sessionId: string; }>;
      };
      /**
       * Schedule cancellation at period end.
       */
      cancelSubscription: {
        /**
         * Schedule cancellation at period end.
         */
        mutate(input: { orgId: string; reason?: string }, opts?: RequestOptions): Promise<{ cancelAtPeriodEnd: boolean; }>;
      };
      /**
       * Paginated invoice list.
       */
      invoices: {
        /**
         * Paginated invoice list.
         */
        query(input: { orgId: string; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ invoices: { id: string; amount: number; currency: string; status: string; date: string; }[]; nextCursor: string | null; }>;
      };
      /**
       * Metered usage breakdown.
       */
      usageReport: {
        /**
         * Metered usage breakdown.
         */
        query(input: { orgId: string; breakdown?: "daily" | "weekly" | "monthly" }, opts?: RequestOptions): Promise<{ period: { start: string; end: string; }; included: number; consumed: number; overage: number; overageCost: number; series: { date: string; units: number; cost: number; }[]; }>;
      };
      /**
       * Apply promotional discount code.
       */
      applyPromoCode: {
        /**
         * Apply promotional discount code.
         */
        mutate(input: { orgId: string; code: string }, opts?: RequestOptions): Promise<{ applied: boolean; discountPercent: number; }>;
      };
      /**
       * Stream invoice line items.
       */
      invoiceStream: {
        /**
         * Stream invoice line items.
         */
        stream(input: { invoiceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "line_item"; description: string; amount: number; currency: string; }, void, unknown>>;
      };
    };
    projectsRoutes2: {
      /**
       * Paginated project list with status filters.
       */
      list: {
        /**
         * Paginated project list with status filters.
         */
        query(input: { orgId: string; status?: Array<"planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled">; search?: string; page?: number }, opts?: RequestOptions): Promise<{ projects: { id: string; orgId: string; name: string; status: "active" | "cancelled" | "planning" | "on_hold" | "completed" | "archived"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: string[]; customFields: Record<string, string | number | boolean | null>; createdAt: string; updatedAt: string; description?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Project by ID.
       */
      get: {
        /**
         * Project by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; status: "active"; visibility: "private"; startDate: null; endDate: null; ownerId: string; tags: never[]; customFields: {}; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create project.
       */
      create: {
        /**
         * Create project.
         */
        mutate(input: { orgId: string; name: string; description?: string; status: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: Array<string>; customFields: Record<string, string | number | boolean | null> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update project fields.
       */
      update: {
        /**
         * Update project fields.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; status?: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility?: "private" | "internal" | "public"; startDate?: string | null; endDate?: string | null; ownerId?: string; tags?: Array<string>; customFields?: Record<string, string | number | boolean | null>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Archive project.
       */
      archive: {
        /**
         * Archive project.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ archived: boolean; }>;
      };
      /**
       * Project member list.
       */
      members: {
        /**
         * Project member list.
         */
        query(input: { projectId: string }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "viewer" | "lead" | "contributor"; joinedAt: string; }[]; }>;
      };
      /**
       * Add project member.
       */
      addMember: {
        /**
         * Add project member.
         */
        mutate(input: { projectId: string; userId: string; role: "lead" | "contributor" | "viewer" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Aggregate project stats.
       */
      stats: {
        /**
         * Aggregate project stats.
         */
        query(input: { projectId: string; period?: "7d" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ tasks: number; completed: number; openIssues: number; velocity: number; burndownPoints: { date: string; remaining: number; }[]; }>;
      };
      /**
       * Clone project structure.
       */
      duplicate: {
        /**
         * Clone project structure.
         */
        mutate(input: { id: string; newName: string; includeTasks?: boolean; includeMembers?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream full project export.
       */
      exportStream: {
        /**
         * Stream full project export.
         */
        stream(input: { id: string; format: "json" | "csv" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; resource: "task"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    tasksRoutes2: {
      /**
       * Create task with full field set.
       */
      create: {
        /**
         * Create task with full field set.
         */
        mutate(input: { projectId: string; title: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }> }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; }>;
      };
      /**
       * Task tree with recursive subtasks.
       */
      get: {
        /**
         * Task tree with recursive subtasks.
         */
        query(input: { id: string; depth?: number }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; subtasks: any[]; customFields: Record<string, { type: "text"; value: string; } | { type: "number"; value: number; } | { type: "date"; value: string; } | { type: "boolean"; value: boolean; } | { type: "select"; value: string; optionId: string; } | { type: "multi"; value: string[]; optionIds: string[]; } | { type: "user"; value: string; } | { type: "url"; value: string; }>; }>;
      };
      /**
       * Patch any task fields.
       */
      update: {
        /**
         * Patch any task fields.
         */
        mutate(input: { projectId?: string; title?: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete task permanently.
       */
      delete: {
        /**
         * Delete task permanently.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Batch status/priority update.
       */
      bulkUpdate: {
        /**
         * Batch status/priority update.
         */
        mutate(input: { ids: Array<string>; patch: { status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; dueDate?: string | null } }, opts?: RequestOptions): Promise<{ updated: number; }>;
      };
      /**
       * Comment thread for a task.
       */
      comments: {
        /**
         * Comment thread for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; body: string; author: string; createdAt: string; reactions: Record<string, string[]>; }[]; }>;
      };
      /**
       * Post comment on task.
       */
      addComment: {
        /**
         * Post comment on task.
         */
        mutate(input: { taskId: string; body: string; mentions?: Array<string> }, opts?: RequestOptions): Promise<{ id: string; body: string; }>;
      };
      /**
       * Typed audit trail for a task.
       */
      timelineEvents: {
        /**
         * Typed audit trail for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ events: ({ type: "created"; actor: string; ts: string; } | { type: "status_change"; actor: string; ts: string; from: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; to: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; } | { type: "assigned"; actor: string; ts: string; userId: string; })[]; }>;
      };
      /**
       * Tasks assigned to current user.
       */
      myTasks: {
        /**
         * Tasks assigned to current user.
         */
        query(input: { status?: Array<"backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix">; projectId?: string; limit?: number }, opts?: RequestOptions): Promise<{ tasks: any[]; total: number; }>;
      };
      /**
       * Live task update stream.
       */
      activityStream: {
        /**
         * Live task update stream.
         */
        stream(input: { projectId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "task_updated"; taskId: string; field: string; value: string; }, void, unknown>>;
      };
    };
    filesRoutes2: {
      /**
       * Files for a resource.
       */
      list: {
        /**
         * Files for a resource.
         */
        query(input: { resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; mimeTypes?: Array<string>; page?: number }, opts?: RequestOptions): Promise<{ files: { id: string; name: string; mimeType: string; sizeBytes: number; status: "deleted" | "pending" | "processing" | "ready" | "failed"; url: string | null; thumbnailUrl: string | null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: Record<string, string | number>; createdAt: string; }[]; total: number; }>;
      };
      /**
       * File metadata by ID.
       */
      get: {
        /**
         * File metadata by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; mimeType: string; sizeBytes: number; status: "ready"; url: string; thumbnailUrl: null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: {}; createdAt: string; }>;
      };
      /**
       * Get presigned S3 upload URL.
       */
      requestUpload: {
        /**
         * Get presigned S3 upload URL.
         */
        mutate(input: { fileName: string; mimeType: string; sizeBytes: number; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string }, opts?: RequestOptions): Promise<{ uploadUrl: string; fileId: string; expiresIn: number; }>;
      };
      /**
       * Mark pending upload complete.
       */
      confirmUpload: {
        /**
         * Mark pending upload complete.
         */
        mutate(input: { fileId: string; etag: string }, opts?: RequestOptions): Promise<{ id: string; confirmed: boolean; url: string; }>;
      };
      /**
       * Delete file and storage object.
       */
      delete: {
        /**
         * Delete file and storage object.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Delete multiple files.
       */
      bulkDelete: {
        /**
         * Delete multiple files.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ deleted: number; }>;
      };
      /**
       * Multipart direct file upload.
       */
      directUpload: {
        /**
         * Multipart direct file upload.
         */
        upload(input: { file: File; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; description?: string } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileId: string; name: string; size: number; }>;
      };
      /**
       * Stream file processing events.
       */
      processStream: {
        /**
         * Stream file processing events.
         */
        stream(input: { fileId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; fileId: string; stage: "ready" | "queued" | "virus_scan" | "thumbnail"; percent: number; }, void, unknown>>;
      };
    };
    notificationsRoutes2: {
      /**
       * Paginated notification list.
       */
      list: {
        /**
         * Paginated notification list.
         */
        query(input: { unreadOnly?: boolean; types?: Array<"mention" | "task_assigned" | "task_completed" | "comment" | "billing" | "security" | "system" | "workflow_run" | "integration_sync">; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ notifications: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }[]; unreadCount: number; nextCursor: string | null; }>;
      };
      /**
       * Mark notifications as read.
       */
      markRead: {
        /**
         * Mark notifications as read.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ marked: number; }>;
      };
      /**
       * Mark all notifications read.
       */
      markAllRead: {
        /**
         * Mark all notifications read.
         */
        mutate(input: any, opts?: RequestOptions): Promise<{ marked: boolean; }>;
      };
      /**
       * Get notification preferences.
       */
      preferences: {
        /**
         * Get notification preferences.
         */
        query(input: any, opts?: RequestOptions): Promise<{ channels: Record<string, boolean>; types: Partial<Record<"billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync", boolean>>; }>;
      };
      /**
       * Update preferences.
       */
      updatePreferences: {
        /**
         * Update preferences.
         */
        mutate(input: { channels?: Record<string, boolean>; types?: Record<string, boolean> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Register Web Push subscription.
       */
      pushSubscribe: {
        /**
         * Register Web Push subscription.
         */
        mutate(input: { endpoint: string; keys: { p256dh: string; auth: string } }, opts?: RequestOptions): Promise<{ subscribed: boolean; }>;
      };
      /**
       * Real-time notification SSE.
       */
      liveStream: {
        /**
         * Real-time notification SSE.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "notification"; notification: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }; }, void, unknown>>;
      };
    };
    searchRoutes2: {
      /**
       * Full-text search across resource types.
       */
      global: {
        /**
         * Full-text search across resource types.
         */
        query(input: { q: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document">; orgId: string; limit?: number; highlight?: boolean }, opts?: RequestOptions): Promise<{ results: ({ type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; })[]; total: number; took: number; }>;
      };
      /**
       * Autocomplete suggestions.
       */
      suggestions: {
        /**
         * Autocomplete suggestions.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ suggestions: { label: string; type: "user" | "file" | "task" | "project" | "comment" | "workflow" | "document"; id: string; }[]; }>;
      };
      /**
       * Faceted filter counts.
       */
      filters: {
        /**
         * Faceted filter counts.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ facets: Record<"user" | "file" | "task" | "project" | "comment" | "workflow" | "document", number>; }>;
      };
      /**
       * Trigger manual reindex for org.
       */
      reindex: {
        /**
         * Trigger manual reindex for org.
         */
        mutate(input: { orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream results progressively.
       */
      stream: {
        /**
         * Stream results progressively.
         */
        stream(input: { q: string; orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "result"; result: { type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; }; }, void, unknown>>;
      };
    };
    analyticsRoutes2: {
      /**
       * Arbitrary analytics query.
       */
      query: {
        /**
         * Arbitrary analytics query.
         */
        mutate(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }, opts?: RequestOptions): Promise<{ rows: never[]; totals: {}; comparison: undefined; meta: { executionMs: number; rowsScanned: number; cached: false; granularity: "minute" | "hour" | "day" | "week" | "month"; }; }>;
      };
      /**
       * Multi-step funnel conversion.
       */
      funnel: {
        /**
         * Multi-step funnel conversion.
         */
        mutate(input: { orgId: string; steps: Array<{ name: string; event: string; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }> }>; dateRange: { from: string; to: string } }, opts?: RequestOptions): Promise<{ steps: { name: string; count: number; conversionRate: number; avgTimeToNextStep: number | null; }[]; overallConversion: number; }>;
      };
      /**
       * Cohort retention analysis.
       */
      retention: {
        /**
         * Cohort retention analysis.
         */
        mutate(input: { orgId: string; cohortEvent: string; returnEvent: string; dateRange: { from: string; to: string }; granularity?: "minute" | "hour" | "day" | "week" | "month" }, opts?: RequestOptions): Promise<{ cohorts: { date: string; size: number; retention: number[]; }[]; }>;
      };
      /**
       * Saved report definitions.
       */
      savedReports: {
        /**
         * Saved report definitions.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; name: string; query: { orgId: string; metrics: string[]; dimensions: string[]; filters: { field: string; operator: { op: "eq"; value: string | number | boolean; } | { op: "neq"; value: string | number | boolean; } | { op: "gt"; value: number; } | { op: "gte"; value: number; } | { op: "lt"; value: number; } | { op: "lte"; value: number; } | { op: "in"; values: (string | number)[]; } | { op: "between"; min: number; max: number; }; }[]; dateRange: { from: string; to: string; granularity: "minute" | "hour" | "day" | "week" | "month"; timezone: string; }; orderBy: { field: string; direction: "asc" | "desc"; }[]; limit: number; compareWith?: { from: string; to: string; } | undefined; }; schedule: { cron: string; recipients: string[]; } | null; lastRunAt: string | null; }[]; }>;
      };
      /**
       * Persist a report definition.
       */
      saveReport: {
        /**
         * Persist a report definition.
         */
        mutate(input: { orgId: string; name: string; query: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }; schedule?: { cron: string; recipients: Array<string> } }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream query results row by row.
       */
      exportStream: {
        /**
         * Stream query results row by row.
         */
        stream(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string }; format?: "json" | "csv" | "parquet" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; index: number; data: Record<string, string | number | null>; }, void, unknown>>;
      };
    };
    webhooksRoutes2: {
      /**
       * Webhooks for an org.
       */
      list: {
        /**
         * Webhooks for an org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ webhooks: { id: string; orgId: string; url: string; events: ("task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed")[]; enabled: boolean; secret: string; headers: Record<string, string>; retryPolicy: { maxAttempts: number; backoffMs: number; }; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Register webhook endpoint.
       */
      create: {
        /**
         * Register webhook endpoint.
         */
        mutate(input: { orgId: string; url: string; description?: string; events: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled: boolean; secret: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number } }, opts?: RequestOptions): Promise<{ id: string; url: string; }>;
      };
      /**
       * Update webhook.
       */
      update: {
        /**
         * Update webhook.
         */
        mutate(input: { orgId?: string; url?: string; description?: string; events?: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled?: boolean; secret?: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Remove webhook.
       */
      delete: {
        /**
         * Remove webhook.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Recent delivery attempts.
       */
      deliveries: {
        /**
         * Recent delivery attempts.
         */
        query(input: { webhookId: string; status?: "success" | "failed" | "pending"; limit?: number }, opts?: RequestOptions): Promise<{ deliveries: { id: string; status: number; attempt: number; ts: string; error: string | null; }[]; }>;
      };
      /**
       * Retry failed delivery.
       */
      redeliver: {
        /**
         * Retry failed delivery.
         */
        mutate(input: { deliveryId: string }, opts?: RequestOptions): Promise<{ queued: boolean; }>;
      };
      /**
       * Send test event to webhook.
       */
      testPing: {
        /**
         * Send test event to webhook.
         */
        mutate(input: { webhookId: string; eventType: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed" }, opts?: RequestOptions): Promise<{ sent: boolean; deliveryId: string; }>;
      };
      /**
       * Stream webhook delivery events.
       */
      logStream: {
        /**
         * Stream webhook delivery events.
         */
        stream(input: { webhookId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "delivery"; status: number; ts: string; }, void, unknown>>;
      };
    };
    aiRoutes2: {
      /**
       * Multi-model streaming chat with tool-call support.
       */
      chat: {
        /**
         * Multi-model streaming chat with tool-call support.
         */
        stream(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "content_delta"; text: string; finishReason?: undefined; usage?: undefined; } | { type: "finish"; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; text?: undefined; }, void, unknown>>;
      };
      /**
       * Non-streaming completion.
       */
      complete: {
        /**
         * Non-streaming completion.
         */
        mutate(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string>; stream?: false }, opts?: RequestOptions): Promise<{ id: string; role: "assistant"; content: string | ({ type: "text"; text: string; } | { type: "image_url"; url: string; detail: "high" | "low" | "auto"; } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown>; } | { type: "tool_result"; toolCallId: string; content: string; isError: boolean; })[]; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; }>;
      };
      /**
       * Batch embedding generation.
       */
      embed: {
        /**
         * Batch embedding generation.
         */
        mutate(input: { inputs: Array<string | { text: string; id: string }>; model: "text-embedding-3-small" | "text-embedding-3-large"; dimensions?: number; outputFormat?: "float" | "base64" | "int8" }, opts?: RequestOptions): Promise<{ embeddings: { index: number; embedding: number[]; inputTokens: number; }[]; model: "text-embedding-3-small" | "text-embedding-3-large"; totalTokens: number; }>;
      };
      /**
       * Run content moderation checks.
       */
      moderateContent: {
        /**
         * Run content moderation checks.
         */
        mutate(input: { inputs: Array<{ id: string; text: string }>; categories?: Array<"hate" | "harassment" | "self_harm" | "sexual" | "violence" | "dangerous"> }, opts?: RequestOptions): Promise<{ results: { id: string; flagged: boolean; categories: Partial<Record<string, boolean>>; scores: Partial<Record<string, number>>; }[]; }>;
      };
      /**
       * Structured text summarization.
       */
      summarize: {
        /**
         * Structured text summarization.
         */
        mutate(input: { text: string; format?: "bullets" | "paragraph" | "tldr"; maxLength?: number }, opts?: RequestOptions): Promise<{ summary: string; wordCount: number; }>;
      };
      /**
       * AI token usage for billing period.
       */
      usageStats: {
        /**
         * AI token usage for billing period.
         */
        query(input: { orgId: string; period?: "current" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ promptTokens: number; completionTokens: number; totalCost: number; byModel: Record<string, { tokens: number; cost: number; }>; }>;
      };
    };
    auditRoutes2: {
      /**
       * Paginated audit log with filters.
       */
      log: {
        /**
         * Paginated audit log with filters.
         */
        query(input: { orgId: string; actorId?: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled">; resourceType?: string; from?: string; to?: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ entries: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }[]; total: number; }>;
      };
      /**
       * Single audit entry by ID.
       */
      getEntry: {
        /**
         * Single audit entry by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; actorId: string; actorEmail: string; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: null; resourceId: null; metadata: {}; ip: string; userAgent: string; ts: string; }>;
      };
      /**
       * Update log retention policy.
       */
      retention: {
        /**
         * Update log retention policy.
         */
        mutate(input: { orgId: string; retentionDays: number }, opts?: RequestOptions): Promise<{ updated: boolean; retentionDays: number; }>;
      };
      /**
       * Stream audit log as NDJSON.
       */
      export: {
        /**
         * Stream audit log as NDJSON.
         */
        stream(input: { orgId: string; from: string; to: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; data: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }; }, void, unknown>>;
      };
    };
    apiKeysRoutes2: {
      /**
       * List API keys for org.
       */
      list: {
        /**
         * List API keys for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ keys: { id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: string | null; lastUsedAt: string | null; createdBy: string; createdAt: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number; } | null; }[]; }>;
      };
      /**
       * Create API key.
       */
      create: {
        /**
         * Create API key.
         */
        mutate(input: { orgId: string; name: string; scopes: Array<"read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write">; expiresAt: string | null; createdBy: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number } | null }, opts?: RequestOptions): Promise<{ key: string; id: string; prefix: string; }>;
      };
      /**
       * Revoke API key.
       */
      revoke: {
        /**
         * Revoke API key.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Rotate key secret.
       */
      rotate: {
        /**
         * Rotate key secret.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ newKey: string; id: string; }>;
      };
      /**
       * API key metadata.
       */
      get: {
        /**
         * API key metadata.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: null; lastUsedAt: null; createdBy: string; createdAt: string; rateLimit: null; }>;
      };
      /**
       * Key request usage stats.
       */
      usage: {
        /**
         * Key request usage stats.
         */
        query(input: { id: string; period?: "1h" | "24h" | "7d" | "30d" }, opts?: RequestOptions): Promise<{ requests: number; errors: number; p50Ms: number; p99Ms: number; series: { ts: string; count: number; }[]; }>;
      };
    };
    integrationsRoutes2: {
      /**
       * All integrations with status.
       */
      list: {
        /**
         * All integrations with status.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ integrations: { id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: boolean; enabled: boolean; status: "error" | "active" | "disconnected" | "syncing"; lastSyncAt: string | null; errorMessage: string | null; config: Record<string, unknown>; }[]; }>;
      };
      /**
       * Single integration status.
       */
      get: {
        /**
         * Single integration status.
         */
        query(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: true; enabled: true; status: "active"; lastSyncAt: string; errorMessage: null; config: {}; }>;
      };
      /**
       * Start OAuth install flow.
       */
      install: {
        /**
         * Start OAuth install flow.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; redirectUri: string; scopes?: Array<string> }, opts?: RequestOptions): Promise<{ authUrl: string; }>;
      };
      /**
       * Exchange OAuth code for tokens.
       */
      oauthCallback: {
        /**
         * Exchange OAuth code for tokens.
         */
        mutate(input: { code: string; state: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ installed: boolean; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; }>;
      };
      /**
       * Remove integration.
       */
      uninstall: {
        /**
         * Remove integration.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ uninstalled: boolean; }>;
      };
      /**
       * Update integration config.
       */
      updateConfig: {
        /**
         * Update integration config.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; config: Record<string, unknown> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Manual sync trigger.
       */
      triggerSync: {
        /**
         * Manual sync trigger.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream sync progress events.
       */
      syncStream: {
        /**
         * Stream sync progress events.
         */
        stream(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; itemsSynced: number; total: number; }, void, unknown>>;
      };
    };
    workflowsRoutes2: {
      /**
       * Create workflow with recursive condition tree.
       */
      create: {
        /**
         * Create workflow with recursive condition tree.
         */
        mutate(input: { name: string; description?: string; trigger: { event: string; filters?: unknown; debounceMs?: number }; steps: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; orgId: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * List workflows with run stats.
       */
      list: {
        /**
         * List workflows with run stats.
         */
        query(input: { orgId: string; enabled?: boolean }, opts?: RequestOptions): Promise<{ workflows: { id: string; name: string; enabled: boolean; lastRunAt: string | null; runCount: number; failureRate: number; }[]; }>;
      };
      /**
       * Workflow detail.
       */
      get: {
        /**
         * Workflow detail.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * Update workflow definition.
       */
      update: {
        /**
         * Update workflow definition.
         */
        mutate(input: { name?: string; description?: string; trigger?: { event: string; filters?: unknown; debounceMs?: number }; steps?: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete workflow.
       */
      delete: {
        /**
         * Delete workflow.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated run history.
       */
      runHistory: {
        /**
         * Paginated run history.
         */
        query(input: { workflowId: string; status?: "running" | "success" | "failed" | "cancelled"; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ runs: { id: string; status: string; startedAt: string; finishedAt: string | null; steps: { stepId: string; status: string; durationMs: number; error?: string | undefined; }[]; }[]; nextCursor: string | null; }>;
      };
      /**
       * Dry-run with streamed step outcomes.
       */
      testRun: {
        /**
         * Dry-run with streamed step outcomes.
         */
        stream(input: { workflowId: string; samplePayload: Record<string, unknown> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "step_result"; stepId: string; status: "success"; durationMs: number; output: Record<string, unknown>; }, void, unknown>>;
      };
    };
    documentsRoutes2: {
      /**
       * Documents in a project.
       */
      list: {
        /**
         * Documents in a project.
         */
        query(input: { projectId: string; page?: number; limit?: number; search?: string }, opts?: RequestOptions): Promise<{ documents: Pick<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "private" | "public" | "project"; authorId: string; collaborators: string[]; version: number; publishedAt: string | null; createdAt: string; updatedAt: string; }, "createdAt" | "id" | "updatedAt" | "title" | "authorId">[]; total: number; }>;
      };
      /**
       * Full document with blocks.
       */
      get: {
        /**
         * Full document with blocks.
         */
        query(input: { id: string; version?: number }, opts?: RequestOptions): Promise<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "project"; authorId: string; collaborators: never[]; version: number; publishedAt: null; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create document.
       */
      create: {
        /**
         * Create document.
         */
        mutate(input: { projectId: string; title: string; blocks: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility: "private" | "project" | "public"; authorId: string; collaborators: Array<string> }, opts?: RequestOptions): Promise<{ id: string; title: string; }>;
      };
      /**
       * Update blocks and metadata.
       */
      update: {
        /**
         * Update blocks and metadata.
         */
        mutate(input: { id: string; title?: string; blocks?: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility?: "private" | "project" | "public" }, opts?: RequestOptions): Promise<{ id: string; version: number; }>;
      };
      /**
       * Delete document.
       */
      delete: {
        /**
         * Delete document.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Publish document publicly.
       */
      publish: {
        /**
         * Publish document publicly.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ publishedAt: string; url: string; }>;
      };
      /**
       * Document version history.
       */
      history: {
        /**
         * Document version history.
         */
        query(input: { id: string; limit?: number }, opts?: RequestOptions): Promise<{ versions: { version: number; editor: string; changedAt: string; summary: string; }[]; }>;
      };
      /**
       * Collaborative edit event stream.
       */
      editStream: {
        /**
         * Collaborative edit event stream.
         */
        stream(input: { docId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "block_updated"; blockId: string; patch: Record<string, unknown>; editorId: string; }, void, unknown>>;
      };
    };
    permissionsRoutes2: {
      /**
       * All ABAC policies for org.
       */
      listPolicies: {
        /**
         * All ABAC policies for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ policies: { id: string; orgId: string; name: string; effect: "allow" | "deny"; resources: ("file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics")[]; actions: ("read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve")[]; conditions: { field: string; operator: "in" | "eq" | "neq" | "owned_by"; value: string | string[]; }[]; subjects: { type: "user" | "role" | "team"; id: string; }[]; priority: number; createdAt: string; }[]; }>;
      };
      /**
       * Create ABAC policy.
       */
      createPolicy: {
        /**
         * Create ABAC policy.
         */
        mutate(input: { orgId: string; name: string; effect: "allow" | "deny"; resources: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update policy definition.
       */
      updatePolicy: {
        /**
         * Update policy definition.
         */
        mutate(input: { orgId?: string; name?: string; effect?: "allow" | "deny"; resources?: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions?: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects?: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete policy.
       */
      deletePolicy: {
        /**
         * Delete policy.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Evaluate access for a subject.
       */
      checkAccess: {
        /**
         * Evaluate access for a subject.
         */
        query(input: { orgId: string; subjectId: string; resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }, opts?: RequestOptions): Promise<{ allowed: boolean; matchedPolicies: string[]; }>;
      };
      /**
       * Batch permission check.
       */
      bulkCheck: {
        /**
         * Batch permission check.
         */
        mutate(input: { orgId: string; subjectId: string; checks: Array<{ resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }> }, opts?: RequestOptions): Promise<{ results: { resource: "file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics"; action: "read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve"; allowed: boolean; }[]; }>;
      };
    };
    teamsRoutes2: {
      /**
       * Teams in an org.
       */
      list: {
        /**
         * Teams in an org.
         */
        query(input: { orgId: string; visibility?: "public" | "private" | "secret" }, opts?: RequestOptions): Promise<{ teams: { id: string; orgId: string; name: string; visibility: "private" | "public" | "secret"; memberCount: number; projectIds: string[]; createdAt: string; description?: string | undefined; color?: string | undefined; icon?: string | undefined; }[]; }>;
      };
      /**
       * Team by ID.
       */
      get: {
        /**
         * Team by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; description: undefined; color: string; icon: string; visibility: "public"; memberCount: number; projectIds: never[]; createdAt: string; }>;
      };
      /**
       * Create team.
       */
      create: {
        /**
         * Create team.
         */
        mutate(input: { orgId: string; name: string; description?: string; color?: string; icon?: string; visibility: "public" | "private" | "secret"; projectIds: Array<string> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update team metadata.
       */
      update: {
        /**
         * Update team metadata.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; color?: string; icon?: string; visibility?: "public" | "private" | "secret"; projectIds?: Array<string>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete team.
       */
      delete: {
        /**
         * Delete team.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Team member list.
       */
      members: {
        /**
         * Team member list.
         */
        query(input: { teamId: string; page?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "member" | "lead"; joinedAt: string; }[]; }>;
      };
      /**
       * Add user to team.
       */
      addMember: {
        /**
         * Add user to team.
         */
        mutate(input: { teamId: string; userId: string; role: "lead" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from team.
       */
      removeMember: {
        /**
         * Remove user from team.
         */
        mutate(input: { teamId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
    };
    invitesRoutes2: {
      /**
       * Pending invites for org.
       */
      list: {
        /**
         * Pending invites for org.
         */
        query(input: { orgId: string; status?: "pending" | "accepted" | "declined" | "expired" | "revoked" }, opts?: RequestOptions): Promise<{ invites: { id: string; orgId: string; email: string; role: "admin" | "owner" | "member" | "viewer"; status: "revoked" | "pending" | "accepted" | "declined" | "expired"; invitedBy: string; expiresAt: string; acceptedAt: string | null; teams: string[]; createdAt: string; personalMessage?: string | undefined; }[]; }>;
      };
      /**
       * Send invite.
       */
      create: {
        /**
         * Send invite.
         */
        mutate(input: { orgId: string; email: string; role: "owner" | "admin" | "member" | "viewer"; teams?: Array<string>; personalMessage?: string }, opts?: RequestOptions): Promise<{ id: string; email: string; }>;
      };
      /**
       * Bulk invite by email list.
       */
      bulkCreate: {
        /**
         * Bulk invite by email list.
         */
        mutate(input: { orgId: string; emails: Array<string>; role?: "admin" | "member" | "viewer"; teams?: Array<string> }, opts?: RequestOptions): Promise<{ invited: number; failed: string[]; }>;
      };
      /**
       * Accept invite via token.
       */
      accept: {
        /**
         * Accept invite via token.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ accepted: boolean; orgId: string; }>;
      };
      /**
       * Decline invite.
       */
      decline: {
        /**
         * Decline invite.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ declined: boolean; }>;
      };
      /**
       * Revoke pending invite.
       */
      revoke: {
        /**
         * Revoke pending invite.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Resend invite email.
       */
      resend: {
        /**
         * Resend invite email.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
    };
    commentsRoutes2: {
      /**
       * Comments for a resource.
       */
      list: {
        /**
         * Comments for a resource.
         */
        query(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; nextCursor: string | null; }>;
      };
      /**
       * Post a comment.
       */
      create: {
        /**
         * Post a comment.
         */
        mutate(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; authorId: string; body: string; mentions: Array<string>; attachments: Array<{ id: string; name: string; url: string }>; parentId: string | null }, opts?: RequestOptions): Promise<{ id: string; }>;
      };
      /**
       * Edit comment body.
       */
      update: {
        /**
         * Edit comment body.
         */
        mutate(input: { id: string; body: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete comment.
       */
      delete: {
        /**
         * Delete comment.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Toggle emoji reaction.
       */
      react: {
        /**
         * Toggle emoji reaction.
         */
        mutate(input: { commentId: string; emoji: string }, opts?: RequestOptions): Promise<{ added: boolean; emoji: string; }>;
      };
      /**
       * Threaded replies.
       */
      thread: {
        /**
         * Threaded replies.
         */
        query(input: { parentId: string; limit?: number }, opts?: RequestOptions): Promise<{ replies: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Stream new comments live.
       */
      liveStream: {
        /**
         * Stream new comments live.
         */
        stream(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "new_comment"; comment: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }; }, void, unknown>>;
      };
    };
    tagsRoutes2: {
      /**
       * All tags for org.
       */
      list: {
        /**
         * All tags for org.
         */
        query(input: { orgId: string; search?: string; sortBy?: "name" | "usageCount" }, opts?: RequestOptions): Promise<{ tags: { id: string; orgId: string; name: string; color: string; usageCount: number; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Create tag.
       */
      create: {
        /**
         * Create tag.
         */
        mutate(input: { orgId: string; name: string; color: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update tag name/color.
       */
      update: {
        /**
         * Update tag name/color.
         */
        mutate(input: { id: string; name?: string; color?: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete tag.
       */
      delete: {
        /**
         * Delete tag.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Merge multiple tags into one.
       */
      merge: {
        /**
         * Merge multiple tags into one.
         */
        mutate(input: { sourceIds: Array<string>; targetId: string }, opts?: RequestOptions): Promise<{ merged: number; targetId: string; }>;
      };
      /**
       * Attach tags to a resource.
       */
      attach: {
        /**
         * Attach tags to a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ attached: number; }>;
      };
      /**
       * Detach tags from a resource.
       */
      detach: {
        /**
         * Detach tags from a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ detached: number; }>;
      };
    };
    reportsRoutes2: {
      /**
       * Saved reports for org.
       */
      list: {
        /**
         * Saved reports for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; orgId: string; name: string; type: "custom" | "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest"; config: Record<string, unknown>; schedule: { cron: string; timezone: string; recipients: string[]; format: "csv" | "pdf" | "html"; } | null; lastRunAt: string | null; lastRunStatus: "success" | "failed" | "running" | null; createdBy: string; createdAt: string; }[]; }>;
      };
      /**
       * Create report definition.
       */
      create: {
        /**
         * Create report definition.
         */
        mutate(input: { orgId: string; name: string; type: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config: Record<string, unknown>; schedule: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update report.
       */
      update: {
        /**
         * Update report.
         */
        mutate(input: { orgId?: string; name?: string; type?: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config?: Record<string, unknown>; schedule?: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy?: string; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete report.
       */
      delete: {
        /**
         * Delete report.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Trigger report run.
       */
      run: {
        /**
         * Trigger report run.
         */
        mutate(input: { id: string; format?: "pdf" | "csv" | "html" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Get report download URL.
       */
      download: {
        /**
         * Get report download URL.
         */
        query(input: { id: string; runId: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream report generation.
       */
      stream: {
        /**
         * Stream report generation.
         */
        stream(input: { id: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; percent: number; stage: "collecting_data"; }, void, unknown>>;
      };
    };
    rateLimitsRoutes2: {
      /**
       * Rate limit rules for org.
       */
      listRules: {
        /**
         * Rate limit rules for org.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ rules: { id: string; orgId: string; name: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; targetId: string | null; endpoint: string | null; limits: { window: "minute" | "hour" | "day" | "second"; max: number; burstAllowance: number; }[]; action: "log" | "throttle" | "block"; enabled: boolean; createdAt: string; }[]; }>;
      };
      /**
       * Create rate limit rule.
       */
      createRule: {
        /**
         * Create rate limit rule.
         */
        mutate(input: { orgId: string; name: string; target: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId: string | null; endpoint: string | null; limits: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update rule.
       */
      updateRule: {
        /**
         * Update rule.
         */
        mutate(input: { orgId?: string; name?: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId?: string | null; endpoint?: string | null; limits?: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete rule.
       */
      deleteRule: {
        /**
         * Delete rule.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Current usage vs. limits.
       */
      currentStatus: {
        /**
         * Current usage vs. limits.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ statuses: { ruleId: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; window: string; current: number; max: number; resetAt: string; }[]; }>;
      };
      /**
       * Manually reset rate counter.
       */
      resetCounter: {
        /**
         * Manually reset rate counter.
         */
        mutate(input: { ruleId: string; targetId: string }, opts?: RequestOptions): Promise<{ reset: boolean; }>;
      };
    };
    exportsRoutes2: {
      /**
       * Queue an export job.
       */
      create: {
        /**
         * Queue an export job.
         */
        mutate(input: { orgId: string; resource: { resource: "tasks"; projectId?: string; filters?: Record<string, unknown> } | { resource: "users"; orgId: string } | { resource: "analytics"; orgId: string; metrics: Array<string>; dateRange: { from: string; to: string } } | { resource: "audit_log"; orgId: string; from: string; to: string } | { resource: "billing"; orgId: string; year: number } | { resource: "files"; resourceType: string; resourceId: string }; format: "json" | "csv" | "xlsx" | "pdf" | "ndjson" | "parquet"; notifyEmail?: string }, opts?: RequestOptions): Promise<{ id: string; status: "queued"; }>;
      };
      /**
       * Export job history.
       */
      list: {
        /**
         * Export job history.
         */
        query(input: { orgId: string; limit?: number }, opts?: RequestOptions): Promise<{ jobs: { id: string; orgId: string; resource: { resource: "tasks"; projectId?: string | undefined; filters?: Record<string, unknown> | undefined; } | { resource: "users"; orgId: string; } | { resource: "analytics"; orgId: string; metrics: string[]; dateRange: { from: string; to: string; }; } | { resource: "audit_log"; orgId: string; from: string; to: string; } | { resource: "billing"; orgId: string; year: number; } | { resource: "files"; resourceType: string; resourceId: string; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done" | "failed" | "queued" | "running"; progress: number; rowCount: number | null; downloadUrl: string | null; expiresAt: string | null; error: string | null; createdAt: string; }[]; }>;
      };
      /**
       * Export job status.
       */
      get: {
        /**
         * Export job status.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; resource: { resource: "tasks"; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done"; progress: number; rowCount: number; downloadUrl: string; expiresAt: string; error: null; createdAt: string; }>;
      };
      /**
       * Cancel queued export.
       */
      cancel: {
        /**
         * Cancel queued export.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ cancelled: boolean; }>;
      };
      /**
       * Fresh signed download URL.
       */
      download: {
        /**
         * Fresh signed download URL.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream export progress.
       */
      progressStream: {
        /**
         * Stream export progress.
         */
        stream(input: { jobId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; jobId: string; percent: number; rowsProcessed: number; }, void, unknown>>;
      };
    };
    authRoutes3: {
      /**
       * Multi-method login — password, OAuth, SAML, magic link, passkey.
       */
      login: {
        /**
         * Multi-method login — password, OAuth, SAML, magic link, passkey.
         */
        mutate(input: { method: "password"; email: string; password: string; totpCode?: string } | { method: "oauth"; provider: "google" | "github" | "microsoft" | "okta"; code: string; redirectUri: string; state: string } | { method: "saml"; orgSlug: string; samlResponse: string; relayState?: string } | { method: "magic_link"; token: string } | { method: "passkey"; credentialId: string; clientDataJSON: string; authenticatorData: string; signature: string } & { device?: { userAgent: string; ip: string; fingerprint?: string; platform?: "web" | "ios" | "android" | "desktop" } }, opts?: RequestOptions): Promise<{ tokens: { accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }; user: { id: UserId; role: "admin"; }; }>;
      };
      /**
       * Rotate refresh token.
       */
      refresh: {
        /**
         * Rotate refresh token.
         */
        mutate(input: { refreshToken: string; sessionId: string }, opts?: RequestOptions): Promise<{ accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }>;
      };
      /**
       * Invalidate session.
       */
      logout: {
        /**
         * Invalidate session.
         */
        mutate(input: { sessionId: string }, opts?: RequestOptions): Promise<{ revoked: boolean; sessionId: SessionId; }>;
      };
      /**
       * Validate MFA code after credential check.
       */
      mfaChallenge: {
        /**
         * Validate MFA code after credential check.
         */
        mutate(input: { challengeToken: string; code: string; method: "totp" | "sms" | "email" | "webauthn" }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Send password reset email.
       */
      forgotPassword: {
        /**
         * Send password reset email.
         */
        mutate(input: { email: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
      /**
       * Apply new password via reset token.
       */
      resetPassword: {
        /**
         * Apply new password via reset token.
         */
        mutate(input: { token: string; newPassword: string }, opts?: RequestOptions): Promise<{ success: boolean; }>;
      };
      /**
       * List active sessions with device info.
       */
      sessions: {
        /**
         * List active sessions with device info.
         */
        query(input: { cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ sessions: { id: SessionId; device: { userAgent: string; ip: string; fingerprint?: string | undefined; platform?: "web" | "ios" | "android" | "desktop" | undefined; }; createdAt: string; current: boolean; }[]; nextCursor: string | null; }>;
      };
      /**
       * Current authenticated user.
       */
      me: {
        /**
         * Current authenticated user.
         */
        query(input: any, opts?: RequestOptions): Promise<{ id: UserId; role: string; }>;
      };
      /**
       * Confirm email address via verification code.
       */
      verifyEmail: {
        /**
         * Confirm email address via verification code.
         */
        mutate(input: { code: string }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Stream login activity events for the current user.
       */
      deviceStream: {
        /**
         * Stream login activity events for the current user.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "session_created"; sessionId: SessionId; ts: string; }, void, unknown>>;
      };
    };
    usersRoutes3: {
      /**
       * Paginated user list with rich filters.
       */
      list: {
        /**
         * Paginated user list with rich filters.
         */
        query(input: { page?: number; limit?: number; roles?: Array<"owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support">; search?: string; sortBy?: "name" | "email" | "createdAt"; sortDir?: "asc" | "desc" }, opts?: RequestOptions): Promise<{ users: { id: string; email: string; emailVerified: boolean; name: string; avatarUrl: string | null; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: string[]; metadata: Record<string, string | number | boolean>; createdAt: string; updatedAt: string; displayName?: string | undefined; bio?: string | undefined; timezone?: string | undefined; locale?: string | undefined; address?: { line1: string; city: string; postalCode: string; country: string; line2?: string | undefined; state?: string | undefined; } | undefined; }[]; total: number; page: number; }>;
      };
      /**
       * Fetch single user with full profile.
       */
      get: {
        /**
         * Fetch single user with full profile.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; email: string; emailVerified: true; name: string; displayName: undefined; avatarUrl: null; bio: undefined; timezone: string; locale: string; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: never[]; metadata: {}; address: undefined; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Partial profile update with nested address.
       */
      update: {
        /**
         * Partial profile update with nested address.
         */
        mutate(input: { name?: string; displayName?: string; bio?: string; timezone?: string; locale?: string; avatarUrl?: string | null; address?: { line1: string; line2?: string; city: string; state?: string; postalCode: string; country: string }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Soft-delete user.
       */
      deactivate: {
        /**
         * Soft-delete user.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ deactivated: boolean; }>;
      };
      /**
       * Change role for many users atomically.
       */
      bulkRoleChange: {
        /**
         * Change role for many users atomically.
         */
        mutate(input: { userIds: Array<string>; role: "owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support"; reason?: string }, opts?: RequestOptions): Promise<{ updated: number; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; }>;
      };
      /**
       * All permissions grouped by resource for a user.
       */
      permissionMatrix: {
        /**
         * All permissions grouped by resource for a user.
         */
        query(input: { userId: string }, opts?: RequestOptions): Promise<{ userId: string; matrix: Record<string, Record<string, boolean>>; }>;
      };
      /**
       * Upload and crop user avatar.
       */
      uploadAvatar: {
        /**
         * Upload and crop user avatar.
         */
        upload(input: { file: File; cropSize?: number } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileName: string; size: number; croppedTo: number; }>;
      };
      /**
       * Change own password — requires MFA.
       */
      changePassword: {
        /**
         * Change own password — requires MFA.
         */
        mutate(input: { currentPassword: string; newPassword: string }, opts?: RequestOptions): Promise<{ changed: boolean; }>;
      };
      /**
       * GDPR data export streamed by resource type.
       */
      exportData: {
        /**
         * GDPR data export streamed by resource type.
         */
        stream(input: { userId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "chunk"; resource: "profile" | "tasks" | "comments" | "files"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    organizationsRoutes3: {
      /**
       * Orgs accessible to caller.
       */
      list: {
        /**
         * Orgs accessible to caller.
         */
        query(input: { page?: number; limit?: number }, opts?: RequestOptions): Promise<{ orgs: { id: string; name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; memberCount: number; logoUrl: string | null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }[]; total: number; }>;
      };
      /**
       * Fetch org by ID.
       */
      get: {
        /**
         * Fetch org by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; slug: string; plan: "enterprise"; memberCount: number; logoUrl: null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }>;
      };
      /**
       * Create new organization.
       */
      create: {
        /**
         * Create new organization.
         */
        mutate(input: { name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; logoUrl: string | null; settings: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> }; ownerId: string }, opts?: RequestOptions): Promise<{ id: string; slug: string; }>;
      };
      /**
       * Update org metadata and settings.
       */
      update: {
        /**
         * Update org metadata and settings.
         */
        mutate(input: { id: string; name?: string; logoUrl?: string | null; settings?: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> } }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Permanently delete org.
       */
      delete: {
        /**
         * Permanently delete org.
         */
        mutate(input: { id: string; confirmSlug: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated org member list.
       */
      members: {
        /**
         * Paginated org member list.
         */
        query(input: { orgId: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: string; joinedAt: string; }[]; total: number; }>;
      };
      /**
       * Add user to org with role.
       */
      addMember: {
        /**
         * Add user to org with role.
         */
        mutate(input: { orgId: string; userId: string; role: "owner" | "admin" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from org.
       */
      removeMember: {
        /**
         * Remove user from org.
         */
        mutate(input: { orgId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
      /**
       * Transfer org ownership — requires MFA.
       */
      transferOwnership: {
        /**
         * Transfer org ownership — requires MFA.
         */
        mutate(input: { orgId: string; newOwnerId: string; reason?: string }, opts?: RequestOptions): Promise<{ transferred: boolean; }>;
      };
      /**
       * Stream org audit log entries live.
       */
      auditStream: {
        /**
         * Stream org audit log entries live.
         */
        stream(input: { orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; action: string; actorId: string; ts: string; }, void, unknown>>;
      };
    };
    billingRoutes3: {
      /**
       * Full subscription with pricing model.
       */
      getSubscription: {
        /**
         * Full subscription with pricing model.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; tier: "pro"; pricing: { model: "per_seat"; pricePerSeatMonth: number; minSeats: number; maxSeats: null; }; status: "active"; seats: number; currentPeriodStart: string; currentPeriodEnd: string; cancelAtPeriodEnd: false; metadata: {}; addons: never[]; }>;
      };
      /**
       * Calculate proration before confirming.
       */
      previewUpgrade: {
        /**
         * Calculate proration before confirming.
         */
        query(input: { orgId: string; targetTier: "starter" | "pro" | "business" | "enterprise"; targetSeats: number; billingInterval: "monthly" | "annual" }, opts?: RequestOptions): Promise<{ currentAmount: number; newAmount: number; prorationAmount: number; effectiveDate: string; lineItems: { description: string; amount: number; currency: string; }[]; }>;
      };
      /**
       * Create Stripe checkout session.
       */
      createCheckout: {
        /**
         * Create Stripe checkout session.
         */
        mutate(input: { orgId: string; tier: "starter" | "pro" | "business" | "enterprise"; seats: number; billingInterval: "monthly" | "annual"; addons?: Array<{ id: string; quantity: number }>; successUrl: string; cancelUrl: string; couponCode?: string }, opts?: RequestOptions): Promise<{ checkoutUrl: string; sessionId: string; }>;
      };
      /**
       * Schedule cancellation at period end.
       */
      cancelSubscription: {
        /**
         * Schedule cancellation at period end.
         */
        mutate(input: { orgId: string; reason?: string }, opts?: RequestOptions): Promise<{ cancelAtPeriodEnd: boolean; }>;
      };
      /**
       * Paginated invoice list.
       */
      invoices: {
        /**
         * Paginated invoice list.
         */
        query(input: { orgId: string; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ invoices: { id: string; amount: number; currency: string; status: string; date: string; }[]; nextCursor: string | null; }>;
      };
      /**
       * Metered usage breakdown.
       */
      usageReport: {
        /**
         * Metered usage breakdown.
         */
        query(input: { orgId: string; breakdown?: "daily" | "weekly" | "monthly" }, opts?: RequestOptions): Promise<{ period: { start: string; end: string; }; included: number; consumed: number; overage: number; overageCost: number; series: { date: string; units: number; cost: number; }[]; }>;
      };
      /**
       * Apply promotional discount code.
       */
      applyPromoCode: {
        /**
         * Apply promotional discount code.
         */
        mutate(input: { orgId: string; code: string }, opts?: RequestOptions): Promise<{ applied: boolean; discountPercent: number; }>;
      };
      /**
       * Stream invoice line items.
       */
      invoiceStream: {
        /**
         * Stream invoice line items.
         */
        stream(input: { invoiceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "line_item"; description: string; amount: number; currency: string; }, void, unknown>>;
      };
    };
    projectsRoutes3: {
      /**
       * Paginated project list with status filters.
       */
      list: {
        /**
         * Paginated project list with status filters.
         */
        query(input: { orgId: string; status?: Array<"planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled">; search?: string; page?: number }, opts?: RequestOptions): Promise<{ projects: { id: string; orgId: string; name: string; status: "active" | "cancelled" | "planning" | "on_hold" | "completed" | "archived"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: string[]; customFields: Record<string, string | number | boolean | null>; createdAt: string; updatedAt: string; description?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Project by ID.
       */
      get: {
        /**
         * Project by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; status: "active"; visibility: "private"; startDate: null; endDate: null; ownerId: string; tags: never[]; customFields: {}; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create project.
       */
      create: {
        /**
         * Create project.
         */
        mutate(input: { orgId: string; name: string; description?: string; status: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: Array<string>; customFields: Record<string, string | number | boolean | null> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update project fields.
       */
      update: {
        /**
         * Update project fields.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; status?: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility?: "private" | "internal" | "public"; startDate?: string | null; endDate?: string | null; ownerId?: string; tags?: Array<string>; customFields?: Record<string, string | number | boolean | null>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Archive project.
       */
      archive: {
        /**
         * Archive project.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ archived: boolean; }>;
      };
      /**
       * Project member list.
       */
      members: {
        /**
         * Project member list.
         */
        query(input: { projectId: string }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "viewer" | "lead" | "contributor"; joinedAt: string; }[]; }>;
      };
      /**
       * Add project member.
       */
      addMember: {
        /**
         * Add project member.
         */
        mutate(input: { projectId: string; userId: string; role: "lead" | "contributor" | "viewer" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Aggregate project stats.
       */
      stats: {
        /**
         * Aggregate project stats.
         */
        query(input: { projectId: string; period?: "7d" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ tasks: number; completed: number; openIssues: number; velocity: number; burndownPoints: { date: string; remaining: number; }[]; }>;
      };
      /**
       * Clone project structure.
       */
      duplicate: {
        /**
         * Clone project structure.
         */
        mutate(input: { id: string; newName: string; includeTasks?: boolean; includeMembers?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream full project export.
       */
      exportStream: {
        /**
         * Stream full project export.
         */
        stream(input: { id: string; format: "json" | "csv" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; resource: "task"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    tasksRoutes3: {
      /**
       * Create task with full field set.
       */
      create: {
        /**
         * Create task with full field set.
         */
        mutate(input: { projectId: string; title: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }> }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; }>;
      };
      /**
       * Task tree with recursive subtasks.
       */
      get: {
        /**
         * Task tree with recursive subtasks.
         */
        query(input: { id: string; depth?: number }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; subtasks: any[]; customFields: Record<string, { type: "text"; value: string; } | { type: "number"; value: number; } | { type: "date"; value: string; } | { type: "boolean"; value: boolean; } | { type: "select"; value: string; optionId: string; } | { type: "multi"; value: string[]; optionIds: string[]; } | { type: "user"; value: string; } | { type: "url"; value: string; }>; }>;
      };
      /**
       * Patch any task fields.
       */
      update: {
        /**
         * Patch any task fields.
         */
        mutate(input: { projectId?: string; title?: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete task permanently.
       */
      delete: {
        /**
         * Delete task permanently.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Batch status/priority update.
       */
      bulkUpdate: {
        /**
         * Batch status/priority update.
         */
        mutate(input: { ids: Array<string>; patch: { status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; dueDate?: string | null } }, opts?: RequestOptions): Promise<{ updated: number; }>;
      };
      /**
       * Comment thread for a task.
       */
      comments: {
        /**
         * Comment thread for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; body: string; author: string; createdAt: string; reactions: Record<string, string[]>; }[]; }>;
      };
      /**
       * Post comment on task.
       */
      addComment: {
        /**
         * Post comment on task.
         */
        mutate(input: { taskId: string; body: string; mentions?: Array<string> }, opts?: RequestOptions): Promise<{ id: string; body: string; }>;
      };
      /**
       * Typed audit trail for a task.
       */
      timelineEvents: {
        /**
         * Typed audit trail for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ events: ({ type: "created"; actor: string; ts: string; } | { type: "status_change"; actor: string; ts: string; from: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; to: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; } | { type: "assigned"; actor: string; ts: string; userId: string; })[]; }>;
      };
      /**
       * Tasks assigned to current user.
       */
      myTasks: {
        /**
         * Tasks assigned to current user.
         */
        query(input: { status?: Array<"backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix">; projectId?: string; limit?: number }, opts?: RequestOptions): Promise<{ tasks: any[]; total: number; }>;
      };
      /**
       * Live task update stream.
       */
      activityStream: {
        /**
         * Live task update stream.
         */
        stream(input: { projectId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "task_updated"; taskId: string; field: string; value: string; }, void, unknown>>;
      };
    };
    filesRoutes3: {
      /**
       * Files for a resource.
       */
      list: {
        /**
         * Files for a resource.
         */
        query(input: { resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; mimeTypes?: Array<string>; page?: number }, opts?: RequestOptions): Promise<{ files: { id: string; name: string; mimeType: string; sizeBytes: number; status: "deleted" | "pending" | "processing" | "ready" | "failed"; url: string | null; thumbnailUrl: string | null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: Record<string, string | number>; createdAt: string; }[]; total: number; }>;
      };
      /**
       * File metadata by ID.
       */
      get: {
        /**
         * File metadata by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; mimeType: string; sizeBytes: number; status: "ready"; url: string; thumbnailUrl: null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: {}; createdAt: string; }>;
      };
      /**
       * Get presigned S3 upload URL.
       */
      requestUpload: {
        /**
         * Get presigned S3 upload URL.
         */
        mutate(input: { fileName: string; mimeType: string; sizeBytes: number; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string }, opts?: RequestOptions): Promise<{ uploadUrl: string; fileId: string; expiresIn: number; }>;
      };
      /**
       * Mark pending upload complete.
       */
      confirmUpload: {
        /**
         * Mark pending upload complete.
         */
        mutate(input: { fileId: string; etag: string }, opts?: RequestOptions): Promise<{ id: string; confirmed: boolean; url: string; }>;
      };
      /**
       * Delete file and storage object.
       */
      delete: {
        /**
         * Delete file and storage object.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Delete multiple files.
       */
      bulkDelete: {
        /**
         * Delete multiple files.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ deleted: number; }>;
      };
      /**
       * Multipart direct file upload.
       */
      directUpload: {
        /**
         * Multipart direct file upload.
         */
        upload(input: { file: File; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; description?: string } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileId: string; name: string; size: number; }>;
      };
      /**
       * Stream file processing events.
       */
      processStream: {
        /**
         * Stream file processing events.
         */
        stream(input: { fileId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; fileId: string; stage: "ready" | "queued" | "virus_scan" | "thumbnail"; percent: number; }, void, unknown>>;
      };
    };
    notificationsRoutes3: {
      /**
       * Paginated notification list.
       */
      list: {
        /**
         * Paginated notification list.
         */
        query(input: { unreadOnly?: boolean; types?: Array<"mention" | "task_assigned" | "task_completed" | "comment" | "billing" | "security" | "system" | "workflow_run" | "integration_sync">; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ notifications: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }[]; unreadCount: number; nextCursor: string | null; }>;
      };
      /**
       * Mark notifications as read.
       */
      markRead: {
        /**
         * Mark notifications as read.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ marked: number; }>;
      };
      /**
       * Mark all notifications read.
       */
      markAllRead: {
        /**
         * Mark all notifications read.
         */
        mutate(input: any, opts?: RequestOptions): Promise<{ marked: boolean; }>;
      };
      /**
       * Get notification preferences.
       */
      preferences: {
        /**
         * Get notification preferences.
         */
        query(input: any, opts?: RequestOptions): Promise<{ channels: Record<string, boolean>; types: Partial<Record<"billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync", boolean>>; }>;
      };
      /**
       * Update preferences.
       */
      updatePreferences: {
        /**
         * Update preferences.
         */
        mutate(input: { channels?: Record<string, boolean>; types?: Record<string, boolean> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Register Web Push subscription.
       */
      pushSubscribe: {
        /**
         * Register Web Push subscription.
         */
        mutate(input: { endpoint: string; keys: { p256dh: string; auth: string } }, opts?: RequestOptions): Promise<{ subscribed: boolean; }>;
      };
      /**
       * Real-time notification SSE.
       */
      liveStream: {
        /**
         * Real-time notification SSE.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "notification"; notification: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }; }, void, unknown>>;
      };
    };
    searchRoutes3: {
      /**
       * Full-text search across resource types.
       */
      global: {
        /**
         * Full-text search across resource types.
         */
        query(input: { q: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document">; orgId: string; limit?: number; highlight?: boolean }, opts?: RequestOptions): Promise<{ results: ({ type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; })[]; total: number; took: number; }>;
      };
      /**
       * Autocomplete suggestions.
       */
      suggestions: {
        /**
         * Autocomplete suggestions.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ suggestions: { label: string; type: "user" | "file" | "task" | "project" | "comment" | "workflow" | "document"; id: string; }[]; }>;
      };
      /**
       * Faceted filter counts.
       */
      filters: {
        /**
         * Faceted filter counts.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ facets: Record<"user" | "file" | "task" | "project" | "comment" | "workflow" | "document", number>; }>;
      };
      /**
       * Trigger manual reindex for org.
       */
      reindex: {
        /**
         * Trigger manual reindex for org.
         */
        mutate(input: { orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream results progressively.
       */
      stream: {
        /**
         * Stream results progressively.
         */
        stream(input: { q: string; orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "result"; result: { type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; }; }, void, unknown>>;
      };
    };
    analyticsRoutes3: {
      /**
       * Arbitrary analytics query.
       */
      query: {
        /**
         * Arbitrary analytics query.
         */
        mutate(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }, opts?: RequestOptions): Promise<{ rows: never[]; totals: {}; comparison: undefined; meta: { executionMs: number; rowsScanned: number; cached: false; granularity: "minute" | "hour" | "day" | "week" | "month"; }; }>;
      };
      /**
       * Multi-step funnel conversion.
       */
      funnel: {
        /**
         * Multi-step funnel conversion.
         */
        mutate(input: { orgId: string; steps: Array<{ name: string; event: string; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }> }>; dateRange: { from: string; to: string } }, opts?: RequestOptions): Promise<{ steps: { name: string; count: number; conversionRate: number; avgTimeToNextStep: number | null; }[]; overallConversion: number; }>;
      };
      /**
       * Cohort retention analysis.
       */
      retention: {
        /**
         * Cohort retention analysis.
         */
        mutate(input: { orgId: string; cohortEvent: string; returnEvent: string; dateRange: { from: string; to: string }; granularity?: "minute" | "hour" | "day" | "week" | "month" }, opts?: RequestOptions): Promise<{ cohorts: { date: string; size: number; retention: number[]; }[]; }>;
      };
      /**
       * Saved report definitions.
       */
      savedReports: {
        /**
         * Saved report definitions.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; name: string; query: { orgId: string; metrics: string[]; dimensions: string[]; filters: { field: string; operator: { op: "eq"; value: string | number | boolean; } | { op: "neq"; value: string | number | boolean; } | { op: "gt"; value: number; } | { op: "gte"; value: number; } | { op: "lt"; value: number; } | { op: "lte"; value: number; } | { op: "in"; values: (string | number)[]; } | { op: "between"; min: number; max: number; }; }[]; dateRange: { from: string; to: string; granularity: "minute" | "hour" | "day" | "week" | "month"; timezone: string; }; orderBy: { field: string; direction: "asc" | "desc"; }[]; limit: number; compareWith?: { from: string; to: string; } | undefined; }; schedule: { cron: string; recipients: string[]; } | null; lastRunAt: string | null; }[]; }>;
      };
      /**
       * Persist a report definition.
       */
      saveReport: {
        /**
         * Persist a report definition.
         */
        mutate(input: { orgId: string; name: string; query: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }; schedule?: { cron: string; recipients: Array<string> } }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream query results row by row.
       */
      exportStream: {
        /**
         * Stream query results row by row.
         */
        stream(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string }; format?: "json" | "csv" | "parquet" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; index: number; data: Record<string, string | number | null>; }, void, unknown>>;
      };
    };
    webhooksRoutes3: {
      /**
       * Webhooks for an org.
       */
      list: {
        /**
         * Webhooks for an org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ webhooks: { id: string; orgId: string; url: string; events: ("task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed")[]; enabled: boolean; secret: string; headers: Record<string, string>; retryPolicy: { maxAttempts: number; backoffMs: number; }; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Register webhook endpoint.
       */
      create: {
        /**
         * Register webhook endpoint.
         */
        mutate(input: { orgId: string; url: string; description?: string; events: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled: boolean; secret: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number } }, opts?: RequestOptions): Promise<{ id: string; url: string; }>;
      };
      /**
       * Update webhook.
       */
      update: {
        /**
         * Update webhook.
         */
        mutate(input: { orgId?: string; url?: string; description?: string; events?: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled?: boolean; secret?: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Remove webhook.
       */
      delete: {
        /**
         * Remove webhook.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Recent delivery attempts.
       */
      deliveries: {
        /**
         * Recent delivery attempts.
         */
        query(input: { webhookId: string; status?: "success" | "failed" | "pending"; limit?: number }, opts?: RequestOptions): Promise<{ deliveries: { id: string; status: number; attempt: number; ts: string; error: string | null; }[]; }>;
      };
      /**
       * Retry failed delivery.
       */
      redeliver: {
        /**
         * Retry failed delivery.
         */
        mutate(input: { deliveryId: string }, opts?: RequestOptions): Promise<{ queued: boolean; }>;
      };
      /**
       * Send test event to webhook.
       */
      testPing: {
        /**
         * Send test event to webhook.
         */
        mutate(input: { webhookId: string; eventType: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed" }, opts?: RequestOptions): Promise<{ sent: boolean; deliveryId: string; }>;
      };
      /**
       * Stream webhook delivery events.
       */
      logStream: {
        /**
         * Stream webhook delivery events.
         */
        stream(input: { webhookId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "delivery"; status: number; ts: string; }, void, unknown>>;
      };
    };
    aiRoutes3: {
      /**
       * Multi-model streaming chat with tool-call support.
       */
      chat: {
        /**
         * Multi-model streaming chat with tool-call support.
         */
        stream(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "content_delta"; text: string; finishReason?: undefined; usage?: undefined; } | { type: "finish"; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; text?: undefined; }, void, unknown>>;
      };
      /**
       * Non-streaming completion.
       */
      complete: {
        /**
         * Non-streaming completion.
         */
        mutate(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string>; stream?: false }, opts?: RequestOptions): Promise<{ id: string; role: "assistant"; content: string | ({ type: "text"; text: string; } | { type: "image_url"; url: string; detail: "high" | "low" | "auto"; } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown>; } | { type: "tool_result"; toolCallId: string; content: string; isError: boolean; })[]; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; }>;
      };
      /**
       * Batch embedding generation.
       */
      embed: {
        /**
         * Batch embedding generation.
         */
        mutate(input: { inputs: Array<string | { text: string; id: string }>; model: "text-embedding-3-small" | "text-embedding-3-large"; dimensions?: number; outputFormat?: "float" | "base64" | "int8" }, opts?: RequestOptions): Promise<{ embeddings: { index: number; embedding: number[]; inputTokens: number; }[]; model: "text-embedding-3-small" | "text-embedding-3-large"; totalTokens: number; }>;
      };
      /**
       * Run content moderation checks.
       */
      moderateContent: {
        /**
         * Run content moderation checks.
         */
        mutate(input: { inputs: Array<{ id: string; text: string }>; categories?: Array<"hate" | "harassment" | "self_harm" | "sexual" | "violence" | "dangerous"> }, opts?: RequestOptions): Promise<{ results: { id: string; flagged: boolean; categories: Partial<Record<string, boolean>>; scores: Partial<Record<string, number>>; }[]; }>;
      };
      /**
       * Structured text summarization.
       */
      summarize: {
        /**
         * Structured text summarization.
         */
        mutate(input: { text: string; format?: "bullets" | "paragraph" | "tldr"; maxLength?: number }, opts?: RequestOptions): Promise<{ summary: string; wordCount: number; }>;
      };
      /**
       * AI token usage for billing period.
       */
      usageStats: {
        /**
         * AI token usage for billing period.
         */
        query(input: { orgId: string; period?: "current" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ promptTokens: number; completionTokens: number; totalCost: number; byModel: Record<string, { tokens: number; cost: number; }>; }>;
      };
    };
    auditRoutes3: {
      /**
       * Paginated audit log with filters.
       */
      log: {
        /**
         * Paginated audit log with filters.
         */
        query(input: { orgId: string; actorId?: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled">; resourceType?: string; from?: string; to?: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ entries: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }[]; total: number; }>;
      };
      /**
       * Single audit entry by ID.
       */
      getEntry: {
        /**
         * Single audit entry by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; actorId: string; actorEmail: string; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: null; resourceId: null; metadata: {}; ip: string; userAgent: string; ts: string; }>;
      };
      /**
       * Update log retention policy.
       */
      retention: {
        /**
         * Update log retention policy.
         */
        mutate(input: { orgId: string; retentionDays: number }, opts?: RequestOptions): Promise<{ updated: boolean; retentionDays: number; }>;
      };
      /**
       * Stream audit log as NDJSON.
       */
      export: {
        /**
         * Stream audit log as NDJSON.
         */
        stream(input: { orgId: string; from: string; to: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; data: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }; }, void, unknown>>;
      };
    };
    apiKeysRoutes3: {
      /**
       * List API keys for org.
       */
      list: {
        /**
         * List API keys for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ keys: { id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: string | null; lastUsedAt: string | null; createdBy: string; createdAt: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number; } | null; }[]; }>;
      };
      /**
       * Create API key.
       */
      create: {
        /**
         * Create API key.
         */
        mutate(input: { orgId: string; name: string; scopes: Array<"read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write">; expiresAt: string | null; createdBy: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number } | null }, opts?: RequestOptions): Promise<{ key: string; id: string; prefix: string; }>;
      };
      /**
       * Revoke API key.
       */
      revoke: {
        /**
         * Revoke API key.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Rotate key secret.
       */
      rotate: {
        /**
         * Rotate key secret.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ newKey: string; id: string; }>;
      };
      /**
       * API key metadata.
       */
      get: {
        /**
         * API key metadata.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: null; lastUsedAt: null; createdBy: string; createdAt: string; rateLimit: null; }>;
      };
      /**
       * Key request usage stats.
       */
      usage: {
        /**
         * Key request usage stats.
         */
        query(input: { id: string; period?: "1h" | "24h" | "7d" | "30d" }, opts?: RequestOptions): Promise<{ requests: number; errors: number; p50Ms: number; p99Ms: number; series: { ts: string; count: number; }[]; }>;
      };
    };
    integrationsRoutes3: {
      /**
       * All integrations with status.
       */
      list: {
        /**
         * All integrations with status.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ integrations: { id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: boolean; enabled: boolean; status: "error" | "active" | "disconnected" | "syncing"; lastSyncAt: string | null; errorMessage: string | null; config: Record<string, unknown>; }[]; }>;
      };
      /**
       * Single integration status.
       */
      get: {
        /**
         * Single integration status.
         */
        query(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: true; enabled: true; status: "active"; lastSyncAt: string; errorMessage: null; config: {}; }>;
      };
      /**
       * Start OAuth install flow.
       */
      install: {
        /**
         * Start OAuth install flow.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; redirectUri: string; scopes?: Array<string> }, opts?: RequestOptions): Promise<{ authUrl: string; }>;
      };
      /**
       * Exchange OAuth code for tokens.
       */
      oauthCallback: {
        /**
         * Exchange OAuth code for tokens.
         */
        mutate(input: { code: string; state: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ installed: boolean; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; }>;
      };
      /**
       * Remove integration.
       */
      uninstall: {
        /**
         * Remove integration.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ uninstalled: boolean; }>;
      };
      /**
       * Update integration config.
       */
      updateConfig: {
        /**
         * Update integration config.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; config: Record<string, unknown> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Manual sync trigger.
       */
      triggerSync: {
        /**
         * Manual sync trigger.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream sync progress events.
       */
      syncStream: {
        /**
         * Stream sync progress events.
         */
        stream(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; itemsSynced: number; total: number; }, void, unknown>>;
      };
    };
    workflowsRoutes3: {
      /**
       * Create workflow with recursive condition tree.
       */
      create: {
        /**
         * Create workflow with recursive condition tree.
         */
        mutate(input: { name: string; description?: string; trigger: { event: string; filters?: unknown; debounceMs?: number }; steps: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; orgId: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * List workflows with run stats.
       */
      list: {
        /**
         * List workflows with run stats.
         */
        query(input: { orgId: string; enabled?: boolean }, opts?: RequestOptions): Promise<{ workflows: { id: string; name: string; enabled: boolean; lastRunAt: string | null; runCount: number; failureRate: number; }[]; }>;
      };
      /**
       * Workflow detail.
       */
      get: {
        /**
         * Workflow detail.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * Update workflow definition.
       */
      update: {
        /**
         * Update workflow definition.
         */
        mutate(input: { name?: string; description?: string; trigger?: { event: string; filters?: unknown; debounceMs?: number }; steps?: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete workflow.
       */
      delete: {
        /**
         * Delete workflow.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated run history.
       */
      runHistory: {
        /**
         * Paginated run history.
         */
        query(input: { workflowId: string; status?: "running" | "success" | "failed" | "cancelled"; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ runs: { id: string; status: string; startedAt: string; finishedAt: string | null; steps: { stepId: string; status: string; durationMs: number; error?: string | undefined; }[]; }[]; nextCursor: string | null; }>;
      };
      /**
       * Dry-run with streamed step outcomes.
       */
      testRun: {
        /**
         * Dry-run with streamed step outcomes.
         */
        stream(input: { workflowId: string; samplePayload: Record<string, unknown> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "step_result"; stepId: string; status: "success"; durationMs: number; output: Record<string, unknown>; }, void, unknown>>;
      };
    };
    documentsRoutes3: {
      /**
       * Documents in a project.
       */
      list: {
        /**
         * Documents in a project.
         */
        query(input: { projectId: string; page?: number; limit?: number; search?: string }, opts?: RequestOptions): Promise<{ documents: Pick<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "private" | "public" | "project"; authorId: string; collaborators: string[]; version: number; publishedAt: string | null; createdAt: string; updatedAt: string; }, "createdAt" | "id" | "updatedAt" | "title" | "authorId">[]; total: number; }>;
      };
      /**
       * Full document with blocks.
       */
      get: {
        /**
         * Full document with blocks.
         */
        query(input: { id: string; version?: number }, opts?: RequestOptions): Promise<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "project"; authorId: string; collaborators: never[]; version: number; publishedAt: null; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create document.
       */
      create: {
        /**
         * Create document.
         */
        mutate(input: { projectId: string; title: string; blocks: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility: "private" | "project" | "public"; authorId: string; collaborators: Array<string> }, opts?: RequestOptions): Promise<{ id: string; title: string; }>;
      };
      /**
       * Update blocks and metadata.
       */
      update: {
        /**
         * Update blocks and metadata.
         */
        mutate(input: { id: string; title?: string; blocks?: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility?: "private" | "project" | "public" }, opts?: RequestOptions): Promise<{ id: string; version: number; }>;
      };
      /**
       * Delete document.
       */
      delete: {
        /**
         * Delete document.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Publish document publicly.
       */
      publish: {
        /**
         * Publish document publicly.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ publishedAt: string; url: string; }>;
      };
      /**
       * Document version history.
       */
      history: {
        /**
         * Document version history.
         */
        query(input: { id: string; limit?: number }, opts?: RequestOptions): Promise<{ versions: { version: number; editor: string; changedAt: string; summary: string; }[]; }>;
      };
      /**
       * Collaborative edit event stream.
       */
      editStream: {
        /**
         * Collaborative edit event stream.
         */
        stream(input: { docId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "block_updated"; blockId: string; patch: Record<string, unknown>; editorId: string; }, void, unknown>>;
      };
    };
    permissionsRoutes3: {
      /**
       * All ABAC policies for org.
       */
      listPolicies: {
        /**
         * All ABAC policies for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ policies: { id: string; orgId: string; name: string; effect: "allow" | "deny"; resources: ("file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics")[]; actions: ("read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve")[]; conditions: { field: string; operator: "in" | "eq" | "neq" | "owned_by"; value: string | string[]; }[]; subjects: { type: "user" | "role" | "team"; id: string; }[]; priority: number; createdAt: string; }[]; }>;
      };
      /**
       * Create ABAC policy.
       */
      createPolicy: {
        /**
         * Create ABAC policy.
         */
        mutate(input: { orgId: string; name: string; effect: "allow" | "deny"; resources: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update policy definition.
       */
      updatePolicy: {
        /**
         * Update policy definition.
         */
        mutate(input: { orgId?: string; name?: string; effect?: "allow" | "deny"; resources?: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions?: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects?: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete policy.
       */
      deletePolicy: {
        /**
         * Delete policy.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Evaluate access for a subject.
       */
      checkAccess: {
        /**
         * Evaluate access for a subject.
         */
        query(input: { orgId: string; subjectId: string; resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }, opts?: RequestOptions): Promise<{ allowed: boolean; matchedPolicies: string[]; }>;
      };
      /**
       * Batch permission check.
       */
      bulkCheck: {
        /**
         * Batch permission check.
         */
        mutate(input: { orgId: string; subjectId: string; checks: Array<{ resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }> }, opts?: RequestOptions): Promise<{ results: { resource: "file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics"; action: "read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve"; allowed: boolean; }[]; }>;
      };
    };
    teamsRoutes3: {
      /**
       * Teams in an org.
       */
      list: {
        /**
         * Teams in an org.
         */
        query(input: { orgId: string; visibility?: "public" | "private" | "secret" }, opts?: RequestOptions): Promise<{ teams: { id: string; orgId: string; name: string; visibility: "private" | "public" | "secret"; memberCount: number; projectIds: string[]; createdAt: string; description?: string | undefined; color?: string | undefined; icon?: string | undefined; }[]; }>;
      };
      /**
       * Team by ID.
       */
      get: {
        /**
         * Team by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; description: undefined; color: string; icon: string; visibility: "public"; memberCount: number; projectIds: never[]; createdAt: string; }>;
      };
      /**
       * Create team.
       */
      create: {
        /**
         * Create team.
         */
        mutate(input: { orgId: string; name: string; description?: string; color?: string; icon?: string; visibility: "public" | "private" | "secret"; projectIds: Array<string> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update team metadata.
       */
      update: {
        /**
         * Update team metadata.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; color?: string; icon?: string; visibility?: "public" | "private" | "secret"; projectIds?: Array<string>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete team.
       */
      delete: {
        /**
         * Delete team.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Team member list.
       */
      members: {
        /**
         * Team member list.
         */
        query(input: { teamId: string; page?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "member" | "lead"; joinedAt: string; }[]; }>;
      };
      /**
       * Add user to team.
       */
      addMember: {
        /**
         * Add user to team.
         */
        mutate(input: { teamId: string; userId: string; role: "lead" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from team.
       */
      removeMember: {
        /**
         * Remove user from team.
         */
        mutate(input: { teamId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
    };
    invitesRoutes3: {
      /**
       * Pending invites for org.
       */
      list: {
        /**
         * Pending invites for org.
         */
        query(input: { orgId: string; status?: "pending" | "accepted" | "declined" | "expired" | "revoked" }, opts?: RequestOptions): Promise<{ invites: { id: string; orgId: string; email: string; role: "admin" | "owner" | "member" | "viewer"; status: "revoked" | "pending" | "accepted" | "declined" | "expired"; invitedBy: string; expiresAt: string; acceptedAt: string | null; teams: string[]; createdAt: string; personalMessage?: string | undefined; }[]; }>;
      };
      /**
       * Send invite.
       */
      create: {
        /**
         * Send invite.
         */
        mutate(input: { orgId: string; email: string; role: "owner" | "admin" | "member" | "viewer"; teams?: Array<string>; personalMessage?: string }, opts?: RequestOptions): Promise<{ id: string; email: string; }>;
      };
      /**
       * Bulk invite by email list.
       */
      bulkCreate: {
        /**
         * Bulk invite by email list.
         */
        mutate(input: { orgId: string; emails: Array<string>; role?: "admin" | "member" | "viewer"; teams?: Array<string> }, opts?: RequestOptions): Promise<{ invited: number; failed: string[]; }>;
      };
      /**
       * Accept invite via token.
       */
      accept: {
        /**
         * Accept invite via token.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ accepted: boolean; orgId: string; }>;
      };
      /**
       * Decline invite.
       */
      decline: {
        /**
         * Decline invite.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ declined: boolean; }>;
      };
      /**
       * Revoke pending invite.
       */
      revoke: {
        /**
         * Revoke pending invite.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Resend invite email.
       */
      resend: {
        /**
         * Resend invite email.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
    };
    commentsRoutes3: {
      /**
       * Comments for a resource.
       */
      list: {
        /**
         * Comments for a resource.
         */
        query(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; nextCursor: string | null; }>;
      };
      /**
       * Post a comment.
       */
      create: {
        /**
         * Post a comment.
         */
        mutate(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; authorId: string; body: string; mentions: Array<string>; attachments: Array<{ id: string; name: string; url: string }>; parentId: string | null }, opts?: RequestOptions): Promise<{ id: string; }>;
      };
      /**
       * Edit comment body.
       */
      update: {
        /**
         * Edit comment body.
         */
        mutate(input: { id: string; body: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete comment.
       */
      delete: {
        /**
         * Delete comment.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Toggle emoji reaction.
       */
      react: {
        /**
         * Toggle emoji reaction.
         */
        mutate(input: { commentId: string; emoji: string }, opts?: RequestOptions): Promise<{ added: boolean; emoji: string; }>;
      };
      /**
       * Threaded replies.
       */
      thread: {
        /**
         * Threaded replies.
         */
        query(input: { parentId: string; limit?: number }, opts?: RequestOptions): Promise<{ replies: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Stream new comments live.
       */
      liveStream: {
        /**
         * Stream new comments live.
         */
        stream(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "new_comment"; comment: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }; }, void, unknown>>;
      };
    };
    tagsRoutes3: {
      /**
       * All tags for org.
       */
      list: {
        /**
         * All tags for org.
         */
        query(input: { orgId: string; search?: string; sortBy?: "name" | "usageCount" }, opts?: RequestOptions): Promise<{ tags: { id: string; orgId: string; name: string; color: string; usageCount: number; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Create tag.
       */
      create: {
        /**
         * Create tag.
         */
        mutate(input: { orgId: string; name: string; color: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update tag name/color.
       */
      update: {
        /**
         * Update tag name/color.
         */
        mutate(input: { id: string; name?: string; color?: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete tag.
       */
      delete: {
        /**
         * Delete tag.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Merge multiple tags into one.
       */
      merge: {
        /**
         * Merge multiple tags into one.
         */
        mutate(input: { sourceIds: Array<string>; targetId: string }, opts?: RequestOptions): Promise<{ merged: number; targetId: string; }>;
      };
      /**
       * Attach tags to a resource.
       */
      attach: {
        /**
         * Attach tags to a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ attached: number; }>;
      };
      /**
       * Detach tags from a resource.
       */
      detach: {
        /**
         * Detach tags from a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ detached: number; }>;
      };
    };
    reportsRoutes3: {
      /**
       * Saved reports for org.
       */
      list: {
        /**
         * Saved reports for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; orgId: string; name: string; type: "custom" | "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest"; config: Record<string, unknown>; schedule: { cron: string; timezone: string; recipients: string[]; format: "csv" | "pdf" | "html"; } | null; lastRunAt: string | null; lastRunStatus: "success" | "failed" | "running" | null; createdBy: string; createdAt: string; }[]; }>;
      };
      /**
       * Create report definition.
       */
      create: {
        /**
         * Create report definition.
         */
        mutate(input: { orgId: string; name: string; type: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config: Record<string, unknown>; schedule: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update report.
       */
      update: {
        /**
         * Update report.
         */
        mutate(input: { orgId?: string; name?: string; type?: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config?: Record<string, unknown>; schedule?: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy?: string; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete report.
       */
      delete: {
        /**
         * Delete report.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Trigger report run.
       */
      run: {
        /**
         * Trigger report run.
         */
        mutate(input: { id: string; format?: "pdf" | "csv" | "html" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Get report download URL.
       */
      download: {
        /**
         * Get report download URL.
         */
        query(input: { id: string; runId: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream report generation.
       */
      stream: {
        /**
         * Stream report generation.
         */
        stream(input: { id: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; percent: number; stage: "collecting_data"; }, void, unknown>>;
      };
    };
    rateLimitsRoutes3: {
      /**
       * Rate limit rules for org.
       */
      listRules: {
        /**
         * Rate limit rules for org.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ rules: { id: string; orgId: string; name: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; targetId: string | null; endpoint: string | null; limits: { window: "minute" | "hour" | "day" | "second"; max: number; burstAllowance: number; }[]; action: "log" | "throttle" | "block"; enabled: boolean; createdAt: string; }[]; }>;
      };
      /**
       * Create rate limit rule.
       */
      createRule: {
        /**
         * Create rate limit rule.
         */
        mutate(input: { orgId: string; name: string; target: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId: string | null; endpoint: string | null; limits: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update rule.
       */
      updateRule: {
        /**
         * Update rule.
         */
        mutate(input: { orgId?: string; name?: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId?: string | null; endpoint?: string | null; limits?: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete rule.
       */
      deleteRule: {
        /**
         * Delete rule.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Current usage vs. limits.
       */
      currentStatus: {
        /**
         * Current usage vs. limits.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ statuses: { ruleId: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; window: string; current: number; max: number; resetAt: string; }[]; }>;
      };
      /**
       * Manually reset rate counter.
       */
      resetCounter: {
        /**
         * Manually reset rate counter.
         */
        mutate(input: { ruleId: string; targetId: string }, opts?: RequestOptions): Promise<{ reset: boolean; }>;
      };
    };
    exportsRoutes3: {
      /**
       * Queue an export job.
       */
      create: {
        /**
         * Queue an export job.
         */
        mutate(input: { orgId: string; resource: { resource: "tasks"; projectId?: string; filters?: Record<string, unknown> } | { resource: "users"; orgId: string } | { resource: "analytics"; orgId: string; metrics: Array<string>; dateRange: { from: string; to: string } } | { resource: "audit_log"; orgId: string; from: string; to: string } | { resource: "billing"; orgId: string; year: number } | { resource: "files"; resourceType: string; resourceId: string }; format: "json" | "csv" | "xlsx" | "pdf" | "ndjson" | "parquet"; notifyEmail?: string }, opts?: RequestOptions): Promise<{ id: string; status: "queued"; }>;
      };
      /**
       * Export job history.
       */
      list: {
        /**
         * Export job history.
         */
        query(input: { orgId: string; limit?: number }, opts?: RequestOptions): Promise<{ jobs: { id: string; orgId: string; resource: { resource: "tasks"; projectId?: string | undefined; filters?: Record<string, unknown> | undefined; } | { resource: "users"; orgId: string; } | { resource: "analytics"; orgId: string; metrics: string[]; dateRange: { from: string; to: string; }; } | { resource: "audit_log"; orgId: string; from: string; to: string; } | { resource: "billing"; orgId: string; year: number; } | { resource: "files"; resourceType: string; resourceId: string; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done" | "failed" | "queued" | "running"; progress: number; rowCount: number | null; downloadUrl: string | null; expiresAt: string | null; error: string | null; createdAt: string; }[]; }>;
      };
      /**
       * Export job status.
       */
      get: {
        /**
         * Export job status.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; resource: { resource: "tasks"; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done"; progress: number; rowCount: number; downloadUrl: string; expiresAt: string; error: null; createdAt: string; }>;
      };
      /**
       * Cancel queued export.
       */
      cancel: {
        /**
         * Cancel queued export.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ cancelled: boolean; }>;
      };
      /**
       * Fresh signed download URL.
       */
      download: {
        /**
         * Fresh signed download URL.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream export progress.
       */
      progressStream: {
        /**
         * Stream export progress.
         */
        stream(input: { jobId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; jobId: string; percent: number; rowsProcessed: number; }, void, unknown>>;
      };
    };
    authRoutes4: {
      /**
       * Multi-method login — password, OAuth, SAML, magic link, passkey.
       */
      login: {
        /**
         * Multi-method login — password, OAuth, SAML, magic link, passkey.
         */
        mutate(input: { method: "password"; email: string; password: string; totpCode?: string } | { method: "oauth"; provider: "google" | "github" | "microsoft" | "okta"; code: string; redirectUri: string; state: string } | { method: "saml"; orgSlug: string; samlResponse: string; relayState?: string } | { method: "magic_link"; token: string } | { method: "passkey"; credentialId: string; clientDataJSON: string; authenticatorData: string; signature: string } & { device?: { userAgent: string; ip: string; fingerprint?: string; platform?: "web" | "ios" | "android" | "desktop" } }, opts?: RequestOptions): Promise<{ tokens: { accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }; user: { id: UserId; role: "admin"; }; }>;
      };
      /**
       * Rotate refresh token.
       */
      refresh: {
        /**
         * Rotate refresh token.
         */
        mutate(input: { refreshToken: string; sessionId: string }, opts?: RequestOptions): Promise<{ accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }>;
      };
      /**
       * Invalidate session.
       */
      logout: {
        /**
         * Invalidate session.
         */
        mutate(input: { sessionId: string }, opts?: RequestOptions): Promise<{ revoked: boolean; sessionId: SessionId; }>;
      };
      /**
       * Validate MFA code after credential check.
       */
      mfaChallenge: {
        /**
         * Validate MFA code after credential check.
         */
        mutate(input: { challengeToken: string; code: string; method: "totp" | "sms" | "email" | "webauthn" }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Send password reset email.
       */
      forgotPassword: {
        /**
         * Send password reset email.
         */
        mutate(input: { email: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
      /**
       * Apply new password via reset token.
       */
      resetPassword: {
        /**
         * Apply new password via reset token.
         */
        mutate(input: { token: string; newPassword: string }, opts?: RequestOptions): Promise<{ success: boolean; }>;
      };
      /**
       * List active sessions with device info.
       */
      sessions: {
        /**
         * List active sessions with device info.
         */
        query(input: { cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ sessions: { id: SessionId; device: { userAgent: string; ip: string; fingerprint?: string | undefined; platform?: "web" | "ios" | "android" | "desktop" | undefined; }; createdAt: string; current: boolean; }[]; nextCursor: string | null; }>;
      };
      /**
       * Current authenticated user.
       */
      me: {
        /**
         * Current authenticated user.
         */
        query(input: any, opts?: RequestOptions): Promise<{ id: UserId; role: string; }>;
      };
      /**
       * Confirm email address via verification code.
       */
      verifyEmail: {
        /**
         * Confirm email address via verification code.
         */
        mutate(input: { code: string }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Stream login activity events for the current user.
       */
      deviceStream: {
        /**
         * Stream login activity events for the current user.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "session_created"; sessionId: SessionId; ts: string; }, void, unknown>>;
      };
    };
    usersRoutes4: {
      /**
       * Paginated user list with rich filters.
       */
      list: {
        /**
         * Paginated user list with rich filters.
         */
        query(input: { page?: number; limit?: number; roles?: Array<"owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support">; search?: string; sortBy?: "name" | "email" | "createdAt"; sortDir?: "asc" | "desc" }, opts?: RequestOptions): Promise<{ users: { id: string; email: string; emailVerified: boolean; name: string; avatarUrl: string | null; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: string[]; metadata: Record<string, string | number | boolean>; createdAt: string; updatedAt: string; displayName?: string | undefined; bio?: string | undefined; timezone?: string | undefined; locale?: string | undefined; address?: { line1: string; city: string; postalCode: string; country: string; line2?: string | undefined; state?: string | undefined; } | undefined; }[]; total: number; page: number; }>;
      };
      /**
       * Fetch single user with full profile.
       */
      get: {
        /**
         * Fetch single user with full profile.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; email: string; emailVerified: true; name: string; displayName: undefined; avatarUrl: null; bio: undefined; timezone: string; locale: string; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: never[]; metadata: {}; address: undefined; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Partial profile update with nested address.
       */
      update: {
        /**
         * Partial profile update with nested address.
         */
        mutate(input: { name?: string; displayName?: string; bio?: string; timezone?: string; locale?: string; avatarUrl?: string | null; address?: { line1: string; line2?: string; city: string; state?: string; postalCode: string; country: string }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Soft-delete user.
       */
      deactivate: {
        /**
         * Soft-delete user.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ deactivated: boolean; }>;
      };
      /**
       * Change role for many users atomically.
       */
      bulkRoleChange: {
        /**
         * Change role for many users atomically.
         */
        mutate(input: { userIds: Array<string>; role: "owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support"; reason?: string }, opts?: RequestOptions): Promise<{ updated: number; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; }>;
      };
      /**
       * All permissions grouped by resource for a user.
       */
      permissionMatrix: {
        /**
         * All permissions grouped by resource for a user.
         */
        query(input: { userId: string }, opts?: RequestOptions): Promise<{ userId: string; matrix: Record<string, Record<string, boolean>>; }>;
      };
      /**
       * Upload and crop user avatar.
       */
      uploadAvatar: {
        /**
         * Upload and crop user avatar.
         */
        upload(input: { file: File; cropSize?: number } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileName: string; size: number; croppedTo: number; }>;
      };
      /**
       * Change own password — requires MFA.
       */
      changePassword: {
        /**
         * Change own password — requires MFA.
         */
        mutate(input: { currentPassword: string; newPassword: string }, opts?: RequestOptions): Promise<{ changed: boolean; }>;
      };
      /**
       * GDPR data export streamed by resource type.
       */
      exportData: {
        /**
         * GDPR data export streamed by resource type.
         */
        stream(input: { userId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "chunk"; resource: "profile" | "tasks" | "comments" | "files"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    organizationsRoutes4: {
      /**
       * Orgs accessible to caller.
       */
      list: {
        /**
         * Orgs accessible to caller.
         */
        query(input: { page?: number; limit?: number }, opts?: RequestOptions): Promise<{ orgs: { id: string; name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; memberCount: number; logoUrl: string | null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }[]; total: number; }>;
      };
      /**
       * Fetch org by ID.
       */
      get: {
        /**
         * Fetch org by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; slug: string; plan: "enterprise"; memberCount: number; logoUrl: null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }>;
      };
      /**
       * Create new organization.
       */
      create: {
        /**
         * Create new organization.
         */
        mutate(input: { name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; logoUrl: string | null; settings: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> }; ownerId: string }, opts?: RequestOptions): Promise<{ id: string; slug: string; }>;
      };
      /**
       * Update org metadata and settings.
       */
      update: {
        /**
         * Update org metadata and settings.
         */
        mutate(input: { id: string; name?: string; logoUrl?: string | null; settings?: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> } }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Permanently delete org.
       */
      delete: {
        /**
         * Permanently delete org.
         */
        mutate(input: { id: string; confirmSlug: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated org member list.
       */
      members: {
        /**
         * Paginated org member list.
         */
        query(input: { orgId: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: string; joinedAt: string; }[]; total: number; }>;
      };
      /**
       * Add user to org with role.
       */
      addMember: {
        /**
         * Add user to org with role.
         */
        mutate(input: { orgId: string; userId: string; role: "owner" | "admin" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from org.
       */
      removeMember: {
        /**
         * Remove user from org.
         */
        mutate(input: { orgId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
      /**
       * Transfer org ownership — requires MFA.
       */
      transferOwnership: {
        /**
         * Transfer org ownership — requires MFA.
         */
        mutate(input: { orgId: string; newOwnerId: string; reason?: string }, opts?: RequestOptions): Promise<{ transferred: boolean; }>;
      };
      /**
       * Stream org audit log entries live.
       */
      auditStream: {
        /**
         * Stream org audit log entries live.
         */
        stream(input: { orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; action: string; actorId: string; ts: string; }, void, unknown>>;
      };
    };
    billingRoutes4: {
      /**
       * Full subscription with pricing model.
       */
      getSubscription: {
        /**
         * Full subscription with pricing model.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; tier: "pro"; pricing: { model: "per_seat"; pricePerSeatMonth: number; minSeats: number; maxSeats: null; }; status: "active"; seats: number; currentPeriodStart: string; currentPeriodEnd: string; cancelAtPeriodEnd: false; metadata: {}; addons: never[]; }>;
      };
      /**
       * Calculate proration before confirming.
       */
      previewUpgrade: {
        /**
         * Calculate proration before confirming.
         */
        query(input: { orgId: string; targetTier: "starter" | "pro" | "business" | "enterprise"; targetSeats: number; billingInterval: "monthly" | "annual" }, opts?: RequestOptions): Promise<{ currentAmount: number; newAmount: number; prorationAmount: number; effectiveDate: string; lineItems: { description: string; amount: number; currency: string; }[]; }>;
      };
      /**
       * Create Stripe checkout session.
       */
      createCheckout: {
        /**
         * Create Stripe checkout session.
         */
        mutate(input: { orgId: string; tier: "starter" | "pro" | "business" | "enterprise"; seats: number; billingInterval: "monthly" | "annual"; addons?: Array<{ id: string; quantity: number }>; successUrl: string; cancelUrl: string; couponCode?: string }, opts?: RequestOptions): Promise<{ checkoutUrl: string; sessionId: string; }>;
      };
      /**
       * Schedule cancellation at period end.
       */
      cancelSubscription: {
        /**
         * Schedule cancellation at period end.
         */
        mutate(input: { orgId: string; reason?: string }, opts?: RequestOptions): Promise<{ cancelAtPeriodEnd: boolean; }>;
      };
      /**
       * Paginated invoice list.
       */
      invoices: {
        /**
         * Paginated invoice list.
         */
        query(input: { orgId: string; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ invoices: { id: string; amount: number; currency: string; status: string; date: string; }[]; nextCursor: string | null; }>;
      };
      /**
       * Metered usage breakdown.
       */
      usageReport: {
        /**
         * Metered usage breakdown.
         */
        query(input: { orgId: string; breakdown?: "daily" | "weekly" | "monthly" }, opts?: RequestOptions): Promise<{ period: { start: string; end: string; }; included: number; consumed: number; overage: number; overageCost: number; series: { date: string; units: number; cost: number; }[]; }>;
      };
      /**
       * Apply promotional discount code.
       */
      applyPromoCode: {
        /**
         * Apply promotional discount code.
         */
        mutate(input: { orgId: string; code: string }, opts?: RequestOptions): Promise<{ applied: boolean; discountPercent: number; }>;
      };
      /**
       * Stream invoice line items.
       */
      invoiceStream: {
        /**
         * Stream invoice line items.
         */
        stream(input: { invoiceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "line_item"; description: string; amount: number; currency: string; }, void, unknown>>;
      };
    };
    projectsRoutes4: {
      /**
       * Paginated project list with status filters.
       */
      list: {
        /**
         * Paginated project list with status filters.
         */
        query(input: { orgId: string; status?: Array<"planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled">; search?: string; page?: number }, opts?: RequestOptions): Promise<{ projects: { id: string; orgId: string; name: string; status: "active" | "cancelled" | "planning" | "on_hold" | "completed" | "archived"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: string[]; customFields: Record<string, string | number | boolean | null>; createdAt: string; updatedAt: string; description?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Project by ID.
       */
      get: {
        /**
         * Project by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; status: "active"; visibility: "private"; startDate: null; endDate: null; ownerId: string; tags: never[]; customFields: {}; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create project.
       */
      create: {
        /**
         * Create project.
         */
        mutate(input: { orgId: string; name: string; description?: string; status: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: Array<string>; customFields: Record<string, string | number | boolean | null> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update project fields.
       */
      update: {
        /**
         * Update project fields.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; status?: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility?: "private" | "internal" | "public"; startDate?: string | null; endDate?: string | null; ownerId?: string; tags?: Array<string>; customFields?: Record<string, string | number | boolean | null>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Archive project.
       */
      archive: {
        /**
         * Archive project.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ archived: boolean; }>;
      };
      /**
       * Project member list.
       */
      members: {
        /**
         * Project member list.
         */
        query(input: { projectId: string }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "viewer" | "lead" | "contributor"; joinedAt: string; }[]; }>;
      };
      /**
       * Add project member.
       */
      addMember: {
        /**
         * Add project member.
         */
        mutate(input: { projectId: string; userId: string; role: "lead" | "contributor" | "viewer" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Aggregate project stats.
       */
      stats: {
        /**
         * Aggregate project stats.
         */
        query(input: { projectId: string; period?: "7d" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ tasks: number; completed: number; openIssues: number; velocity: number; burndownPoints: { date: string; remaining: number; }[]; }>;
      };
      /**
       * Clone project structure.
       */
      duplicate: {
        /**
         * Clone project structure.
         */
        mutate(input: { id: string; newName: string; includeTasks?: boolean; includeMembers?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream full project export.
       */
      exportStream: {
        /**
         * Stream full project export.
         */
        stream(input: { id: string; format: "json" | "csv" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; resource: "task"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    tasksRoutes4: {
      /**
       * Create task with full field set.
       */
      create: {
        /**
         * Create task with full field set.
         */
        mutate(input: { projectId: string; title: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }> }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; }>;
      };
      /**
       * Task tree with recursive subtasks.
       */
      get: {
        /**
         * Task tree with recursive subtasks.
         */
        query(input: { id: string; depth?: number }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; subtasks: any[]; customFields: Record<string, { type: "text"; value: string; } | { type: "number"; value: number; } | { type: "date"; value: string; } | { type: "boolean"; value: boolean; } | { type: "select"; value: string; optionId: string; } | { type: "multi"; value: string[]; optionIds: string[]; } | { type: "user"; value: string; } | { type: "url"; value: string; }>; }>;
      };
      /**
       * Patch any task fields.
       */
      update: {
        /**
         * Patch any task fields.
         */
        mutate(input: { projectId?: string; title?: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete task permanently.
       */
      delete: {
        /**
         * Delete task permanently.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Batch status/priority update.
       */
      bulkUpdate: {
        /**
         * Batch status/priority update.
         */
        mutate(input: { ids: Array<string>; patch: { status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; dueDate?: string | null } }, opts?: RequestOptions): Promise<{ updated: number; }>;
      };
      /**
       * Comment thread for a task.
       */
      comments: {
        /**
         * Comment thread for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; body: string; author: string; createdAt: string; reactions: Record<string, string[]>; }[]; }>;
      };
      /**
       * Post comment on task.
       */
      addComment: {
        /**
         * Post comment on task.
         */
        mutate(input: { taskId: string; body: string; mentions?: Array<string> }, opts?: RequestOptions): Promise<{ id: string; body: string; }>;
      };
      /**
       * Typed audit trail for a task.
       */
      timelineEvents: {
        /**
         * Typed audit trail for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ events: ({ type: "created"; actor: string; ts: string; } | { type: "status_change"; actor: string; ts: string; from: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; to: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; } | { type: "assigned"; actor: string; ts: string; userId: string; })[]; }>;
      };
      /**
       * Tasks assigned to current user.
       */
      myTasks: {
        /**
         * Tasks assigned to current user.
         */
        query(input: { status?: Array<"backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix">; projectId?: string; limit?: number }, opts?: RequestOptions): Promise<{ tasks: any[]; total: number; }>;
      };
      /**
       * Live task update stream.
       */
      activityStream: {
        /**
         * Live task update stream.
         */
        stream(input: { projectId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "task_updated"; taskId: string; field: string; value: string; }, void, unknown>>;
      };
    };
    filesRoutes4: {
      /**
       * Files for a resource.
       */
      list: {
        /**
         * Files for a resource.
         */
        query(input: { resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; mimeTypes?: Array<string>; page?: number }, opts?: RequestOptions): Promise<{ files: { id: string; name: string; mimeType: string; sizeBytes: number; status: "deleted" | "pending" | "processing" | "ready" | "failed"; url: string | null; thumbnailUrl: string | null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: Record<string, string | number>; createdAt: string; }[]; total: number; }>;
      };
      /**
       * File metadata by ID.
       */
      get: {
        /**
         * File metadata by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; mimeType: string; sizeBytes: number; status: "ready"; url: string; thumbnailUrl: null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: {}; createdAt: string; }>;
      };
      /**
       * Get presigned S3 upload URL.
       */
      requestUpload: {
        /**
         * Get presigned S3 upload URL.
         */
        mutate(input: { fileName: string; mimeType: string; sizeBytes: number; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string }, opts?: RequestOptions): Promise<{ uploadUrl: string; fileId: string; expiresIn: number; }>;
      };
      /**
       * Mark pending upload complete.
       */
      confirmUpload: {
        /**
         * Mark pending upload complete.
         */
        mutate(input: { fileId: string; etag: string }, opts?: RequestOptions): Promise<{ id: string; confirmed: boolean; url: string; }>;
      };
      /**
       * Delete file and storage object.
       */
      delete: {
        /**
         * Delete file and storage object.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Delete multiple files.
       */
      bulkDelete: {
        /**
         * Delete multiple files.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ deleted: number; }>;
      };
      /**
       * Multipart direct file upload.
       */
      directUpload: {
        /**
         * Multipart direct file upload.
         */
        upload(input: { file: File; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; description?: string } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileId: string; name: string; size: number; }>;
      };
      /**
       * Stream file processing events.
       */
      processStream: {
        /**
         * Stream file processing events.
         */
        stream(input: { fileId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; fileId: string; stage: "ready" | "queued" | "virus_scan" | "thumbnail"; percent: number; }, void, unknown>>;
      };
    };
    notificationsRoutes4: {
      /**
       * Paginated notification list.
       */
      list: {
        /**
         * Paginated notification list.
         */
        query(input: { unreadOnly?: boolean; types?: Array<"mention" | "task_assigned" | "task_completed" | "comment" | "billing" | "security" | "system" | "workflow_run" | "integration_sync">; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ notifications: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }[]; unreadCount: number; nextCursor: string | null; }>;
      };
      /**
       * Mark notifications as read.
       */
      markRead: {
        /**
         * Mark notifications as read.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ marked: number; }>;
      };
      /**
       * Mark all notifications read.
       */
      markAllRead: {
        /**
         * Mark all notifications read.
         */
        mutate(input: any, opts?: RequestOptions): Promise<{ marked: boolean; }>;
      };
      /**
       * Get notification preferences.
       */
      preferences: {
        /**
         * Get notification preferences.
         */
        query(input: any, opts?: RequestOptions): Promise<{ channels: Record<string, boolean>; types: Partial<Record<"billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync", boolean>>; }>;
      };
      /**
       * Update preferences.
       */
      updatePreferences: {
        /**
         * Update preferences.
         */
        mutate(input: { channels?: Record<string, boolean>; types?: Record<string, boolean> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Register Web Push subscription.
       */
      pushSubscribe: {
        /**
         * Register Web Push subscription.
         */
        mutate(input: { endpoint: string; keys: { p256dh: string; auth: string } }, opts?: RequestOptions): Promise<{ subscribed: boolean; }>;
      };
      /**
       * Real-time notification SSE.
       */
      liveStream: {
        /**
         * Real-time notification SSE.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "notification"; notification: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }; }, void, unknown>>;
      };
    };
    searchRoutes4: {
      /**
       * Full-text search across resource types.
       */
      global: {
        /**
         * Full-text search across resource types.
         */
        query(input: { q: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document">; orgId: string; limit?: number; highlight?: boolean }, opts?: RequestOptions): Promise<{ results: ({ type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; })[]; total: number; took: number; }>;
      };
      /**
       * Autocomplete suggestions.
       */
      suggestions: {
        /**
         * Autocomplete suggestions.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ suggestions: { label: string; type: "user" | "file" | "task" | "project" | "comment" | "workflow" | "document"; id: string; }[]; }>;
      };
      /**
       * Faceted filter counts.
       */
      filters: {
        /**
         * Faceted filter counts.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ facets: Record<"user" | "file" | "task" | "project" | "comment" | "workflow" | "document", number>; }>;
      };
      /**
       * Trigger manual reindex for org.
       */
      reindex: {
        /**
         * Trigger manual reindex for org.
         */
        mutate(input: { orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream results progressively.
       */
      stream: {
        /**
         * Stream results progressively.
         */
        stream(input: { q: string; orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "result"; result: { type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; }; }, void, unknown>>;
      };
    };
    analyticsRoutes4: {
      /**
       * Arbitrary analytics query.
       */
      query: {
        /**
         * Arbitrary analytics query.
         */
        mutate(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }, opts?: RequestOptions): Promise<{ rows: never[]; totals: {}; comparison: undefined; meta: { executionMs: number; rowsScanned: number; cached: false; granularity: "minute" | "hour" | "day" | "week" | "month"; }; }>;
      };
      /**
       * Multi-step funnel conversion.
       */
      funnel: {
        /**
         * Multi-step funnel conversion.
         */
        mutate(input: { orgId: string; steps: Array<{ name: string; event: string; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }> }>; dateRange: { from: string; to: string } }, opts?: RequestOptions): Promise<{ steps: { name: string; count: number; conversionRate: number; avgTimeToNextStep: number | null; }[]; overallConversion: number; }>;
      };
      /**
       * Cohort retention analysis.
       */
      retention: {
        /**
         * Cohort retention analysis.
         */
        mutate(input: { orgId: string; cohortEvent: string; returnEvent: string; dateRange: { from: string; to: string }; granularity?: "minute" | "hour" | "day" | "week" | "month" }, opts?: RequestOptions): Promise<{ cohorts: { date: string; size: number; retention: number[]; }[]; }>;
      };
      /**
       * Saved report definitions.
       */
      savedReports: {
        /**
         * Saved report definitions.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; name: string; query: { orgId: string; metrics: string[]; dimensions: string[]; filters: { field: string; operator: { op: "eq"; value: string | number | boolean; } | { op: "neq"; value: string | number | boolean; } | { op: "gt"; value: number; } | { op: "gte"; value: number; } | { op: "lt"; value: number; } | { op: "lte"; value: number; } | { op: "in"; values: (string | number)[]; } | { op: "between"; min: number; max: number; }; }[]; dateRange: { from: string; to: string; granularity: "minute" | "hour" | "day" | "week" | "month"; timezone: string; }; orderBy: { field: string; direction: "asc" | "desc"; }[]; limit: number; compareWith?: { from: string; to: string; } | undefined; }; schedule: { cron: string; recipients: string[]; } | null; lastRunAt: string | null; }[]; }>;
      };
      /**
       * Persist a report definition.
       */
      saveReport: {
        /**
         * Persist a report definition.
         */
        mutate(input: { orgId: string; name: string; query: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }; schedule?: { cron: string; recipients: Array<string> } }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream query results row by row.
       */
      exportStream: {
        /**
         * Stream query results row by row.
         */
        stream(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string }; format?: "json" | "csv" | "parquet" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; index: number; data: Record<string, string | number | null>; }, void, unknown>>;
      };
    };
    webhooksRoutes4: {
      /**
       * Webhooks for an org.
       */
      list: {
        /**
         * Webhooks for an org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ webhooks: { id: string; orgId: string; url: string; events: ("task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed")[]; enabled: boolean; secret: string; headers: Record<string, string>; retryPolicy: { maxAttempts: number; backoffMs: number; }; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Register webhook endpoint.
       */
      create: {
        /**
         * Register webhook endpoint.
         */
        mutate(input: { orgId: string; url: string; description?: string; events: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled: boolean; secret: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number } }, opts?: RequestOptions): Promise<{ id: string; url: string; }>;
      };
      /**
       * Update webhook.
       */
      update: {
        /**
         * Update webhook.
         */
        mutate(input: { orgId?: string; url?: string; description?: string; events?: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled?: boolean; secret?: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Remove webhook.
       */
      delete: {
        /**
         * Remove webhook.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Recent delivery attempts.
       */
      deliveries: {
        /**
         * Recent delivery attempts.
         */
        query(input: { webhookId: string; status?: "success" | "failed" | "pending"; limit?: number }, opts?: RequestOptions): Promise<{ deliveries: { id: string; status: number; attempt: number; ts: string; error: string | null; }[]; }>;
      };
      /**
       * Retry failed delivery.
       */
      redeliver: {
        /**
         * Retry failed delivery.
         */
        mutate(input: { deliveryId: string }, opts?: RequestOptions): Promise<{ queued: boolean; }>;
      };
      /**
       * Send test event to webhook.
       */
      testPing: {
        /**
         * Send test event to webhook.
         */
        mutate(input: { webhookId: string; eventType: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed" }, opts?: RequestOptions): Promise<{ sent: boolean; deliveryId: string; }>;
      };
      /**
       * Stream webhook delivery events.
       */
      logStream: {
        /**
         * Stream webhook delivery events.
         */
        stream(input: { webhookId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "delivery"; status: number; ts: string; }, void, unknown>>;
      };
    };
    aiRoutes4: {
      /**
       * Multi-model streaming chat with tool-call support.
       */
      chat: {
        /**
         * Multi-model streaming chat with tool-call support.
         */
        stream(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "content_delta"; text: string; finishReason?: undefined; usage?: undefined; } | { type: "finish"; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; text?: undefined; }, void, unknown>>;
      };
      /**
       * Non-streaming completion.
       */
      complete: {
        /**
         * Non-streaming completion.
         */
        mutate(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string>; stream?: false }, opts?: RequestOptions): Promise<{ id: string; role: "assistant"; content: string | ({ type: "text"; text: string; } | { type: "image_url"; url: string; detail: "high" | "low" | "auto"; } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown>; } | { type: "tool_result"; toolCallId: string; content: string; isError: boolean; })[]; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; }>;
      };
      /**
       * Batch embedding generation.
       */
      embed: {
        /**
         * Batch embedding generation.
         */
        mutate(input: { inputs: Array<string | { text: string; id: string }>; model: "text-embedding-3-small" | "text-embedding-3-large"; dimensions?: number; outputFormat?: "float" | "base64" | "int8" }, opts?: RequestOptions): Promise<{ embeddings: { index: number; embedding: number[]; inputTokens: number; }[]; model: "text-embedding-3-small" | "text-embedding-3-large"; totalTokens: number; }>;
      };
      /**
       * Run content moderation checks.
       */
      moderateContent: {
        /**
         * Run content moderation checks.
         */
        mutate(input: { inputs: Array<{ id: string; text: string }>; categories?: Array<"hate" | "harassment" | "self_harm" | "sexual" | "violence" | "dangerous"> }, opts?: RequestOptions): Promise<{ results: { id: string; flagged: boolean; categories: Partial<Record<string, boolean>>; scores: Partial<Record<string, number>>; }[]; }>;
      };
      /**
       * Structured text summarization.
       */
      summarize: {
        /**
         * Structured text summarization.
         */
        mutate(input: { text: string; format?: "bullets" | "paragraph" | "tldr"; maxLength?: number }, opts?: RequestOptions): Promise<{ summary: string; wordCount: number; }>;
      };
      /**
       * AI token usage for billing period.
       */
      usageStats: {
        /**
         * AI token usage for billing period.
         */
        query(input: { orgId: string; period?: "current" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ promptTokens: number; completionTokens: number; totalCost: number; byModel: Record<string, { tokens: number; cost: number; }>; }>;
      };
    };
    auditRoutes4: {
      /**
       * Paginated audit log with filters.
       */
      log: {
        /**
         * Paginated audit log with filters.
         */
        query(input: { orgId: string; actorId?: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled">; resourceType?: string; from?: string; to?: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ entries: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }[]; total: number; }>;
      };
      /**
       * Single audit entry by ID.
       */
      getEntry: {
        /**
         * Single audit entry by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; actorId: string; actorEmail: string; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: null; resourceId: null; metadata: {}; ip: string; userAgent: string; ts: string; }>;
      };
      /**
       * Update log retention policy.
       */
      retention: {
        /**
         * Update log retention policy.
         */
        mutate(input: { orgId: string; retentionDays: number }, opts?: RequestOptions): Promise<{ updated: boolean; retentionDays: number; }>;
      };
      /**
       * Stream audit log as NDJSON.
       */
      export: {
        /**
         * Stream audit log as NDJSON.
         */
        stream(input: { orgId: string; from: string; to: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; data: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }; }, void, unknown>>;
      };
    };
    apiKeysRoutes4: {
      /**
       * List API keys for org.
       */
      list: {
        /**
         * List API keys for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ keys: { id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: string | null; lastUsedAt: string | null; createdBy: string; createdAt: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number; } | null; }[]; }>;
      };
      /**
       * Create API key.
       */
      create: {
        /**
         * Create API key.
         */
        mutate(input: { orgId: string; name: string; scopes: Array<"read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write">; expiresAt: string | null; createdBy: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number } | null }, opts?: RequestOptions): Promise<{ key: string; id: string; prefix: string; }>;
      };
      /**
       * Revoke API key.
       */
      revoke: {
        /**
         * Revoke API key.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Rotate key secret.
       */
      rotate: {
        /**
         * Rotate key secret.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ newKey: string; id: string; }>;
      };
      /**
       * API key metadata.
       */
      get: {
        /**
         * API key metadata.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: null; lastUsedAt: null; createdBy: string; createdAt: string; rateLimit: null; }>;
      };
      /**
       * Key request usage stats.
       */
      usage: {
        /**
         * Key request usage stats.
         */
        query(input: { id: string; period?: "1h" | "24h" | "7d" | "30d" }, opts?: RequestOptions): Promise<{ requests: number; errors: number; p50Ms: number; p99Ms: number; series: { ts: string; count: number; }[]; }>;
      };
    };
    integrationsRoutes4: {
      /**
       * All integrations with status.
       */
      list: {
        /**
         * All integrations with status.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ integrations: { id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: boolean; enabled: boolean; status: "error" | "active" | "disconnected" | "syncing"; lastSyncAt: string | null; errorMessage: string | null; config: Record<string, unknown>; }[]; }>;
      };
      /**
       * Single integration status.
       */
      get: {
        /**
         * Single integration status.
         */
        query(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: true; enabled: true; status: "active"; lastSyncAt: string; errorMessage: null; config: {}; }>;
      };
      /**
       * Start OAuth install flow.
       */
      install: {
        /**
         * Start OAuth install flow.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; redirectUri: string; scopes?: Array<string> }, opts?: RequestOptions): Promise<{ authUrl: string; }>;
      };
      /**
       * Exchange OAuth code for tokens.
       */
      oauthCallback: {
        /**
         * Exchange OAuth code for tokens.
         */
        mutate(input: { code: string; state: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ installed: boolean; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; }>;
      };
      /**
       * Remove integration.
       */
      uninstall: {
        /**
         * Remove integration.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ uninstalled: boolean; }>;
      };
      /**
       * Update integration config.
       */
      updateConfig: {
        /**
         * Update integration config.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; config: Record<string, unknown> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Manual sync trigger.
       */
      triggerSync: {
        /**
         * Manual sync trigger.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream sync progress events.
       */
      syncStream: {
        /**
         * Stream sync progress events.
         */
        stream(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; itemsSynced: number; total: number; }, void, unknown>>;
      };
    };
    workflowsRoutes4: {
      /**
       * Create workflow with recursive condition tree.
       */
      create: {
        /**
         * Create workflow with recursive condition tree.
         */
        mutate(input: { name: string; description?: string; trigger: { event: string; filters?: unknown; debounceMs?: number }; steps: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; orgId: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * List workflows with run stats.
       */
      list: {
        /**
         * List workflows with run stats.
         */
        query(input: { orgId: string; enabled?: boolean }, opts?: RequestOptions): Promise<{ workflows: { id: string; name: string; enabled: boolean; lastRunAt: string | null; runCount: number; failureRate: number; }[]; }>;
      };
      /**
       * Workflow detail.
       */
      get: {
        /**
         * Workflow detail.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * Update workflow definition.
       */
      update: {
        /**
         * Update workflow definition.
         */
        mutate(input: { name?: string; description?: string; trigger?: { event: string; filters?: unknown; debounceMs?: number }; steps?: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete workflow.
       */
      delete: {
        /**
         * Delete workflow.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated run history.
       */
      runHistory: {
        /**
         * Paginated run history.
         */
        query(input: { workflowId: string; status?: "running" | "success" | "failed" | "cancelled"; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ runs: { id: string; status: string; startedAt: string; finishedAt: string | null; steps: { stepId: string; status: string; durationMs: number; error?: string | undefined; }[]; }[]; nextCursor: string | null; }>;
      };
      /**
       * Dry-run with streamed step outcomes.
       */
      testRun: {
        /**
         * Dry-run with streamed step outcomes.
         */
        stream(input: { workflowId: string; samplePayload: Record<string, unknown> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "step_result"; stepId: string; status: "success"; durationMs: number; output: Record<string, unknown>; }, void, unknown>>;
      };
    };
    documentsRoutes4: {
      /**
       * Documents in a project.
       */
      list: {
        /**
         * Documents in a project.
         */
        query(input: { projectId: string; page?: number; limit?: number; search?: string }, opts?: RequestOptions): Promise<{ documents: Pick<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "private" | "public" | "project"; authorId: string; collaborators: string[]; version: number; publishedAt: string | null; createdAt: string; updatedAt: string; }, "createdAt" | "id" | "updatedAt" | "title" | "authorId">[]; total: number; }>;
      };
      /**
       * Full document with blocks.
       */
      get: {
        /**
         * Full document with blocks.
         */
        query(input: { id: string; version?: number }, opts?: RequestOptions): Promise<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "project"; authorId: string; collaborators: never[]; version: number; publishedAt: null; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create document.
       */
      create: {
        /**
         * Create document.
         */
        mutate(input: { projectId: string; title: string; blocks: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility: "private" | "project" | "public"; authorId: string; collaborators: Array<string> }, opts?: RequestOptions): Promise<{ id: string; title: string; }>;
      };
      /**
       * Update blocks and metadata.
       */
      update: {
        /**
         * Update blocks and metadata.
         */
        mutate(input: { id: string; title?: string; blocks?: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility?: "private" | "project" | "public" }, opts?: RequestOptions): Promise<{ id: string; version: number; }>;
      };
      /**
       * Delete document.
       */
      delete: {
        /**
         * Delete document.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Publish document publicly.
       */
      publish: {
        /**
         * Publish document publicly.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ publishedAt: string; url: string; }>;
      };
      /**
       * Document version history.
       */
      history: {
        /**
         * Document version history.
         */
        query(input: { id: string; limit?: number }, opts?: RequestOptions): Promise<{ versions: { version: number; editor: string; changedAt: string; summary: string; }[]; }>;
      };
      /**
       * Collaborative edit event stream.
       */
      editStream: {
        /**
         * Collaborative edit event stream.
         */
        stream(input: { docId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "block_updated"; blockId: string; patch: Record<string, unknown>; editorId: string; }, void, unknown>>;
      };
    };
    permissionsRoutes4: {
      /**
       * All ABAC policies for org.
       */
      listPolicies: {
        /**
         * All ABAC policies for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ policies: { id: string; orgId: string; name: string; effect: "allow" | "deny"; resources: ("file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics")[]; actions: ("read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve")[]; conditions: { field: string; operator: "in" | "eq" | "neq" | "owned_by"; value: string | string[]; }[]; subjects: { type: "user" | "role" | "team"; id: string; }[]; priority: number; createdAt: string; }[]; }>;
      };
      /**
       * Create ABAC policy.
       */
      createPolicy: {
        /**
         * Create ABAC policy.
         */
        mutate(input: { orgId: string; name: string; effect: "allow" | "deny"; resources: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update policy definition.
       */
      updatePolicy: {
        /**
         * Update policy definition.
         */
        mutate(input: { orgId?: string; name?: string; effect?: "allow" | "deny"; resources?: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions?: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects?: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete policy.
       */
      deletePolicy: {
        /**
         * Delete policy.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Evaluate access for a subject.
       */
      checkAccess: {
        /**
         * Evaluate access for a subject.
         */
        query(input: { orgId: string; subjectId: string; resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }, opts?: RequestOptions): Promise<{ allowed: boolean; matchedPolicies: string[]; }>;
      };
      /**
       * Batch permission check.
       */
      bulkCheck: {
        /**
         * Batch permission check.
         */
        mutate(input: { orgId: string; subjectId: string; checks: Array<{ resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }> }, opts?: RequestOptions): Promise<{ results: { resource: "file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics"; action: "read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve"; allowed: boolean; }[]; }>;
      };
    };
    teamsRoutes4: {
      /**
       * Teams in an org.
       */
      list: {
        /**
         * Teams in an org.
         */
        query(input: { orgId: string; visibility?: "public" | "private" | "secret" }, opts?: RequestOptions): Promise<{ teams: { id: string; orgId: string; name: string; visibility: "private" | "public" | "secret"; memberCount: number; projectIds: string[]; createdAt: string; description?: string | undefined; color?: string | undefined; icon?: string | undefined; }[]; }>;
      };
      /**
       * Team by ID.
       */
      get: {
        /**
         * Team by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; description: undefined; color: string; icon: string; visibility: "public"; memberCount: number; projectIds: never[]; createdAt: string; }>;
      };
      /**
       * Create team.
       */
      create: {
        /**
         * Create team.
         */
        mutate(input: { orgId: string; name: string; description?: string; color?: string; icon?: string; visibility: "public" | "private" | "secret"; projectIds: Array<string> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update team metadata.
       */
      update: {
        /**
         * Update team metadata.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; color?: string; icon?: string; visibility?: "public" | "private" | "secret"; projectIds?: Array<string>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete team.
       */
      delete: {
        /**
         * Delete team.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Team member list.
       */
      members: {
        /**
         * Team member list.
         */
        query(input: { teamId: string; page?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "member" | "lead"; joinedAt: string; }[]; }>;
      };
      /**
       * Add user to team.
       */
      addMember: {
        /**
         * Add user to team.
         */
        mutate(input: { teamId: string; userId: string; role: "lead" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from team.
       */
      removeMember: {
        /**
         * Remove user from team.
         */
        mutate(input: { teamId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
    };
    invitesRoutes4: {
      /**
       * Pending invites for org.
       */
      list: {
        /**
         * Pending invites for org.
         */
        query(input: { orgId: string; status?: "pending" | "accepted" | "declined" | "expired" | "revoked" }, opts?: RequestOptions): Promise<{ invites: { id: string; orgId: string; email: string; role: "admin" | "owner" | "member" | "viewer"; status: "revoked" | "pending" | "accepted" | "declined" | "expired"; invitedBy: string; expiresAt: string; acceptedAt: string | null; teams: string[]; createdAt: string; personalMessage?: string | undefined; }[]; }>;
      };
      /**
       * Send invite.
       */
      create: {
        /**
         * Send invite.
         */
        mutate(input: { orgId: string; email: string; role: "owner" | "admin" | "member" | "viewer"; teams?: Array<string>; personalMessage?: string }, opts?: RequestOptions): Promise<{ id: string; email: string; }>;
      };
      /**
       * Bulk invite by email list.
       */
      bulkCreate: {
        /**
         * Bulk invite by email list.
         */
        mutate(input: { orgId: string; emails: Array<string>; role?: "admin" | "member" | "viewer"; teams?: Array<string> }, opts?: RequestOptions): Promise<{ invited: number; failed: string[]; }>;
      };
      /**
       * Accept invite via token.
       */
      accept: {
        /**
         * Accept invite via token.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ accepted: boolean; orgId: string; }>;
      };
      /**
       * Decline invite.
       */
      decline: {
        /**
         * Decline invite.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ declined: boolean; }>;
      };
      /**
       * Revoke pending invite.
       */
      revoke: {
        /**
         * Revoke pending invite.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Resend invite email.
       */
      resend: {
        /**
         * Resend invite email.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
    };
    commentsRoutes4: {
      /**
       * Comments for a resource.
       */
      list: {
        /**
         * Comments for a resource.
         */
        query(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; nextCursor: string | null; }>;
      };
      /**
       * Post a comment.
       */
      create: {
        /**
         * Post a comment.
         */
        mutate(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; authorId: string; body: string; mentions: Array<string>; attachments: Array<{ id: string; name: string; url: string }>; parentId: string | null }, opts?: RequestOptions): Promise<{ id: string; }>;
      };
      /**
       * Edit comment body.
       */
      update: {
        /**
         * Edit comment body.
         */
        mutate(input: { id: string; body: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete comment.
       */
      delete: {
        /**
         * Delete comment.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Toggle emoji reaction.
       */
      react: {
        /**
         * Toggle emoji reaction.
         */
        mutate(input: { commentId: string; emoji: string }, opts?: RequestOptions): Promise<{ added: boolean; emoji: string; }>;
      };
      /**
       * Threaded replies.
       */
      thread: {
        /**
         * Threaded replies.
         */
        query(input: { parentId: string; limit?: number }, opts?: RequestOptions): Promise<{ replies: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Stream new comments live.
       */
      liveStream: {
        /**
         * Stream new comments live.
         */
        stream(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "new_comment"; comment: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }; }, void, unknown>>;
      };
    };
    tagsRoutes4: {
      /**
       * All tags for org.
       */
      list: {
        /**
         * All tags for org.
         */
        query(input: { orgId: string; search?: string; sortBy?: "name" | "usageCount" }, opts?: RequestOptions): Promise<{ tags: { id: string; orgId: string; name: string; color: string; usageCount: number; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Create tag.
       */
      create: {
        /**
         * Create tag.
         */
        mutate(input: { orgId: string; name: string; color: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update tag name/color.
       */
      update: {
        /**
         * Update tag name/color.
         */
        mutate(input: { id: string; name?: string; color?: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete tag.
       */
      delete: {
        /**
         * Delete tag.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Merge multiple tags into one.
       */
      merge: {
        /**
         * Merge multiple tags into one.
         */
        mutate(input: { sourceIds: Array<string>; targetId: string }, opts?: RequestOptions): Promise<{ merged: number; targetId: string; }>;
      };
      /**
       * Attach tags to a resource.
       */
      attach: {
        /**
         * Attach tags to a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ attached: number; }>;
      };
      /**
       * Detach tags from a resource.
       */
      detach: {
        /**
         * Detach tags from a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ detached: number; }>;
      };
    };
    reportsRoutes4: {
      /**
       * Saved reports for org.
       */
      list: {
        /**
         * Saved reports for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; orgId: string; name: string; type: "custom" | "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest"; config: Record<string, unknown>; schedule: { cron: string; timezone: string; recipients: string[]; format: "csv" | "pdf" | "html"; } | null; lastRunAt: string | null; lastRunStatus: "success" | "failed" | "running" | null; createdBy: string; createdAt: string; }[]; }>;
      };
      /**
       * Create report definition.
       */
      create: {
        /**
         * Create report definition.
         */
        mutate(input: { orgId: string; name: string; type: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config: Record<string, unknown>; schedule: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update report.
       */
      update: {
        /**
         * Update report.
         */
        mutate(input: { orgId?: string; name?: string; type?: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config?: Record<string, unknown>; schedule?: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy?: string; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete report.
       */
      delete: {
        /**
         * Delete report.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Trigger report run.
       */
      run: {
        /**
         * Trigger report run.
         */
        mutate(input: { id: string; format?: "pdf" | "csv" | "html" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Get report download URL.
       */
      download: {
        /**
         * Get report download URL.
         */
        query(input: { id: string; runId: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream report generation.
       */
      stream: {
        /**
         * Stream report generation.
         */
        stream(input: { id: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; percent: number; stage: "collecting_data"; }, void, unknown>>;
      };
    };
    rateLimitsRoutes4: {
      /**
       * Rate limit rules for org.
       */
      listRules: {
        /**
         * Rate limit rules for org.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ rules: { id: string; orgId: string; name: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; targetId: string | null; endpoint: string | null; limits: { window: "minute" | "hour" | "day" | "second"; max: number; burstAllowance: number; }[]; action: "log" | "throttle" | "block"; enabled: boolean; createdAt: string; }[]; }>;
      };
      /**
       * Create rate limit rule.
       */
      createRule: {
        /**
         * Create rate limit rule.
         */
        mutate(input: { orgId: string; name: string; target: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId: string | null; endpoint: string | null; limits: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update rule.
       */
      updateRule: {
        /**
         * Update rule.
         */
        mutate(input: { orgId?: string; name?: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId?: string | null; endpoint?: string | null; limits?: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete rule.
       */
      deleteRule: {
        /**
         * Delete rule.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Current usage vs. limits.
       */
      currentStatus: {
        /**
         * Current usage vs. limits.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ statuses: { ruleId: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; window: string; current: number; max: number; resetAt: string; }[]; }>;
      };
      /**
       * Manually reset rate counter.
       */
      resetCounter: {
        /**
         * Manually reset rate counter.
         */
        mutate(input: { ruleId: string; targetId: string }, opts?: RequestOptions): Promise<{ reset: boolean; }>;
      };
    };
    exportsRoutes4: {
      /**
       * Queue an export job.
       */
      create: {
        /**
         * Queue an export job.
         */
        mutate(input: { orgId: string; resource: { resource: "tasks"; projectId?: string; filters?: Record<string, unknown> } | { resource: "users"; orgId: string } | { resource: "analytics"; orgId: string; metrics: Array<string>; dateRange: { from: string; to: string } } | { resource: "audit_log"; orgId: string; from: string; to: string } | { resource: "billing"; orgId: string; year: number } | { resource: "files"; resourceType: string; resourceId: string }; format: "json" | "csv" | "xlsx" | "pdf" | "ndjson" | "parquet"; notifyEmail?: string }, opts?: RequestOptions): Promise<{ id: string; status: "queued"; }>;
      };
      /**
       * Export job history.
       */
      list: {
        /**
         * Export job history.
         */
        query(input: { orgId: string; limit?: number }, opts?: RequestOptions): Promise<{ jobs: { id: string; orgId: string; resource: { resource: "tasks"; projectId?: string | undefined; filters?: Record<string, unknown> | undefined; } | { resource: "users"; orgId: string; } | { resource: "analytics"; orgId: string; metrics: string[]; dateRange: { from: string; to: string; }; } | { resource: "audit_log"; orgId: string; from: string; to: string; } | { resource: "billing"; orgId: string; year: number; } | { resource: "files"; resourceType: string; resourceId: string; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done" | "failed" | "queued" | "running"; progress: number; rowCount: number | null; downloadUrl: string | null; expiresAt: string | null; error: string | null; createdAt: string; }[]; }>;
      };
      /**
       * Export job status.
       */
      get: {
        /**
         * Export job status.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; resource: { resource: "tasks"; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done"; progress: number; rowCount: number; downloadUrl: string; expiresAt: string; error: null; createdAt: string; }>;
      };
      /**
       * Cancel queued export.
       */
      cancel: {
        /**
         * Cancel queued export.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ cancelled: boolean; }>;
      };
      /**
       * Fresh signed download URL.
       */
      download: {
        /**
         * Fresh signed download URL.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream export progress.
       */
      progressStream: {
        /**
         * Stream export progress.
         */
        stream(input: { jobId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; jobId: string; percent: number; rowsProcessed: number; }, void, unknown>>;
      };
    };
    authRoutes5: {
      /**
       * Multi-method login — password, OAuth, SAML, magic link, passkey.
       */
      login: {
        /**
         * Multi-method login — password, OAuth, SAML, magic link, passkey.
         */
        mutate(input: { method: "password"; email: string; password: string; totpCode?: string } | { method: "oauth"; provider: "google" | "github" | "microsoft" | "okta"; code: string; redirectUri: string; state: string } | { method: "saml"; orgSlug: string; samlResponse: string; relayState?: string } | { method: "magic_link"; token: string } | { method: "passkey"; credentialId: string; clientDataJSON: string; authenticatorData: string; signature: string } & { device?: { userAgent: string; ip: string; fingerprint?: string; platform?: "web" | "ios" | "android" | "desktop" } }, opts?: RequestOptions): Promise<{ tokens: { accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }; user: { id: UserId; role: "admin"; }; }>;
      };
      /**
       * Rotate refresh token.
       */
      refresh: {
        /**
         * Rotate refresh token.
         */
        mutate(input: { refreshToken: string; sessionId: string }, opts?: RequestOptions): Promise<{ accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }>;
      };
      /**
       * Invalidate session.
       */
      logout: {
        /**
         * Invalidate session.
         */
        mutate(input: { sessionId: string }, opts?: RequestOptions): Promise<{ revoked: boolean; sessionId: SessionId; }>;
      };
      /**
       * Validate MFA code after credential check.
       */
      mfaChallenge: {
        /**
         * Validate MFA code after credential check.
         */
        mutate(input: { challengeToken: string; code: string; method: "totp" | "sms" | "email" | "webauthn" }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Send password reset email.
       */
      forgotPassword: {
        /**
         * Send password reset email.
         */
        mutate(input: { email: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
      /**
       * Apply new password via reset token.
       */
      resetPassword: {
        /**
         * Apply new password via reset token.
         */
        mutate(input: { token: string; newPassword: string }, opts?: RequestOptions): Promise<{ success: boolean; }>;
      };
      /**
       * List active sessions with device info.
       */
      sessions: {
        /**
         * List active sessions with device info.
         */
        query(input: { cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ sessions: { id: SessionId; device: { userAgent: string; ip: string; fingerprint?: string | undefined; platform?: "web" | "ios" | "android" | "desktop" | undefined; }; createdAt: string; current: boolean; }[]; nextCursor: string | null; }>;
      };
      /**
       * Current authenticated user.
       */
      me: {
        /**
         * Current authenticated user.
         */
        query(input: any, opts?: RequestOptions): Promise<{ id: UserId; role: string; }>;
      };
      /**
       * Confirm email address via verification code.
       */
      verifyEmail: {
        /**
         * Confirm email address via verification code.
         */
        mutate(input: { code: string }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Stream login activity events for the current user.
       */
      deviceStream: {
        /**
         * Stream login activity events for the current user.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "session_created"; sessionId: SessionId; ts: string; }, void, unknown>>;
      };
    };
    usersRoutes5: {
      /**
       * Paginated user list with rich filters.
       */
      list: {
        /**
         * Paginated user list with rich filters.
         */
        query(input: { page?: number; limit?: number; roles?: Array<"owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support">; search?: string; sortBy?: "name" | "email" | "createdAt"; sortDir?: "asc" | "desc" }, opts?: RequestOptions): Promise<{ users: { id: string; email: string; emailVerified: boolean; name: string; avatarUrl: string | null; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: string[]; metadata: Record<string, string | number | boolean>; createdAt: string; updatedAt: string; displayName?: string | undefined; bio?: string | undefined; timezone?: string | undefined; locale?: string | undefined; address?: { line1: string; city: string; postalCode: string; country: string; line2?: string | undefined; state?: string | undefined; } | undefined; }[]; total: number; page: number; }>;
      };
      /**
       * Fetch single user with full profile.
       */
      get: {
        /**
         * Fetch single user with full profile.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; email: string; emailVerified: true; name: string; displayName: undefined; avatarUrl: null; bio: undefined; timezone: string; locale: string; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: never[]; metadata: {}; address: undefined; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Partial profile update with nested address.
       */
      update: {
        /**
         * Partial profile update with nested address.
         */
        mutate(input: { name?: string; displayName?: string; bio?: string; timezone?: string; locale?: string; avatarUrl?: string | null; address?: { line1: string; line2?: string; city: string; state?: string; postalCode: string; country: string }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Soft-delete user.
       */
      deactivate: {
        /**
         * Soft-delete user.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ deactivated: boolean; }>;
      };
      /**
       * Change role for many users atomically.
       */
      bulkRoleChange: {
        /**
         * Change role for many users atomically.
         */
        mutate(input: { userIds: Array<string>; role: "owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support"; reason?: string }, opts?: RequestOptions): Promise<{ updated: number; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; }>;
      };
      /**
       * All permissions grouped by resource for a user.
       */
      permissionMatrix: {
        /**
         * All permissions grouped by resource for a user.
         */
        query(input: { userId: string }, opts?: RequestOptions): Promise<{ userId: string; matrix: Record<string, Record<string, boolean>>; }>;
      };
      /**
       * Upload and crop user avatar.
       */
      uploadAvatar: {
        /**
         * Upload and crop user avatar.
         */
        upload(input: { file: File; cropSize?: number } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileName: string; size: number; croppedTo: number; }>;
      };
      /**
       * Change own password — requires MFA.
       */
      changePassword: {
        /**
         * Change own password — requires MFA.
         */
        mutate(input: { currentPassword: string; newPassword: string }, opts?: RequestOptions): Promise<{ changed: boolean; }>;
      };
      /**
       * GDPR data export streamed by resource type.
       */
      exportData: {
        /**
         * GDPR data export streamed by resource type.
         */
        stream(input: { userId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "chunk"; resource: "profile" | "tasks" | "comments" | "files"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    organizationsRoutes5: {
      /**
       * Orgs accessible to caller.
       */
      list: {
        /**
         * Orgs accessible to caller.
         */
        query(input: { page?: number; limit?: number }, opts?: RequestOptions): Promise<{ orgs: { id: string; name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; memberCount: number; logoUrl: string | null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }[]; total: number; }>;
      };
      /**
       * Fetch org by ID.
       */
      get: {
        /**
         * Fetch org by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; slug: string; plan: "enterprise"; memberCount: number; logoUrl: null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }>;
      };
      /**
       * Create new organization.
       */
      create: {
        /**
         * Create new organization.
         */
        mutate(input: { name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; logoUrl: string | null; settings: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> }; ownerId: string }, opts?: RequestOptions): Promise<{ id: string; slug: string; }>;
      };
      /**
       * Update org metadata and settings.
       */
      update: {
        /**
         * Update org metadata and settings.
         */
        mutate(input: { id: string; name?: string; logoUrl?: string | null; settings?: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> } }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Permanently delete org.
       */
      delete: {
        /**
         * Permanently delete org.
         */
        mutate(input: { id: string; confirmSlug: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated org member list.
       */
      members: {
        /**
         * Paginated org member list.
         */
        query(input: { orgId: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: string; joinedAt: string; }[]; total: number; }>;
      };
      /**
       * Add user to org with role.
       */
      addMember: {
        /**
         * Add user to org with role.
         */
        mutate(input: { orgId: string; userId: string; role: "owner" | "admin" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from org.
       */
      removeMember: {
        /**
         * Remove user from org.
         */
        mutate(input: { orgId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
      /**
       * Transfer org ownership — requires MFA.
       */
      transferOwnership: {
        /**
         * Transfer org ownership — requires MFA.
         */
        mutate(input: { orgId: string; newOwnerId: string; reason?: string }, opts?: RequestOptions): Promise<{ transferred: boolean; }>;
      };
      /**
       * Stream org audit log entries live.
       */
      auditStream: {
        /**
         * Stream org audit log entries live.
         */
        stream(input: { orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; action: string; actorId: string; ts: string; }, void, unknown>>;
      };
    };
    billingRoutes5: {
      /**
       * Full subscription with pricing model.
       */
      getSubscription: {
        /**
         * Full subscription with pricing model.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; tier: "pro"; pricing: { model: "per_seat"; pricePerSeatMonth: number; minSeats: number; maxSeats: null; }; status: "active"; seats: number; currentPeriodStart: string; currentPeriodEnd: string; cancelAtPeriodEnd: false; metadata: {}; addons: never[]; }>;
      };
      /**
       * Calculate proration before confirming.
       */
      previewUpgrade: {
        /**
         * Calculate proration before confirming.
         */
        query(input: { orgId: string; targetTier: "starter" | "pro" | "business" | "enterprise"; targetSeats: number; billingInterval: "monthly" | "annual" }, opts?: RequestOptions): Promise<{ currentAmount: number; newAmount: number; prorationAmount: number; effectiveDate: string; lineItems: { description: string; amount: number; currency: string; }[]; }>;
      };
      /**
       * Create Stripe checkout session.
       */
      createCheckout: {
        /**
         * Create Stripe checkout session.
         */
        mutate(input: { orgId: string; tier: "starter" | "pro" | "business" | "enterprise"; seats: number; billingInterval: "monthly" | "annual"; addons?: Array<{ id: string; quantity: number }>; successUrl: string; cancelUrl: string; couponCode?: string }, opts?: RequestOptions): Promise<{ checkoutUrl: string; sessionId: string; }>;
      };
      /**
       * Schedule cancellation at period end.
       */
      cancelSubscription: {
        /**
         * Schedule cancellation at period end.
         */
        mutate(input: { orgId: string; reason?: string }, opts?: RequestOptions): Promise<{ cancelAtPeriodEnd: boolean; }>;
      };
      /**
       * Paginated invoice list.
       */
      invoices: {
        /**
         * Paginated invoice list.
         */
        query(input: { orgId: string; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ invoices: { id: string; amount: number; currency: string; status: string; date: string; }[]; nextCursor: string | null; }>;
      };
      /**
       * Metered usage breakdown.
       */
      usageReport: {
        /**
         * Metered usage breakdown.
         */
        query(input: { orgId: string; breakdown?: "daily" | "weekly" | "monthly" }, opts?: RequestOptions): Promise<{ period: { start: string; end: string; }; included: number; consumed: number; overage: number; overageCost: number; series: { date: string; units: number; cost: number; }[]; }>;
      };
      /**
       * Apply promotional discount code.
       */
      applyPromoCode: {
        /**
         * Apply promotional discount code.
         */
        mutate(input: { orgId: string; code: string }, opts?: RequestOptions): Promise<{ applied: boolean; discountPercent: number; }>;
      };
      /**
       * Stream invoice line items.
       */
      invoiceStream: {
        /**
         * Stream invoice line items.
         */
        stream(input: { invoiceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "line_item"; description: string; amount: number; currency: string; }, void, unknown>>;
      };
    };
    projectsRoutes5: {
      /**
       * Paginated project list with status filters.
       */
      list: {
        /**
         * Paginated project list with status filters.
         */
        query(input: { orgId: string; status?: Array<"planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled">; search?: string; page?: number }, opts?: RequestOptions): Promise<{ projects: { id: string; orgId: string; name: string; status: "active" | "cancelled" | "planning" | "on_hold" | "completed" | "archived"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: string[]; customFields: Record<string, string | number | boolean | null>; createdAt: string; updatedAt: string; description?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Project by ID.
       */
      get: {
        /**
         * Project by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; status: "active"; visibility: "private"; startDate: null; endDate: null; ownerId: string; tags: never[]; customFields: {}; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create project.
       */
      create: {
        /**
         * Create project.
         */
        mutate(input: { orgId: string; name: string; description?: string; status: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: Array<string>; customFields: Record<string, string | number | boolean | null> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update project fields.
       */
      update: {
        /**
         * Update project fields.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; status?: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility?: "private" | "internal" | "public"; startDate?: string | null; endDate?: string | null; ownerId?: string; tags?: Array<string>; customFields?: Record<string, string | number | boolean | null>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Archive project.
       */
      archive: {
        /**
         * Archive project.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ archived: boolean; }>;
      };
      /**
       * Project member list.
       */
      members: {
        /**
         * Project member list.
         */
        query(input: { projectId: string }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "viewer" | "lead" | "contributor"; joinedAt: string; }[]; }>;
      };
      /**
       * Add project member.
       */
      addMember: {
        /**
         * Add project member.
         */
        mutate(input: { projectId: string; userId: string; role: "lead" | "contributor" | "viewer" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Aggregate project stats.
       */
      stats: {
        /**
         * Aggregate project stats.
         */
        query(input: { projectId: string; period?: "7d" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ tasks: number; completed: number; openIssues: number; velocity: number; burndownPoints: { date: string; remaining: number; }[]; }>;
      };
      /**
       * Clone project structure.
       */
      duplicate: {
        /**
         * Clone project structure.
         */
        mutate(input: { id: string; newName: string; includeTasks?: boolean; includeMembers?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream full project export.
       */
      exportStream: {
        /**
         * Stream full project export.
         */
        stream(input: { id: string; format: "json" | "csv" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; resource: "task"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    tasksRoutes5: {
      /**
       * Create task with full field set.
       */
      create: {
        /**
         * Create task with full field set.
         */
        mutate(input: { projectId: string; title: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }> }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; }>;
      };
      /**
       * Task tree with recursive subtasks.
       */
      get: {
        /**
         * Task tree with recursive subtasks.
         */
        query(input: { id: string; depth?: number }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; subtasks: any[]; customFields: Record<string, { type: "text"; value: string; } | { type: "number"; value: number; } | { type: "date"; value: string; } | { type: "boolean"; value: boolean; } | { type: "select"; value: string; optionId: string; } | { type: "multi"; value: string[]; optionIds: string[]; } | { type: "user"; value: string; } | { type: "url"; value: string; }>; }>;
      };
      /**
       * Patch any task fields.
       */
      update: {
        /**
         * Patch any task fields.
         */
        mutate(input: { projectId?: string; title?: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete task permanently.
       */
      delete: {
        /**
         * Delete task permanently.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Batch status/priority update.
       */
      bulkUpdate: {
        /**
         * Batch status/priority update.
         */
        mutate(input: { ids: Array<string>; patch: { status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; dueDate?: string | null } }, opts?: RequestOptions): Promise<{ updated: number; }>;
      };
      /**
       * Comment thread for a task.
       */
      comments: {
        /**
         * Comment thread for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; body: string; author: string; createdAt: string; reactions: Record<string, string[]>; }[]; }>;
      };
      /**
       * Post comment on task.
       */
      addComment: {
        /**
         * Post comment on task.
         */
        mutate(input: { taskId: string; body: string; mentions?: Array<string> }, opts?: RequestOptions): Promise<{ id: string; body: string; }>;
      };
      /**
       * Typed audit trail for a task.
       */
      timelineEvents: {
        /**
         * Typed audit trail for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ events: ({ type: "created"; actor: string; ts: string; } | { type: "status_change"; actor: string; ts: string; from: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; to: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; } | { type: "assigned"; actor: string; ts: string; userId: string; })[]; }>;
      };
      /**
       * Tasks assigned to current user.
       */
      myTasks: {
        /**
         * Tasks assigned to current user.
         */
        query(input: { status?: Array<"backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix">; projectId?: string; limit?: number }, opts?: RequestOptions): Promise<{ tasks: any[]; total: number; }>;
      };
      /**
       * Live task update stream.
       */
      activityStream: {
        /**
         * Live task update stream.
         */
        stream(input: { projectId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "task_updated"; taskId: string; field: string; value: string; }, void, unknown>>;
      };
    };
    filesRoutes5: {
      /**
       * Files for a resource.
       */
      list: {
        /**
         * Files for a resource.
         */
        query(input: { resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; mimeTypes?: Array<string>; page?: number }, opts?: RequestOptions): Promise<{ files: { id: string; name: string; mimeType: string; sizeBytes: number; status: "deleted" | "pending" | "processing" | "ready" | "failed"; url: string | null; thumbnailUrl: string | null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: Record<string, string | number>; createdAt: string; }[]; total: number; }>;
      };
      /**
       * File metadata by ID.
       */
      get: {
        /**
         * File metadata by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; mimeType: string; sizeBytes: number; status: "ready"; url: string; thumbnailUrl: null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: {}; createdAt: string; }>;
      };
      /**
       * Get presigned S3 upload URL.
       */
      requestUpload: {
        /**
         * Get presigned S3 upload URL.
         */
        mutate(input: { fileName: string; mimeType: string; sizeBytes: number; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string }, opts?: RequestOptions): Promise<{ uploadUrl: string; fileId: string; expiresIn: number; }>;
      };
      /**
       * Mark pending upload complete.
       */
      confirmUpload: {
        /**
         * Mark pending upload complete.
         */
        mutate(input: { fileId: string; etag: string }, opts?: RequestOptions): Promise<{ id: string; confirmed: boolean; url: string; }>;
      };
      /**
       * Delete file and storage object.
       */
      delete: {
        /**
         * Delete file and storage object.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Delete multiple files.
       */
      bulkDelete: {
        /**
         * Delete multiple files.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ deleted: number; }>;
      };
      /**
       * Multipart direct file upload.
       */
      directUpload: {
        /**
         * Multipart direct file upload.
         */
        upload(input: { file: File; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; description?: string } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileId: string; name: string; size: number; }>;
      };
      /**
       * Stream file processing events.
       */
      processStream: {
        /**
         * Stream file processing events.
         */
        stream(input: { fileId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; fileId: string; stage: "ready" | "queued" | "virus_scan" | "thumbnail"; percent: number; }, void, unknown>>;
      };
    };
    notificationsRoutes5: {
      /**
       * Paginated notification list.
       */
      list: {
        /**
         * Paginated notification list.
         */
        query(input: { unreadOnly?: boolean; types?: Array<"mention" | "task_assigned" | "task_completed" | "comment" | "billing" | "security" | "system" | "workflow_run" | "integration_sync">; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ notifications: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }[]; unreadCount: number; nextCursor: string | null; }>;
      };
      /**
       * Mark notifications as read.
       */
      markRead: {
        /**
         * Mark notifications as read.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ marked: number; }>;
      };
      /**
       * Mark all notifications read.
       */
      markAllRead: {
        /**
         * Mark all notifications read.
         */
        mutate(input: any, opts?: RequestOptions): Promise<{ marked: boolean; }>;
      };
      /**
       * Get notification preferences.
       */
      preferences: {
        /**
         * Get notification preferences.
         */
        query(input: any, opts?: RequestOptions): Promise<{ channels: Record<string, boolean>; types: Partial<Record<"billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync", boolean>>; }>;
      };
      /**
       * Update preferences.
       */
      updatePreferences: {
        /**
         * Update preferences.
         */
        mutate(input: { channels?: Record<string, boolean>; types?: Record<string, boolean> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Register Web Push subscription.
       */
      pushSubscribe: {
        /**
         * Register Web Push subscription.
         */
        mutate(input: { endpoint: string; keys: { p256dh: string; auth: string } }, opts?: RequestOptions): Promise<{ subscribed: boolean; }>;
      };
      /**
       * Real-time notification SSE.
       */
      liveStream: {
        /**
         * Real-time notification SSE.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "notification"; notification: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }; }, void, unknown>>;
      };
    };
    searchRoutes5: {
      /**
       * Full-text search across resource types.
       */
      global: {
        /**
         * Full-text search across resource types.
         */
        query(input: { q: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document">; orgId: string; limit?: number; highlight?: boolean }, opts?: RequestOptions): Promise<{ results: ({ type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; })[]; total: number; took: number; }>;
      };
      /**
       * Autocomplete suggestions.
       */
      suggestions: {
        /**
         * Autocomplete suggestions.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ suggestions: { label: string; type: "user" | "file" | "task" | "project" | "comment" | "workflow" | "document"; id: string; }[]; }>;
      };
      /**
       * Faceted filter counts.
       */
      filters: {
        /**
         * Faceted filter counts.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ facets: Record<"user" | "file" | "task" | "project" | "comment" | "workflow" | "document", number>; }>;
      };
      /**
       * Trigger manual reindex for org.
       */
      reindex: {
        /**
         * Trigger manual reindex for org.
         */
        mutate(input: { orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream results progressively.
       */
      stream: {
        /**
         * Stream results progressively.
         */
        stream(input: { q: string; orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "result"; result: { type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; }; }, void, unknown>>;
      };
    };
    analyticsRoutes5: {
      /**
       * Arbitrary analytics query.
       */
      query: {
        /**
         * Arbitrary analytics query.
         */
        mutate(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }, opts?: RequestOptions): Promise<{ rows: never[]; totals: {}; comparison: undefined; meta: { executionMs: number; rowsScanned: number; cached: false; granularity: "minute" | "hour" | "day" | "week" | "month"; }; }>;
      };
      /**
       * Multi-step funnel conversion.
       */
      funnel: {
        /**
         * Multi-step funnel conversion.
         */
        mutate(input: { orgId: string; steps: Array<{ name: string; event: string; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }> }>; dateRange: { from: string; to: string } }, opts?: RequestOptions): Promise<{ steps: { name: string; count: number; conversionRate: number; avgTimeToNextStep: number | null; }[]; overallConversion: number; }>;
      };
      /**
       * Cohort retention analysis.
       */
      retention: {
        /**
         * Cohort retention analysis.
         */
        mutate(input: { orgId: string; cohortEvent: string; returnEvent: string; dateRange: { from: string; to: string }; granularity?: "minute" | "hour" | "day" | "week" | "month" }, opts?: RequestOptions): Promise<{ cohorts: { date: string; size: number; retention: number[]; }[]; }>;
      };
      /**
       * Saved report definitions.
       */
      savedReports: {
        /**
         * Saved report definitions.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; name: string; query: { orgId: string; metrics: string[]; dimensions: string[]; filters: { field: string; operator: { op: "eq"; value: string | number | boolean; } | { op: "neq"; value: string | number | boolean; } | { op: "gt"; value: number; } | { op: "gte"; value: number; } | { op: "lt"; value: number; } | { op: "lte"; value: number; } | { op: "in"; values: (string | number)[]; } | { op: "between"; min: number; max: number; }; }[]; dateRange: { from: string; to: string; granularity: "minute" | "hour" | "day" | "week" | "month"; timezone: string; }; orderBy: { field: string; direction: "asc" | "desc"; }[]; limit: number; compareWith?: { from: string; to: string; } | undefined; }; schedule: { cron: string; recipients: string[]; } | null; lastRunAt: string | null; }[]; }>;
      };
      /**
       * Persist a report definition.
       */
      saveReport: {
        /**
         * Persist a report definition.
         */
        mutate(input: { orgId: string; name: string; query: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }; schedule?: { cron: string; recipients: Array<string> } }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream query results row by row.
       */
      exportStream: {
        /**
         * Stream query results row by row.
         */
        stream(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string }; format?: "json" | "csv" | "parquet" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; index: number; data: Record<string, string | number | null>; }, void, unknown>>;
      };
    };
    webhooksRoutes5: {
      /**
       * Webhooks for an org.
       */
      list: {
        /**
         * Webhooks for an org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ webhooks: { id: string; orgId: string; url: string; events: ("task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed")[]; enabled: boolean; secret: string; headers: Record<string, string>; retryPolicy: { maxAttempts: number; backoffMs: number; }; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Register webhook endpoint.
       */
      create: {
        /**
         * Register webhook endpoint.
         */
        mutate(input: { orgId: string; url: string; description?: string; events: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled: boolean; secret: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number } }, opts?: RequestOptions): Promise<{ id: string; url: string; }>;
      };
      /**
       * Update webhook.
       */
      update: {
        /**
         * Update webhook.
         */
        mutate(input: { orgId?: string; url?: string; description?: string; events?: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled?: boolean; secret?: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Remove webhook.
       */
      delete: {
        /**
         * Remove webhook.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Recent delivery attempts.
       */
      deliveries: {
        /**
         * Recent delivery attempts.
         */
        query(input: { webhookId: string; status?: "success" | "failed" | "pending"; limit?: number }, opts?: RequestOptions): Promise<{ deliveries: { id: string; status: number; attempt: number; ts: string; error: string | null; }[]; }>;
      };
      /**
       * Retry failed delivery.
       */
      redeliver: {
        /**
         * Retry failed delivery.
         */
        mutate(input: { deliveryId: string }, opts?: RequestOptions): Promise<{ queued: boolean; }>;
      };
      /**
       * Send test event to webhook.
       */
      testPing: {
        /**
         * Send test event to webhook.
         */
        mutate(input: { webhookId: string; eventType: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed" }, opts?: RequestOptions): Promise<{ sent: boolean; deliveryId: string; }>;
      };
      /**
       * Stream webhook delivery events.
       */
      logStream: {
        /**
         * Stream webhook delivery events.
         */
        stream(input: { webhookId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "delivery"; status: number; ts: string; }, void, unknown>>;
      };
    };
    aiRoutes5: {
      /**
       * Multi-model streaming chat with tool-call support.
       */
      chat: {
        /**
         * Multi-model streaming chat with tool-call support.
         */
        stream(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "content_delta"; text: string; finishReason?: undefined; usage?: undefined; } | { type: "finish"; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; text?: undefined; }, void, unknown>>;
      };
      /**
       * Non-streaming completion.
       */
      complete: {
        /**
         * Non-streaming completion.
         */
        mutate(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string>; stream?: false }, opts?: RequestOptions): Promise<{ id: string; role: "assistant"; content: string | ({ type: "text"; text: string; } | { type: "image_url"; url: string; detail: "high" | "low" | "auto"; } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown>; } | { type: "tool_result"; toolCallId: string; content: string; isError: boolean; })[]; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; }>;
      };
      /**
       * Batch embedding generation.
       */
      embed: {
        /**
         * Batch embedding generation.
         */
        mutate(input: { inputs: Array<string | { text: string; id: string }>; model: "text-embedding-3-small" | "text-embedding-3-large"; dimensions?: number; outputFormat?: "float" | "base64" | "int8" }, opts?: RequestOptions): Promise<{ embeddings: { index: number; embedding: number[]; inputTokens: number; }[]; model: "text-embedding-3-small" | "text-embedding-3-large"; totalTokens: number; }>;
      };
      /**
       * Run content moderation checks.
       */
      moderateContent: {
        /**
         * Run content moderation checks.
         */
        mutate(input: { inputs: Array<{ id: string; text: string }>; categories?: Array<"hate" | "harassment" | "self_harm" | "sexual" | "violence" | "dangerous"> }, opts?: RequestOptions): Promise<{ results: { id: string; flagged: boolean; categories: Partial<Record<string, boolean>>; scores: Partial<Record<string, number>>; }[]; }>;
      };
      /**
       * Structured text summarization.
       */
      summarize: {
        /**
         * Structured text summarization.
         */
        mutate(input: { text: string; format?: "bullets" | "paragraph" | "tldr"; maxLength?: number }, opts?: RequestOptions): Promise<{ summary: string; wordCount: number; }>;
      };
      /**
       * AI token usage for billing period.
       */
      usageStats: {
        /**
         * AI token usage for billing period.
         */
        query(input: { orgId: string; period?: "current" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ promptTokens: number; completionTokens: number; totalCost: number; byModel: Record<string, { tokens: number; cost: number; }>; }>;
      };
    };
    auditRoutes5: {
      /**
       * Paginated audit log with filters.
       */
      log: {
        /**
         * Paginated audit log with filters.
         */
        query(input: { orgId: string; actorId?: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled">; resourceType?: string; from?: string; to?: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ entries: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }[]; total: number; }>;
      };
      /**
       * Single audit entry by ID.
       */
      getEntry: {
        /**
         * Single audit entry by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; actorId: string; actorEmail: string; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: null; resourceId: null; metadata: {}; ip: string; userAgent: string; ts: string; }>;
      };
      /**
       * Update log retention policy.
       */
      retention: {
        /**
         * Update log retention policy.
         */
        mutate(input: { orgId: string; retentionDays: number }, opts?: RequestOptions): Promise<{ updated: boolean; retentionDays: number; }>;
      };
      /**
       * Stream audit log as NDJSON.
       */
      export: {
        /**
         * Stream audit log as NDJSON.
         */
        stream(input: { orgId: string; from: string; to: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; data: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }; }, void, unknown>>;
      };
    };
    apiKeysRoutes5: {
      /**
       * List API keys for org.
       */
      list: {
        /**
         * List API keys for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ keys: { id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: string | null; lastUsedAt: string | null; createdBy: string; createdAt: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number; } | null; }[]; }>;
      };
      /**
       * Create API key.
       */
      create: {
        /**
         * Create API key.
         */
        mutate(input: { orgId: string; name: string; scopes: Array<"read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write">; expiresAt: string | null; createdBy: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number } | null }, opts?: RequestOptions): Promise<{ key: string; id: string; prefix: string; }>;
      };
      /**
       * Revoke API key.
       */
      revoke: {
        /**
         * Revoke API key.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Rotate key secret.
       */
      rotate: {
        /**
         * Rotate key secret.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ newKey: string; id: string; }>;
      };
      /**
       * API key metadata.
       */
      get: {
        /**
         * API key metadata.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: null; lastUsedAt: null; createdBy: string; createdAt: string; rateLimit: null; }>;
      };
      /**
       * Key request usage stats.
       */
      usage: {
        /**
         * Key request usage stats.
         */
        query(input: { id: string; period?: "1h" | "24h" | "7d" | "30d" }, opts?: RequestOptions): Promise<{ requests: number; errors: number; p50Ms: number; p99Ms: number; series: { ts: string; count: number; }[]; }>;
      };
    };
    integrationsRoutes5: {
      /**
       * All integrations with status.
       */
      list: {
        /**
         * All integrations with status.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ integrations: { id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: boolean; enabled: boolean; status: "error" | "active" | "disconnected" | "syncing"; lastSyncAt: string | null; errorMessage: string | null; config: Record<string, unknown>; }[]; }>;
      };
      /**
       * Single integration status.
       */
      get: {
        /**
         * Single integration status.
         */
        query(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: true; enabled: true; status: "active"; lastSyncAt: string; errorMessage: null; config: {}; }>;
      };
      /**
       * Start OAuth install flow.
       */
      install: {
        /**
         * Start OAuth install flow.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; redirectUri: string; scopes?: Array<string> }, opts?: RequestOptions): Promise<{ authUrl: string; }>;
      };
      /**
       * Exchange OAuth code for tokens.
       */
      oauthCallback: {
        /**
         * Exchange OAuth code for tokens.
         */
        mutate(input: { code: string; state: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ installed: boolean; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; }>;
      };
      /**
       * Remove integration.
       */
      uninstall: {
        /**
         * Remove integration.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ uninstalled: boolean; }>;
      };
      /**
       * Update integration config.
       */
      updateConfig: {
        /**
         * Update integration config.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; config: Record<string, unknown> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Manual sync trigger.
       */
      triggerSync: {
        /**
         * Manual sync trigger.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream sync progress events.
       */
      syncStream: {
        /**
         * Stream sync progress events.
         */
        stream(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; itemsSynced: number; total: number; }, void, unknown>>;
      };
    };
    workflowsRoutes5: {
      /**
       * Create workflow with recursive condition tree.
       */
      create: {
        /**
         * Create workflow with recursive condition tree.
         */
        mutate(input: { name: string; description?: string; trigger: { event: string; filters?: unknown; debounceMs?: number }; steps: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; orgId: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * List workflows with run stats.
       */
      list: {
        /**
         * List workflows with run stats.
         */
        query(input: { orgId: string; enabled?: boolean }, opts?: RequestOptions): Promise<{ workflows: { id: string; name: string; enabled: boolean; lastRunAt: string | null; runCount: number; failureRate: number; }[]; }>;
      };
      /**
       * Workflow detail.
       */
      get: {
        /**
         * Workflow detail.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * Update workflow definition.
       */
      update: {
        /**
         * Update workflow definition.
         */
        mutate(input: { name?: string; description?: string; trigger?: { event: string; filters?: unknown; debounceMs?: number }; steps?: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete workflow.
       */
      delete: {
        /**
         * Delete workflow.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated run history.
       */
      runHistory: {
        /**
         * Paginated run history.
         */
        query(input: { workflowId: string; status?: "running" | "success" | "failed" | "cancelled"; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ runs: { id: string; status: string; startedAt: string; finishedAt: string | null; steps: { stepId: string; status: string; durationMs: number; error?: string | undefined; }[]; }[]; nextCursor: string | null; }>;
      };
      /**
       * Dry-run with streamed step outcomes.
       */
      testRun: {
        /**
         * Dry-run with streamed step outcomes.
         */
        stream(input: { workflowId: string; samplePayload: Record<string, unknown> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "step_result"; stepId: string; status: "success"; durationMs: number; output: Record<string, unknown>; }, void, unknown>>;
      };
    };
    documentsRoutes5: {
      /**
       * Documents in a project.
       */
      list: {
        /**
         * Documents in a project.
         */
        query(input: { projectId: string; page?: number; limit?: number; search?: string }, opts?: RequestOptions): Promise<{ documents: Pick<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "private" | "public" | "project"; authorId: string; collaborators: string[]; version: number; publishedAt: string | null; createdAt: string; updatedAt: string; }, "createdAt" | "id" | "updatedAt" | "title" | "authorId">[]; total: number; }>;
      };
      /**
       * Full document with blocks.
       */
      get: {
        /**
         * Full document with blocks.
         */
        query(input: { id: string; version?: number }, opts?: RequestOptions): Promise<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "project"; authorId: string; collaborators: never[]; version: number; publishedAt: null; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create document.
       */
      create: {
        /**
         * Create document.
         */
        mutate(input: { projectId: string; title: string; blocks: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility: "private" | "project" | "public"; authorId: string; collaborators: Array<string> }, opts?: RequestOptions): Promise<{ id: string; title: string; }>;
      };
      /**
       * Update blocks and metadata.
       */
      update: {
        /**
         * Update blocks and metadata.
         */
        mutate(input: { id: string; title?: string; blocks?: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility?: "private" | "project" | "public" }, opts?: RequestOptions): Promise<{ id: string; version: number; }>;
      };
      /**
       * Delete document.
       */
      delete: {
        /**
         * Delete document.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Publish document publicly.
       */
      publish: {
        /**
         * Publish document publicly.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ publishedAt: string; url: string; }>;
      };
      /**
       * Document version history.
       */
      history: {
        /**
         * Document version history.
         */
        query(input: { id: string; limit?: number }, opts?: RequestOptions): Promise<{ versions: { version: number; editor: string; changedAt: string; summary: string; }[]; }>;
      };
      /**
       * Collaborative edit event stream.
       */
      editStream: {
        /**
         * Collaborative edit event stream.
         */
        stream(input: { docId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "block_updated"; blockId: string; patch: Record<string, unknown>; editorId: string; }, void, unknown>>;
      };
    };
    permissionsRoutes5: {
      /**
       * All ABAC policies for org.
       */
      listPolicies: {
        /**
         * All ABAC policies for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ policies: { id: string; orgId: string; name: string; effect: "allow" | "deny"; resources: ("file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics")[]; actions: ("read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve")[]; conditions: { field: string; operator: "in" | "eq" | "neq" | "owned_by"; value: string | string[]; }[]; subjects: { type: "user" | "role" | "team"; id: string; }[]; priority: number; createdAt: string; }[]; }>;
      };
      /**
       * Create ABAC policy.
       */
      createPolicy: {
        /**
         * Create ABAC policy.
         */
        mutate(input: { orgId: string; name: string; effect: "allow" | "deny"; resources: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update policy definition.
       */
      updatePolicy: {
        /**
         * Update policy definition.
         */
        mutate(input: { orgId?: string; name?: string; effect?: "allow" | "deny"; resources?: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions?: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects?: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete policy.
       */
      deletePolicy: {
        /**
         * Delete policy.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Evaluate access for a subject.
       */
      checkAccess: {
        /**
         * Evaluate access for a subject.
         */
        query(input: { orgId: string; subjectId: string; resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }, opts?: RequestOptions): Promise<{ allowed: boolean; matchedPolicies: string[]; }>;
      };
      /**
       * Batch permission check.
       */
      bulkCheck: {
        /**
         * Batch permission check.
         */
        mutate(input: { orgId: string; subjectId: string; checks: Array<{ resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }> }, opts?: RequestOptions): Promise<{ results: { resource: "file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics"; action: "read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve"; allowed: boolean; }[]; }>;
      };
    };
    teamsRoutes5: {
      /**
       * Teams in an org.
       */
      list: {
        /**
         * Teams in an org.
         */
        query(input: { orgId: string; visibility?: "public" | "private" | "secret" }, opts?: RequestOptions): Promise<{ teams: { id: string; orgId: string; name: string; visibility: "private" | "public" | "secret"; memberCount: number; projectIds: string[]; createdAt: string; description?: string | undefined; color?: string | undefined; icon?: string | undefined; }[]; }>;
      };
      /**
       * Team by ID.
       */
      get: {
        /**
         * Team by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; description: undefined; color: string; icon: string; visibility: "public"; memberCount: number; projectIds: never[]; createdAt: string; }>;
      };
      /**
       * Create team.
       */
      create: {
        /**
         * Create team.
         */
        mutate(input: { orgId: string; name: string; description?: string; color?: string; icon?: string; visibility: "public" | "private" | "secret"; projectIds: Array<string> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update team metadata.
       */
      update: {
        /**
         * Update team metadata.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; color?: string; icon?: string; visibility?: "public" | "private" | "secret"; projectIds?: Array<string>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete team.
       */
      delete: {
        /**
         * Delete team.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Team member list.
       */
      members: {
        /**
         * Team member list.
         */
        query(input: { teamId: string; page?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "member" | "lead"; joinedAt: string; }[]; }>;
      };
      /**
       * Add user to team.
       */
      addMember: {
        /**
         * Add user to team.
         */
        mutate(input: { teamId: string; userId: string; role: "lead" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from team.
       */
      removeMember: {
        /**
         * Remove user from team.
         */
        mutate(input: { teamId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
    };
    invitesRoutes5: {
      /**
       * Pending invites for org.
       */
      list: {
        /**
         * Pending invites for org.
         */
        query(input: { orgId: string; status?: "pending" | "accepted" | "declined" | "expired" | "revoked" }, opts?: RequestOptions): Promise<{ invites: { id: string; orgId: string; email: string; role: "admin" | "owner" | "member" | "viewer"; status: "revoked" | "pending" | "accepted" | "declined" | "expired"; invitedBy: string; expiresAt: string; acceptedAt: string | null; teams: string[]; createdAt: string; personalMessage?: string | undefined; }[]; }>;
      };
      /**
       * Send invite.
       */
      create: {
        /**
         * Send invite.
         */
        mutate(input: { orgId: string; email: string; role: "owner" | "admin" | "member" | "viewer"; teams?: Array<string>; personalMessage?: string }, opts?: RequestOptions): Promise<{ id: string; email: string; }>;
      };
      /**
       * Bulk invite by email list.
       */
      bulkCreate: {
        /**
         * Bulk invite by email list.
         */
        mutate(input: { orgId: string; emails: Array<string>; role?: "admin" | "member" | "viewer"; teams?: Array<string> }, opts?: RequestOptions): Promise<{ invited: number; failed: string[]; }>;
      };
      /**
       * Accept invite via token.
       */
      accept: {
        /**
         * Accept invite via token.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ accepted: boolean; orgId: string; }>;
      };
      /**
       * Decline invite.
       */
      decline: {
        /**
         * Decline invite.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ declined: boolean; }>;
      };
      /**
       * Revoke pending invite.
       */
      revoke: {
        /**
         * Revoke pending invite.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Resend invite email.
       */
      resend: {
        /**
         * Resend invite email.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
    };
    commentsRoutes5: {
      /**
       * Comments for a resource.
       */
      list: {
        /**
         * Comments for a resource.
         */
        query(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; nextCursor: string | null; }>;
      };
      /**
       * Post a comment.
       */
      create: {
        /**
         * Post a comment.
         */
        mutate(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; authorId: string; body: string; mentions: Array<string>; attachments: Array<{ id: string; name: string; url: string }>; parentId: string | null }, opts?: RequestOptions): Promise<{ id: string; }>;
      };
      /**
       * Edit comment body.
       */
      update: {
        /**
         * Edit comment body.
         */
        mutate(input: { id: string; body: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete comment.
       */
      delete: {
        /**
         * Delete comment.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Toggle emoji reaction.
       */
      react: {
        /**
         * Toggle emoji reaction.
         */
        mutate(input: { commentId: string; emoji: string }, opts?: RequestOptions): Promise<{ added: boolean; emoji: string; }>;
      };
      /**
       * Threaded replies.
       */
      thread: {
        /**
         * Threaded replies.
         */
        query(input: { parentId: string; limit?: number }, opts?: RequestOptions): Promise<{ replies: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Stream new comments live.
       */
      liveStream: {
        /**
         * Stream new comments live.
         */
        stream(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "new_comment"; comment: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }; }, void, unknown>>;
      };
    };
    tagsRoutes5: {
      /**
       * All tags for org.
       */
      list: {
        /**
         * All tags for org.
         */
        query(input: { orgId: string; search?: string; sortBy?: "name" | "usageCount" }, opts?: RequestOptions): Promise<{ tags: { id: string; orgId: string; name: string; color: string; usageCount: number; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Create tag.
       */
      create: {
        /**
         * Create tag.
         */
        mutate(input: { orgId: string; name: string; color: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update tag name/color.
       */
      update: {
        /**
         * Update tag name/color.
         */
        mutate(input: { id: string; name?: string; color?: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete tag.
       */
      delete: {
        /**
         * Delete tag.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Merge multiple tags into one.
       */
      merge: {
        /**
         * Merge multiple tags into one.
         */
        mutate(input: { sourceIds: Array<string>; targetId: string }, opts?: RequestOptions): Promise<{ merged: number; targetId: string; }>;
      };
      /**
       * Attach tags to a resource.
       */
      attach: {
        /**
         * Attach tags to a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ attached: number; }>;
      };
      /**
       * Detach tags from a resource.
       */
      detach: {
        /**
         * Detach tags from a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ detached: number; }>;
      };
    };
    reportsRoutes5: {
      /**
       * Saved reports for org.
       */
      list: {
        /**
         * Saved reports for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; orgId: string; name: string; type: "custom" | "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest"; config: Record<string, unknown>; schedule: { cron: string; timezone: string; recipients: string[]; format: "csv" | "pdf" | "html"; } | null; lastRunAt: string | null; lastRunStatus: "success" | "failed" | "running" | null; createdBy: string; createdAt: string; }[]; }>;
      };
      /**
       * Create report definition.
       */
      create: {
        /**
         * Create report definition.
         */
        mutate(input: { orgId: string; name: string; type: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config: Record<string, unknown>; schedule: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update report.
       */
      update: {
        /**
         * Update report.
         */
        mutate(input: { orgId?: string; name?: string; type?: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config?: Record<string, unknown>; schedule?: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy?: string; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete report.
       */
      delete: {
        /**
         * Delete report.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Trigger report run.
       */
      run: {
        /**
         * Trigger report run.
         */
        mutate(input: { id: string; format?: "pdf" | "csv" | "html" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Get report download URL.
       */
      download: {
        /**
         * Get report download URL.
         */
        query(input: { id: string; runId: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream report generation.
       */
      stream: {
        /**
         * Stream report generation.
         */
        stream(input: { id: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; percent: number; stage: "collecting_data"; }, void, unknown>>;
      };
    };
    rateLimitsRoutes5: {
      /**
       * Rate limit rules for org.
       */
      listRules: {
        /**
         * Rate limit rules for org.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ rules: { id: string; orgId: string; name: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; targetId: string | null; endpoint: string | null; limits: { window: "minute" | "hour" | "day" | "second"; max: number; burstAllowance: number; }[]; action: "log" | "throttle" | "block"; enabled: boolean; createdAt: string; }[]; }>;
      };
      /**
       * Create rate limit rule.
       */
      createRule: {
        /**
         * Create rate limit rule.
         */
        mutate(input: { orgId: string; name: string; target: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId: string | null; endpoint: string | null; limits: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update rule.
       */
      updateRule: {
        /**
         * Update rule.
         */
        mutate(input: { orgId?: string; name?: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId?: string | null; endpoint?: string | null; limits?: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete rule.
       */
      deleteRule: {
        /**
         * Delete rule.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Current usage vs. limits.
       */
      currentStatus: {
        /**
         * Current usage vs. limits.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ statuses: { ruleId: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; window: string; current: number; max: number; resetAt: string; }[]; }>;
      };
      /**
       * Manually reset rate counter.
       */
      resetCounter: {
        /**
         * Manually reset rate counter.
         */
        mutate(input: { ruleId: string; targetId: string }, opts?: RequestOptions): Promise<{ reset: boolean; }>;
      };
    };
    exportsRoutes5: {
      /**
       * Queue an export job.
       */
      create: {
        /**
         * Queue an export job.
         */
        mutate(input: { orgId: string; resource: { resource: "tasks"; projectId?: string; filters?: Record<string, unknown> } | { resource: "users"; orgId: string } | { resource: "analytics"; orgId: string; metrics: Array<string>; dateRange: { from: string; to: string } } | { resource: "audit_log"; orgId: string; from: string; to: string } | { resource: "billing"; orgId: string; year: number } | { resource: "files"; resourceType: string; resourceId: string }; format: "json" | "csv" | "xlsx" | "pdf" | "ndjson" | "parquet"; notifyEmail?: string }, opts?: RequestOptions): Promise<{ id: string; status: "queued"; }>;
      };
      /**
       * Export job history.
       */
      list: {
        /**
         * Export job history.
         */
        query(input: { orgId: string; limit?: number }, opts?: RequestOptions): Promise<{ jobs: { id: string; orgId: string; resource: { resource: "tasks"; projectId?: string | undefined; filters?: Record<string, unknown> | undefined; } | { resource: "users"; orgId: string; } | { resource: "analytics"; orgId: string; metrics: string[]; dateRange: { from: string; to: string; }; } | { resource: "audit_log"; orgId: string; from: string; to: string; } | { resource: "billing"; orgId: string; year: number; } | { resource: "files"; resourceType: string; resourceId: string; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done" | "failed" | "queued" | "running"; progress: number; rowCount: number | null; downloadUrl: string | null; expiresAt: string | null; error: string | null; createdAt: string; }[]; }>;
      };
      /**
       * Export job status.
       */
      get: {
        /**
         * Export job status.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; resource: { resource: "tasks"; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done"; progress: number; rowCount: number; downloadUrl: string; expiresAt: string; error: null; createdAt: string; }>;
      };
      /**
       * Cancel queued export.
       */
      cancel: {
        /**
         * Cancel queued export.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ cancelled: boolean; }>;
      };
      /**
       * Fresh signed download URL.
       */
      download: {
        /**
         * Fresh signed download URL.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream export progress.
       */
      progressStream: {
        /**
         * Stream export progress.
         */
        stream(input: { jobId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; jobId: string; percent: number; rowsProcessed: number; }, void, unknown>>;
      };
    };
    authRoutes6: {
      /**
       * Multi-method login — password, OAuth, SAML, magic link, passkey.
       */
      login: {
        /**
         * Multi-method login — password, OAuth, SAML, magic link, passkey.
         */
        mutate(input: { method: "password"; email: string; password: string; totpCode?: string } | { method: "oauth"; provider: "google" | "github" | "microsoft" | "okta"; code: string; redirectUri: string; state: string } | { method: "saml"; orgSlug: string; samlResponse: string; relayState?: string } | { method: "magic_link"; token: string } | { method: "passkey"; credentialId: string; clientDataJSON: string; authenticatorData: string; signature: string } & { device?: { userAgent: string; ip: string; fingerprint?: string; platform?: "web" | "ios" | "android" | "desktop" } }, opts?: RequestOptions): Promise<{ tokens: { accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }; user: { id: UserId; role: "admin"; }; }>;
      };
      /**
       * Rotate refresh token.
       */
      refresh: {
        /**
         * Rotate refresh token.
         */
        mutate(input: { refreshToken: string; sessionId: string }, opts?: RequestOptions): Promise<{ accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }>;
      };
      /**
       * Invalidate session.
       */
      logout: {
        /**
         * Invalidate session.
         */
        mutate(input: { sessionId: string }, opts?: RequestOptions): Promise<{ revoked: boolean; sessionId: SessionId; }>;
      };
      /**
       * Validate MFA code after credential check.
       */
      mfaChallenge: {
        /**
         * Validate MFA code after credential check.
         */
        mutate(input: { challengeToken: string; code: string; method: "totp" | "sms" | "email" | "webauthn" }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Send password reset email.
       */
      forgotPassword: {
        /**
         * Send password reset email.
         */
        mutate(input: { email: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
      /**
       * Apply new password via reset token.
       */
      resetPassword: {
        /**
         * Apply new password via reset token.
         */
        mutate(input: { token: string; newPassword: string }, opts?: RequestOptions): Promise<{ success: boolean; }>;
      };
      /**
       * List active sessions with device info.
       */
      sessions: {
        /**
         * List active sessions with device info.
         */
        query(input: { cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ sessions: { id: SessionId; device: { userAgent: string; ip: string; fingerprint?: string | undefined; platform?: "web" | "ios" | "android" | "desktop" | undefined; }; createdAt: string; current: boolean; }[]; nextCursor: string | null; }>;
      };
      /**
       * Current authenticated user.
       */
      me: {
        /**
         * Current authenticated user.
         */
        query(input: any, opts?: RequestOptions): Promise<{ id: UserId; role: string; }>;
      };
      /**
       * Confirm email address via verification code.
       */
      verifyEmail: {
        /**
         * Confirm email address via verification code.
         */
        mutate(input: { code: string }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Stream login activity events for the current user.
       */
      deviceStream: {
        /**
         * Stream login activity events for the current user.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "session_created"; sessionId: SessionId; ts: string; }, void, unknown>>;
      };
    };
    usersRoutes6: {
      /**
       * Paginated user list with rich filters.
       */
      list: {
        /**
         * Paginated user list with rich filters.
         */
        query(input: { page?: number; limit?: number; roles?: Array<"owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support">; search?: string; sortBy?: "name" | "email" | "createdAt"; sortDir?: "asc" | "desc" }, opts?: RequestOptions): Promise<{ users: { id: string; email: string; emailVerified: boolean; name: string; avatarUrl: string | null; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: string[]; metadata: Record<string, string | number | boolean>; createdAt: string; updatedAt: string; displayName?: string | undefined; bio?: string | undefined; timezone?: string | undefined; locale?: string | undefined; address?: { line1: string; city: string; postalCode: string; country: string; line2?: string | undefined; state?: string | undefined; } | undefined; }[]; total: number; page: number; }>;
      };
      /**
       * Fetch single user with full profile.
       */
      get: {
        /**
         * Fetch single user with full profile.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; email: string; emailVerified: true; name: string; displayName: undefined; avatarUrl: null; bio: undefined; timezone: string; locale: string; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: never[]; metadata: {}; address: undefined; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Partial profile update with nested address.
       */
      update: {
        /**
         * Partial profile update with nested address.
         */
        mutate(input: { name?: string; displayName?: string; bio?: string; timezone?: string; locale?: string; avatarUrl?: string | null; address?: { line1: string; line2?: string; city: string; state?: string; postalCode: string; country: string }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Soft-delete user.
       */
      deactivate: {
        /**
         * Soft-delete user.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ deactivated: boolean; }>;
      };
      /**
       * Change role for many users atomically.
       */
      bulkRoleChange: {
        /**
         * Change role for many users atomically.
         */
        mutate(input: { userIds: Array<string>; role: "owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support"; reason?: string }, opts?: RequestOptions): Promise<{ updated: number; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; }>;
      };
      /**
       * All permissions grouped by resource for a user.
       */
      permissionMatrix: {
        /**
         * All permissions grouped by resource for a user.
         */
        query(input: { userId: string }, opts?: RequestOptions): Promise<{ userId: string; matrix: Record<string, Record<string, boolean>>; }>;
      };
      /**
       * Upload and crop user avatar.
       */
      uploadAvatar: {
        /**
         * Upload and crop user avatar.
         */
        upload(input: { file: File; cropSize?: number } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileName: string; size: number; croppedTo: number; }>;
      };
      /**
       * Change own password — requires MFA.
       */
      changePassword: {
        /**
         * Change own password — requires MFA.
         */
        mutate(input: { currentPassword: string; newPassword: string }, opts?: RequestOptions): Promise<{ changed: boolean; }>;
      };
      /**
       * GDPR data export streamed by resource type.
       */
      exportData: {
        /**
         * GDPR data export streamed by resource type.
         */
        stream(input: { userId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "chunk"; resource: "profile" | "tasks" | "comments" | "files"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    organizationsRoutes6: {
      /**
       * Orgs accessible to caller.
       */
      list: {
        /**
         * Orgs accessible to caller.
         */
        query(input: { page?: number; limit?: number }, opts?: RequestOptions): Promise<{ orgs: { id: string; name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; memberCount: number; logoUrl: string | null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }[]; total: number; }>;
      };
      /**
       * Fetch org by ID.
       */
      get: {
        /**
         * Fetch org by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; slug: string; plan: "enterprise"; memberCount: number; logoUrl: null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }>;
      };
      /**
       * Create new organization.
       */
      create: {
        /**
         * Create new organization.
         */
        mutate(input: { name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; logoUrl: string | null; settings: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> }; ownerId: string }, opts?: RequestOptions): Promise<{ id: string; slug: string; }>;
      };
      /**
       * Update org metadata and settings.
       */
      update: {
        /**
         * Update org metadata and settings.
         */
        mutate(input: { id: string; name?: string; logoUrl?: string | null; settings?: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> } }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Permanently delete org.
       */
      delete: {
        /**
         * Permanently delete org.
         */
        mutate(input: { id: string; confirmSlug: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated org member list.
       */
      members: {
        /**
         * Paginated org member list.
         */
        query(input: { orgId: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: string; joinedAt: string; }[]; total: number; }>;
      };
      /**
       * Add user to org with role.
       */
      addMember: {
        /**
         * Add user to org with role.
         */
        mutate(input: { orgId: string; userId: string; role: "owner" | "admin" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from org.
       */
      removeMember: {
        /**
         * Remove user from org.
         */
        mutate(input: { orgId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
      /**
       * Transfer org ownership — requires MFA.
       */
      transferOwnership: {
        /**
         * Transfer org ownership — requires MFA.
         */
        mutate(input: { orgId: string; newOwnerId: string; reason?: string }, opts?: RequestOptions): Promise<{ transferred: boolean; }>;
      };
      /**
       * Stream org audit log entries live.
       */
      auditStream: {
        /**
         * Stream org audit log entries live.
         */
        stream(input: { orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; action: string; actorId: string; ts: string; }, void, unknown>>;
      };
    };
    billingRoutes6: {
      /**
       * Full subscription with pricing model.
       */
      getSubscription: {
        /**
         * Full subscription with pricing model.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; tier: "pro"; pricing: { model: "per_seat"; pricePerSeatMonth: number; minSeats: number; maxSeats: null; }; status: "active"; seats: number; currentPeriodStart: string; currentPeriodEnd: string; cancelAtPeriodEnd: false; metadata: {}; addons: never[]; }>;
      };
      /**
       * Calculate proration before confirming.
       */
      previewUpgrade: {
        /**
         * Calculate proration before confirming.
         */
        query(input: { orgId: string; targetTier: "starter" | "pro" | "business" | "enterprise"; targetSeats: number; billingInterval: "monthly" | "annual" }, opts?: RequestOptions): Promise<{ currentAmount: number; newAmount: number; prorationAmount: number; effectiveDate: string; lineItems: { description: string; amount: number; currency: string; }[]; }>;
      };
      /**
       * Create Stripe checkout session.
       */
      createCheckout: {
        /**
         * Create Stripe checkout session.
         */
        mutate(input: { orgId: string; tier: "starter" | "pro" | "business" | "enterprise"; seats: number; billingInterval: "monthly" | "annual"; addons?: Array<{ id: string; quantity: number }>; successUrl: string; cancelUrl: string; couponCode?: string }, opts?: RequestOptions): Promise<{ checkoutUrl: string; sessionId: string; }>;
      };
      /**
       * Schedule cancellation at period end.
       */
      cancelSubscription: {
        /**
         * Schedule cancellation at period end.
         */
        mutate(input: { orgId: string; reason?: string }, opts?: RequestOptions): Promise<{ cancelAtPeriodEnd: boolean; }>;
      };
      /**
       * Paginated invoice list.
       */
      invoices: {
        /**
         * Paginated invoice list.
         */
        query(input: { orgId: string; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ invoices: { id: string; amount: number; currency: string; status: string; date: string; }[]; nextCursor: string | null; }>;
      };
      /**
       * Metered usage breakdown.
       */
      usageReport: {
        /**
         * Metered usage breakdown.
         */
        query(input: { orgId: string; breakdown?: "daily" | "weekly" | "monthly" }, opts?: RequestOptions): Promise<{ period: { start: string; end: string; }; included: number; consumed: number; overage: number; overageCost: number; series: { date: string; units: number; cost: number; }[]; }>;
      };
      /**
       * Apply promotional discount code.
       */
      applyPromoCode: {
        /**
         * Apply promotional discount code.
         */
        mutate(input: { orgId: string; code: string }, opts?: RequestOptions): Promise<{ applied: boolean; discountPercent: number; }>;
      };
      /**
       * Stream invoice line items.
       */
      invoiceStream: {
        /**
         * Stream invoice line items.
         */
        stream(input: { invoiceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "line_item"; description: string; amount: number; currency: string; }, void, unknown>>;
      };
    };
    projectsRoutes6: {
      /**
       * Paginated project list with status filters.
       */
      list: {
        /**
         * Paginated project list with status filters.
         */
        query(input: { orgId: string; status?: Array<"planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled">; search?: string; page?: number }, opts?: RequestOptions): Promise<{ projects: { id: string; orgId: string; name: string; status: "active" | "cancelled" | "planning" | "on_hold" | "completed" | "archived"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: string[]; customFields: Record<string, string | number | boolean | null>; createdAt: string; updatedAt: string; description?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Project by ID.
       */
      get: {
        /**
         * Project by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; status: "active"; visibility: "private"; startDate: null; endDate: null; ownerId: string; tags: never[]; customFields: {}; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create project.
       */
      create: {
        /**
         * Create project.
         */
        mutate(input: { orgId: string; name: string; description?: string; status: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: Array<string>; customFields: Record<string, string | number | boolean | null> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update project fields.
       */
      update: {
        /**
         * Update project fields.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; status?: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility?: "private" | "internal" | "public"; startDate?: string | null; endDate?: string | null; ownerId?: string; tags?: Array<string>; customFields?: Record<string, string | number | boolean | null>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Archive project.
       */
      archive: {
        /**
         * Archive project.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ archived: boolean; }>;
      };
      /**
       * Project member list.
       */
      members: {
        /**
         * Project member list.
         */
        query(input: { projectId: string }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "viewer" | "lead" | "contributor"; joinedAt: string; }[]; }>;
      };
      /**
       * Add project member.
       */
      addMember: {
        /**
         * Add project member.
         */
        mutate(input: { projectId: string; userId: string; role: "lead" | "contributor" | "viewer" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Aggregate project stats.
       */
      stats: {
        /**
         * Aggregate project stats.
         */
        query(input: { projectId: string; period?: "7d" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ tasks: number; completed: number; openIssues: number; velocity: number; burndownPoints: { date: string; remaining: number; }[]; }>;
      };
      /**
       * Clone project structure.
       */
      duplicate: {
        /**
         * Clone project structure.
         */
        mutate(input: { id: string; newName: string; includeTasks?: boolean; includeMembers?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream full project export.
       */
      exportStream: {
        /**
         * Stream full project export.
         */
        stream(input: { id: string; format: "json" | "csv" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; resource: "task"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    tasksRoutes6: {
      /**
       * Create task with full field set.
       */
      create: {
        /**
         * Create task with full field set.
         */
        mutate(input: { projectId: string; title: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }> }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; }>;
      };
      /**
       * Task tree with recursive subtasks.
       */
      get: {
        /**
         * Task tree with recursive subtasks.
         */
        query(input: { id: string; depth?: number }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; subtasks: any[]; customFields: Record<string, { type: "text"; value: string; } | { type: "number"; value: number; } | { type: "date"; value: string; } | { type: "boolean"; value: boolean; } | { type: "select"; value: string; optionId: string; } | { type: "multi"; value: string[]; optionIds: string[]; } | { type: "user"; value: string; } | { type: "url"; value: string; }>; }>;
      };
      /**
       * Patch any task fields.
       */
      update: {
        /**
         * Patch any task fields.
         */
        mutate(input: { projectId?: string; title?: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete task permanently.
       */
      delete: {
        /**
         * Delete task permanently.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Batch status/priority update.
       */
      bulkUpdate: {
        /**
         * Batch status/priority update.
         */
        mutate(input: { ids: Array<string>; patch: { status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; dueDate?: string | null } }, opts?: RequestOptions): Promise<{ updated: number; }>;
      };
      /**
       * Comment thread for a task.
       */
      comments: {
        /**
         * Comment thread for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; body: string; author: string; createdAt: string; reactions: Record<string, string[]>; }[]; }>;
      };
      /**
       * Post comment on task.
       */
      addComment: {
        /**
         * Post comment on task.
         */
        mutate(input: { taskId: string; body: string; mentions?: Array<string> }, opts?: RequestOptions): Promise<{ id: string; body: string; }>;
      };
      /**
       * Typed audit trail for a task.
       */
      timelineEvents: {
        /**
         * Typed audit trail for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ events: ({ type: "created"; actor: string; ts: string; } | { type: "status_change"; actor: string; ts: string; from: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; to: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; } | { type: "assigned"; actor: string; ts: string; userId: string; })[]; }>;
      };
      /**
       * Tasks assigned to current user.
       */
      myTasks: {
        /**
         * Tasks assigned to current user.
         */
        query(input: { status?: Array<"backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix">; projectId?: string; limit?: number }, opts?: RequestOptions): Promise<{ tasks: any[]; total: number; }>;
      };
      /**
       * Live task update stream.
       */
      activityStream: {
        /**
         * Live task update stream.
         */
        stream(input: { projectId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "task_updated"; taskId: string; field: string; value: string; }, void, unknown>>;
      };
    };
    filesRoutes6: {
      /**
       * Files for a resource.
       */
      list: {
        /**
         * Files for a resource.
         */
        query(input: { resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; mimeTypes?: Array<string>; page?: number }, opts?: RequestOptions): Promise<{ files: { id: string; name: string; mimeType: string; sizeBytes: number; status: "deleted" | "pending" | "processing" | "ready" | "failed"; url: string | null; thumbnailUrl: string | null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: Record<string, string | number>; createdAt: string; }[]; total: number; }>;
      };
      /**
       * File metadata by ID.
       */
      get: {
        /**
         * File metadata by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; mimeType: string; sizeBytes: number; status: "ready"; url: string; thumbnailUrl: null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: {}; createdAt: string; }>;
      };
      /**
       * Get presigned S3 upload URL.
       */
      requestUpload: {
        /**
         * Get presigned S3 upload URL.
         */
        mutate(input: { fileName: string; mimeType: string; sizeBytes: number; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string }, opts?: RequestOptions): Promise<{ uploadUrl: string; fileId: string; expiresIn: number; }>;
      };
      /**
       * Mark pending upload complete.
       */
      confirmUpload: {
        /**
         * Mark pending upload complete.
         */
        mutate(input: { fileId: string; etag: string }, opts?: RequestOptions): Promise<{ id: string; confirmed: boolean; url: string; }>;
      };
      /**
       * Delete file and storage object.
       */
      delete: {
        /**
         * Delete file and storage object.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Delete multiple files.
       */
      bulkDelete: {
        /**
         * Delete multiple files.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ deleted: number; }>;
      };
      /**
       * Multipart direct file upload.
       */
      directUpload: {
        /**
         * Multipart direct file upload.
         */
        upload(input: { file: File; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; description?: string } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileId: string; name: string; size: number; }>;
      };
      /**
       * Stream file processing events.
       */
      processStream: {
        /**
         * Stream file processing events.
         */
        stream(input: { fileId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; fileId: string; stage: "ready" | "queued" | "virus_scan" | "thumbnail"; percent: number; }, void, unknown>>;
      };
    };
    notificationsRoutes6: {
      /**
       * Paginated notification list.
       */
      list: {
        /**
         * Paginated notification list.
         */
        query(input: { unreadOnly?: boolean; types?: Array<"mention" | "task_assigned" | "task_completed" | "comment" | "billing" | "security" | "system" | "workflow_run" | "integration_sync">; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ notifications: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }[]; unreadCount: number; nextCursor: string | null; }>;
      };
      /**
       * Mark notifications as read.
       */
      markRead: {
        /**
         * Mark notifications as read.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ marked: number; }>;
      };
      /**
       * Mark all notifications read.
       */
      markAllRead: {
        /**
         * Mark all notifications read.
         */
        mutate(input: any, opts?: RequestOptions): Promise<{ marked: boolean; }>;
      };
      /**
       * Get notification preferences.
       */
      preferences: {
        /**
         * Get notification preferences.
         */
        query(input: any, opts?: RequestOptions): Promise<{ channels: Record<string, boolean>; types: Partial<Record<"billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync", boolean>>; }>;
      };
      /**
       * Update preferences.
       */
      updatePreferences: {
        /**
         * Update preferences.
         */
        mutate(input: { channels?: Record<string, boolean>; types?: Record<string, boolean> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Register Web Push subscription.
       */
      pushSubscribe: {
        /**
         * Register Web Push subscription.
         */
        mutate(input: { endpoint: string; keys: { p256dh: string; auth: string } }, opts?: RequestOptions): Promise<{ subscribed: boolean; }>;
      };
      /**
       * Real-time notification SSE.
       */
      liveStream: {
        /**
         * Real-time notification SSE.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "notification"; notification: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }; }, void, unknown>>;
      };
    };
    searchRoutes6: {
      /**
       * Full-text search across resource types.
       */
      global: {
        /**
         * Full-text search across resource types.
         */
        query(input: { q: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document">; orgId: string; limit?: number; highlight?: boolean }, opts?: RequestOptions): Promise<{ results: ({ type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; })[]; total: number; took: number; }>;
      };
      /**
       * Autocomplete suggestions.
       */
      suggestions: {
        /**
         * Autocomplete suggestions.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ suggestions: { label: string; type: "user" | "file" | "task" | "project" | "comment" | "workflow" | "document"; id: string; }[]; }>;
      };
      /**
       * Faceted filter counts.
       */
      filters: {
        /**
         * Faceted filter counts.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ facets: Record<"user" | "file" | "task" | "project" | "comment" | "workflow" | "document", number>; }>;
      };
      /**
       * Trigger manual reindex for org.
       */
      reindex: {
        /**
         * Trigger manual reindex for org.
         */
        mutate(input: { orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream results progressively.
       */
      stream: {
        /**
         * Stream results progressively.
         */
        stream(input: { q: string; orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "result"; result: { type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; }; }, void, unknown>>;
      };
    };
    analyticsRoutes6: {
      /**
       * Arbitrary analytics query.
       */
      query: {
        /**
         * Arbitrary analytics query.
         */
        mutate(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }, opts?: RequestOptions): Promise<{ rows: never[]; totals: {}; comparison: undefined; meta: { executionMs: number; rowsScanned: number; cached: false; granularity: "minute" | "hour" | "day" | "week" | "month"; }; }>;
      };
      /**
       * Multi-step funnel conversion.
       */
      funnel: {
        /**
         * Multi-step funnel conversion.
         */
        mutate(input: { orgId: string; steps: Array<{ name: string; event: string; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }> }>; dateRange: { from: string; to: string } }, opts?: RequestOptions): Promise<{ steps: { name: string; count: number; conversionRate: number; avgTimeToNextStep: number | null; }[]; overallConversion: number; }>;
      };
      /**
       * Cohort retention analysis.
       */
      retention: {
        /**
         * Cohort retention analysis.
         */
        mutate(input: { orgId: string; cohortEvent: string; returnEvent: string; dateRange: { from: string; to: string }; granularity?: "minute" | "hour" | "day" | "week" | "month" }, opts?: RequestOptions): Promise<{ cohorts: { date: string; size: number; retention: number[]; }[]; }>;
      };
      /**
       * Saved report definitions.
       */
      savedReports: {
        /**
         * Saved report definitions.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; name: string; query: { orgId: string; metrics: string[]; dimensions: string[]; filters: { field: string; operator: { op: "eq"; value: string | number | boolean; } | { op: "neq"; value: string | number | boolean; } | { op: "gt"; value: number; } | { op: "gte"; value: number; } | { op: "lt"; value: number; } | { op: "lte"; value: number; } | { op: "in"; values: (string | number)[]; } | { op: "between"; min: number; max: number; }; }[]; dateRange: { from: string; to: string; granularity: "minute" | "hour" | "day" | "week" | "month"; timezone: string; }; orderBy: { field: string; direction: "asc" | "desc"; }[]; limit: number; compareWith?: { from: string; to: string; } | undefined; }; schedule: { cron: string; recipients: string[]; } | null; lastRunAt: string | null; }[]; }>;
      };
      /**
       * Persist a report definition.
       */
      saveReport: {
        /**
         * Persist a report definition.
         */
        mutate(input: { orgId: string; name: string; query: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }; schedule?: { cron: string; recipients: Array<string> } }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream query results row by row.
       */
      exportStream: {
        /**
         * Stream query results row by row.
         */
        stream(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string }; format?: "json" | "csv" | "parquet" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; index: number; data: Record<string, string | number | null>; }, void, unknown>>;
      };
    };
    webhooksRoutes6: {
      /**
       * Webhooks for an org.
       */
      list: {
        /**
         * Webhooks for an org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ webhooks: { id: string; orgId: string; url: string; events: ("task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed")[]; enabled: boolean; secret: string; headers: Record<string, string>; retryPolicy: { maxAttempts: number; backoffMs: number; }; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Register webhook endpoint.
       */
      create: {
        /**
         * Register webhook endpoint.
         */
        mutate(input: { orgId: string; url: string; description?: string; events: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled: boolean; secret: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number } }, opts?: RequestOptions): Promise<{ id: string; url: string; }>;
      };
      /**
       * Update webhook.
       */
      update: {
        /**
         * Update webhook.
         */
        mutate(input: { orgId?: string; url?: string; description?: string; events?: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled?: boolean; secret?: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Remove webhook.
       */
      delete: {
        /**
         * Remove webhook.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Recent delivery attempts.
       */
      deliveries: {
        /**
         * Recent delivery attempts.
         */
        query(input: { webhookId: string; status?: "success" | "failed" | "pending"; limit?: number }, opts?: RequestOptions): Promise<{ deliveries: { id: string; status: number; attempt: number; ts: string; error: string | null; }[]; }>;
      };
      /**
       * Retry failed delivery.
       */
      redeliver: {
        /**
         * Retry failed delivery.
         */
        mutate(input: { deliveryId: string }, opts?: RequestOptions): Promise<{ queued: boolean; }>;
      };
      /**
       * Send test event to webhook.
       */
      testPing: {
        /**
         * Send test event to webhook.
         */
        mutate(input: { webhookId: string; eventType: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed" }, opts?: RequestOptions): Promise<{ sent: boolean; deliveryId: string; }>;
      };
      /**
       * Stream webhook delivery events.
       */
      logStream: {
        /**
         * Stream webhook delivery events.
         */
        stream(input: { webhookId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "delivery"; status: number; ts: string; }, void, unknown>>;
      };
    };
    aiRoutes6: {
      /**
       * Multi-model streaming chat with tool-call support.
       */
      chat: {
        /**
         * Multi-model streaming chat with tool-call support.
         */
        stream(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "content_delta"; text: string; finishReason?: undefined; usage?: undefined; } | { type: "finish"; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; text?: undefined; }, void, unknown>>;
      };
      /**
       * Non-streaming completion.
       */
      complete: {
        /**
         * Non-streaming completion.
         */
        mutate(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string>; stream?: false }, opts?: RequestOptions): Promise<{ id: string; role: "assistant"; content: string | ({ type: "text"; text: string; } | { type: "image_url"; url: string; detail: "high" | "low" | "auto"; } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown>; } | { type: "tool_result"; toolCallId: string; content: string; isError: boolean; })[]; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; }>;
      };
      /**
       * Batch embedding generation.
       */
      embed: {
        /**
         * Batch embedding generation.
         */
        mutate(input: { inputs: Array<string | { text: string; id: string }>; model: "text-embedding-3-small" | "text-embedding-3-large"; dimensions?: number; outputFormat?: "float" | "base64" | "int8" }, opts?: RequestOptions): Promise<{ embeddings: { index: number; embedding: number[]; inputTokens: number; }[]; model: "text-embedding-3-small" | "text-embedding-3-large"; totalTokens: number; }>;
      };
      /**
       * Run content moderation checks.
       */
      moderateContent: {
        /**
         * Run content moderation checks.
         */
        mutate(input: { inputs: Array<{ id: string; text: string }>; categories?: Array<"hate" | "harassment" | "self_harm" | "sexual" | "violence" | "dangerous"> }, opts?: RequestOptions): Promise<{ results: { id: string; flagged: boolean; categories: Partial<Record<string, boolean>>; scores: Partial<Record<string, number>>; }[]; }>;
      };
      /**
       * Structured text summarization.
       */
      summarize: {
        /**
         * Structured text summarization.
         */
        mutate(input: { text: string; format?: "bullets" | "paragraph" | "tldr"; maxLength?: number }, opts?: RequestOptions): Promise<{ summary: string; wordCount: number; }>;
      };
      /**
       * AI token usage for billing period.
       */
      usageStats: {
        /**
         * AI token usage for billing period.
         */
        query(input: { orgId: string; period?: "current" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ promptTokens: number; completionTokens: number; totalCost: number; byModel: Record<string, { tokens: number; cost: number; }>; }>;
      };
    };
    auditRoutes6: {
      /**
       * Paginated audit log with filters.
       */
      log: {
        /**
         * Paginated audit log with filters.
         */
        query(input: { orgId: string; actorId?: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled">; resourceType?: string; from?: string; to?: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ entries: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }[]; total: number; }>;
      };
      /**
       * Single audit entry by ID.
       */
      getEntry: {
        /**
         * Single audit entry by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; actorId: string; actorEmail: string; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: null; resourceId: null; metadata: {}; ip: string; userAgent: string; ts: string; }>;
      };
      /**
       * Update log retention policy.
       */
      retention: {
        /**
         * Update log retention policy.
         */
        mutate(input: { orgId: string; retentionDays: number }, opts?: RequestOptions): Promise<{ updated: boolean; retentionDays: number; }>;
      };
      /**
       * Stream audit log as NDJSON.
       */
      export: {
        /**
         * Stream audit log as NDJSON.
         */
        stream(input: { orgId: string; from: string; to: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; data: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }; }, void, unknown>>;
      };
    };
    apiKeysRoutes6: {
      /**
       * List API keys for org.
       */
      list: {
        /**
         * List API keys for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ keys: { id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: string | null; lastUsedAt: string | null; createdBy: string; createdAt: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number; } | null; }[]; }>;
      };
      /**
       * Create API key.
       */
      create: {
        /**
         * Create API key.
         */
        mutate(input: { orgId: string; name: string; scopes: Array<"read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write">; expiresAt: string | null; createdBy: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number } | null }, opts?: RequestOptions): Promise<{ key: string; id: string; prefix: string; }>;
      };
      /**
       * Revoke API key.
       */
      revoke: {
        /**
         * Revoke API key.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Rotate key secret.
       */
      rotate: {
        /**
         * Rotate key secret.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ newKey: string; id: string; }>;
      };
      /**
       * API key metadata.
       */
      get: {
        /**
         * API key metadata.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: null; lastUsedAt: null; createdBy: string; createdAt: string; rateLimit: null; }>;
      };
      /**
       * Key request usage stats.
       */
      usage: {
        /**
         * Key request usage stats.
         */
        query(input: { id: string; period?: "1h" | "24h" | "7d" | "30d" }, opts?: RequestOptions): Promise<{ requests: number; errors: number; p50Ms: number; p99Ms: number; series: { ts: string; count: number; }[]; }>;
      };
    };
    integrationsRoutes6: {
      /**
       * All integrations with status.
       */
      list: {
        /**
         * All integrations with status.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ integrations: { id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: boolean; enabled: boolean; status: "error" | "active" | "disconnected" | "syncing"; lastSyncAt: string | null; errorMessage: string | null; config: Record<string, unknown>; }[]; }>;
      };
      /**
       * Single integration status.
       */
      get: {
        /**
         * Single integration status.
         */
        query(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: true; enabled: true; status: "active"; lastSyncAt: string; errorMessage: null; config: {}; }>;
      };
      /**
       * Start OAuth install flow.
       */
      install: {
        /**
         * Start OAuth install flow.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; redirectUri: string; scopes?: Array<string> }, opts?: RequestOptions): Promise<{ authUrl: string; }>;
      };
      /**
       * Exchange OAuth code for tokens.
       */
      oauthCallback: {
        /**
         * Exchange OAuth code for tokens.
         */
        mutate(input: { code: string; state: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ installed: boolean; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; }>;
      };
      /**
       * Remove integration.
       */
      uninstall: {
        /**
         * Remove integration.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ uninstalled: boolean; }>;
      };
      /**
       * Update integration config.
       */
      updateConfig: {
        /**
         * Update integration config.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; config: Record<string, unknown> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Manual sync trigger.
       */
      triggerSync: {
        /**
         * Manual sync trigger.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream sync progress events.
       */
      syncStream: {
        /**
         * Stream sync progress events.
         */
        stream(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; itemsSynced: number; total: number; }, void, unknown>>;
      };
    };
    workflowsRoutes6: {
      /**
       * Create workflow with recursive condition tree.
       */
      create: {
        /**
         * Create workflow with recursive condition tree.
         */
        mutate(input: { name: string; description?: string; trigger: { event: string; filters?: unknown; debounceMs?: number }; steps: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; orgId: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * List workflows with run stats.
       */
      list: {
        /**
         * List workflows with run stats.
         */
        query(input: { orgId: string; enabled?: boolean }, opts?: RequestOptions): Promise<{ workflows: { id: string; name: string; enabled: boolean; lastRunAt: string | null; runCount: number; failureRate: number; }[]; }>;
      };
      /**
       * Workflow detail.
       */
      get: {
        /**
         * Workflow detail.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * Update workflow definition.
       */
      update: {
        /**
         * Update workflow definition.
         */
        mutate(input: { name?: string; description?: string; trigger?: { event: string; filters?: unknown; debounceMs?: number }; steps?: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete workflow.
       */
      delete: {
        /**
         * Delete workflow.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated run history.
       */
      runHistory: {
        /**
         * Paginated run history.
         */
        query(input: { workflowId: string; status?: "running" | "success" | "failed" | "cancelled"; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ runs: { id: string; status: string; startedAt: string; finishedAt: string | null; steps: { stepId: string; status: string; durationMs: number; error?: string | undefined; }[]; }[]; nextCursor: string | null; }>;
      };
      /**
       * Dry-run with streamed step outcomes.
       */
      testRun: {
        /**
         * Dry-run with streamed step outcomes.
         */
        stream(input: { workflowId: string; samplePayload: Record<string, unknown> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "step_result"; stepId: string; status: "success"; durationMs: number; output: Record<string, unknown>; }, void, unknown>>;
      };
    };
    documentsRoutes6: {
      /**
       * Documents in a project.
       */
      list: {
        /**
         * Documents in a project.
         */
        query(input: { projectId: string; page?: number; limit?: number; search?: string }, opts?: RequestOptions): Promise<{ documents: Pick<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "private" | "public" | "project"; authorId: string; collaborators: string[]; version: number; publishedAt: string | null; createdAt: string; updatedAt: string; }, "createdAt" | "id" | "updatedAt" | "title" | "authorId">[]; total: number; }>;
      };
      /**
       * Full document with blocks.
       */
      get: {
        /**
         * Full document with blocks.
         */
        query(input: { id: string; version?: number }, opts?: RequestOptions): Promise<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "project"; authorId: string; collaborators: never[]; version: number; publishedAt: null; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create document.
       */
      create: {
        /**
         * Create document.
         */
        mutate(input: { projectId: string; title: string; blocks: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility: "private" | "project" | "public"; authorId: string; collaborators: Array<string> }, opts?: RequestOptions): Promise<{ id: string; title: string; }>;
      };
      /**
       * Update blocks and metadata.
       */
      update: {
        /**
         * Update blocks and metadata.
         */
        mutate(input: { id: string; title?: string; blocks?: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility?: "private" | "project" | "public" }, opts?: RequestOptions): Promise<{ id: string; version: number; }>;
      };
      /**
       * Delete document.
       */
      delete: {
        /**
         * Delete document.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Publish document publicly.
       */
      publish: {
        /**
         * Publish document publicly.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ publishedAt: string; url: string; }>;
      };
      /**
       * Document version history.
       */
      history: {
        /**
         * Document version history.
         */
        query(input: { id: string; limit?: number }, opts?: RequestOptions): Promise<{ versions: { version: number; editor: string; changedAt: string; summary: string; }[]; }>;
      };
      /**
       * Collaborative edit event stream.
       */
      editStream: {
        /**
         * Collaborative edit event stream.
         */
        stream(input: { docId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "block_updated"; blockId: string; patch: Record<string, unknown>; editorId: string; }, void, unknown>>;
      };
    };
    permissionsRoutes6: {
      /**
       * All ABAC policies for org.
       */
      listPolicies: {
        /**
         * All ABAC policies for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ policies: { id: string; orgId: string; name: string; effect: "allow" | "deny"; resources: ("file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics")[]; actions: ("read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve")[]; conditions: { field: string; operator: "in" | "eq" | "neq" | "owned_by"; value: string | string[]; }[]; subjects: { type: "user" | "role" | "team"; id: string; }[]; priority: number; createdAt: string; }[]; }>;
      };
      /**
       * Create ABAC policy.
       */
      createPolicy: {
        /**
         * Create ABAC policy.
         */
        mutate(input: { orgId: string; name: string; effect: "allow" | "deny"; resources: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update policy definition.
       */
      updatePolicy: {
        /**
         * Update policy definition.
         */
        mutate(input: { orgId?: string; name?: string; effect?: "allow" | "deny"; resources?: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions?: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects?: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete policy.
       */
      deletePolicy: {
        /**
         * Delete policy.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Evaluate access for a subject.
       */
      checkAccess: {
        /**
         * Evaluate access for a subject.
         */
        query(input: { orgId: string; subjectId: string; resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }, opts?: RequestOptions): Promise<{ allowed: boolean; matchedPolicies: string[]; }>;
      };
      /**
       * Batch permission check.
       */
      bulkCheck: {
        /**
         * Batch permission check.
         */
        mutate(input: { orgId: string; subjectId: string; checks: Array<{ resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }> }, opts?: RequestOptions): Promise<{ results: { resource: "file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics"; action: "read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve"; allowed: boolean; }[]; }>;
      };
    };
    teamsRoutes6: {
      /**
       * Teams in an org.
       */
      list: {
        /**
         * Teams in an org.
         */
        query(input: { orgId: string; visibility?: "public" | "private" | "secret" }, opts?: RequestOptions): Promise<{ teams: { id: string; orgId: string; name: string; visibility: "private" | "public" | "secret"; memberCount: number; projectIds: string[]; createdAt: string; description?: string | undefined; color?: string | undefined; icon?: string | undefined; }[]; }>;
      };
      /**
       * Team by ID.
       */
      get: {
        /**
         * Team by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; description: undefined; color: string; icon: string; visibility: "public"; memberCount: number; projectIds: never[]; createdAt: string; }>;
      };
      /**
       * Create team.
       */
      create: {
        /**
         * Create team.
         */
        mutate(input: { orgId: string; name: string; description?: string; color?: string; icon?: string; visibility: "public" | "private" | "secret"; projectIds: Array<string> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update team metadata.
       */
      update: {
        /**
         * Update team metadata.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; color?: string; icon?: string; visibility?: "public" | "private" | "secret"; projectIds?: Array<string>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete team.
       */
      delete: {
        /**
         * Delete team.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Team member list.
       */
      members: {
        /**
         * Team member list.
         */
        query(input: { teamId: string; page?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "member" | "lead"; joinedAt: string; }[]; }>;
      };
      /**
       * Add user to team.
       */
      addMember: {
        /**
         * Add user to team.
         */
        mutate(input: { teamId: string; userId: string; role: "lead" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from team.
       */
      removeMember: {
        /**
         * Remove user from team.
         */
        mutate(input: { teamId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
    };
    invitesRoutes6: {
      /**
       * Pending invites for org.
       */
      list: {
        /**
         * Pending invites for org.
         */
        query(input: { orgId: string; status?: "pending" | "accepted" | "declined" | "expired" | "revoked" }, opts?: RequestOptions): Promise<{ invites: { id: string; orgId: string; email: string; role: "admin" | "owner" | "member" | "viewer"; status: "revoked" | "pending" | "accepted" | "declined" | "expired"; invitedBy: string; expiresAt: string; acceptedAt: string | null; teams: string[]; createdAt: string; personalMessage?: string | undefined; }[]; }>;
      };
      /**
       * Send invite.
       */
      create: {
        /**
         * Send invite.
         */
        mutate(input: { orgId: string; email: string; role: "owner" | "admin" | "member" | "viewer"; teams?: Array<string>; personalMessage?: string }, opts?: RequestOptions): Promise<{ id: string; email: string; }>;
      };
      /**
       * Bulk invite by email list.
       */
      bulkCreate: {
        /**
         * Bulk invite by email list.
         */
        mutate(input: { orgId: string; emails: Array<string>; role?: "admin" | "member" | "viewer"; teams?: Array<string> }, opts?: RequestOptions): Promise<{ invited: number; failed: string[]; }>;
      };
      /**
       * Accept invite via token.
       */
      accept: {
        /**
         * Accept invite via token.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ accepted: boolean; orgId: string; }>;
      };
      /**
       * Decline invite.
       */
      decline: {
        /**
         * Decline invite.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ declined: boolean; }>;
      };
      /**
       * Revoke pending invite.
       */
      revoke: {
        /**
         * Revoke pending invite.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Resend invite email.
       */
      resend: {
        /**
         * Resend invite email.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
    };
    commentsRoutes6: {
      /**
       * Comments for a resource.
       */
      list: {
        /**
         * Comments for a resource.
         */
        query(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; nextCursor: string | null; }>;
      };
      /**
       * Post a comment.
       */
      create: {
        /**
         * Post a comment.
         */
        mutate(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; authorId: string; body: string; mentions: Array<string>; attachments: Array<{ id: string; name: string; url: string }>; parentId: string | null }, opts?: RequestOptions): Promise<{ id: string; }>;
      };
      /**
       * Edit comment body.
       */
      update: {
        /**
         * Edit comment body.
         */
        mutate(input: { id: string; body: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete comment.
       */
      delete: {
        /**
         * Delete comment.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Toggle emoji reaction.
       */
      react: {
        /**
         * Toggle emoji reaction.
         */
        mutate(input: { commentId: string; emoji: string }, opts?: RequestOptions): Promise<{ added: boolean; emoji: string; }>;
      };
      /**
       * Threaded replies.
       */
      thread: {
        /**
         * Threaded replies.
         */
        query(input: { parentId: string; limit?: number }, opts?: RequestOptions): Promise<{ replies: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Stream new comments live.
       */
      liveStream: {
        /**
         * Stream new comments live.
         */
        stream(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "new_comment"; comment: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }; }, void, unknown>>;
      };
    };
    tagsRoutes6: {
      /**
       * All tags for org.
       */
      list: {
        /**
         * All tags for org.
         */
        query(input: { orgId: string; search?: string; sortBy?: "name" | "usageCount" }, opts?: RequestOptions): Promise<{ tags: { id: string; orgId: string; name: string; color: string; usageCount: number; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Create tag.
       */
      create: {
        /**
         * Create tag.
         */
        mutate(input: { orgId: string; name: string; color: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update tag name/color.
       */
      update: {
        /**
         * Update tag name/color.
         */
        mutate(input: { id: string; name?: string; color?: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete tag.
       */
      delete: {
        /**
         * Delete tag.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Merge multiple tags into one.
       */
      merge: {
        /**
         * Merge multiple tags into one.
         */
        mutate(input: { sourceIds: Array<string>; targetId: string }, opts?: RequestOptions): Promise<{ merged: number; targetId: string; }>;
      };
      /**
       * Attach tags to a resource.
       */
      attach: {
        /**
         * Attach tags to a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ attached: number; }>;
      };
      /**
       * Detach tags from a resource.
       */
      detach: {
        /**
         * Detach tags from a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ detached: number; }>;
      };
    };
    reportsRoutes6: {
      /**
       * Saved reports for org.
       */
      list: {
        /**
         * Saved reports for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; orgId: string; name: string; type: "custom" | "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest"; config: Record<string, unknown>; schedule: { cron: string; timezone: string; recipients: string[]; format: "csv" | "pdf" | "html"; } | null; lastRunAt: string | null; lastRunStatus: "success" | "failed" | "running" | null; createdBy: string; createdAt: string; }[]; }>;
      };
      /**
       * Create report definition.
       */
      create: {
        /**
         * Create report definition.
         */
        mutate(input: { orgId: string; name: string; type: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config: Record<string, unknown>; schedule: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update report.
       */
      update: {
        /**
         * Update report.
         */
        mutate(input: { orgId?: string; name?: string; type?: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config?: Record<string, unknown>; schedule?: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy?: string; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete report.
       */
      delete: {
        /**
         * Delete report.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Trigger report run.
       */
      run: {
        /**
         * Trigger report run.
         */
        mutate(input: { id: string; format?: "pdf" | "csv" | "html" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Get report download URL.
       */
      download: {
        /**
         * Get report download URL.
         */
        query(input: { id: string; runId: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream report generation.
       */
      stream: {
        /**
         * Stream report generation.
         */
        stream(input: { id: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; percent: number; stage: "collecting_data"; }, void, unknown>>;
      };
    };
    rateLimitsRoutes6: {
      /**
       * Rate limit rules for org.
       */
      listRules: {
        /**
         * Rate limit rules for org.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ rules: { id: string; orgId: string; name: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; targetId: string | null; endpoint: string | null; limits: { window: "minute" | "hour" | "day" | "second"; max: number; burstAllowance: number; }[]; action: "log" | "throttle" | "block"; enabled: boolean; createdAt: string; }[]; }>;
      };
      /**
       * Create rate limit rule.
       */
      createRule: {
        /**
         * Create rate limit rule.
         */
        mutate(input: { orgId: string; name: string; target: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId: string | null; endpoint: string | null; limits: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update rule.
       */
      updateRule: {
        /**
         * Update rule.
         */
        mutate(input: { orgId?: string; name?: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId?: string | null; endpoint?: string | null; limits?: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete rule.
       */
      deleteRule: {
        /**
         * Delete rule.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Current usage vs. limits.
       */
      currentStatus: {
        /**
         * Current usage vs. limits.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ statuses: { ruleId: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; window: string; current: number; max: number; resetAt: string; }[]; }>;
      };
      /**
       * Manually reset rate counter.
       */
      resetCounter: {
        /**
         * Manually reset rate counter.
         */
        mutate(input: { ruleId: string; targetId: string }, opts?: RequestOptions): Promise<{ reset: boolean; }>;
      };
    };
    exportsRoutes6: {
      /**
       * Queue an export job.
       */
      create: {
        /**
         * Queue an export job.
         */
        mutate(input: { orgId: string; resource: { resource: "tasks"; projectId?: string; filters?: Record<string, unknown> } | { resource: "users"; orgId: string } | { resource: "analytics"; orgId: string; metrics: Array<string>; dateRange: { from: string; to: string } } | { resource: "audit_log"; orgId: string; from: string; to: string } | { resource: "billing"; orgId: string; year: number } | { resource: "files"; resourceType: string; resourceId: string }; format: "json" | "csv" | "xlsx" | "pdf" | "ndjson" | "parquet"; notifyEmail?: string }, opts?: RequestOptions): Promise<{ id: string; status: "queued"; }>;
      };
      /**
       * Export job history.
       */
      list: {
        /**
         * Export job history.
         */
        query(input: { orgId: string; limit?: number }, opts?: RequestOptions): Promise<{ jobs: { id: string; orgId: string; resource: { resource: "tasks"; projectId?: string | undefined; filters?: Record<string, unknown> | undefined; } | { resource: "users"; orgId: string; } | { resource: "analytics"; orgId: string; metrics: string[]; dateRange: { from: string; to: string; }; } | { resource: "audit_log"; orgId: string; from: string; to: string; } | { resource: "billing"; orgId: string; year: number; } | { resource: "files"; resourceType: string; resourceId: string; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done" | "failed" | "queued" | "running"; progress: number; rowCount: number | null; downloadUrl: string | null; expiresAt: string | null; error: string | null; createdAt: string; }[]; }>;
      };
      /**
       * Export job status.
       */
      get: {
        /**
         * Export job status.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; resource: { resource: "tasks"; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done"; progress: number; rowCount: number; downloadUrl: string; expiresAt: string; error: null; createdAt: string; }>;
      };
      /**
       * Cancel queued export.
       */
      cancel: {
        /**
         * Cancel queued export.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ cancelled: boolean; }>;
      };
      /**
       * Fresh signed download URL.
       */
      download: {
        /**
         * Fresh signed download URL.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream export progress.
       */
      progressStream: {
        /**
         * Stream export progress.
         */
        stream(input: { jobId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; jobId: string; percent: number; rowsProcessed: number; }, void, unknown>>;
      };
    };
    authRoutes7: {
      /**
       * Multi-method login — password, OAuth, SAML, magic link, passkey.
       */
      login: {
        /**
         * Multi-method login — password, OAuth, SAML, magic link, passkey.
         */
        mutate(input: { method: "password"; email: string; password: string; totpCode?: string } | { method: "oauth"; provider: "google" | "github" | "microsoft" | "okta"; code: string; redirectUri: string; state: string } | { method: "saml"; orgSlug: string; samlResponse: string; relayState?: string } | { method: "magic_link"; token: string } | { method: "passkey"; credentialId: string; clientDataJSON: string; authenticatorData: string; signature: string } & { device?: { userAgent: string; ip: string; fingerprint?: string; platform?: "web" | "ios" | "android" | "desktop" } }, opts?: RequestOptions): Promise<{ tokens: { accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }; user: { id: UserId; role: "admin"; }; }>;
      };
      /**
       * Rotate refresh token.
       */
      refresh: {
        /**
         * Rotate refresh token.
         */
        mutate(input: { refreshToken: string; sessionId: string }, opts?: RequestOptions): Promise<{ accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }>;
      };
      /**
       * Invalidate session.
       */
      logout: {
        /**
         * Invalidate session.
         */
        mutate(input: { sessionId: string }, opts?: RequestOptions): Promise<{ revoked: boolean; sessionId: SessionId; }>;
      };
      /**
       * Validate MFA code after credential check.
       */
      mfaChallenge: {
        /**
         * Validate MFA code after credential check.
         */
        mutate(input: { challengeToken: string; code: string; method: "totp" | "sms" | "email" | "webauthn" }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Send password reset email.
       */
      forgotPassword: {
        /**
         * Send password reset email.
         */
        mutate(input: { email: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
      /**
       * Apply new password via reset token.
       */
      resetPassword: {
        /**
         * Apply new password via reset token.
         */
        mutate(input: { token: string; newPassword: string }, opts?: RequestOptions): Promise<{ success: boolean; }>;
      };
      /**
       * List active sessions with device info.
       */
      sessions: {
        /**
         * List active sessions with device info.
         */
        query(input: { cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ sessions: { id: SessionId; device: { userAgent: string; ip: string; fingerprint?: string | undefined; platform?: "web" | "ios" | "android" | "desktop" | undefined; }; createdAt: string; current: boolean; }[]; nextCursor: string | null; }>;
      };
      /**
       * Current authenticated user.
       */
      me: {
        /**
         * Current authenticated user.
         */
        query(input: any, opts?: RequestOptions): Promise<{ id: UserId; role: string; }>;
      };
      /**
       * Confirm email address via verification code.
       */
      verifyEmail: {
        /**
         * Confirm email address via verification code.
         */
        mutate(input: { code: string }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Stream login activity events for the current user.
       */
      deviceStream: {
        /**
         * Stream login activity events for the current user.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "session_created"; sessionId: SessionId; ts: string; }, void, unknown>>;
      };
    };
    usersRoutes7: {
      /**
       * Paginated user list with rich filters.
       */
      list: {
        /**
         * Paginated user list with rich filters.
         */
        query(input: { page?: number; limit?: number; roles?: Array<"owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support">; search?: string; sortBy?: "name" | "email" | "createdAt"; sortDir?: "asc" | "desc" }, opts?: RequestOptions): Promise<{ users: { id: string; email: string; emailVerified: boolean; name: string; avatarUrl: string | null; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: string[]; metadata: Record<string, string | number | boolean>; createdAt: string; updatedAt: string; displayName?: string | undefined; bio?: string | undefined; timezone?: string | undefined; locale?: string | undefined; address?: { line1: string; city: string; postalCode: string; country: string; line2?: string | undefined; state?: string | undefined; } | undefined; }[]; total: number; page: number; }>;
      };
      /**
       * Fetch single user with full profile.
       */
      get: {
        /**
         * Fetch single user with full profile.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; email: string; emailVerified: true; name: string; displayName: undefined; avatarUrl: null; bio: undefined; timezone: string; locale: string; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: never[]; metadata: {}; address: undefined; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Partial profile update with nested address.
       */
      update: {
        /**
         * Partial profile update with nested address.
         */
        mutate(input: { name?: string; displayName?: string; bio?: string; timezone?: string; locale?: string; avatarUrl?: string | null; address?: { line1: string; line2?: string; city: string; state?: string; postalCode: string; country: string }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Soft-delete user.
       */
      deactivate: {
        /**
         * Soft-delete user.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ deactivated: boolean; }>;
      };
      /**
       * Change role for many users atomically.
       */
      bulkRoleChange: {
        /**
         * Change role for many users atomically.
         */
        mutate(input: { userIds: Array<string>; role: "owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support"; reason?: string }, opts?: RequestOptions): Promise<{ updated: number; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; }>;
      };
      /**
       * All permissions grouped by resource for a user.
       */
      permissionMatrix: {
        /**
         * All permissions grouped by resource for a user.
         */
        query(input: { userId: string }, opts?: RequestOptions): Promise<{ userId: string; matrix: Record<string, Record<string, boolean>>; }>;
      };
      /**
       * Upload and crop user avatar.
       */
      uploadAvatar: {
        /**
         * Upload and crop user avatar.
         */
        upload(input: { file: File; cropSize?: number } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileName: string; size: number; croppedTo: number; }>;
      };
      /**
       * Change own password — requires MFA.
       */
      changePassword: {
        /**
         * Change own password — requires MFA.
         */
        mutate(input: { currentPassword: string; newPassword: string }, opts?: RequestOptions): Promise<{ changed: boolean; }>;
      };
      /**
       * GDPR data export streamed by resource type.
       */
      exportData: {
        /**
         * GDPR data export streamed by resource type.
         */
        stream(input: { userId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "chunk"; resource: "profile" | "tasks" | "comments" | "files"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    organizationsRoutes7: {
      /**
       * Orgs accessible to caller.
       */
      list: {
        /**
         * Orgs accessible to caller.
         */
        query(input: { page?: number; limit?: number }, opts?: RequestOptions): Promise<{ orgs: { id: string; name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; memberCount: number; logoUrl: string | null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }[]; total: number; }>;
      };
      /**
       * Fetch org by ID.
       */
      get: {
        /**
         * Fetch org by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; slug: string; plan: "enterprise"; memberCount: number; logoUrl: null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }>;
      };
      /**
       * Create new organization.
       */
      create: {
        /**
         * Create new organization.
         */
        mutate(input: { name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; logoUrl: string | null; settings: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> }; ownerId: string }, opts?: RequestOptions): Promise<{ id: string; slug: string; }>;
      };
      /**
       * Update org metadata and settings.
       */
      update: {
        /**
         * Update org metadata and settings.
         */
        mutate(input: { id: string; name?: string; logoUrl?: string | null; settings?: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> } }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Permanently delete org.
       */
      delete: {
        /**
         * Permanently delete org.
         */
        mutate(input: { id: string; confirmSlug: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated org member list.
       */
      members: {
        /**
         * Paginated org member list.
         */
        query(input: { orgId: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: string; joinedAt: string; }[]; total: number; }>;
      };
      /**
       * Add user to org with role.
       */
      addMember: {
        /**
         * Add user to org with role.
         */
        mutate(input: { orgId: string; userId: string; role: "owner" | "admin" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from org.
       */
      removeMember: {
        /**
         * Remove user from org.
         */
        mutate(input: { orgId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
      /**
       * Transfer org ownership — requires MFA.
       */
      transferOwnership: {
        /**
         * Transfer org ownership — requires MFA.
         */
        mutate(input: { orgId: string; newOwnerId: string; reason?: string }, opts?: RequestOptions): Promise<{ transferred: boolean; }>;
      };
      /**
       * Stream org audit log entries live.
       */
      auditStream: {
        /**
         * Stream org audit log entries live.
         */
        stream(input: { orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; action: string; actorId: string; ts: string; }, void, unknown>>;
      };
    };
    billingRoutes7: {
      /**
       * Full subscription with pricing model.
       */
      getSubscription: {
        /**
         * Full subscription with pricing model.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; tier: "pro"; pricing: { model: "per_seat"; pricePerSeatMonth: number; minSeats: number; maxSeats: null; }; status: "active"; seats: number; currentPeriodStart: string; currentPeriodEnd: string; cancelAtPeriodEnd: false; metadata: {}; addons: never[]; }>;
      };
      /**
       * Calculate proration before confirming.
       */
      previewUpgrade: {
        /**
         * Calculate proration before confirming.
         */
        query(input: { orgId: string; targetTier: "starter" | "pro" | "business" | "enterprise"; targetSeats: number; billingInterval: "monthly" | "annual" }, opts?: RequestOptions): Promise<{ currentAmount: number; newAmount: number; prorationAmount: number; effectiveDate: string; lineItems: { description: string; amount: number; currency: string; }[]; }>;
      };
      /**
       * Create Stripe checkout session.
       */
      createCheckout: {
        /**
         * Create Stripe checkout session.
         */
        mutate(input: { orgId: string; tier: "starter" | "pro" | "business" | "enterprise"; seats: number; billingInterval: "monthly" | "annual"; addons?: Array<{ id: string; quantity: number }>; successUrl: string; cancelUrl: string; couponCode?: string }, opts?: RequestOptions): Promise<{ checkoutUrl: string; sessionId: string; }>;
      };
      /**
       * Schedule cancellation at period end.
       */
      cancelSubscription: {
        /**
         * Schedule cancellation at period end.
         */
        mutate(input: { orgId: string; reason?: string }, opts?: RequestOptions): Promise<{ cancelAtPeriodEnd: boolean; }>;
      };
      /**
       * Paginated invoice list.
       */
      invoices: {
        /**
         * Paginated invoice list.
         */
        query(input: { orgId: string; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ invoices: { id: string; amount: number; currency: string; status: string; date: string; }[]; nextCursor: string | null; }>;
      };
      /**
       * Metered usage breakdown.
       */
      usageReport: {
        /**
         * Metered usage breakdown.
         */
        query(input: { orgId: string; breakdown?: "daily" | "weekly" | "monthly" }, opts?: RequestOptions): Promise<{ period: { start: string; end: string; }; included: number; consumed: number; overage: number; overageCost: number; series: { date: string; units: number; cost: number; }[]; }>;
      };
      /**
       * Apply promotional discount code.
       */
      applyPromoCode: {
        /**
         * Apply promotional discount code.
         */
        mutate(input: { orgId: string; code: string }, opts?: RequestOptions): Promise<{ applied: boolean; discountPercent: number; }>;
      };
      /**
       * Stream invoice line items.
       */
      invoiceStream: {
        /**
         * Stream invoice line items.
         */
        stream(input: { invoiceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "line_item"; description: string; amount: number; currency: string; }, void, unknown>>;
      };
    };
    projectsRoutes7: {
      /**
       * Paginated project list with status filters.
       */
      list: {
        /**
         * Paginated project list with status filters.
         */
        query(input: { orgId: string; status?: Array<"planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled">; search?: string; page?: number }, opts?: RequestOptions): Promise<{ projects: { id: string; orgId: string; name: string; status: "active" | "cancelled" | "planning" | "on_hold" | "completed" | "archived"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: string[]; customFields: Record<string, string | number | boolean | null>; createdAt: string; updatedAt: string; description?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Project by ID.
       */
      get: {
        /**
         * Project by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; status: "active"; visibility: "private"; startDate: null; endDate: null; ownerId: string; tags: never[]; customFields: {}; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create project.
       */
      create: {
        /**
         * Create project.
         */
        mutate(input: { orgId: string; name: string; description?: string; status: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: Array<string>; customFields: Record<string, string | number | boolean | null> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update project fields.
       */
      update: {
        /**
         * Update project fields.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; status?: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility?: "private" | "internal" | "public"; startDate?: string | null; endDate?: string | null; ownerId?: string; tags?: Array<string>; customFields?: Record<string, string | number | boolean | null>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Archive project.
       */
      archive: {
        /**
         * Archive project.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ archived: boolean; }>;
      };
      /**
       * Project member list.
       */
      members: {
        /**
         * Project member list.
         */
        query(input: { projectId: string }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "viewer" | "lead" | "contributor"; joinedAt: string; }[]; }>;
      };
      /**
       * Add project member.
       */
      addMember: {
        /**
         * Add project member.
         */
        mutate(input: { projectId: string; userId: string; role: "lead" | "contributor" | "viewer" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Aggregate project stats.
       */
      stats: {
        /**
         * Aggregate project stats.
         */
        query(input: { projectId: string; period?: "7d" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ tasks: number; completed: number; openIssues: number; velocity: number; burndownPoints: { date: string; remaining: number; }[]; }>;
      };
      /**
       * Clone project structure.
       */
      duplicate: {
        /**
         * Clone project structure.
         */
        mutate(input: { id: string; newName: string; includeTasks?: boolean; includeMembers?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream full project export.
       */
      exportStream: {
        /**
         * Stream full project export.
         */
        stream(input: { id: string; format: "json" | "csv" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; resource: "task"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    tasksRoutes7: {
      /**
       * Create task with full field set.
       */
      create: {
        /**
         * Create task with full field set.
         */
        mutate(input: { projectId: string; title: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }> }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; }>;
      };
      /**
       * Task tree with recursive subtasks.
       */
      get: {
        /**
         * Task tree with recursive subtasks.
         */
        query(input: { id: string; depth?: number }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; subtasks: any[]; customFields: Record<string, { type: "text"; value: string; } | { type: "number"; value: number; } | { type: "date"; value: string; } | { type: "boolean"; value: boolean; } | { type: "select"; value: string; optionId: string; } | { type: "multi"; value: string[]; optionIds: string[]; } | { type: "user"; value: string; } | { type: "url"; value: string; }>; }>;
      };
      /**
       * Patch any task fields.
       */
      update: {
        /**
         * Patch any task fields.
         */
        mutate(input: { projectId?: string; title?: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete task permanently.
       */
      delete: {
        /**
         * Delete task permanently.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Batch status/priority update.
       */
      bulkUpdate: {
        /**
         * Batch status/priority update.
         */
        mutate(input: { ids: Array<string>; patch: { status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; dueDate?: string | null } }, opts?: RequestOptions): Promise<{ updated: number; }>;
      };
      /**
       * Comment thread for a task.
       */
      comments: {
        /**
         * Comment thread for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; body: string; author: string; createdAt: string; reactions: Record<string, string[]>; }[]; }>;
      };
      /**
       * Post comment on task.
       */
      addComment: {
        /**
         * Post comment on task.
         */
        mutate(input: { taskId: string; body: string; mentions?: Array<string> }, opts?: RequestOptions): Promise<{ id: string; body: string; }>;
      };
      /**
       * Typed audit trail for a task.
       */
      timelineEvents: {
        /**
         * Typed audit trail for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ events: ({ type: "created"; actor: string; ts: string; } | { type: "status_change"; actor: string; ts: string; from: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; to: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; } | { type: "assigned"; actor: string; ts: string; userId: string; })[]; }>;
      };
      /**
       * Tasks assigned to current user.
       */
      myTasks: {
        /**
         * Tasks assigned to current user.
         */
        query(input: { status?: Array<"backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix">; projectId?: string; limit?: number }, opts?: RequestOptions): Promise<{ tasks: any[]; total: number; }>;
      };
      /**
       * Live task update stream.
       */
      activityStream: {
        /**
         * Live task update stream.
         */
        stream(input: { projectId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "task_updated"; taskId: string; field: string; value: string; }, void, unknown>>;
      };
    };
    filesRoutes7: {
      /**
       * Files for a resource.
       */
      list: {
        /**
         * Files for a resource.
         */
        query(input: { resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; mimeTypes?: Array<string>; page?: number }, opts?: RequestOptions): Promise<{ files: { id: string; name: string; mimeType: string; sizeBytes: number; status: "deleted" | "pending" | "processing" | "ready" | "failed"; url: string | null; thumbnailUrl: string | null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: Record<string, string | number>; createdAt: string; }[]; total: number; }>;
      };
      /**
       * File metadata by ID.
       */
      get: {
        /**
         * File metadata by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; mimeType: string; sizeBytes: number; status: "ready"; url: string; thumbnailUrl: null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: {}; createdAt: string; }>;
      };
      /**
       * Get presigned S3 upload URL.
       */
      requestUpload: {
        /**
         * Get presigned S3 upload URL.
         */
        mutate(input: { fileName: string; mimeType: string; sizeBytes: number; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string }, opts?: RequestOptions): Promise<{ uploadUrl: string; fileId: string; expiresIn: number; }>;
      };
      /**
       * Mark pending upload complete.
       */
      confirmUpload: {
        /**
         * Mark pending upload complete.
         */
        mutate(input: { fileId: string; etag: string }, opts?: RequestOptions): Promise<{ id: string; confirmed: boolean; url: string; }>;
      };
      /**
       * Delete file and storage object.
       */
      delete: {
        /**
         * Delete file and storage object.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Delete multiple files.
       */
      bulkDelete: {
        /**
         * Delete multiple files.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ deleted: number; }>;
      };
      /**
       * Multipart direct file upload.
       */
      directUpload: {
        /**
         * Multipart direct file upload.
         */
        upload(input: { file: File; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; description?: string } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileId: string; name: string; size: number; }>;
      };
      /**
       * Stream file processing events.
       */
      processStream: {
        /**
         * Stream file processing events.
         */
        stream(input: { fileId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; fileId: string; stage: "ready" | "queued" | "virus_scan" | "thumbnail"; percent: number; }, void, unknown>>;
      };
    };
    notificationsRoutes7: {
      /**
       * Paginated notification list.
       */
      list: {
        /**
         * Paginated notification list.
         */
        query(input: { unreadOnly?: boolean; types?: Array<"mention" | "task_assigned" | "task_completed" | "comment" | "billing" | "security" | "system" | "workflow_run" | "integration_sync">; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ notifications: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }[]; unreadCount: number; nextCursor: string | null; }>;
      };
      /**
       * Mark notifications as read.
       */
      markRead: {
        /**
         * Mark notifications as read.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ marked: number; }>;
      };
      /**
       * Mark all notifications read.
       */
      markAllRead: {
        /**
         * Mark all notifications read.
         */
        mutate(input: any, opts?: RequestOptions): Promise<{ marked: boolean; }>;
      };
      /**
       * Get notification preferences.
       */
      preferences: {
        /**
         * Get notification preferences.
         */
        query(input: any, opts?: RequestOptions): Promise<{ channels: Record<string, boolean>; types: Partial<Record<"billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync", boolean>>; }>;
      };
      /**
       * Update preferences.
       */
      updatePreferences: {
        /**
         * Update preferences.
         */
        mutate(input: { channels?: Record<string, boolean>; types?: Record<string, boolean> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Register Web Push subscription.
       */
      pushSubscribe: {
        /**
         * Register Web Push subscription.
         */
        mutate(input: { endpoint: string; keys: { p256dh: string; auth: string } }, opts?: RequestOptions): Promise<{ subscribed: boolean; }>;
      };
      /**
       * Real-time notification SSE.
       */
      liveStream: {
        /**
         * Real-time notification SSE.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "notification"; notification: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }; }, void, unknown>>;
      };
    };
    searchRoutes7: {
      /**
       * Full-text search across resource types.
       */
      global: {
        /**
         * Full-text search across resource types.
         */
        query(input: { q: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document">; orgId: string; limit?: number; highlight?: boolean }, opts?: RequestOptions): Promise<{ results: ({ type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; })[]; total: number; took: number; }>;
      };
      /**
       * Autocomplete suggestions.
       */
      suggestions: {
        /**
         * Autocomplete suggestions.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ suggestions: { label: string; type: "user" | "file" | "task" | "project" | "comment" | "workflow" | "document"; id: string; }[]; }>;
      };
      /**
       * Faceted filter counts.
       */
      filters: {
        /**
         * Faceted filter counts.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ facets: Record<"user" | "file" | "task" | "project" | "comment" | "workflow" | "document", number>; }>;
      };
      /**
       * Trigger manual reindex for org.
       */
      reindex: {
        /**
         * Trigger manual reindex for org.
         */
        mutate(input: { orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream results progressively.
       */
      stream: {
        /**
         * Stream results progressively.
         */
        stream(input: { q: string; orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "result"; result: { type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; }; }, void, unknown>>;
      };
    };
    analyticsRoutes7: {
      /**
       * Arbitrary analytics query.
       */
      query: {
        /**
         * Arbitrary analytics query.
         */
        mutate(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }, opts?: RequestOptions): Promise<{ rows: never[]; totals: {}; comparison: undefined; meta: { executionMs: number; rowsScanned: number; cached: false; granularity: "minute" | "hour" | "day" | "week" | "month"; }; }>;
      };
      /**
       * Multi-step funnel conversion.
       */
      funnel: {
        /**
         * Multi-step funnel conversion.
         */
        mutate(input: { orgId: string; steps: Array<{ name: string; event: string; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }> }>; dateRange: { from: string; to: string } }, opts?: RequestOptions): Promise<{ steps: { name: string; count: number; conversionRate: number; avgTimeToNextStep: number | null; }[]; overallConversion: number; }>;
      };
      /**
       * Cohort retention analysis.
       */
      retention: {
        /**
         * Cohort retention analysis.
         */
        mutate(input: { orgId: string; cohortEvent: string; returnEvent: string; dateRange: { from: string; to: string }; granularity?: "minute" | "hour" | "day" | "week" | "month" }, opts?: RequestOptions): Promise<{ cohorts: { date: string; size: number; retention: number[]; }[]; }>;
      };
      /**
       * Saved report definitions.
       */
      savedReports: {
        /**
         * Saved report definitions.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; name: string; query: { orgId: string; metrics: string[]; dimensions: string[]; filters: { field: string; operator: { op: "eq"; value: string | number | boolean; } | { op: "neq"; value: string | number | boolean; } | { op: "gt"; value: number; } | { op: "gte"; value: number; } | { op: "lt"; value: number; } | { op: "lte"; value: number; } | { op: "in"; values: (string | number)[]; } | { op: "between"; min: number; max: number; }; }[]; dateRange: { from: string; to: string; granularity: "minute" | "hour" | "day" | "week" | "month"; timezone: string; }; orderBy: { field: string; direction: "asc" | "desc"; }[]; limit: number; compareWith?: { from: string; to: string; } | undefined; }; schedule: { cron: string; recipients: string[]; } | null; lastRunAt: string | null; }[]; }>;
      };
      /**
       * Persist a report definition.
       */
      saveReport: {
        /**
         * Persist a report definition.
         */
        mutate(input: { orgId: string; name: string; query: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }; schedule?: { cron: string; recipients: Array<string> } }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream query results row by row.
       */
      exportStream: {
        /**
         * Stream query results row by row.
         */
        stream(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string }; format?: "json" | "csv" | "parquet" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; index: number; data: Record<string, string | number | null>; }, void, unknown>>;
      };
    };
    webhooksRoutes7: {
      /**
       * Webhooks for an org.
       */
      list: {
        /**
         * Webhooks for an org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ webhooks: { id: string; orgId: string; url: string; events: ("task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed")[]; enabled: boolean; secret: string; headers: Record<string, string>; retryPolicy: { maxAttempts: number; backoffMs: number; }; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Register webhook endpoint.
       */
      create: {
        /**
         * Register webhook endpoint.
         */
        mutate(input: { orgId: string; url: string; description?: string; events: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled: boolean; secret: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number } }, opts?: RequestOptions): Promise<{ id: string; url: string; }>;
      };
      /**
       * Update webhook.
       */
      update: {
        /**
         * Update webhook.
         */
        mutate(input: { orgId?: string; url?: string; description?: string; events?: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled?: boolean; secret?: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Remove webhook.
       */
      delete: {
        /**
         * Remove webhook.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Recent delivery attempts.
       */
      deliveries: {
        /**
         * Recent delivery attempts.
         */
        query(input: { webhookId: string; status?: "success" | "failed" | "pending"; limit?: number }, opts?: RequestOptions): Promise<{ deliveries: { id: string; status: number; attempt: number; ts: string; error: string | null; }[]; }>;
      };
      /**
       * Retry failed delivery.
       */
      redeliver: {
        /**
         * Retry failed delivery.
         */
        mutate(input: { deliveryId: string }, opts?: RequestOptions): Promise<{ queued: boolean; }>;
      };
      /**
       * Send test event to webhook.
       */
      testPing: {
        /**
         * Send test event to webhook.
         */
        mutate(input: { webhookId: string; eventType: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed" }, opts?: RequestOptions): Promise<{ sent: boolean; deliveryId: string; }>;
      };
      /**
       * Stream webhook delivery events.
       */
      logStream: {
        /**
         * Stream webhook delivery events.
         */
        stream(input: { webhookId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "delivery"; status: number; ts: string; }, void, unknown>>;
      };
    };
    aiRoutes7: {
      /**
       * Multi-model streaming chat with tool-call support.
       */
      chat: {
        /**
         * Multi-model streaming chat with tool-call support.
         */
        stream(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "content_delta"; text: string; finishReason?: undefined; usage?: undefined; } | { type: "finish"; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; text?: undefined; }, void, unknown>>;
      };
      /**
       * Non-streaming completion.
       */
      complete: {
        /**
         * Non-streaming completion.
         */
        mutate(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string>; stream?: false }, opts?: RequestOptions): Promise<{ id: string; role: "assistant"; content: string | ({ type: "text"; text: string; } | { type: "image_url"; url: string; detail: "high" | "low" | "auto"; } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown>; } | { type: "tool_result"; toolCallId: string; content: string; isError: boolean; })[]; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; }>;
      };
      /**
       * Batch embedding generation.
       */
      embed: {
        /**
         * Batch embedding generation.
         */
        mutate(input: { inputs: Array<string | { text: string; id: string }>; model: "text-embedding-3-small" | "text-embedding-3-large"; dimensions?: number; outputFormat?: "float" | "base64" | "int8" }, opts?: RequestOptions): Promise<{ embeddings: { index: number; embedding: number[]; inputTokens: number; }[]; model: "text-embedding-3-small" | "text-embedding-3-large"; totalTokens: number; }>;
      };
      /**
       * Run content moderation checks.
       */
      moderateContent: {
        /**
         * Run content moderation checks.
         */
        mutate(input: { inputs: Array<{ id: string; text: string }>; categories?: Array<"hate" | "harassment" | "self_harm" | "sexual" | "violence" | "dangerous"> }, opts?: RequestOptions): Promise<{ results: { id: string; flagged: boolean; categories: Partial<Record<string, boolean>>; scores: Partial<Record<string, number>>; }[]; }>;
      };
      /**
       * Structured text summarization.
       */
      summarize: {
        /**
         * Structured text summarization.
         */
        mutate(input: { text: string; format?: "bullets" | "paragraph" | "tldr"; maxLength?: number }, opts?: RequestOptions): Promise<{ summary: string; wordCount: number; }>;
      };
      /**
       * AI token usage for billing period.
       */
      usageStats: {
        /**
         * AI token usage for billing period.
         */
        query(input: { orgId: string; period?: "current" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ promptTokens: number; completionTokens: number; totalCost: number; byModel: Record<string, { tokens: number; cost: number; }>; }>;
      };
    };
    auditRoutes7: {
      /**
       * Paginated audit log with filters.
       */
      log: {
        /**
         * Paginated audit log with filters.
         */
        query(input: { orgId: string; actorId?: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled">; resourceType?: string; from?: string; to?: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ entries: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }[]; total: number; }>;
      };
      /**
       * Single audit entry by ID.
       */
      getEntry: {
        /**
         * Single audit entry by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; actorId: string; actorEmail: string; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: null; resourceId: null; metadata: {}; ip: string; userAgent: string; ts: string; }>;
      };
      /**
       * Update log retention policy.
       */
      retention: {
        /**
         * Update log retention policy.
         */
        mutate(input: { orgId: string; retentionDays: number }, opts?: RequestOptions): Promise<{ updated: boolean; retentionDays: number; }>;
      };
      /**
       * Stream audit log as NDJSON.
       */
      export: {
        /**
         * Stream audit log as NDJSON.
         */
        stream(input: { orgId: string; from: string; to: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; data: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }; }, void, unknown>>;
      };
    };
    apiKeysRoutes7: {
      /**
       * List API keys for org.
       */
      list: {
        /**
         * List API keys for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ keys: { id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: string | null; lastUsedAt: string | null; createdBy: string; createdAt: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number; } | null; }[]; }>;
      };
      /**
       * Create API key.
       */
      create: {
        /**
         * Create API key.
         */
        mutate(input: { orgId: string; name: string; scopes: Array<"read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write">; expiresAt: string | null; createdBy: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number } | null }, opts?: RequestOptions): Promise<{ key: string; id: string; prefix: string; }>;
      };
      /**
       * Revoke API key.
       */
      revoke: {
        /**
         * Revoke API key.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Rotate key secret.
       */
      rotate: {
        /**
         * Rotate key secret.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ newKey: string; id: string; }>;
      };
      /**
       * API key metadata.
       */
      get: {
        /**
         * API key metadata.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: null; lastUsedAt: null; createdBy: string; createdAt: string; rateLimit: null; }>;
      };
      /**
       * Key request usage stats.
       */
      usage: {
        /**
         * Key request usage stats.
         */
        query(input: { id: string; period?: "1h" | "24h" | "7d" | "30d" }, opts?: RequestOptions): Promise<{ requests: number; errors: number; p50Ms: number; p99Ms: number; series: { ts: string; count: number; }[]; }>;
      };
    };
    integrationsRoutes7: {
      /**
       * All integrations with status.
       */
      list: {
        /**
         * All integrations with status.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ integrations: { id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: boolean; enabled: boolean; status: "error" | "active" | "disconnected" | "syncing"; lastSyncAt: string | null; errorMessage: string | null; config: Record<string, unknown>; }[]; }>;
      };
      /**
       * Single integration status.
       */
      get: {
        /**
         * Single integration status.
         */
        query(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: true; enabled: true; status: "active"; lastSyncAt: string; errorMessage: null; config: {}; }>;
      };
      /**
       * Start OAuth install flow.
       */
      install: {
        /**
         * Start OAuth install flow.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; redirectUri: string; scopes?: Array<string> }, opts?: RequestOptions): Promise<{ authUrl: string; }>;
      };
      /**
       * Exchange OAuth code for tokens.
       */
      oauthCallback: {
        /**
         * Exchange OAuth code for tokens.
         */
        mutate(input: { code: string; state: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ installed: boolean; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; }>;
      };
      /**
       * Remove integration.
       */
      uninstall: {
        /**
         * Remove integration.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ uninstalled: boolean; }>;
      };
      /**
       * Update integration config.
       */
      updateConfig: {
        /**
         * Update integration config.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; config: Record<string, unknown> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Manual sync trigger.
       */
      triggerSync: {
        /**
         * Manual sync trigger.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream sync progress events.
       */
      syncStream: {
        /**
         * Stream sync progress events.
         */
        stream(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; itemsSynced: number; total: number; }, void, unknown>>;
      };
    };
    workflowsRoutes7: {
      /**
       * Create workflow with recursive condition tree.
       */
      create: {
        /**
         * Create workflow with recursive condition tree.
         */
        mutate(input: { name: string; description?: string; trigger: { event: string; filters?: unknown; debounceMs?: number }; steps: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; orgId: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * List workflows with run stats.
       */
      list: {
        /**
         * List workflows with run stats.
         */
        query(input: { orgId: string; enabled?: boolean }, opts?: RequestOptions): Promise<{ workflows: { id: string; name: string; enabled: boolean; lastRunAt: string | null; runCount: number; failureRate: number; }[]; }>;
      };
      /**
       * Workflow detail.
       */
      get: {
        /**
         * Workflow detail.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * Update workflow definition.
       */
      update: {
        /**
         * Update workflow definition.
         */
        mutate(input: { name?: string; description?: string; trigger?: { event: string; filters?: unknown; debounceMs?: number }; steps?: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete workflow.
       */
      delete: {
        /**
         * Delete workflow.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated run history.
       */
      runHistory: {
        /**
         * Paginated run history.
         */
        query(input: { workflowId: string; status?: "running" | "success" | "failed" | "cancelled"; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ runs: { id: string; status: string; startedAt: string; finishedAt: string | null; steps: { stepId: string; status: string; durationMs: number; error?: string | undefined; }[]; }[]; nextCursor: string | null; }>;
      };
      /**
       * Dry-run with streamed step outcomes.
       */
      testRun: {
        /**
         * Dry-run with streamed step outcomes.
         */
        stream(input: { workflowId: string; samplePayload: Record<string, unknown> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "step_result"; stepId: string; status: "success"; durationMs: number; output: Record<string, unknown>; }, void, unknown>>;
      };
    };
    documentsRoutes7: {
      /**
       * Documents in a project.
       */
      list: {
        /**
         * Documents in a project.
         */
        query(input: { projectId: string; page?: number; limit?: number; search?: string }, opts?: RequestOptions): Promise<{ documents: Pick<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "private" | "public" | "project"; authorId: string; collaborators: string[]; version: number; publishedAt: string | null; createdAt: string; updatedAt: string; }, "createdAt" | "id" | "updatedAt" | "title" | "authorId">[]; total: number; }>;
      };
      /**
       * Full document with blocks.
       */
      get: {
        /**
         * Full document with blocks.
         */
        query(input: { id: string; version?: number }, opts?: RequestOptions): Promise<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "project"; authorId: string; collaborators: never[]; version: number; publishedAt: null; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create document.
       */
      create: {
        /**
         * Create document.
         */
        mutate(input: { projectId: string; title: string; blocks: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility: "private" | "project" | "public"; authorId: string; collaborators: Array<string> }, opts?: RequestOptions): Promise<{ id: string; title: string; }>;
      };
      /**
       * Update blocks and metadata.
       */
      update: {
        /**
         * Update blocks and metadata.
         */
        mutate(input: { id: string; title?: string; blocks?: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility?: "private" | "project" | "public" }, opts?: RequestOptions): Promise<{ id: string; version: number; }>;
      };
      /**
       * Delete document.
       */
      delete: {
        /**
         * Delete document.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Publish document publicly.
       */
      publish: {
        /**
         * Publish document publicly.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ publishedAt: string; url: string; }>;
      };
      /**
       * Document version history.
       */
      history: {
        /**
         * Document version history.
         */
        query(input: { id: string; limit?: number }, opts?: RequestOptions): Promise<{ versions: { version: number; editor: string; changedAt: string; summary: string; }[]; }>;
      };
      /**
       * Collaborative edit event stream.
       */
      editStream: {
        /**
         * Collaborative edit event stream.
         */
        stream(input: { docId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "block_updated"; blockId: string; patch: Record<string, unknown>; editorId: string; }, void, unknown>>;
      };
    };
    permissionsRoutes7: {
      /**
       * All ABAC policies for org.
       */
      listPolicies: {
        /**
         * All ABAC policies for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ policies: { id: string; orgId: string; name: string; effect: "allow" | "deny"; resources: ("file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics")[]; actions: ("read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve")[]; conditions: { field: string; operator: "in" | "eq" | "neq" | "owned_by"; value: string | string[]; }[]; subjects: { type: "user" | "role" | "team"; id: string; }[]; priority: number; createdAt: string; }[]; }>;
      };
      /**
       * Create ABAC policy.
       */
      createPolicy: {
        /**
         * Create ABAC policy.
         */
        mutate(input: { orgId: string; name: string; effect: "allow" | "deny"; resources: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update policy definition.
       */
      updatePolicy: {
        /**
         * Update policy definition.
         */
        mutate(input: { orgId?: string; name?: string; effect?: "allow" | "deny"; resources?: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions?: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects?: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete policy.
       */
      deletePolicy: {
        /**
         * Delete policy.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Evaluate access for a subject.
       */
      checkAccess: {
        /**
         * Evaluate access for a subject.
         */
        query(input: { orgId: string; subjectId: string; resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }, opts?: RequestOptions): Promise<{ allowed: boolean; matchedPolicies: string[]; }>;
      };
      /**
       * Batch permission check.
       */
      bulkCheck: {
        /**
         * Batch permission check.
         */
        mutate(input: { orgId: string; subjectId: string; checks: Array<{ resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }> }, opts?: RequestOptions): Promise<{ results: { resource: "file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics"; action: "read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve"; allowed: boolean; }[]; }>;
      };
    };
    teamsRoutes7: {
      /**
       * Teams in an org.
       */
      list: {
        /**
         * Teams in an org.
         */
        query(input: { orgId: string; visibility?: "public" | "private" | "secret" }, opts?: RequestOptions): Promise<{ teams: { id: string; orgId: string; name: string; visibility: "private" | "public" | "secret"; memberCount: number; projectIds: string[]; createdAt: string; description?: string | undefined; color?: string | undefined; icon?: string | undefined; }[]; }>;
      };
      /**
       * Team by ID.
       */
      get: {
        /**
         * Team by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; description: undefined; color: string; icon: string; visibility: "public"; memberCount: number; projectIds: never[]; createdAt: string; }>;
      };
      /**
       * Create team.
       */
      create: {
        /**
         * Create team.
         */
        mutate(input: { orgId: string; name: string; description?: string; color?: string; icon?: string; visibility: "public" | "private" | "secret"; projectIds: Array<string> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update team metadata.
       */
      update: {
        /**
         * Update team metadata.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; color?: string; icon?: string; visibility?: "public" | "private" | "secret"; projectIds?: Array<string>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete team.
       */
      delete: {
        /**
         * Delete team.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Team member list.
       */
      members: {
        /**
         * Team member list.
         */
        query(input: { teamId: string; page?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "member" | "lead"; joinedAt: string; }[]; }>;
      };
      /**
       * Add user to team.
       */
      addMember: {
        /**
         * Add user to team.
         */
        mutate(input: { teamId: string; userId: string; role: "lead" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from team.
       */
      removeMember: {
        /**
         * Remove user from team.
         */
        mutate(input: { teamId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
    };
    invitesRoutes7: {
      /**
       * Pending invites for org.
       */
      list: {
        /**
         * Pending invites for org.
         */
        query(input: { orgId: string; status?: "pending" | "accepted" | "declined" | "expired" | "revoked" }, opts?: RequestOptions): Promise<{ invites: { id: string; orgId: string; email: string; role: "admin" | "owner" | "member" | "viewer"; status: "revoked" | "pending" | "accepted" | "declined" | "expired"; invitedBy: string; expiresAt: string; acceptedAt: string | null; teams: string[]; createdAt: string; personalMessage?: string | undefined; }[]; }>;
      };
      /**
       * Send invite.
       */
      create: {
        /**
         * Send invite.
         */
        mutate(input: { orgId: string; email: string; role: "owner" | "admin" | "member" | "viewer"; teams?: Array<string>; personalMessage?: string }, opts?: RequestOptions): Promise<{ id: string; email: string; }>;
      };
      /**
       * Bulk invite by email list.
       */
      bulkCreate: {
        /**
         * Bulk invite by email list.
         */
        mutate(input: { orgId: string; emails: Array<string>; role?: "admin" | "member" | "viewer"; teams?: Array<string> }, opts?: RequestOptions): Promise<{ invited: number; failed: string[]; }>;
      };
      /**
       * Accept invite via token.
       */
      accept: {
        /**
         * Accept invite via token.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ accepted: boolean; orgId: string; }>;
      };
      /**
       * Decline invite.
       */
      decline: {
        /**
         * Decline invite.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ declined: boolean; }>;
      };
      /**
       * Revoke pending invite.
       */
      revoke: {
        /**
         * Revoke pending invite.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Resend invite email.
       */
      resend: {
        /**
         * Resend invite email.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
    };
    commentsRoutes7: {
      /**
       * Comments for a resource.
       */
      list: {
        /**
         * Comments for a resource.
         */
        query(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; nextCursor: string | null; }>;
      };
      /**
       * Post a comment.
       */
      create: {
        /**
         * Post a comment.
         */
        mutate(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; authorId: string; body: string; mentions: Array<string>; attachments: Array<{ id: string; name: string; url: string }>; parentId: string | null }, opts?: RequestOptions): Promise<{ id: string; }>;
      };
      /**
       * Edit comment body.
       */
      update: {
        /**
         * Edit comment body.
         */
        mutate(input: { id: string; body: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete comment.
       */
      delete: {
        /**
         * Delete comment.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Toggle emoji reaction.
       */
      react: {
        /**
         * Toggle emoji reaction.
         */
        mutate(input: { commentId: string; emoji: string }, opts?: RequestOptions): Promise<{ added: boolean; emoji: string; }>;
      };
      /**
       * Threaded replies.
       */
      thread: {
        /**
         * Threaded replies.
         */
        query(input: { parentId: string; limit?: number }, opts?: RequestOptions): Promise<{ replies: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Stream new comments live.
       */
      liveStream: {
        /**
         * Stream new comments live.
         */
        stream(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "new_comment"; comment: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }; }, void, unknown>>;
      };
    };
    tagsRoutes7: {
      /**
       * All tags for org.
       */
      list: {
        /**
         * All tags for org.
         */
        query(input: { orgId: string; search?: string; sortBy?: "name" | "usageCount" }, opts?: RequestOptions): Promise<{ tags: { id: string; orgId: string; name: string; color: string; usageCount: number; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Create tag.
       */
      create: {
        /**
         * Create tag.
         */
        mutate(input: { orgId: string; name: string; color: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update tag name/color.
       */
      update: {
        /**
         * Update tag name/color.
         */
        mutate(input: { id: string; name?: string; color?: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete tag.
       */
      delete: {
        /**
         * Delete tag.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Merge multiple tags into one.
       */
      merge: {
        /**
         * Merge multiple tags into one.
         */
        mutate(input: { sourceIds: Array<string>; targetId: string }, opts?: RequestOptions): Promise<{ merged: number; targetId: string; }>;
      };
      /**
       * Attach tags to a resource.
       */
      attach: {
        /**
         * Attach tags to a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ attached: number; }>;
      };
      /**
       * Detach tags from a resource.
       */
      detach: {
        /**
         * Detach tags from a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ detached: number; }>;
      };
    };
    reportsRoutes7: {
      /**
       * Saved reports for org.
       */
      list: {
        /**
         * Saved reports for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; orgId: string; name: string; type: "custom" | "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest"; config: Record<string, unknown>; schedule: { cron: string; timezone: string; recipients: string[]; format: "csv" | "pdf" | "html"; } | null; lastRunAt: string | null; lastRunStatus: "success" | "failed" | "running" | null; createdBy: string; createdAt: string; }[]; }>;
      };
      /**
       * Create report definition.
       */
      create: {
        /**
         * Create report definition.
         */
        mutate(input: { orgId: string; name: string; type: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config: Record<string, unknown>; schedule: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update report.
       */
      update: {
        /**
         * Update report.
         */
        mutate(input: { orgId?: string; name?: string; type?: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config?: Record<string, unknown>; schedule?: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy?: string; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete report.
       */
      delete: {
        /**
         * Delete report.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Trigger report run.
       */
      run: {
        /**
         * Trigger report run.
         */
        mutate(input: { id: string; format?: "pdf" | "csv" | "html" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Get report download URL.
       */
      download: {
        /**
         * Get report download URL.
         */
        query(input: { id: string; runId: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream report generation.
       */
      stream: {
        /**
         * Stream report generation.
         */
        stream(input: { id: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; percent: number; stage: "collecting_data"; }, void, unknown>>;
      };
    };
    rateLimitsRoutes7: {
      /**
       * Rate limit rules for org.
       */
      listRules: {
        /**
         * Rate limit rules for org.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ rules: { id: string; orgId: string; name: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; targetId: string | null; endpoint: string | null; limits: { window: "minute" | "hour" | "day" | "second"; max: number; burstAllowance: number; }[]; action: "log" | "throttle" | "block"; enabled: boolean; createdAt: string; }[]; }>;
      };
      /**
       * Create rate limit rule.
       */
      createRule: {
        /**
         * Create rate limit rule.
         */
        mutate(input: { orgId: string; name: string; target: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId: string | null; endpoint: string | null; limits: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update rule.
       */
      updateRule: {
        /**
         * Update rule.
         */
        mutate(input: { orgId?: string; name?: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId?: string | null; endpoint?: string | null; limits?: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete rule.
       */
      deleteRule: {
        /**
         * Delete rule.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Current usage vs. limits.
       */
      currentStatus: {
        /**
         * Current usage vs. limits.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ statuses: { ruleId: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; window: string; current: number; max: number; resetAt: string; }[]; }>;
      };
      /**
       * Manually reset rate counter.
       */
      resetCounter: {
        /**
         * Manually reset rate counter.
         */
        mutate(input: { ruleId: string; targetId: string }, opts?: RequestOptions): Promise<{ reset: boolean; }>;
      };
    };
    exportsRoutes7: {
      /**
       * Queue an export job.
       */
      create: {
        /**
         * Queue an export job.
         */
        mutate(input: { orgId: string; resource: { resource: "tasks"; projectId?: string; filters?: Record<string, unknown> } | { resource: "users"; orgId: string } | { resource: "analytics"; orgId: string; metrics: Array<string>; dateRange: { from: string; to: string } } | { resource: "audit_log"; orgId: string; from: string; to: string } | { resource: "billing"; orgId: string; year: number } | { resource: "files"; resourceType: string; resourceId: string }; format: "json" | "csv" | "xlsx" | "pdf" | "ndjson" | "parquet"; notifyEmail?: string }, opts?: RequestOptions): Promise<{ id: string; status: "queued"; }>;
      };
      /**
       * Export job history.
       */
      list: {
        /**
         * Export job history.
         */
        query(input: { orgId: string; limit?: number }, opts?: RequestOptions): Promise<{ jobs: { id: string; orgId: string; resource: { resource: "tasks"; projectId?: string | undefined; filters?: Record<string, unknown> | undefined; } | { resource: "users"; orgId: string; } | { resource: "analytics"; orgId: string; metrics: string[]; dateRange: { from: string; to: string; }; } | { resource: "audit_log"; orgId: string; from: string; to: string; } | { resource: "billing"; orgId: string; year: number; } | { resource: "files"; resourceType: string; resourceId: string; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done" | "failed" | "queued" | "running"; progress: number; rowCount: number | null; downloadUrl: string | null; expiresAt: string | null; error: string | null; createdAt: string; }[]; }>;
      };
      /**
       * Export job status.
       */
      get: {
        /**
         * Export job status.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; resource: { resource: "tasks"; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done"; progress: number; rowCount: number; downloadUrl: string; expiresAt: string; error: null; createdAt: string; }>;
      };
      /**
       * Cancel queued export.
       */
      cancel: {
        /**
         * Cancel queued export.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ cancelled: boolean; }>;
      };
      /**
       * Fresh signed download URL.
       */
      download: {
        /**
         * Fresh signed download URL.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream export progress.
       */
      progressStream: {
        /**
         * Stream export progress.
         */
        stream(input: { jobId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; jobId: string; percent: number; rowsProcessed: number; }, void, unknown>>;
      };
    };
    authRoutes8: {
      /**
       * Multi-method login — password, OAuth, SAML, magic link, passkey.
       */
      login: {
        /**
         * Multi-method login — password, OAuth, SAML, magic link, passkey.
         */
        mutate(input: { method: "password"; email: string; password: string; totpCode?: string } | { method: "oauth"; provider: "google" | "github" | "microsoft" | "okta"; code: string; redirectUri: string; state: string } | { method: "saml"; orgSlug: string; samlResponse: string; relayState?: string } | { method: "magic_link"; token: string } | { method: "passkey"; credentialId: string; clientDataJSON: string; authenticatorData: string; signature: string } & { device?: { userAgent: string; ip: string; fingerprint?: string; platform?: "web" | "ios" | "android" | "desktop" } }, opts?: RequestOptions): Promise<{ tokens: { accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }; user: { id: UserId; role: "admin"; }; }>;
      };
      /**
       * Rotate refresh token.
       */
      refresh: {
        /**
         * Rotate refresh token.
         */
        mutate(input: { refreshToken: string; sessionId: string }, opts?: RequestOptions): Promise<{ accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }>;
      };
      /**
       * Invalidate session.
       */
      logout: {
        /**
         * Invalidate session.
         */
        mutate(input: { sessionId: string }, opts?: RequestOptions): Promise<{ revoked: boolean; sessionId: SessionId; }>;
      };
      /**
       * Validate MFA code after credential check.
       */
      mfaChallenge: {
        /**
         * Validate MFA code after credential check.
         */
        mutate(input: { challengeToken: string; code: string; method: "totp" | "sms" | "email" | "webauthn" }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Send password reset email.
       */
      forgotPassword: {
        /**
         * Send password reset email.
         */
        mutate(input: { email: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
      /**
       * Apply new password via reset token.
       */
      resetPassword: {
        /**
         * Apply new password via reset token.
         */
        mutate(input: { token: string; newPassword: string }, opts?: RequestOptions): Promise<{ success: boolean; }>;
      };
      /**
       * List active sessions with device info.
       */
      sessions: {
        /**
         * List active sessions with device info.
         */
        query(input: { cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ sessions: { id: SessionId; device: { userAgent: string; ip: string; fingerprint?: string | undefined; platform?: "web" | "ios" | "android" | "desktop" | undefined; }; createdAt: string; current: boolean; }[]; nextCursor: string | null; }>;
      };
      /**
       * Current authenticated user.
       */
      me: {
        /**
         * Current authenticated user.
         */
        query(input: any, opts?: RequestOptions): Promise<{ id: UserId; role: string; }>;
      };
      /**
       * Confirm email address via verification code.
       */
      verifyEmail: {
        /**
         * Confirm email address via verification code.
         */
        mutate(input: { code: string }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Stream login activity events for the current user.
       */
      deviceStream: {
        /**
         * Stream login activity events for the current user.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "session_created"; sessionId: SessionId; ts: string; }, void, unknown>>;
      };
    };
    usersRoutes8: {
      /**
       * Paginated user list with rich filters.
       */
      list: {
        /**
         * Paginated user list with rich filters.
         */
        query(input: { page?: number; limit?: number; roles?: Array<"owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support">; search?: string; sortBy?: "name" | "email" | "createdAt"; sortDir?: "asc" | "desc" }, opts?: RequestOptions): Promise<{ users: { id: string; email: string; emailVerified: boolean; name: string; avatarUrl: string | null; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: string[]; metadata: Record<string, string | number | boolean>; createdAt: string; updatedAt: string; displayName?: string | undefined; bio?: string | undefined; timezone?: string | undefined; locale?: string | undefined; address?: { line1: string; city: string; postalCode: string; country: string; line2?: string | undefined; state?: string | undefined; } | undefined; }[]; total: number; page: number; }>;
      };
      /**
       * Fetch single user with full profile.
       */
      get: {
        /**
         * Fetch single user with full profile.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; email: string; emailVerified: true; name: string; displayName: undefined; avatarUrl: null; bio: undefined; timezone: string; locale: string; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: never[]; metadata: {}; address: undefined; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Partial profile update with nested address.
       */
      update: {
        /**
         * Partial profile update with nested address.
         */
        mutate(input: { name?: string; displayName?: string; bio?: string; timezone?: string; locale?: string; avatarUrl?: string | null; address?: { line1: string; line2?: string; city: string; state?: string; postalCode: string; country: string }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Soft-delete user.
       */
      deactivate: {
        /**
         * Soft-delete user.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ deactivated: boolean; }>;
      };
      /**
       * Change role for many users atomically.
       */
      bulkRoleChange: {
        /**
         * Change role for many users atomically.
         */
        mutate(input: { userIds: Array<string>; role: "owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support"; reason?: string }, opts?: RequestOptions): Promise<{ updated: number; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; }>;
      };
      /**
       * All permissions grouped by resource for a user.
       */
      permissionMatrix: {
        /**
         * All permissions grouped by resource for a user.
         */
        query(input: { userId: string }, opts?: RequestOptions): Promise<{ userId: string; matrix: Record<string, Record<string, boolean>>; }>;
      };
      /**
       * Upload and crop user avatar.
       */
      uploadAvatar: {
        /**
         * Upload and crop user avatar.
         */
        upload(input: { file: File; cropSize?: number } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileName: string; size: number; croppedTo: number; }>;
      };
      /**
       * Change own password — requires MFA.
       */
      changePassword: {
        /**
         * Change own password — requires MFA.
         */
        mutate(input: { currentPassword: string; newPassword: string }, opts?: RequestOptions): Promise<{ changed: boolean; }>;
      };
      /**
       * GDPR data export streamed by resource type.
       */
      exportData: {
        /**
         * GDPR data export streamed by resource type.
         */
        stream(input: { userId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "chunk"; resource: "profile" | "tasks" | "comments" | "files"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    organizationsRoutes8: {
      /**
       * Orgs accessible to caller.
       */
      list: {
        /**
         * Orgs accessible to caller.
         */
        query(input: { page?: number; limit?: number }, opts?: RequestOptions): Promise<{ orgs: { id: string; name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; memberCount: number; logoUrl: string | null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }[]; total: number; }>;
      };
      /**
       * Fetch org by ID.
       */
      get: {
        /**
         * Fetch org by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; slug: string; plan: "enterprise"; memberCount: number; logoUrl: null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }>;
      };
      /**
       * Create new organization.
       */
      create: {
        /**
         * Create new organization.
         */
        mutate(input: { name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; logoUrl: string | null; settings: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> }; ownerId: string }, opts?: RequestOptions): Promise<{ id: string; slug: string; }>;
      };
      /**
       * Update org metadata and settings.
       */
      update: {
        /**
         * Update org metadata and settings.
         */
        mutate(input: { id: string; name?: string; logoUrl?: string | null; settings?: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> } }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Permanently delete org.
       */
      delete: {
        /**
         * Permanently delete org.
         */
        mutate(input: { id: string; confirmSlug: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated org member list.
       */
      members: {
        /**
         * Paginated org member list.
         */
        query(input: { orgId: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: string; joinedAt: string; }[]; total: number; }>;
      };
      /**
       * Add user to org with role.
       */
      addMember: {
        /**
         * Add user to org with role.
         */
        mutate(input: { orgId: string; userId: string; role: "owner" | "admin" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from org.
       */
      removeMember: {
        /**
         * Remove user from org.
         */
        mutate(input: { orgId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
      /**
       * Transfer org ownership — requires MFA.
       */
      transferOwnership: {
        /**
         * Transfer org ownership — requires MFA.
         */
        mutate(input: { orgId: string; newOwnerId: string; reason?: string }, opts?: RequestOptions): Promise<{ transferred: boolean; }>;
      };
      /**
       * Stream org audit log entries live.
       */
      auditStream: {
        /**
         * Stream org audit log entries live.
         */
        stream(input: { orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; action: string; actorId: string; ts: string; }, void, unknown>>;
      };
    };
    billingRoutes8: {
      /**
       * Full subscription with pricing model.
       */
      getSubscription: {
        /**
         * Full subscription with pricing model.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; tier: "pro"; pricing: { model: "per_seat"; pricePerSeatMonth: number; minSeats: number; maxSeats: null; }; status: "active"; seats: number; currentPeriodStart: string; currentPeriodEnd: string; cancelAtPeriodEnd: false; metadata: {}; addons: never[]; }>;
      };
      /**
       * Calculate proration before confirming.
       */
      previewUpgrade: {
        /**
         * Calculate proration before confirming.
         */
        query(input: { orgId: string; targetTier: "starter" | "pro" | "business" | "enterprise"; targetSeats: number; billingInterval: "monthly" | "annual" }, opts?: RequestOptions): Promise<{ currentAmount: number; newAmount: number; prorationAmount: number; effectiveDate: string; lineItems: { description: string; amount: number; currency: string; }[]; }>;
      };
      /**
       * Create Stripe checkout session.
       */
      createCheckout: {
        /**
         * Create Stripe checkout session.
         */
        mutate(input: { orgId: string; tier: "starter" | "pro" | "business" | "enterprise"; seats: number; billingInterval: "monthly" | "annual"; addons?: Array<{ id: string; quantity: number }>; successUrl: string; cancelUrl: string; couponCode?: string }, opts?: RequestOptions): Promise<{ checkoutUrl: string; sessionId: string; }>;
      };
      /**
       * Schedule cancellation at period end.
       */
      cancelSubscription: {
        /**
         * Schedule cancellation at period end.
         */
        mutate(input: { orgId: string; reason?: string }, opts?: RequestOptions): Promise<{ cancelAtPeriodEnd: boolean; }>;
      };
      /**
       * Paginated invoice list.
       */
      invoices: {
        /**
         * Paginated invoice list.
         */
        query(input: { orgId: string; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ invoices: { id: string; amount: number; currency: string; status: string; date: string; }[]; nextCursor: string | null; }>;
      };
      /**
       * Metered usage breakdown.
       */
      usageReport: {
        /**
         * Metered usage breakdown.
         */
        query(input: { orgId: string; breakdown?: "daily" | "weekly" | "monthly" }, opts?: RequestOptions): Promise<{ period: { start: string; end: string; }; included: number; consumed: number; overage: number; overageCost: number; series: { date: string; units: number; cost: number; }[]; }>;
      };
      /**
       * Apply promotional discount code.
       */
      applyPromoCode: {
        /**
         * Apply promotional discount code.
         */
        mutate(input: { orgId: string; code: string }, opts?: RequestOptions): Promise<{ applied: boolean; discountPercent: number; }>;
      };
      /**
       * Stream invoice line items.
       */
      invoiceStream: {
        /**
         * Stream invoice line items.
         */
        stream(input: { invoiceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "line_item"; description: string; amount: number; currency: string; }, void, unknown>>;
      };
    };
    projectsRoutes8: {
      /**
       * Paginated project list with status filters.
       */
      list: {
        /**
         * Paginated project list with status filters.
         */
        query(input: { orgId: string; status?: Array<"planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled">; search?: string; page?: number }, opts?: RequestOptions): Promise<{ projects: { id: string; orgId: string; name: string; status: "active" | "cancelled" | "planning" | "on_hold" | "completed" | "archived"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: string[]; customFields: Record<string, string | number | boolean | null>; createdAt: string; updatedAt: string; description?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Project by ID.
       */
      get: {
        /**
         * Project by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; status: "active"; visibility: "private"; startDate: null; endDate: null; ownerId: string; tags: never[]; customFields: {}; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create project.
       */
      create: {
        /**
         * Create project.
         */
        mutate(input: { orgId: string; name: string; description?: string; status: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: Array<string>; customFields: Record<string, string | number | boolean | null> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update project fields.
       */
      update: {
        /**
         * Update project fields.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; status?: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility?: "private" | "internal" | "public"; startDate?: string | null; endDate?: string | null; ownerId?: string; tags?: Array<string>; customFields?: Record<string, string | number | boolean | null>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Archive project.
       */
      archive: {
        /**
         * Archive project.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ archived: boolean; }>;
      };
      /**
       * Project member list.
       */
      members: {
        /**
         * Project member list.
         */
        query(input: { projectId: string }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "viewer" | "lead" | "contributor"; joinedAt: string; }[]; }>;
      };
      /**
       * Add project member.
       */
      addMember: {
        /**
         * Add project member.
         */
        mutate(input: { projectId: string; userId: string; role: "lead" | "contributor" | "viewer" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Aggregate project stats.
       */
      stats: {
        /**
         * Aggregate project stats.
         */
        query(input: { projectId: string; period?: "7d" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ tasks: number; completed: number; openIssues: number; velocity: number; burndownPoints: { date: string; remaining: number; }[]; }>;
      };
      /**
       * Clone project structure.
       */
      duplicate: {
        /**
         * Clone project structure.
         */
        mutate(input: { id: string; newName: string; includeTasks?: boolean; includeMembers?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream full project export.
       */
      exportStream: {
        /**
         * Stream full project export.
         */
        stream(input: { id: string; format: "json" | "csv" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; resource: "task"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    tasksRoutes8: {
      /**
       * Create task with full field set.
       */
      create: {
        /**
         * Create task with full field set.
         */
        mutate(input: { projectId: string; title: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }> }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; }>;
      };
      /**
       * Task tree with recursive subtasks.
       */
      get: {
        /**
         * Task tree with recursive subtasks.
         */
        query(input: { id: string; depth?: number }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; subtasks: any[]; customFields: Record<string, { type: "text"; value: string; } | { type: "number"; value: number; } | { type: "date"; value: string; } | { type: "boolean"; value: boolean; } | { type: "select"; value: string; optionId: string; } | { type: "multi"; value: string[]; optionIds: string[]; } | { type: "user"; value: string; } | { type: "url"; value: string; }>; }>;
      };
      /**
       * Patch any task fields.
       */
      update: {
        /**
         * Patch any task fields.
         */
        mutate(input: { projectId?: string; title?: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete task permanently.
       */
      delete: {
        /**
         * Delete task permanently.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Batch status/priority update.
       */
      bulkUpdate: {
        /**
         * Batch status/priority update.
         */
        mutate(input: { ids: Array<string>; patch: { status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; dueDate?: string | null } }, opts?: RequestOptions): Promise<{ updated: number; }>;
      };
      /**
       * Comment thread for a task.
       */
      comments: {
        /**
         * Comment thread for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; body: string; author: string; createdAt: string; reactions: Record<string, string[]>; }[]; }>;
      };
      /**
       * Post comment on task.
       */
      addComment: {
        /**
         * Post comment on task.
         */
        mutate(input: { taskId: string; body: string; mentions?: Array<string> }, opts?: RequestOptions): Promise<{ id: string; body: string; }>;
      };
      /**
       * Typed audit trail for a task.
       */
      timelineEvents: {
        /**
         * Typed audit trail for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ events: ({ type: "created"; actor: string; ts: string; } | { type: "status_change"; actor: string; ts: string; from: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; to: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; } | { type: "assigned"; actor: string; ts: string; userId: string; })[]; }>;
      };
      /**
       * Tasks assigned to current user.
       */
      myTasks: {
        /**
         * Tasks assigned to current user.
         */
        query(input: { status?: Array<"backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix">; projectId?: string; limit?: number }, opts?: RequestOptions): Promise<{ tasks: any[]; total: number; }>;
      };
      /**
       * Live task update stream.
       */
      activityStream: {
        /**
         * Live task update stream.
         */
        stream(input: { projectId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "task_updated"; taskId: string; field: string; value: string; }, void, unknown>>;
      };
    };
    filesRoutes8: {
      /**
       * Files for a resource.
       */
      list: {
        /**
         * Files for a resource.
         */
        query(input: { resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; mimeTypes?: Array<string>; page?: number }, opts?: RequestOptions): Promise<{ files: { id: string; name: string; mimeType: string; sizeBytes: number; status: "deleted" | "pending" | "processing" | "ready" | "failed"; url: string | null; thumbnailUrl: string | null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: Record<string, string | number>; createdAt: string; }[]; total: number; }>;
      };
      /**
       * File metadata by ID.
       */
      get: {
        /**
         * File metadata by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; mimeType: string; sizeBytes: number; status: "ready"; url: string; thumbnailUrl: null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: {}; createdAt: string; }>;
      };
      /**
       * Get presigned S3 upload URL.
       */
      requestUpload: {
        /**
         * Get presigned S3 upload URL.
         */
        mutate(input: { fileName: string; mimeType: string; sizeBytes: number; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string }, opts?: RequestOptions): Promise<{ uploadUrl: string; fileId: string; expiresIn: number; }>;
      };
      /**
       * Mark pending upload complete.
       */
      confirmUpload: {
        /**
         * Mark pending upload complete.
         */
        mutate(input: { fileId: string; etag: string }, opts?: RequestOptions): Promise<{ id: string; confirmed: boolean; url: string; }>;
      };
      /**
       * Delete file and storage object.
       */
      delete: {
        /**
         * Delete file and storage object.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Delete multiple files.
       */
      bulkDelete: {
        /**
         * Delete multiple files.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ deleted: number; }>;
      };
      /**
       * Multipart direct file upload.
       */
      directUpload: {
        /**
         * Multipart direct file upload.
         */
        upload(input: { file: File; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; description?: string } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileId: string; name: string; size: number; }>;
      };
      /**
       * Stream file processing events.
       */
      processStream: {
        /**
         * Stream file processing events.
         */
        stream(input: { fileId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; fileId: string; stage: "ready" | "queued" | "virus_scan" | "thumbnail"; percent: number; }, void, unknown>>;
      };
    };
    notificationsRoutes8: {
      /**
       * Paginated notification list.
       */
      list: {
        /**
         * Paginated notification list.
         */
        query(input: { unreadOnly?: boolean; types?: Array<"mention" | "task_assigned" | "task_completed" | "comment" | "billing" | "security" | "system" | "workflow_run" | "integration_sync">; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ notifications: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }[]; unreadCount: number; nextCursor: string | null; }>;
      };
      /**
       * Mark notifications as read.
       */
      markRead: {
        /**
         * Mark notifications as read.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ marked: number; }>;
      };
      /**
       * Mark all notifications read.
       */
      markAllRead: {
        /**
         * Mark all notifications read.
         */
        mutate(input: any, opts?: RequestOptions): Promise<{ marked: boolean; }>;
      };
      /**
       * Get notification preferences.
       */
      preferences: {
        /**
         * Get notification preferences.
         */
        query(input: any, opts?: RequestOptions): Promise<{ channels: Record<string, boolean>; types: Partial<Record<"billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync", boolean>>; }>;
      };
      /**
       * Update preferences.
       */
      updatePreferences: {
        /**
         * Update preferences.
         */
        mutate(input: { channels?: Record<string, boolean>; types?: Record<string, boolean> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Register Web Push subscription.
       */
      pushSubscribe: {
        /**
         * Register Web Push subscription.
         */
        mutate(input: { endpoint: string; keys: { p256dh: string; auth: string } }, opts?: RequestOptions): Promise<{ subscribed: boolean; }>;
      };
      /**
       * Real-time notification SSE.
       */
      liveStream: {
        /**
         * Real-time notification SSE.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "notification"; notification: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }; }, void, unknown>>;
      };
    };
    searchRoutes8: {
      /**
       * Full-text search across resource types.
       */
      global: {
        /**
         * Full-text search across resource types.
         */
        query(input: { q: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document">; orgId: string; limit?: number; highlight?: boolean }, opts?: RequestOptions): Promise<{ results: ({ type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; })[]; total: number; took: number; }>;
      };
      /**
       * Autocomplete suggestions.
       */
      suggestions: {
        /**
         * Autocomplete suggestions.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ suggestions: { label: string; type: "user" | "file" | "task" | "project" | "comment" | "workflow" | "document"; id: string; }[]; }>;
      };
      /**
       * Faceted filter counts.
       */
      filters: {
        /**
         * Faceted filter counts.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ facets: Record<"user" | "file" | "task" | "project" | "comment" | "workflow" | "document", number>; }>;
      };
      /**
       * Trigger manual reindex for org.
       */
      reindex: {
        /**
         * Trigger manual reindex for org.
         */
        mutate(input: { orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream results progressively.
       */
      stream: {
        /**
         * Stream results progressively.
         */
        stream(input: { q: string; orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "result"; result: { type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; }; }, void, unknown>>;
      };
    };
    analyticsRoutes8: {
      /**
       * Arbitrary analytics query.
       */
      query: {
        /**
         * Arbitrary analytics query.
         */
        mutate(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }, opts?: RequestOptions): Promise<{ rows: never[]; totals: {}; comparison: undefined; meta: { executionMs: number; rowsScanned: number; cached: false; granularity: "minute" | "hour" | "day" | "week" | "month"; }; }>;
      };
      /**
       * Multi-step funnel conversion.
       */
      funnel: {
        /**
         * Multi-step funnel conversion.
         */
        mutate(input: { orgId: string; steps: Array<{ name: string; event: string; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }> }>; dateRange: { from: string; to: string } }, opts?: RequestOptions): Promise<{ steps: { name: string; count: number; conversionRate: number; avgTimeToNextStep: number | null; }[]; overallConversion: number; }>;
      };
      /**
       * Cohort retention analysis.
       */
      retention: {
        /**
         * Cohort retention analysis.
         */
        mutate(input: { orgId: string; cohortEvent: string; returnEvent: string; dateRange: { from: string; to: string }; granularity?: "minute" | "hour" | "day" | "week" | "month" }, opts?: RequestOptions): Promise<{ cohorts: { date: string; size: number; retention: number[]; }[]; }>;
      };
      /**
       * Saved report definitions.
       */
      savedReports: {
        /**
         * Saved report definitions.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; name: string; query: { orgId: string; metrics: string[]; dimensions: string[]; filters: { field: string; operator: { op: "eq"; value: string | number | boolean; } | { op: "neq"; value: string | number | boolean; } | { op: "gt"; value: number; } | { op: "gte"; value: number; } | { op: "lt"; value: number; } | { op: "lte"; value: number; } | { op: "in"; values: (string | number)[]; } | { op: "between"; min: number; max: number; }; }[]; dateRange: { from: string; to: string; granularity: "minute" | "hour" | "day" | "week" | "month"; timezone: string; }; orderBy: { field: string; direction: "asc" | "desc"; }[]; limit: number; compareWith?: { from: string; to: string; } | undefined; }; schedule: { cron: string; recipients: string[]; } | null; lastRunAt: string | null; }[]; }>;
      };
      /**
       * Persist a report definition.
       */
      saveReport: {
        /**
         * Persist a report definition.
         */
        mutate(input: { orgId: string; name: string; query: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }; schedule?: { cron: string; recipients: Array<string> } }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream query results row by row.
       */
      exportStream: {
        /**
         * Stream query results row by row.
         */
        stream(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string }; format?: "json" | "csv" | "parquet" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; index: number; data: Record<string, string | number | null>; }, void, unknown>>;
      };
    };
    webhooksRoutes8: {
      /**
       * Webhooks for an org.
       */
      list: {
        /**
         * Webhooks for an org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ webhooks: { id: string; orgId: string; url: string; events: ("task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed")[]; enabled: boolean; secret: string; headers: Record<string, string>; retryPolicy: { maxAttempts: number; backoffMs: number; }; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Register webhook endpoint.
       */
      create: {
        /**
         * Register webhook endpoint.
         */
        mutate(input: { orgId: string; url: string; description?: string; events: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled: boolean; secret: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number } }, opts?: RequestOptions): Promise<{ id: string; url: string; }>;
      };
      /**
       * Update webhook.
       */
      update: {
        /**
         * Update webhook.
         */
        mutate(input: { orgId?: string; url?: string; description?: string; events?: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled?: boolean; secret?: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Remove webhook.
       */
      delete: {
        /**
         * Remove webhook.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Recent delivery attempts.
       */
      deliveries: {
        /**
         * Recent delivery attempts.
         */
        query(input: { webhookId: string; status?: "success" | "failed" | "pending"; limit?: number }, opts?: RequestOptions): Promise<{ deliveries: { id: string; status: number; attempt: number; ts: string; error: string | null; }[]; }>;
      };
      /**
       * Retry failed delivery.
       */
      redeliver: {
        /**
         * Retry failed delivery.
         */
        mutate(input: { deliveryId: string }, opts?: RequestOptions): Promise<{ queued: boolean; }>;
      };
      /**
       * Send test event to webhook.
       */
      testPing: {
        /**
         * Send test event to webhook.
         */
        mutate(input: { webhookId: string; eventType: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed" }, opts?: RequestOptions): Promise<{ sent: boolean; deliveryId: string; }>;
      };
      /**
       * Stream webhook delivery events.
       */
      logStream: {
        /**
         * Stream webhook delivery events.
         */
        stream(input: { webhookId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "delivery"; status: number; ts: string; }, void, unknown>>;
      };
    };
    aiRoutes8: {
      /**
       * Multi-model streaming chat with tool-call support.
       */
      chat: {
        /**
         * Multi-model streaming chat with tool-call support.
         */
        stream(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "content_delta"; text: string; finishReason?: undefined; usage?: undefined; } | { type: "finish"; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; text?: undefined; }, void, unknown>>;
      };
      /**
       * Non-streaming completion.
       */
      complete: {
        /**
         * Non-streaming completion.
         */
        mutate(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string>; stream?: false }, opts?: RequestOptions): Promise<{ id: string; role: "assistant"; content: string | ({ type: "text"; text: string; } | { type: "image_url"; url: string; detail: "high" | "low" | "auto"; } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown>; } | { type: "tool_result"; toolCallId: string; content: string; isError: boolean; })[]; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; }>;
      };
      /**
       * Batch embedding generation.
       */
      embed: {
        /**
         * Batch embedding generation.
         */
        mutate(input: { inputs: Array<string | { text: string; id: string }>; model: "text-embedding-3-small" | "text-embedding-3-large"; dimensions?: number; outputFormat?: "float" | "base64" | "int8" }, opts?: RequestOptions): Promise<{ embeddings: { index: number; embedding: number[]; inputTokens: number; }[]; model: "text-embedding-3-small" | "text-embedding-3-large"; totalTokens: number; }>;
      };
      /**
       * Run content moderation checks.
       */
      moderateContent: {
        /**
         * Run content moderation checks.
         */
        mutate(input: { inputs: Array<{ id: string; text: string }>; categories?: Array<"hate" | "harassment" | "self_harm" | "sexual" | "violence" | "dangerous"> }, opts?: RequestOptions): Promise<{ results: { id: string; flagged: boolean; categories: Partial<Record<string, boolean>>; scores: Partial<Record<string, number>>; }[]; }>;
      };
      /**
       * Structured text summarization.
       */
      summarize: {
        /**
         * Structured text summarization.
         */
        mutate(input: { text: string; format?: "bullets" | "paragraph" | "tldr"; maxLength?: number }, opts?: RequestOptions): Promise<{ summary: string; wordCount: number; }>;
      };
      /**
       * AI token usage for billing period.
       */
      usageStats: {
        /**
         * AI token usage for billing period.
         */
        query(input: { orgId: string; period?: "current" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ promptTokens: number; completionTokens: number; totalCost: number; byModel: Record<string, { tokens: number; cost: number; }>; }>;
      };
    };
    auditRoutes8: {
      /**
       * Paginated audit log with filters.
       */
      log: {
        /**
         * Paginated audit log with filters.
         */
        query(input: { orgId: string; actorId?: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled">; resourceType?: string; from?: string; to?: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ entries: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }[]; total: number; }>;
      };
      /**
       * Single audit entry by ID.
       */
      getEntry: {
        /**
         * Single audit entry by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; actorId: string; actorEmail: string; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: null; resourceId: null; metadata: {}; ip: string; userAgent: string; ts: string; }>;
      };
      /**
       * Update log retention policy.
       */
      retention: {
        /**
         * Update log retention policy.
         */
        mutate(input: { orgId: string; retentionDays: number }, opts?: RequestOptions): Promise<{ updated: boolean; retentionDays: number; }>;
      };
      /**
       * Stream audit log as NDJSON.
       */
      export: {
        /**
         * Stream audit log as NDJSON.
         */
        stream(input: { orgId: string; from: string; to: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; data: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }; }, void, unknown>>;
      };
    };
    apiKeysRoutes8: {
      /**
       * List API keys for org.
       */
      list: {
        /**
         * List API keys for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ keys: { id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: string | null; lastUsedAt: string | null; createdBy: string; createdAt: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number; } | null; }[]; }>;
      };
      /**
       * Create API key.
       */
      create: {
        /**
         * Create API key.
         */
        mutate(input: { orgId: string; name: string; scopes: Array<"read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write">; expiresAt: string | null; createdBy: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number } | null }, opts?: RequestOptions): Promise<{ key: string; id: string; prefix: string; }>;
      };
      /**
       * Revoke API key.
       */
      revoke: {
        /**
         * Revoke API key.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Rotate key secret.
       */
      rotate: {
        /**
         * Rotate key secret.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ newKey: string; id: string; }>;
      };
      /**
       * API key metadata.
       */
      get: {
        /**
         * API key metadata.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: null; lastUsedAt: null; createdBy: string; createdAt: string; rateLimit: null; }>;
      };
      /**
       * Key request usage stats.
       */
      usage: {
        /**
         * Key request usage stats.
         */
        query(input: { id: string; period?: "1h" | "24h" | "7d" | "30d" }, opts?: RequestOptions): Promise<{ requests: number; errors: number; p50Ms: number; p99Ms: number; series: { ts: string; count: number; }[]; }>;
      };
    };
    integrationsRoutes8: {
      /**
       * All integrations with status.
       */
      list: {
        /**
         * All integrations with status.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ integrations: { id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: boolean; enabled: boolean; status: "error" | "active" | "disconnected" | "syncing"; lastSyncAt: string | null; errorMessage: string | null; config: Record<string, unknown>; }[]; }>;
      };
      /**
       * Single integration status.
       */
      get: {
        /**
         * Single integration status.
         */
        query(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: true; enabled: true; status: "active"; lastSyncAt: string; errorMessage: null; config: {}; }>;
      };
      /**
       * Start OAuth install flow.
       */
      install: {
        /**
         * Start OAuth install flow.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; redirectUri: string; scopes?: Array<string> }, opts?: RequestOptions): Promise<{ authUrl: string; }>;
      };
      /**
       * Exchange OAuth code for tokens.
       */
      oauthCallback: {
        /**
         * Exchange OAuth code for tokens.
         */
        mutate(input: { code: string; state: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ installed: boolean; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; }>;
      };
      /**
       * Remove integration.
       */
      uninstall: {
        /**
         * Remove integration.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ uninstalled: boolean; }>;
      };
      /**
       * Update integration config.
       */
      updateConfig: {
        /**
         * Update integration config.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; config: Record<string, unknown> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Manual sync trigger.
       */
      triggerSync: {
        /**
         * Manual sync trigger.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream sync progress events.
       */
      syncStream: {
        /**
         * Stream sync progress events.
         */
        stream(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; itemsSynced: number; total: number; }, void, unknown>>;
      };
    };
    workflowsRoutes8: {
      /**
       * Create workflow with recursive condition tree.
       */
      create: {
        /**
         * Create workflow with recursive condition tree.
         */
        mutate(input: { name: string; description?: string; trigger: { event: string; filters?: unknown; debounceMs?: number }; steps: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; orgId: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * List workflows with run stats.
       */
      list: {
        /**
         * List workflows with run stats.
         */
        query(input: { orgId: string; enabled?: boolean }, opts?: RequestOptions): Promise<{ workflows: { id: string; name: string; enabled: boolean; lastRunAt: string | null; runCount: number; failureRate: number; }[]; }>;
      };
      /**
       * Workflow detail.
       */
      get: {
        /**
         * Workflow detail.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * Update workflow definition.
       */
      update: {
        /**
         * Update workflow definition.
         */
        mutate(input: { name?: string; description?: string; trigger?: { event: string; filters?: unknown; debounceMs?: number }; steps?: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete workflow.
       */
      delete: {
        /**
         * Delete workflow.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated run history.
       */
      runHistory: {
        /**
         * Paginated run history.
         */
        query(input: { workflowId: string; status?: "running" | "success" | "failed" | "cancelled"; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ runs: { id: string; status: string; startedAt: string; finishedAt: string | null; steps: { stepId: string; status: string; durationMs: number; error?: string | undefined; }[]; }[]; nextCursor: string | null; }>;
      };
      /**
       * Dry-run with streamed step outcomes.
       */
      testRun: {
        /**
         * Dry-run with streamed step outcomes.
         */
        stream(input: { workflowId: string; samplePayload: Record<string, unknown> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "step_result"; stepId: string; status: "success"; durationMs: number; output: Record<string, unknown>; }, void, unknown>>;
      };
    };
    documentsRoutes8: {
      /**
       * Documents in a project.
       */
      list: {
        /**
         * Documents in a project.
         */
        query(input: { projectId: string; page?: number; limit?: number; search?: string }, opts?: RequestOptions): Promise<{ documents: Pick<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "private" | "public" | "project"; authorId: string; collaborators: string[]; version: number; publishedAt: string | null; createdAt: string; updatedAt: string; }, "createdAt" | "id" | "updatedAt" | "title" | "authorId">[]; total: number; }>;
      };
      /**
       * Full document with blocks.
       */
      get: {
        /**
         * Full document with blocks.
         */
        query(input: { id: string; version?: number }, opts?: RequestOptions): Promise<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "project"; authorId: string; collaborators: never[]; version: number; publishedAt: null; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create document.
       */
      create: {
        /**
         * Create document.
         */
        mutate(input: { projectId: string; title: string; blocks: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility: "private" | "project" | "public"; authorId: string; collaborators: Array<string> }, opts?: RequestOptions): Promise<{ id: string; title: string; }>;
      };
      /**
       * Update blocks and metadata.
       */
      update: {
        /**
         * Update blocks and metadata.
         */
        mutate(input: { id: string; title?: string; blocks?: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility?: "private" | "project" | "public" }, opts?: RequestOptions): Promise<{ id: string; version: number; }>;
      };
      /**
       * Delete document.
       */
      delete: {
        /**
         * Delete document.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Publish document publicly.
       */
      publish: {
        /**
         * Publish document publicly.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ publishedAt: string; url: string; }>;
      };
      /**
       * Document version history.
       */
      history: {
        /**
         * Document version history.
         */
        query(input: { id: string; limit?: number }, opts?: RequestOptions): Promise<{ versions: { version: number; editor: string; changedAt: string; summary: string; }[]; }>;
      };
      /**
       * Collaborative edit event stream.
       */
      editStream: {
        /**
         * Collaborative edit event stream.
         */
        stream(input: { docId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "block_updated"; blockId: string; patch: Record<string, unknown>; editorId: string; }, void, unknown>>;
      };
    };
    permissionsRoutes8: {
      /**
       * All ABAC policies for org.
       */
      listPolicies: {
        /**
         * All ABAC policies for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ policies: { id: string; orgId: string; name: string; effect: "allow" | "deny"; resources: ("file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics")[]; actions: ("read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve")[]; conditions: { field: string; operator: "in" | "eq" | "neq" | "owned_by"; value: string | string[]; }[]; subjects: { type: "user" | "role" | "team"; id: string; }[]; priority: number; createdAt: string; }[]; }>;
      };
      /**
       * Create ABAC policy.
       */
      createPolicy: {
        /**
         * Create ABAC policy.
         */
        mutate(input: { orgId: string; name: string; effect: "allow" | "deny"; resources: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update policy definition.
       */
      updatePolicy: {
        /**
         * Update policy definition.
         */
        mutate(input: { orgId?: string; name?: string; effect?: "allow" | "deny"; resources?: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions?: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects?: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete policy.
       */
      deletePolicy: {
        /**
         * Delete policy.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Evaluate access for a subject.
       */
      checkAccess: {
        /**
         * Evaluate access for a subject.
         */
        query(input: { orgId: string; subjectId: string; resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }, opts?: RequestOptions): Promise<{ allowed: boolean; matchedPolicies: string[]; }>;
      };
      /**
       * Batch permission check.
       */
      bulkCheck: {
        /**
         * Batch permission check.
         */
        mutate(input: { orgId: string; subjectId: string; checks: Array<{ resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }> }, opts?: RequestOptions): Promise<{ results: { resource: "file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics"; action: "read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve"; allowed: boolean; }[]; }>;
      };
    };
    teamsRoutes8: {
      /**
       * Teams in an org.
       */
      list: {
        /**
         * Teams in an org.
         */
        query(input: { orgId: string; visibility?: "public" | "private" | "secret" }, opts?: RequestOptions): Promise<{ teams: { id: string; orgId: string; name: string; visibility: "private" | "public" | "secret"; memberCount: number; projectIds: string[]; createdAt: string; description?: string | undefined; color?: string | undefined; icon?: string | undefined; }[]; }>;
      };
      /**
       * Team by ID.
       */
      get: {
        /**
         * Team by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; description: undefined; color: string; icon: string; visibility: "public"; memberCount: number; projectIds: never[]; createdAt: string; }>;
      };
      /**
       * Create team.
       */
      create: {
        /**
         * Create team.
         */
        mutate(input: { orgId: string; name: string; description?: string; color?: string; icon?: string; visibility: "public" | "private" | "secret"; projectIds: Array<string> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update team metadata.
       */
      update: {
        /**
         * Update team metadata.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; color?: string; icon?: string; visibility?: "public" | "private" | "secret"; projectIds?: Array<string>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete team.
       */
      delete: {
        /**
         * Delete team.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Team member list.
       */
      members: {
        /**
         * Team member list.
         */
        query(input: { teamId: string; page?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "member" | "lead"; joinedAt: string; }[]; }>;
      };
      /**
       * Add user to team.
       */
      addMember: {
        /**
         * Add user to team.
         */
        mutate(input: { teamId: string; userId: string; role: "lead" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from team.
       */
      removeMember: {
        /**
         * Remove user from team.
         */
        mutate(input: { teamId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
    };
    invitesRoutes8: {
      /**
       * Pending invites for org.
       */
      list: {
        /**
         * Pending invites for org.
         */
        query(input: { orgId: string; status?: "pending" | "accepted" | "declined" | "expired" | "revoked" }, opts?: RequestOptions): Promise<{ invites: { id: string; orgId: string; email: string; role: "admin" | "owner" | "member" | "viewer"; status: "revoked" | "pending" | "accepted" | "declined" | "expired"; invitedBy: string; expiresAt: string; acceptedAt: string | null; teams: string[]; createdAt: string; personalMessage?: string | undefined; }[]; }>;
      };
      /**
       * Send invite.
       */
      create: {
        /**
         * Send invite.
         */
        mutate(input: { orgId: string; email: string; role: "owner" | "admin" | "member" | "viewer"; teams?: Array<string>; personalMessage?: string }, opts?: RequestOptions): Promise<{ id: string; email: string; }>;
      };
      /**
       * Bulk invite by email list.
       */
      bulkCreate: {
        /**
         * Bulk invite by email list.
         */
        mutate(input: { orgId: string; emails: Array<string>; role?: "admin" | "member" | "viewer"; teams?: Array<string> }, opts?: RequestOptions): Promise<{ invited: number; failed: string[]; }>;
      };
      /**
       * Accept invite via token.
       */
      accept: {
        /**
         * Accept invite via token.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ accepted: boolean; orgId: string; }>;
      };
      /**
       * Decline invite.
       */
      decline: {
        /**
         * Decline invite.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ declined: boolean; }>;
      };
      /**
       * Revoke pending invite.
       */
      revoke: {
        /**
         * Revoke pending invite.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Resend invite email.
       */
      resend: {
        /**
         * Resend invite email.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
    };
    commentsRoutes8: {
      /**
       * Comments for a resource.
       */
      list: {
        /**
         * Comments for a resource.
         */
        query(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; nextCursor: string | null; }>;
      };
      /**
       * Post a comment.
       */
      create: {
        /**
         * Post a comment.
         */
        mutate(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; authorId: string; body: string; mentions: Array<string>; attachments: Array<{ id: string; name: string; url: string }>; parentId: string | null }, opts?: RequestOptions): Promise<{ id: string; }>;
      };
      /**
       * Edit comment body.
       */
      update: {
        /**
         * Edit comment body.
         */
        mutate(input: { id: string; body: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete comment.
       */
      delete: {
        /**
         * Delete comment.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Toggle emoji reaction.
       */
      react: {
        /**
         * Toggle emoji reaction.
         */
        mutate(input: { commentId: string; emoji: string }, opts?: RequestOptions): Promise<{ added: boolean; emoji: string; }>;
      };
      /**
       * Threaded replies.
       */
      thread: {
        /**
         * Threaded replies.
         */
        query(input: { parentId: string; limit?: number }, opts?: RequestOptions): Promise<{ replies: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Stream new comments live.
       */
      liveStream: {
        /**
         * Stream new comments live.
         */
        stream(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "new_comment"; comment: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }; }, void, unknown>>;
      };
    };
    tagsRoutes8: {
      /**
       * All tags for org.
       */
      list: {
        /**
         * All tags for org.
         */
        query(input: { orgId: string; search?: string; sortBy?: "name" | "usageCount" }, opts?: RequestOptions): Promise<{ tags: { id: string; orgId: string; name: string; color: string; usageCount: number; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Create tag.
       */
      create: {
        /**
         * Create tag.
         */
        mutate(input: { orgId: string; name: string; color: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update tag name/color.
       */
      update: {
        /**
         * Update tag name/color.
         */
        mutate(input: { id: string; name?: string; color?: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete tag.
       */
      delete: {
        /**
         * Delete tag.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Merge multiple tags into one.
       */
      merge: {
        /**
         * Merge multiple tags into one.
         */
        mutate(input: { sourceIds: Array<string>; targetId: string }, opts?: RequestOptions): Promise<{ merged: number; targetId: string; }>;
      };
      /**
       * Attach tags to a resource.
       */
      attach: {
        /**
         * Attach tags to a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ attached: number; }>;
      };
      /**
       * Detach tags from a resource.
       */
      detach: {
        /**
         * Detach tags from a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ detached: number; }>;
      };
    };
    reportsRoutes8: {
      /**
       * Saved reports for org.
       */
      list: {
        /**
         * Saved reports for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; orgId: string; name: string; type: "custom" | "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest"; config: Record<string, unknown>; schedule: { cron: string; timezone: string; recipients: string[]; format: "csv" | "pdf" | "html"; } | null; lastRunAt: string | null; lastRunStatus: "success" | "failed" | "running" | null; createdBy: string; createdAt: string; }[]; }>;
      };
      /**
       * Create report definition.
       */
      create: {
        /**
         * Create report definition.
         */
        mutate(input: { orgId: string; name: string; type: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config: Record<string, unknown>; schedule: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update report.
       */
      update: {
        /**
         * Update report.
         */
        mutate(input: { orgId?: string; name?: string; type?: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config?: Record<string, unknown>; schedule?: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy?: string; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete report.
       */
      delete: {
        /**
         * Delete report.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Trigger report run.
       */
      run: {
        /**
         * Trigger report run.
         */
        mutate(input: { id: string; format?: "pdf" | "csv" | "html" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Get report download URL.
       */
      download: {
        /**
         * Get report download URL.
         */
        query(input: { id: string; runId: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream report generation.
       */
      stream: {
        /**
         * Stream report generation.
         */
        stream(input: { id: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; percent: number; stage: "collecting_data"; }, void, unknown>>;
      };
    };
    rateLimitsRoutes8: {
      /**
       * Rate limit rules for org.
       */
      listRules: {
        /**
         * Rate limit rules for org.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ rules: { id: string; orgId: string; name: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; targetId: string | null; endpoint: string | null; limits: { window: "minute" | "hour" | "day" | "second"; max: number; burstAllowance: number; }[]; action: "log" | "throttle" | "block"; enabled: boolean; createdAt: string; }[]; }>;
      };
      /**
       * Create rate limit rule.
       */
      createRule: {
        /**
         * Create rate limit rule.
         */
        mutate(input: { orgId: string; name: string; target: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId: string | null; endpoint: string | null; limits: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update rule.
       */
      updateRule: {
        /**
         * Update rule.
         */
        mutate(input: { orgId?: string; name?: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId?: string | null; endpoint?: string | null; limits?: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete rule.
       */
      deleteRule: {
        /**
         * Delete rule.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Current usage vs. limits.
       */
      currentStatus: {
        /**
         * Current usage vs. limits.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ statuses: { ruleId: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; window: string; current: number; max: number; resetAt: string; }[]; }>;
      };
      /**
       * Manually reset rate counter.
       */
      resetCounter: {
        /**
         * Manually reset rate counter.
         */
        mutate(input: { ruleId: string; targetId: string }, opts?: RequestOptions): Promise<{ reset: boolean; }>;
      };
    };
    exportsRoutes8: {
      /**
       * Queue an export job.
       */
      create: {
        /**
         * Queue an export job.
         */
        mutate(input: { orgId: string; resource: { resource: "tasks"; projectId?: string; filters?: Record<string, unknown> } | { resource: "users"; orgId: string } | { resource: "analytics"; orgId: string; metrics: Array<string>; dateRange: { from: string; to: string } } | { resource: "audit_log"; orgId: string; from: string; to: string } | { resource: "billing"; orgId: string; year: number } | { resource: "files"; resourceType: string; resourceId: string }; format: "json" | "csv" | "xlsx" | "pdf" | "ndjson" | "parquet"; notifyEmail?: string }, opts?: RequestOptions): Promise<{ id: string; status: "queued"; }>;
      };
      /**
       * Export job history.
       */
      list: {
        /**
         * Export job history.
         */
        query(input: { orgId: string; limit?: number }, opts?: RequestOptions): Promise<{ jobs: { id: string; orgId: string; resource: { resource: "tasks"; projectId?: string | undefined; filters?: Record<string, unknown> | undefined; } | { resource: "users"; orgId: string; } | { resource: "analytics"; orgId: string; metrics: string[]; dateRange: { from: string; to: string; }; } | { resource: "audit_log"; orgId: string; from: string; to: string; } | { resource: "billing"; orgId: string; year: number; } | { resource: "files"; resourceType: string; resourceId: string; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done" | "failed" | "queued" | "running"; progress: number; rowCount: number | null; downloadUrl: string | null; expiresAt: string | null; error: string | null; createdAt: string; }[]; }>;
      };
      /**
       * Export job status.
       */
      get: {
        /**
         * Export job status.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; resource: { resource: "tasks"; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done"; progress: number; rowCount: number; downloadUrl: string; expiresAt: string; error: null; createdAt: string; }>;
      };
      /**
       * Cancel queued export.
       */
      cancel: {
        /**
         * Cancel queued export.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ cancelled: boolean; }>;
      };
      /**
       * Fresh signed download URL.
       */
      download: {
        /**
         * Fresh signed download URL.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream export progress.
       */
      progressStream: {
        /**
         * Stream export progress.
         */
        stream(input: { jobId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; jobId: string; percent: number; rowsProcessed: number; }, void, unknown>>;
      };
    };
    authRoutes9: {
      /**
       * Multi-method login — password, OAuth, SAML, magic link, passkey.
       */
      login: {
        /**
         * Multi-method login — password, OAuth, SAML, magic link, passkey.
         */
        mutate(input: { method: "password"; email: string; password: string; totpCode?: string } | { method: "oauth"; provider: "google" | "github" | "microsoft" | "okta"; code: string; redirectUri: string; state: string } | { method: "saml"; orgSlug: string; samlResponse: string; relayState?: string } | { method: "magic_link"; token: string } | { method: "passkey"; credentialId: string; clientDataJSON: string; authenticatorData: string; signature: string } & { device?: { userAgent: string; ip: string; fingerprint?: string; platform?: "web" | "ios" | "android" | "desktop" } }, opts?: RequestOptions): Promise<{ tokens: { accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }; user: { id: UserId; role: "admin"; }; }>;
      };
      /**
       * Rotate refresh token.
       */
      refresh: {
        /**
         * Rotate refresh token.
         */
        mutate(input: { refreshToken: string; sessionId: string }, opts?: RequestOptions): Promise<{ accessToken: string; refreshToken: string; expiresIn: number; tokenType: "Bearer"; scope: string[]; }>;
      };
      /**
       * Invalidate session.
       */
      logout: {
        /**
         * Invalidate session.
         */
        mutate(input: { sessionId: string }, opts?: RequestOptions): Promise<{ revoked: boolean; sessionId: SessionId; }>;
      };
      /**
       * Validate MFA code after credential check.
       */
      mfaChallenge: {
        /**
         * Validate MFA code after credential check.
         */
        mutate(input: { challengeToken: string; code: string; method: "totp" | "sms" | "email" | "webauthn" }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Send password reset email.
       */
      forgotPassword: {
        /**
         * Send password reset email.
         */
        mutate(input: { email: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
      /**
       * Apply new password via reset token.
       */
      resetPassword: {
        /**
         * Apply new password via reset token.
         */
        mutate(input: { token: string; newPassword: string }, opts?: RequestOptions): Promise<{ success: boolean; }>;
      };
      /**
       * List active sessions with device info.
       */
      sessions: {
        /**
         * List active sessions with device info.
         */
        query(input: { cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ sessions: { id: SessionId; device: { userAgent: string; ip: string; fingerprint?: string | undefined; platform?: "web" | "ios" | "android" | "desktop" | undefined; }; createdAt: string; current: boolean; }[]; nextCursor: string | null; }>;
      };
      /**
       * Current authenticated user.
       */
      me: {
        /**
         * Current authenticated user.
         */
        query(input: any, opts?: RequestOptions): Promise<{ id: UserId; role: string; }>;
      };
      /**
       * Confirm email address via verification code.
       */
      verifyEmail: {
        /**
         * Confirm email address via verification code.
         */
        mutate(input: { code: string }, opts?: RequestOptions): Promise<{ verified: boolean; }>;
      };
      /**
       * Stream login activity events for the current user.
       */
      deviceStream: {
        /**
         * Stream login activity events for the current user.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "session_created"; sessionId: SessionId; ts: string; }, void, unknown>>;
      };
    };
    usersRoutes9: {
      /**
       * Paginated user list with rich filters.
       */
      list: {
        /**
         * Paginated user list with rich filters.
         */
        query(input: { page?: number; limit?: number; roles?: Array<"owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support">; search?: string; sortBy?: "name" | "email" | "createdAt"; sortDir?: "asc" | "desc" }, opts?: RequestOptions): Promise<{ users: { id: string; email: string; emailVerified: boolean; name: string; avatarUrl: string | null; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: string[]; metadata: Record<string, string | number | boolean>; createdAt: string; updatedAt: string; displayName?: string | undefined; bio?: string | undefined; timezone?: string | undefined; locale?: string | undefined; address?: { line1: string; city: string; postalCode: string; country: string; line2?: string | undefined; state?: string | undefined; } | undefined; }[]; total: number; page: number; }>;
      };
      /**
       * Fetch single user with full profile.
       */
      get: {
        /**
         * Fetch single user with full profile.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; email: string; emailVerified: true; name: string; displayName: undefined; avatarUrl: null; bio: undefined; timezone: string; locale: string; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; permissions: never[]; metadata: {}; address: undefined; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Partial profile update with nested address.
       */
      update: {
        /**
         * Partial profile update with nested address.
         */
        mutate(input: { name?: string; displayName?: string; bio?: string; timezone?: string; locale?: string; avatarUrl?: string | null; address?: { line1: string; line2?: string; city: string; state?: string; postalCode: string; country: string }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Soft-delete user.
       */
      deactivate: {
        /**
         * Soft-delete user.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ deactivated: boolean; }>;
      };
      /**
       * Change role for many users atomically.
       */
      bulkRoleChange: {
        /**
         * Change role for many users atomically.
         */
        mutate(input: { userIds: Array<string>; role: "owner" | "admin" | "member" | "viewer" | "billing" | "developer" | "support"; reason?: string }, opts?: RequestOptions): Promise<{ updated: number; role: "admin" | "owner" | "member" | "viewer" | "billing" | "developer" | "support"; }>;
      };
      /**
       * All permissions grouped by resource for a user.
       */
      permissionMatrix: {
        /**
         * All permissions grouped by resource for a user.
         */
        query(input: { userId: string }, opts?: RequestOptions): Promise<{ userId: string; matrix: Record<string, Record<string, boolean>>; }>;
      };
      /**
       * Upload and crop user avatar.
       */
      uploadAvatar: {
        /**
         * Upload and crop user avatar.
         */
        upload(input: { file: File; cropSize?: number } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileName: string; size: number; croppedTo: number; }>;
      };
      /**
       * Change own password — requires MFA.
       */
      changePassword: {
        /**
         * Change own password — requires MFA.
         */
        mutate(input: { currentPassword: string; newPassword: string }, opts?: RequestOptions): Promise<{ changed: boolean; }>;
      };
      /**
       * GDPR data export streamed by resource type.
       */
      exportData: {
        /**
         * GDPR data export streamed by resource type.
         */
        stream(input: { userId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "chunk"; resource: "profile" | "tasks" | "comments" | "files"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    organizationsRoutes9: {
      /**
       * Orgs accessible to caller.
       */
      list: {
        /**
         * Orgs accessible to caller.
         */
        query(input: { page?: number; limit?: number }, opts?: RequestOptions): Promise<{ orgs: { id: string; name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; memberCount: number; logoUrl: string | null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }[]; total: number; }>;
      };
      /**
       * Fetch org by ID.
       */
      get: {
        /**
         * Fetch org by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; slug: string; plan: "enterprise"; memberCount: number; logoUrl: null; settings: { allowedDomains: string[]; ssoRequired: boolean; mfaRequired: boolean; ipAllowlist: string[]; defaultRole: "member" | "viewer"; sessionTimeoutMinutes: number; dataRetentionDays: number | null; features: Record<string, boolean>; }; createdAt: string; }>;
      };
      /**
       * Create new organization.
       */
      create: {
        /**
         * Create new organization.
         */
        mutate(input: { name: string; slug: string; plan: "free" | "starter" | "pro" | "business" | "enterprise"; logoUrl: string | null; settings: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> }; ownerId: string }, opts?: RequestOptions): Promise<{ id: string; slug: string; }>;
      };
      /**
       * Update org metadata and settings.
       */
      update: {
        /**
         * Update org metadata and settings.
         */
        mutate(input: { id: string; name?: string; logoUrl?: string | null; settings?: { allowedDomains?: Array<string>; ssoRequired?: boolean; mfaRequired?: boolean; ipAllowlist?: Array<string>; defaultRole?: "member" | "viewer"; sessionTimeoutMinutes?: number; dataRetentionDays?: number | null; features?: Record<string, boolean> } }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Permanently delete org.
       */
      delete: {
        /**
         * Permanently delete org.
         */
        mutate(input: { id: string; confirmSlug: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated org member list.
       */
      members: {
        /**
         * Paginated org member list.
         */
        query(input: { orgId: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: string; joinedAt: string; }[]; total: number; }>;
      };
      /**
       * Add user to org with role.
       */
      addMember: {
        /**
         * Add user to org with role.
         */
        mutate(input: { orgId: string; userId: string; role: "owner" | "admin" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from org.
       */
      removeMember: {
        /**
         * Remove user from org.
         */
        mutate(input: { orgId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
      /**
       * Transfer org ownership — requires MFA.
       */
      transferOwnership: {
        /**
         * Transfer org ownership — requires MFA.
         */
        mutate(input: { orgId: string; newOwnerId: string; reason?: string }, opts?: RequestOptions): Promise<{ transferred: boolean; }>;
      };
      /**
       * Stream org audit log entries live.
       */
      auditStream: {
        /**
         * Stream org audit log entries live.
         */
        stream(input: { orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; action: string; actorId: string; ts: string; }, void, unknown>>;
      };
    };
    billingRoutes9: {
      /**
       * Full subscription with pricing model.
       */
      getSubscription: {
        /**
         * Full subscription with pricing model.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; tier: "pro"; pricing: { model: "per_seat"; pricePerSeatMonth: number; minSeats: number; maxSeats: null; }; status: "active"; seats: number; currentPeriodStart: string; currentPeriodEnd: string; cancelAtPeriodEnd: false; metadata: {}; addons: never[]; }>;
      };
      /**
       * Calculate proration before confirming.
       */
      previewUpgrade: {
        /**
         * Calculate proration before confirming.
         */
        query(input: { orgId: string; targetTier: "starter" | "pro" | "business" | "enterprise"; targetSeats: number; billingInterval: "monthly" | "annual" }, opts?: RequestOptions): Promise<{ currentAmount: number; newAmount: number; prorationAmount: number; effectiveDate: string; lineItems: { description: string; amount: number; currency: string; }[]; }>;
      };
      /**
       * Create Stripe checkout session.
       */
      createCheckout: {
        /**
         * Create Stripe checkout session.
         */
        mutate(input: { orgId: string; tier: "starter" | "pro" | "business" | "enterprise"; seats: number; billingInterval: "monthly" | "annual"; addons?: Array<{ id: string; quantity: number }>; successUrl: string; cancelUrl: string; couponCode?: string }, opts?: RequestOptions): Promise<{ checkoutUrl: string; sessionId: string; }>;
      };
      /**
       * Schedule cancellation at period end.
       */
      cancelSubscription: {
        /**
         * Schedule cancellation at period end.
         */
        mutate(input: { orgId: string; reason?: string }, opts?: RequestOptions): Promise<{ cancelAtPeriodEnd: boolean; }>;
      };
      /**
       * Paginated invoice list.
       */
      invoices: {
        /**
         * Paginated invoice list.
         */
        query(input: { orgId: string; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ invoices: { id: string; amount: number; currency: string; status: string; date: string; }[]; nextCursor: string | null; }>;
      };
      /**
       * Metered usage breakdown.
       */
      usageReport: {
        /**
         * Metered usage breakdown.
         */
        query(input: { orgId: string; breakdown?: "daily" | "weekly" | "monthly" }, opts?: RequestOptions): Promise<{ period: { start: string; end: string; }; included: number; consumed: number; overage: number; overageCost: number; series: { date: string; units: number; cost: number; }[]; }>;
      };
      /**
       * Apply promotional discount code.
       */
      applyPromoCode: {
        /**
         * Apply promotional discount code.
         */
        mutate(input: { orgId: string; code: string }, opts?: RequestOptions): Promise<{ applied: boolean; discountPercent: number; }>;
      };
      /**
       * Stream invoice line items.
       */
      invoiceStream: {
        /**
         * Stream invoice line items.
         */
        stream(input: { invoiceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "line_item"; description: string; amount: number; currency: string; }, void, unknown>>;
      };
    };
    projectsRoutes9: {
      /**
       * Paginated project list with status filters.
       */
      list: {
        /**
         * Paginated project list with status filters.
         */
        query(input: { orgId: string; status?: Array<"planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled">; search?: string; page?: number }, opts?: RequestOptions): Promise<{ projects: { id: string; orgId: string; name: string; status: "active" | "cancelled" | "planning" | "on_hold" | "completed" | "archived"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: string[]; customFields: Record<string, string | number | boolean | null>; createdAt: string; updatedAt: string; description?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Project by ID.
       */
      get: {
        /**
         * Project by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; status: "active"; visibility: "private"; startDate: null; endDate: null; ownerId: string; tags: never[]; customFields: {}; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create project.
       */
      create: {
        /**
         * Create project.
         */
        mutate(input: { orgId: string; name: string; description?: string; status: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility: "private" | "internal" | "public"; startDate: string | null; endDate: string | null; ownerId: string; tags: Array<string>; customFields: Record<string, string | number | boolean | null> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update project fields.
       */
      update: {
        /**
         * Update project fields.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; status?: "planning" | "active" | "on_hold" | "completed" | "archived" | "cancelled"; visibility?: "private" | "internal" | "public"; startDate?: string | null; endDate?: string | null; ownerId?: string; tags?: Array<string>; customFields?: Record<string, string | number | boolean | null>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Archive project.
       */
      archive: {
        /**
         * Archive project.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ archived: boolean; }>;
      };
      /**
       * Project member list.
       */
      members: {
        /**
         * Project member list.
         */
        query(input: { projectId: string }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "viewer" | "lead" | "contributor"; joinedAt: string; }[]; }>;
      };
      /**
       * Add project member.
       */
      addMember: {
        /**
         * Add project member.
         */
        mutate(input: { projectId: string; userId: string; role: "lead" | "contributor" | "viewer" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Aggregate project stats.
       */
      stats: {
        /**
         * Aggregate project stats.
         */
        query(input: { projectId: string; period?: "7d" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ tasks: number; completed: number; openIssues: number; velocity: number; burndownPoints: { date: string; remaining: number; }[]; }>;
      };
      /**
       * Clone project structure.
       */
      duplicate: {
        /**
         * Clone project structure.
         */
        mutate(input: { id: string; newName: string; includeTasks?: boolean; includeMembers?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream full project export.
       */
      exportStream: {
        /**
         * Stream full project export.
         */
        stream(input: { id: string; format: "json" | "csv" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; resource: "task"; data: Record<string, unknown>; }, void, unknown>>;
      };
    };
    tasksRoutes9: {
      /**
       * Create task with full field set.
       */
      create: {
        /**
         * Create task with full field set.
         */
        mutate(input: { projectId: string; title: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }> }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; }>;
      };
      /**
       * Task tree with recursive subtasks.
       */
      get: {
        /**
         * Task tree with recursive subtasks.
         */
        query(input: { id: string; depth?: number }, opts?: RequestOptions): Promise<{ id: string; title: string; status: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; priority: "critical" | "high" | "medium" | "low" | "none"; subtasks: any[]; customFields: Record<string, { type: "text"; value: string; } | { type: "number"; value: number; } | { type: "date"; value: string; } | { type: "boolean"; value: boolean; } | { type: "select"; value: string; optionId: string; } | { type: "multi"; value: string[]; optionIds: string[]; } | { type: "user"; value: string; } | { type: "url"; value: string; }>; }>;
      };
      /**
       * Patch any task fields.
       */
      update: {
        /**
         * Patch any task fields.
         */
        mutate(input: { projectId?: string; title?: string; description?: string; status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; parentId?: string; dueDate?: string | null; startDate?: string | null; labels?: Array<string>; estimateHours?: number; customFields?: Record<string, { type: "text"; value: string } | { type: "number"; value: number } | { type: "date"; value: string } | { type: "boolean"; value: boolean } | { type: "select"; value: string; optionId: string } | { type: "multi"; value: Array<string>; optionIds: Array<string> } | { type: "user"; value: string } | { type: "url"; value: string }>; dependencies?: Array<{ taskId: string; type: "blocks" | "blocked_by" | "relates_to" | "duplicates" }>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete task permanently.
       */
      delete: {
        /**
         * Delete task permanently.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Batch status/priority update.
       */
      bulkUpdate: {
        /**
         * Batch status/priority update.
         */
        mutate(input: { ids: Array<string>; patch: { status?: "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix"; priority?: "critical" | "high" | "medium" | "low" | "none"; assigneeIds?: Array<string>; dueDate?: string | null } }, opts?: RequestOptions): Promise<{ updated: number; }>;
      };
      /**
       * Comment thread for a task.
       */
      comments: {
        /**
         * Comment thread for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; body: string; author: string; createdAt: string; reactions: Record<string, string[]>; }[]; }>;
      };
      /**
       * Post comment on task.
       */
      addComment: {
        /**
         * Post comment on task.
         */
        mutate(input: { taskId: string; body: string; mentions?: Array<string> }, opts?: RequestOptions): Promise<{ id: string; body: string; }>;
      };
      /**
       * Typed audit trail for a task.
       */
      timelineEvents: {
        /**
         * Typed audit trail for a task.
         */
        query(input: { taskId: string; limit?: number }, opts?: RequestOptions): Promise<{ events: ({ type: "created"; actor: string; ts: string; } | { type: "status_change"; actor: string; ts: string; from: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; to: "done" | "cancelled" | "backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "wont_fix"; } | { type: "assigned"; actor: string; ts: string; userId: string; })[]; }>;
      };
      /**
       * Tasks assigned to current user.
       */
      myTasks: {
        /**
         * Tasks assigned to current user.
         */
        query(input: { status?: Array<"backlog" | "todo" | "in_progress" | "in_review" | "blocked" | "done" | "cancelled" | "wont_fix">; projectId?: string; limit?: number }, opts?: RequestOptions): Promise<{ tasks: any[]; total: number; }>;
      };
      /**
       * Live task update stream.
       */
      activityStream: {
        /**
         * Live task update stream.
         */
        stream(input: { projectId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "task_updated"; taskId: string; field: string; value: string; }, void, unknown>>;
      };
    };
    filesRoutes9: {
      /**
       * Files for a resource.
       */
      list: {
        /**
         * Files for a resource.
         */
        query(input: { resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; mimeTypes?: Array<string>; page?: number }, opts?: RequestOptions): Promise<{ files: { id: string; name: string; mimeType: string; sizeBytes: number; status: "deleted" | "pending" | "processing" | "ready" | "failed"; url: string | null; thumbnailUrl: string | null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: Record<string, string | number>; createdAt: string; }[]; total: number; }>;
      };
      /**
       * File metadata by ID.
       */
      get: {
        /**
         * File metadata by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; mimeType: string; sizeBytes: number; status: "ready"; url: string; thumbnailUrl: null; resourceType: "message" | "profile" | "task" | "project" | "comment"; resourceId: string; uploadedBy: string; metadata: {}; createdAt: string; }>;
      };
      /**
       * Get presigned S3 upload URL.
       */
      requestUpload: {
        /**
         * Get presigned S3 upload URL.
         */
        mutate(input: { fileName: string; mimeType: string; sizeBytes: number; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string }, opts?: RequestOptions): Promise<{ uploadUrl: string; fileId: string; expiresIn: number; }>;
      };
      /**
       * Mark pending upload complete.
       */
      confirmUpload: {
        /**
         * Mark pending upload complete.
         */
        mutate(input: { fileId: string; etag: string }, opts?: RequestOptions): Promise<{ id: string; confirmed: boolean; url: string; }>;
      };
      /**
       * Delete file and storage object.
       */
      delete: {
        /**
         * Delete file and storage object.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Delete multiple files.
       */
      bulkDelete: {
        /**
         * Delete multiple files.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ deleted: number; }>;
      };
      /**
       * Multipart direct file upload.
       */
      directUpload: {
        /**
         * Multipart direct file upload.
         */
        upload(input: { file: File; resourceType: "project" | "task" | "comment" | "message" | "profile"; resourceId: string; description?: string } | FormData | Record<string, any>, opts?: RequestOptions): Promise<{ fileId: string; name: string; size: number; }>;
      };
      /**
       * Stream file processing events.
       */
      processStream: {
        /**
         * Stream file processing events.
         */
        stream(input: { fileId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; fileId: string; stage: "ready" | "queued" | "virus_scan" | "thumbnail"; percent: number; }, void, unknown>>;
      };
    };
    notificationsRoutes9: {
      /**
       * Paginated notification list.
       */
      list: {
        /**
         * Paginated notification list.
         */
        query(input: { unreadOnly?: boolean; types?: Array<"mention" | "task_assigned" | "task_completed" | "comment" | "billing" | "security" | "system" | "workflow_run" | "integration_sync">; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ notifications: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }[]; unreadCount: number; nextCursor: string | null; }>;
      };
      /**
       * Mark notifications as read.
       */
      markRead: {
        /**
         * Mark notifications as read.
         */
        mutate(input: { ids: Array<string> }, opts?: RequestOptions): Promise<{ marked: number; }>;
      };
      /**
       * Mark all notifications read.
       */
      markAllRead: {
        /**
         * Mark all notifications read.
         */
        mutate(input: any, opts?: RequestOptions): Promise<{ marked: boolean; }>;
      };
      /**
       * Get notification preferences.
       */
      preferences: {
        /**
         * Get notification preferences.
         */
        query(input: any, opts?: RequestOptions): Promise<{ channels: Record<string, boolean>; types: Partial<Record<"billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync", boolean>>; }>;
      };
      /**
       * Update preferences.
       */
      updatePreferences: {
        /**
         * Update preferences.
         */
        mutate(input: { channels?: Record<string, boolean>; types?: Record<string, boolean> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Register Web Push subscription.
       */
      pushSubscribe: {
        /**
         * Register Web Push subscription.
         */
        mutate(input: { endpoint: string; keys: { p256dh: string; auth: string } }, opts?: RequestOptions): Promise<{ subscribed: boolean; }>;
      };
      /**
       * Real-time notification SSE.
       */
      liveStream: {
        /**
         * Real-time notification SSE.
         */
        stream(input: any, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "notification"; notification: { id: string; type: "billing" | "comment" | "mention" | "task_assigned" | "task_completed" | "security" | "system" | "workflow_run" | "integration_sync"; read: boolean; payload: { type: "mention"; commentId: string; resourceType: string; resourceId: string; } | { type: "task_assigned"; taskId: string; projectId: string; } | { type: "task_completed"; taskId: string; completedBy: string; } | { type: "comment"; commentId: string; taskId: string; } | { type: "billing"; invoiceId: string; amount: number; currency: string; } | { type: "security"; event: string; ip?: string | undefined; } | { type: "system"; message: string; } | { type: "workflow_run"; workflowId: string; status: "success" | "failed"; } | { type: "integration_sync"; integrationId: string; itemsSynced: number; }; createdAt: string; actorId: string | null; }; }, void, unknown>>;
      };
    };
    searchRoutes9: {
      /**
       * Full-text search across resource types.
       */
      global: {
        /**
         * Full-text search across resource types.
         */
        query(input: { q: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document">; orgId: string; limit?: number; highlight?: boolean }, opts?: RequestOptions): Promise<{ results: ({ type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; })[]; total: number; took: number; }>;
      };
      /**
       * Autocomplete suggestions.
       */
      suggestions: {
        /**
         * Autocomplete suggestions.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ suggestions: { label: string; type: "user" | "file" | "task" | "project" | "comment" | "workflow" | "document"; id: string; }[]; }>;
      };
      /**
       * Faceted filter counts.
       */
      filters: {
        /**
         * Faceted filter counts.
         */
        query(input: { q: string; orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ facets: Record<"user" | "file" | "task" | "project" | "comment" | "workflow" | "document", number>; }>;
      };
      /**
       * Trigger manual reindex for org.
       */
      reindex: {
        /**
         * Trigger manual reindex for org.
         */
        mutate(input: { orgId: string; types?: Array<"task" | "project" | "user" | "file" | "comment" | "workflow" | "document"> }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream results progressively.
       */
      stream: {
        /**
         * Stream results progressively.
         */
        stream(input: { q: string; orgId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "result"; result: { type: "task"; id: string; title: string; status: string; projectId: string; score: number; } | { type: "project"; id: string; name: string; status: string; score: number; } | { type: "user"; id: string; name: string; email: string; score: number; } | { type: "file"; id: string; name: string; mimeType: string; score: number; } | { type: "comment"; id: string; body: string; resourceId: string; score: number; } | { type: "workflow"; id: string; name: string; score: number; } | { type: "document"; id: string; title: string; score: number; }; }, void, unknown>>;
      };
    };
    analyticsRoutes9: {
      /**
       * Arbitrary analytics query.
       */
      query: {
        /**
         * Arbitrary analytics query.
         */
        mutate(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }, opts?: RequestOptions): Promise<{ rows: never[]; totals: {}; comparison: undefined; meta: { executionMs: number; rowsScanned: number; cached: false; granularity: "minute" | "hour" | "day" | "week" | "month"; }; }>;
      };
      /**
       * Multi-step funnel conversion.
       */
      funnel: {
        /**
         * Multi-step funnel conversion.
         */
        mutate(input: { orgId: string; steps: Array<{ name: string; event: string; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }> }>; dateRange: { from: string; to: string } }, opts?: RequestOptions): Promise<{ steps: { name: string; count: number; conversionRate: number; avgTimeToNextStep: number | null; }[]; overallConversion: number; }>;
      };
      /**
       * Cohort retention analysis.
       */
      retention: {
        /**
         * Cohort retention analysis.
         */
        mutate(input: { orgId: string; cohortEvent: string; returnEvent: string; dateRange: { from: string; to: string }; granularity?: "minute" | "hour" | "day" | "week" | "month" }, opts?: RequestOptions): Promise<{ cohorts: { date: string; size: number; retention: number[]; }[]; }>;
      };
      /**
       * Saved report definitions.
       */
      savedReports: {
        /**
         * Saved report definitions.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; name: string; query: { orgId: string; metrics: string[]; dimensions: string[]; filters: { field: string; operator: { op: "eq"; value: string | number | boolean; } | { op: "neq"; value: string | number | boolean; } | { op: "gt"; value: number; } | { op: "gte"; value: number; } | { op: "lt"; value: number; } | { op: "lte"; value: number; } | { op: "in"; values: (string | number)[]; } | { op: "between"; min: number; max: number; }; }[]; dateRange: { from: string; to: string; granularity: "minute" | "hour" | "day" | "week" | "month"; timezone: string; }; orderBy: { field: string; direction: "asc" | "desc"; }[]; limit: number; compareWith?: { from: string; to: string; } | undefined; }; schedule: { cron: string; recipients: string[]; } | null; lastRunAt: string | null; }[]; }>;
      };
      /**
       * Persist a report definition.
       */
      saveReport: {
        /**
         * Persist a report definition.
         */
        mutate(input: { orgId: string; name: string; query: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string } }; schedule?: { cron: string; recipients: Array<string> } }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Stream query results row by row.
       */
      exportStream: {
        /**
         * Stream query results row by row.
         */
        stream(input: { orgId: string; metrics: Array<string>; dimensions?: Array<string>; filters?: Array<{ field: string; operator: { op: "eq"; value: string | number | boolean } | { op: "neq"; value: string | number | boolean } | { op: "gt"; value: number } | { op: "gte"; value: number } | { op: "lt"; value: number } | { op: "lte"; value: number } | { op: "in"; values: Array<string | number> } | { op: "between"; min: number; max: number } }>; dateRange: { from: string; to: string; granularity?: "minute" | "hour" | "day" | "week" | "month"; timezone?: string }; orderBy?: Array<{ field: string; direction: "asc" | "desc" }>; limit?: number; compareWith?: { from: string; to: string }; format?: "json" | "csv" | "parquet" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "row"; index: number; data: Record<string, string | number | null>; }, void, unknown>>;
      };
    };
    webhooksRoutes9: {
      /**
       * Webhooks for an org.
       */
      list: {
        /**
         * Webhooks for an org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ webhooks: { id: string; orgId: string; url: string; events: ("task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed")[]; enabled: boolean; secret: string; headers: Record<string, string>; retryPolicy: { maxAttempts: number; backoffMs: number; }; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Register webhook endpoint.
       */
      create: {
        /**
         * Register webhook endpoint.
         */
        mutate(input: { orgId: string; url: string; description?: string; events: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled: boolean; secret: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number } }, opts?: RequestOptions): Promise<{ id: string; url: string; }>;
      };
      /**
       * Update webhook.
       */
      update: {
        /**
         * Update webhook.
         */
        mutate(input: { orgId?: string; url?: string; description?: string; events?: Array<"task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed">; enabled?: boolean; secret?: string; headers?: Record<string, string>; retryPolicy?: { maxAttempts: number; backoffMs: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Remove webhook.
       */
      delete: {
        /**
         * Remove webhook.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Recent delivery attempts.
       */
      deliveries: {
        /**
         * Recent delivery attempts.
         */
        query(input: { webhookId: string; status?: "success" | "failed" | "pending"; limit?: number }, opts?: RequestOptions): Promise<{ deliveries: { id: string; status: number; attempt: number; ts: string; error: string | null; }[]; }>;
      };
      /**
       * Retry failed delivery.
       */
      redeliver: {
        /**
         * Retry failed delivery.
         */
        mutate(input: { deliveryId: string }, opts?: RequestOptions): Promise<{ queued: boolean; }>;
      };
      /**
       * Send test event to webhook.
       */
      testPing: {
        /**
         * Send test event to webhook.
         */
        mutate(input: { webhookId: string; eventType: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "project.created" | "billing.invoice_paid" | "billing.payment_failed" | "file.uploaded" | "workflow.run_completed" | "workflow.run_failed" }, opts?: RequestOptions): Promise<{ sent: boolean; deliveryId: string; }>;
      };
      /**
       * Stream webhook delivery events.
       */
      logStream: {
        /**
         * Stream webhook delivery events.
         */
        stream(input: { webhookId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "delivery"; status: number; ts: string; }, void, unknown>>;
      };
    };
    aiRoutes9: {
      /**
       * Multi-model streaming chat with tool-call support.
       */
      chat: {
        /**
         * Multi-model streaming chat with tool-call support.
         */
        stream(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "content_delta"; text: string; finishReason?: undefined; usage?: undefined; } | { type: "finish"; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; text?: undefined; }, void, unknown>>;
      };
      /**
       * Non-streaming completion.
       */
      complete: {
        /**
         * Non-streaming completion.
         */
        mutate(input: { messages: Array<{ role: "system" | "user" | "assistant" | "tool"; content: string | Array<{ type: "text"; text: string } | { type: "image_url"; url: string; detail?: "auto" | "low" | "high" } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown> } | { type: "tool_result"; toolCallId: string; content: string; isError?: boolean }>; name?: string }>; model: "gpt-4o" | "gpt-4o-mini" | "claude-sonnet-4-5" | "claude-haiku-4-5" | "gemini-1.5-pro"; systemPrompt?: string; tools?: Array<{ name: string; description: string; parameters: Record<string, unknown>; strict?: boolean }>; toolChoice?: "auto" | "none" | "required" | { type: "function"; name: string }; temperature?: number; maxTokens?: number; responseFormat?: { type: "text" } | { type: "json_object" } | { type: "json_schema"; schema: Record<string, unknown>; strict?: boolean }; stop?: Array<string>; stream?: false }, opts?: RequestOptions): Promise<{ id: string; role: "assistant"; content: string | ({ type: "text"; text: string; } | { type: "image_url"; url: string; detail: "high" | "low" | "auto"; } | { type: "tool_use"; toolCallId: string; name: string; input: Record<string, unknown>; } | { type: "tool_result"; toolCallId: string; content: string; isError: boolean; })[]; finishReason: "stop"; usage: { promptTokens: number; completionTokens: number; }; }>;
      };
      /**
       * Batch embedding generation.
       */
      embed: {
        /**
         * Batch embedding generation.
         */
        mutate(input: { inputs: Array<string | { text: string; id: string }>; model: "text-embedding-3-small" | "text-embedding-3-large"; dimensions?: number; outputFormat?: "float" | "base64" | "int8" }, opts?: RequestOptions): Promise<{ embeddings: { index: number; embedding: number[]; inputTokens: number; }[]; model: "text-embedding-3-small" | "text-embedding-3-large"; totalTokens: number; }>;
      };
      /**
       * Run content moderation checks.
       */
      moderateContent: {
        /**
         * Run content moderation checks.
         */
        mutate(input: { inputs: Array<{ id: string; text: string }>; categories?: Array<"hate" | "harassment" | "self_harm" | "sexual" | "violence" | "dangerous"> }, opts?: RequestOptions): Promise<{ results: { id: string; flagged: boolean; categories: Partial<Record<string, boolean>>; scores: Partial<Record<string, number>>; }[]; }>;
      };
      /**
       * Structured text summarization.
       */
      summarize: {
        /**
         * Structured text summarization.
         */
        mutate(input: { text: string; format?: "bullets" | "paragraph" | "tldr"; maxLength?: number }, opts?: RequestOptions): Promise<{ summary: string; wordCount: number; }>;
      };
      /**
       * AI token usage for billing period.
       */
      usageStats: {
        /**
         * AI token usage for billing period.
         */
        query(input: { orgId: string; period?: "current" | "30d" | "90d" }, opts?: RequestOptions): Promise<{ promptTokens: number; completionTokens: number; totalCost: number; byModel: Record<string, { tokens: number; cost: number; }>; }>;
      };
    };
    auditRoutes9: {
      /**
       * Paginated audit log with filters.
       */
      log: {
        /**
         * Paginated audit log with filters.
         */
        query(input: { orgId: string; actorId?: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled">; resourceType?: string; from?: string; to?: string; page?: number; limit?: number }, opts?: RequestOptions): Promise<{ entries: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }[]; total: number; }>;
      };
      /**
       * Single audit entry by ID.
       */
      getEntry: {
        /**
         * Single audit entry by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; actorId: string; actorEmail: string; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: null; resourceId: null; metadata: {}; ip: string; userAgent: string; ts: string; }>;
      };
      /**
       * Update log retention policy.
       */
      retention: {
        /**
         * Update log retention policy.
         */
        mutate(input: { orgId: string; retentionDays: number }, opts?: RequestOptions): Promise<{ updated: boolean; retentionDays: number; }>;
      };
      /**
       * Stream audit log as NDJSON.
       */
      export: {
        /**
         * Stream audit log as NDJSON.
         */
        stream(input: { orgId: string; from: string; to: string; actions?: Array<"user.login" | "user.logout" | "user.created" | "user.updated" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "task.created" | "task.updated" | "task.deleted" | "billing.plan_changed" | "billing.payment_failed" | "file.uploaded" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "entry"; data: { id: string; orgId: string; actorId: string | null; actorEmail: string | null; action: "task.created" | "task.updated" | "task.deleted" | "user.created" | "user.updated" | "billing.payment_failed" | "file.uploaded" | "user.login" | "user.logout" | "user.deactivated" | "org.created" | "org.updated" | "org.deleted" | "org.member_added" | "org.member_removed" | "billing.plan_changed" | "file.deleted" | "webhook.created" | "webhook.deleted" | "api_key.created" | "api_key.revoked" | "settings.updated" | "sso.enabled" | "sso.disabled"; resourceType: string | null; resourceId: string | null; metadata: Record<string, unknown>; ip: string | null; userAgent: string | null; ts: string; }; }, void, unknown>>;
      };
    };
    apiKeysRoutes9: {
      /**
       * List API keys for org.
       */
      list: {
        /**
         * List API keys for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ keys: { id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: string | null; lastUsedAt: string | null; createdBy: string; createdAt: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number; } | null; }[]; }>;
      };
      /**
       * Create API key.
       */
      create: {
        /**
         * Create API key.
         */
        mutate(input: { orgId: string; name: string; scopes: Array<"read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write">; expiresAt: string | null; createdBy: string; rateLimit: { requestsPerMinute: number; requestsPerDay: number } | null }, opts?: RequestOptions): Promise<{ key: string; id: string; prefix: string; }>;
      };
      /**
       * Revoke API key.
       */
      revoke: {
        /**
         * Revoke API key.
         */
        mutate(input: { id: string; reason?: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Rotate key secret.
       */
      rotate: {
        /**
         * Rotate key secret.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ newKey: string; id: string; }>;
      };
      /**
       * API key metadata.
       */
      get: {
        /**
         * API key metadata.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; prefix: string; scopes: ("read:all" | "write:all" | "tasks:read" | "tasks:write" | "users:read" | "users:write" | "billing:read" | "analytics:read" | "webhooks:manage" | "files:read" | "files:write")[]; expiresAt: null; lastUsedAt: null; createdBy: string; createdAt: string; rateLimit: null; }>;
      };
      /**
       * Key request usage stats.
       */
      usage: {
        /**
         * Key request usage stats.
         */
        query(input: { id: string; period?: "1h" | "24h" | "7d" | "30d" }, opts?: RequestOptions): Promise<{ requests: number; errors: number; p50Ms: number; p99Ms: number; series: { ts: string; count: number; }[]; }>;
      };
    };
    integrationsRoutes9: {
      /**
       * All integrations with status.
       */
      list: {
        /**
         * All integrations with status.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ integrations: { id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: boolean; enabled: boolean; status: "error" | "active" | "disconnected" | "syncing"; lastSyncAt: string | null; errorMessage: string | null; config: Record<string, unknown>; }[]; }>;
      };
      /**
       * Single integration status.
       */
      get: {
        /**
         * Single integration status.
         */
        query(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ id: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; name: string; installed: true; enabled: true; status: "active"; lastSyncAt: string; errorMessage: null; config: {}; }>;
      };
      /**
       * Start OAuth install flow.
       */
      install: {
        /**
         * Start OAuth install flow.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; redirectUri: string; scopes?: Array<string> }, opts?: RequestOptions): Promise<{ authUrl: string; }>;
      };
      /**
       * Exchange OAuth code for tokens.
       */
      oauthCallback: {
        /**
         * Exchange OAuth code for tokens.
         */
        mutate(input: { code: string; state: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ installed: boolean; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; }>;
      };
      /**
       * Remove integration.
       */
      uninstall: {
        /**
         * Remove integration.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ uninstalled: boolean; }>;
      };
      /**
       * Update integration config.
       */
      updateConfig: {
        /**
         * Update integration config.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; config: Record<string, unknown> }, opts?: RequestOptions): Promise<{ updated: boolean; }>;
      };
      /**
       * Manual sync trigger.
       */
      triggerSync: {
        /**
         * Manual sync trigger.
         */
        mutate(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Stream sync progress events.
       */
      syncStream: {
        /**
         * Stream sync progress events.
         */
        stream(input: { orgId: string; integrationId: "slack" | "github" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty" }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; integrationId: "github" | "slack" | "jira" | "linear" | "notion" | "figma" | "google_drive" | "dropbox" | "salesforce" | "hubspot" | "zendesk" | "pagerduty"; itemsSynced: number; total: number; }, void, unknown>>;
      };
    };
    workflowsRoutes9: {
      /**
       * Create workflow with recursive condition tree.
       */
      create: {
        /**
         * Create workflow with recursive condition tree.
         */
        mutate(input: { name: string; description?: string; trigger: { event: string; filters?: unknown; debounceMs?: number }; steps: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; orgId: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * List workflows with run stats.
       */
      list: {
        /**
         * List workflows with run stats.
         */
        query(input: { orgId: string; enabled?: boolean }, opts?: RequestOptions): Promise<{ workflows: { id: string; name: string; enabled: boolean; lastRunAt: string | null; runCount: number; failureRate: number; }[]; }>;
      };
      /**
       * Workflow detail.
       */
      get: {
        /**
         * Workflow detail.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; name: string; enabled: boolean; }>;
      };
      /**
       * Update workflow definition.
       */
      update: {
        /**
         * Update workflow definition.
         */
        mutate(input: { name?: string; description?: string; trigger?: { event: string; filters?: unknown; debounceMs?: number }; steps?: Array<{ id: string; name: string; type: "condition" | "action" | "delay" | "parallel"; condition?: unknown; action?: { type: "send_email"; to: Array<string>; subject: string; templateId: string } | { type: "send_slack"; channelId: string; message: string } | { type: "webhook"; url: string; method: "GET" | "POST" | "PUT" | "PATCH"; headers?: Record<string, string>; body?: Record<string, unknown> } | { type: "update_field"; resourceType: string; resourceId: string; field: string; value: unknown } | { type: "create_task"; projectId: string; title: string; assigneeId?: string } | { type: "ai_action"; prompt: string; outputField: string; model?: string }; delayMs?: number; nextStepIds?: Array<string> }>; enabled?: boolean; retryPolicy?: { maxAttempts: number; backoffMs: number; backoffMultiplier: number }; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete workflow.
       */
      delete: {
        /**
         * Delete workflow.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Paginated run history.
       */
      runHistory: {
        /**
         * Paginated run history.
         */
        query(input: { workflowId: string; status?: "running" | "success" | "failed" | "cancelled"; limit?: number; cursor?: string }, opts?: RequestOptions): Promise<{ runs: { id: string; status: string; startedAt: string; finishedAt: string | null; steps: { stepId: string; status: string; durationMs: number; error?: string | undefined; }[]; }[]; nextCursor: string | null; }>;
      };
      /**
       * Dry-run with streamed step outcomes.
       */
      testRun: {
        /**
         * Dry-run with streamed step outcomes.
         */
        stream(input: { workflowId: string; samplePayload: Record<string, unknown> }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "step_result"; stepId: string; status: "success"; durationMs: number; output: Record<string, unknown>; }, void, unknown>>;
      };
    };
    documentsRoutes9: {
      /**
       * Documents in a project.
       */
      list: {
        /**
         * Documents in a project.
         */
        query(input: { projectId: string; page?: number; limit?: number; search?: string }, opts?: RequestOptions): Promise<{ documents: Pick<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "private" | "public" | "project"; authorId: string; collaborators: string[]; version: number; publishedAt: string | null; createdAt: string; updatedAt: string; }, "createdAt" | "id" | "updatedAt" | "title" | "authorId">[]; total: number; }>;
      };
      /**
       * Full document with blocks.
       */
      get: {
        /**
         * Full document with blocks.
         */
        query(input: { id: string; version?: number }, opts?: RequestOptions): Promise<{ id: string; projectId: string; title: string; blocks: { id: string; type: "code" | "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; children: string[]; content?: { text: string; marks?: { bold?: boolean | undefined; italic?: boolean | undefined; code?: boolean | undefined; link?: string | undefined; color?: string | undefined; } | undefined; }[] | undefined; attrs?: Record<string, string | number | boolean> | undefined; }[]; visibility: "project"; authorId: string; collaborators: never[]; version: number; publishedAt: null; createdAt: string; updatedAt: string; }>;
      };
      /**
       * Create document.
       */
      create: {
        /**
         * Create document.
         */
        mutate(input: { projectId: string; title: string; blocks: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility: "private" | "project" | "public"; authorId: string; collaborators: Array<string> }, opts?: RequestOptions): Promise<{ id: string; title: string; }>;
      };
      /**
       * Update blocks and metadata.
       */
      update: {
        /**
         * Update blocks and metadata.
         */
        mutate(input: { id: string; title?: string; blocks?: Array<{ id: string; type: "paragraph" | "heading1" | "heading2" | "heading3" | "bullet_list" | "numbered_list" | "code" | "quote" | "divider" | "image" | "table" | "callout" | "toggle"; content?: Array<{ text: string; marks?: { bold?: boolean; italic?: boolean; code?: boolean; link?: string; color?: string } }>; children?: Array<string>; attrs?: Record<string, string | number | boolean> }>; visibility?: "private" | "project" | "public" }, opts?: RequestOptions): Promise<{ id: string; version: number; }>;
      };
      /**
       * Delete document.
       */
      delete: {
        /**
         * Delete document.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Publish document publicly.
       */
      publish: {
        /**
         * Publish document publicly.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ publishedAt: string; url: string; }>;
      };
      /**
       * Document version history.
       */
      history: {
        /**
         * Document version history.
         */
        query(input: { id: string; limit?: number }, opts?: RequestOptions): Promise<{ versions: { version: number; editor: string; changedAt: string; summary: string; }[]; }>;
      };
      /**
       * Collaborative edit event stream.
       */
      editStream: {
        /**
         * Collaborative edit event stream.
         */
        stream(input: { docId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "block_updated"; blockId: string; patch: Record<string, unknown>; editorId: string; }, void, unknown>>;
      };
    };
    permissionsRoutes9: {
      /**
       * All ABAC policies for org.
       */
      listPolicies: {
        /**
         * All ABAC policies for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ policies: { id: string; orgId: string; name: string; effect: "allow" | "deny"; resources: ("file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics")[]; actions: ("read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve")[]; conditions: { field: string; operator: "in" | "eq" | "neq" | "owned_by"; value: string | string[]; }[]; subjects: { type: "user" | "role" | "team"; id: string; }[]; priority: number; createdAt: string; }[]; }>;
      };
      /**
       * Create ABAC policy.
       */
      createPolicy: {
        /**
         * Create ABAC policy.
         */
        mutate(input: { orgId: string; name: string; effect: "allow" | "deny"; resources: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update policy definition.
       */
      updatePolicy: {
        /**
         * Update policy definition.
         */
        mutate(input: { orgId?: string; name?: string; effect?: "allow" | "deny"; resources?: Array<"task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics">; actions?: Array<"create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve">; conditions?: Array<{ field: string; operator: "eq" | "neq" | "in" | "owned_by"; value: string | Array<string> | "$userId" | "$orgId" }>; subjects?: Array<{ type: "user" | "team" | "role"; id: string }>; priority?: number; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete policy.
       */
      deletePolicy: {
        /**
         * Delete policy.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Evaluate access for a subject.
       */
      checkAccess: {
        /**
         * Evaluate access for a subject.
         */
        query(input: { orgId: string; subjectId: string; resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }, opts?: RequestOptions): Promise<{ allowed: boolean; matchedPolicies: string[]; }>;
      };
      /**
       * Batch permission check.
       */
      bulkCheck: {
        /**
         * Batch permission check.
         */
        mutate(input: { orgId: string; subjectId: string; checks: Array<{ resource: "task" | "project" | "document" | "file" | "comment" | "team" | "org" | "billing" | "webhook" | "api_key" | "integration" | "workflow" | "analytics"; action: "create" | "read" | "update" | "delete" | "manage" | "share" | "export" | "import" | "approve"; resourceId?: string }> }, opts?: RequestOptions): Promise<{ results: { resource: "file" | "billing" | "task" | "project" | "comment" | "workflow" | "document" | "webhook" | "team" | "org" | "api_key" | "integration" | "analytics"; action: "read" | "update" | "create" | "delete" | "export" | "manage" | "share" | "import" | "approve"; allowed: boolean; }[]; }>;
      };
    };
    teamsRoutes9: {
      /**
       * Teams in an org.
       */
      list: {
        /**
         * Teams in an org.
         */
        query(input: { orgId: string; visibility?: "public" | "private" | "secret" }, opts?: RequestOptions): Promise<{ teams: { id: string; orgId: string; name: string; visibility: "private" | "public" | "secret"; memberCount: number; projectIds: string[]; createdAt: string; description?: string | undefined; color?: string | undefined; icon?: string | undefined; }[]; }>;
      };
      /**
       * Team by ID.
       */
      get: {
        /**
         * Team by ID.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; name: string; description: undefined; color: string; icon: string; visibility: "public"; memberCount: number; projectIds: never[]; createdAt: string; }>;
      };
      /**
       * Create team.
       */
      create: {
        /**
         * Create team.
         */
        mutate(input: { orgId: string; name: string; description?: string; color?: string; icon?: string; visibility: "public" | "private" | "secret"; projectIds: Array<string> }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update team metadata.
       */
      update: {
        /**
         * Update team metadata.
         */
        mutate(input: { orgId?: string; name?: string; description?: string; color?: string; icon?: string; visibility?: "public" | "private" | "secret"; projectIds?: Array<string>; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete team.
       */
      delete: {
        /**
         * Delete team.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Team member list.
       */
      members: {
        /**
         * Team member list.
         */
        query(input: { teamId: string; page?: number }, opts?: RequestOptions): Promise<{ members: { userId: string; role: "member" | "lead"; joinedAt: string; }[]; }>;
      };
      /**
       * Add user to team.
       */
      addMember: {
        /**
         * Add user to team.
         */
        mutate(input: { teamId: string; userId: string; role: "lead" | "member" }, opts?: RequestOptions): Promise<{ added: boolean; }>;
      };
      /**
       * Remove user from team.
       */
      removeMember: {
        /**
         * Remove user from team.
         */
        mutate(input: { teamId: string; userId: string }, opts?: RequestOptions): Promise<{ removed: boolean; }>;
      };
    };
    invitesRoutes9: {
      /**
       * Pending invites for org.
       */
      list: {
        /**
         * Pending invites for org.
         */
        query(input: { orgId: string; status?: "pending" | "accepted" | "declined" | "expired" | "revoked" }, opts?: RequestOptions): Promise<{ invites: { id: string; orgId: string; email: string; role: "admin" | "owner" | "member" | "viewer"; status: "revoked" | "pending" | "accepted" | "declined" | "expired"; invitedBy: string; expiresAt: string; acceptedAt: string | null; teams: string[]; createdAt: string; personalMessage?: string | undefined; }[]; }>;
      };
      /**
       * Send invite.
       */
      create: {
        /**
         * Send invite.
         */
        mutate(input: { orgId: string; email: string; role: "owner" | "admin" | "member" | "viewer"; teams?: Array<string>; personalMessage?: string }, opts?: RequestOptions): Promise<{ id: string; email: string; }>;
      };
      /**
       * Bulk invite by email list.
       */
      bulkCreate: {
        /**
         * Bulk invite by email list.
         */
        mutate(input: { orgId: string; emails: Array<string>; role?: "admin" | "member" | "viewer"; teams?: Array<string> }, opts?: RequestOptions): Promise<{ invited: number; failed: string[]; }>;
      };
      /**
       * Accept invite via token.
       */
      accept: {
        /**
         * Accept invite via token.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ accepted: boolean; orgId: string; }>;
      };
      /**
       * Decline invite.
       */
      decline: {
        /**
         * Decline invite.
         */
        mutate(input: { token: string }, opts?: RequestOptions): Promise<{ declined: boolean; }>;
      };
      /**
       * Revoke pending invite.
       */
      revoke: {
        /**
         * Revoke pending invite.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ revoked: boolean; }>;
      };
      /**
       * Resend invite email.
       */
      resend: {
        /**
         * Resend invite email.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ sent: boolean; }>;
      };
    };
    commentsRoutes9: {
      /**
       * Comments for a resource.
       */
      list: {
        /**
         * Comments for a resource.
         */
        query(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; cursor?: string; limit?: number }, opts?: RequestOptions): Promise<{ comments: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; nextCursor: string | null; }>;
      };
      /**
       * Post a comment.
       */
      create: {
        /**
         * Post a comment.
         */
        mutate(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string; authorId: string; body: string; mentions: Array<string>; attachments: Array<{ id: string; name: string; url: string }>; parentId: string | null }, opts?: RequestOptions): Promise<{ id: string; }>;
      };
      /**
       * Edit comment body.
       */
      update: {
        /**
         * Edit comment body.
         */
        mutate(input: { id: string; body: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete comment.
       */
      delete: {
        /**
         * Delete comment.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Toggle emoji reaction.
       */
      react: {
        /**
         * Toggle emoji reaction.
         */
        mutate(input: { commentId: string; emoji: string }, opts?: RequestOptions): Promise<{ added: boolean; emoji: string; }>;
      };
      /**
       * Threaded replies.
       */
      thread: {
        /**
         * Threaded replies.
         */
        query(input: { parentId: string; limit?: number }, opts?: RequestOptions): Promise<{ replies: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }[]; total: number; }>;
      };
      /**
       * Stream new comments live.
       */
      liveStream: {
        /**
         * Stream new comments live.
         */
        stream(input: { resourceType: "task" | "project" | "document" | "file" | "post"; resourceId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "new_comment"; comment: { id: string; resourceType: "file" | "task" | "project" | "document" | "post"; resourceId: string; authorId: string; body: string; mentions: string[]; reactions: { emoji: string; users: string[]; }[]; attachments: { id: string; name: string; url: string; }[]; parentId: string | null; editedAt: string | null; createdAt: string; bodyHtml?: string | undefined; }; }, void, unknown>>;
      };
    };
    tagsRoutes9: {
      /**
       * All tags for org.
       */
      list: {
        /**
         * All tags for org.
         */
        query(input: { orgId: string; search?: string; sortBy?: "name" | "usageCount" }, opts?: RequestOptions): Promise<{ tags: { id: string; orgId: string; name: string; color: string; usageCount: number; createdAt: string; description?: string | undefined; }[]; }>;
      };
      /**
       * Create tag.
       */
      create: {
        /**
         * Create tag.
         */
        mutate(input: { orgId: string; name: string; color: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update tag name/color.
       */
      update: {
        /**
         * Update tag name/color.
         */
        mutate(input: { id: string; name?: string; color?: string; description?: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete tag.
       */
      delete: {
        /**
         * Delete tag.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Merge multiple tags into one.
       */
      merge: {
        /**
         * Merge multiple tags into one.
         */
        mutate(input: { sourceIds: Array<string>; targetId: string }, opts?: RequestOptions): Promise<{ merged: number; targetId: string; }>;
      };
      /**
       * Attach tags to a resource.
       */
      attach: {
        /**
         * Attach tags to a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ attached: number; }>;
      };
      /**
       * Detach tags from a resource.
       */
      detach: {
        /**
         * Detach tags from a resource.
         */
        mutate(input: { tagIds: Array<string>; resourceType: "task" | "project" | "document"; resourceId: string }, opts?: RequestOptions): Promise<{ detached: number; }>;
      };
    };
    reportsRoutes9: {
      /**
       * Saved reports for org.
       */
      list: {
        /**
         * Saved reports for org.
         */
        query(input: { orgId: string }, opts?: RequestOptions): Promise<{ reports: { id: string; orgId: string; name: string; type: "custom" | "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest"; config: Record<string, unknown>; schedule: { cron: string; timezone: string; recipients: string[]; format: "csv" | "pdf" | "html"; } | null; lastRunAt: string | null; lastRunStatus: "success" | "failed" | "running" | null; createdBy: string; createdAt: string; }[]; }>;
      };
      /**
       * Create report definition.
       */
      create: {
        /**
         * Create report definition.
         */
        mutate(input: { orgId: string; name: string; type: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config: Record<string, unknown>; schedule: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy: string }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update report.
       */
      update: {
        /**
         * Update report.
         */
        mutate(input: { orgId?: string; name?: string; type?: "task_summary" | "user_activity" | "project_velocity" | "billing_usage" | "audit_digest" | "custom"; config?: Record<string, unknown>; schedule?: { cron: string; timezone?: string; recipients: Array<string>; format?: "pdf" | "csv" | "html" } | null; createdBy?: string; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete report.
       */
      delete: {
        /**
         * Delete report.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Trigger report run.
       */
      run: {
        /**
         * Trigger report run.
         */
        mutate(input: { id: string; format?: "pdf" | "csv" | "html" }, opts?: RequestOptions): Promise<{ jobId: string; queued: boolean; }>;
      };
      /**
       * Get report download URL.
       */
      download: {
        /**
         * Get report download URL.
         */
        query(input: { id: string; runId: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream report generation.
       */
      stream: {
        /**
         * Stream report generation.
         */
        stream(input: { id: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; percent: number; stage: "collecting_data"; }, void, unknown>>;
      };
    };
    rateLimitsRoutes9: {
      /**
       * Rate limit rules for org.
       */
      listRules: {
        /**
         * Rate limit rules for org.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ rules: { id: string; orgId: string; name: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; targetId: string | null; endpoint: string | null; limits: { window: "minute" | "hour" | "day" | "second"; max: number; burstAllowance: number; }[]; action: "log" | "throttle" | "block"; enabled: boolean; createdAt: string; }[]; }>;
      };
      /**
       * Create rate limit rule.
       */
      createRule: {
        /**
         * Create rate limit rule.
         */
        mutate(input: { orgId: string; name: string; target: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId: string | null; endpoint: string | null; limits: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean }, opts?: RequestOptions): Promise<{ id: string; name: string; }>;
      };
      /**
       * Update rule.
       */
      updateRule: {
        /**
         * Update rule.
         */
        mutate(input: { orgId?: string; name?: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint"; targetId?: string | null; endpoint?: string | null; limits?: Array<{ window: "second" | "minute" | "hour" | "day"; max: number; burstAllowance?: number }>; action?: "throttle" | "block" | "log"; enabled?: boolean; id: string }, opts?: RequestOptions): Promise<{ id: string; updated: boolean; }>;
      };
      /**
       * Delete rule.
       */
      deleteRule: {
        /**
         * Delete rule.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ deleted: boolean; }>;
      };
      /**
       * Current usage vs. limits.
       */
      currentStatus: {
        /**
         * Current usage vs. limits.
         */
        query(input: { orgId: string; target?: "api_key" | "user" | "org" | "ip" | "endpoint" }, opts?: RequestOptions): Promise<{ statuses: { ruleId: string; target: "user" | "ip" | "endpoint" | "org" | "api_key"; window: string; current: number; max: number; resetAt: string; }[]; }>;
      };
      /**
       * Manually reset rate counter.
       */
      resetCounter: {
        /**
         * Manually reset rate counter.
         */
        mutate(input: { ruleId: string; targetId: string }, opts?: RequestOptions): Promise<{ reset: boolean; }>;
      };
    };
    exportsRoutes9: {
      /**
       * Queue an export job.
       */
      create: {
        /**
         * Queue an export job.
         */
        mutate(input: { orgId: string; resource: { resource: "tasks"; projectId?: string; filters?: Record<string, unknown> } | { resource: "users"; orgId: string } | { resource: "analytics"; orgId: string; metrics: Array<string>; dateRange: { from: string; to: string } } | { resource: "audit_log"; orgId: string; from: string; to: string } | { resource: "billing"; orgId: string; year: number } | { resource: "files"; resourceType: string; resourceId: string }; format: "json" | "csv" | "xlsx" | "pdf" | "ndjson" | "parquet"; notifyEmail?: string }, opts?: RequestOptions): Promise<{ id: string; status: "queued"; }>;
      };
      /**
       * Export job history.
       */
      list: {
        /**
         * Export job history.
         */
        query(input: { orgId: string; limit?: number }, opts?: RequestOptions): Promise<{ jobs: { id: string; orgId: string; resource: { resource: "tasks"; projectId?: string | undefined; filters?: Record<string, unknown> | undefined; } | { resource: "users"; orgId: string; } | { resource: "analytics"; orgId: string; metrics: string[]; dateRange: { from: string; to: string; }; } | { resource: "audit_log"; orgId: string; from: string; to: string; } | { resource: "billing"; orgId: string; year: number; } | { resource: "files"; resourceType: string; resourceId: string; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done" | "failed" | "queued" | "running"; progress: number; rowCount: number | null; downloadUrl: string | null; expiresAt: string | null; error: string | null; createdAt: string; }[]; }>;
      };
      /**
       * Export job status.
       */
      get: {
        /**
         * Export job status.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ id: string; orgId: string; resource: { resource: "tasks"; }; format: "json" | "csv" | "parquet" | "pdf" | "xlsx" | "ndjson"; status: "done"; progress: number; rowCount: number; downloadUrl: string; expiresAt: string; error: null; createdAt: string; }>;
      };
      /**
       * Cancel queued export.
       */
      cancel: {
        /**
         * Cancel queued export.
         */
        mutate(input: { id: string }, opts?: RequestOptions): Promise<{ cancelled: boolean; }>;
      };
      /**
       * Fresh signed download URL.
       */
      download: {
        /**
         * Fresh signed download URL.
         */
        query(input: { id: string }, opts?: RequestOptions): Promise<{ url: string; expiresIn: number; }>;
      };
      /**
       * Stream export progress.
       */
      progressStream: {
        /**
         * Stream export progress.
         */
        stream(input: { jobId: string }, opts?: RequestOptions): Promise<AsyncGenerator<{ type: "progress"; jobId: string; percent: number; rowsProcessed: number; }, void, unknown>>;
      };
    };
  };
}
